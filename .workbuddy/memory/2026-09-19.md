# 2026-09-19

## 服务人员小程序 W01／W02／W04 真实链路验证

- 目标：确认服务人员小程序已开发的任务功能能否完整跑通、每步接口是否通过、数据是否正确。
- 结论：**主链可完整跑通**。W01 只读 42/42、W02 写动作 62/62、W04 履约 104/105（唯一失败为刻意植入的缺陷探针）、端侧运行时结构校验 65/65。
- 环境：本地聚合后端 127.0.0.1:8080（`/api/api/...`）、Redis 127.0.0.1:16379、测试库 `gaia_wh_init_wzy`。无 mysql 客户端，用 `.workbuddy` venv 的 PyMySQL 直连。
- 会话：无可用账号密码，经用户授权直接向 `afs_service_personnel_session` 插入会话行（id=8，账号 2／人员 5／站点 5／企业 161，当日到期）。
- 夹具：自建 `W2TEST-20260919-*` 11 单（9509191001～9511，人员／站点 5）＋个人配件余额，含配套清理。
- 验证台已归档：`.workbuddy/verify/service-personnel-w01w02w04/`（含 README 说明运行顺序与三条纪律）。

### 踩过的坑（复用价值高）

1. **夹具必须每轮重跑**：`fixture_w2.py` 先清空 9509191xxx 区间再重建。沿用上轮数据会让草稿 revision、幂等请求号、执行事实残留，产生大批假失败。
2. **库存单据必须一起清理**：`afs_part_inventory_document` 唯一键 `uk_..._request` = `document_type` + `client_request_id`，而 `consumeParts` 的 `client_request_id` 由「工单号 + 请求号」派生。只删工单不删单据 → 下次完工提交撞唯一键 → `503 数据操作未完成`。**这不是业务缺陷**，一度被误判为事务问题。
3. **手工种数据后必须校准主键序列**：`GaiaIdGenerator` 走 Redis `ID_KE_<table>`，计数器小于 `max(id)` 时 fail-closed 抛 503「售后业务主键序列未同步」。夹具用固定高段 ID，所以必须跑 `calibrate_seq.py`。
4. 移动端路由前缀是 `/afterSales/mobile/service-personnel/tasks`，不是 `/service-personnel/tasks`；路径写错会得到 4001 未登录（误导性强）。
5. 列表响应字段是 `data.rows` 而非 `records`／`total`。
6. 单次 Bash 调用 2 分钟会超时被杀（exit 137），长验证一律用后台运行。

### 发现的业务缺陷

`DEF-20260919-01`：同工单用不同请求号重复调用签到／远程开始，服务端无「已有活动执行」判断，`ON DUPLICATE KEY UPDATE` 覆盖 `started_at`、清空 `ended_at` 并追加第二条审计。同请求号的重复调用已被幂等回放正确拦截，完工提交也有回放保护，故仅签到／远程开始受影响。

### 文档回写

- `specs/2026-09-18-服务人员小程序完整开发计划.md`：14.3 新增验证记录行；「完成内容」与「剩余事项」补充结论与缺陷。
- `03-开发管理/06-服务人员小程序开发进度.md`：更新日期、当前结论、模块概览、W01／W02／W04 行；新增 3 段叙述。
- 按项目规范**不计** W01／W02／W04 的真实实现与验收计数（研发验证不等于业务验收）；G3 不变，G4／G5 未达到。
- 遗留：`W2TEST-20260919-*` 夹具数据与会话行 id=8 保留在测试库待清理（清理命令见验证台 README）。

## 服务人员小程序：首页「正在服务」卡片多单展示优化

- 仓库 `mobile/gaia-customer-service-uni`，改动 `src/pages/task/index.vue` 与 `scripts/runtime.test.mjs`。
- 问题：`currentServices.length > 1` 时顶部卡片 `v-for` 平铺全部任务，客户名／地址／进度／两个按钮逐条重复，首屏被占满。
- 关键约束：后端 `ServicePersonnelTaskMapper.xml` 的 `excludeCurrentService` 让正在服务任务不进下方列表，顶部卡片是这些单据的唯一入口；`02-技术设计/13-服务工单状态机与单据状态说明.md` 第 79 行原规定「遇到多单，全部展示并保留各自入口」。
- 用户确认方案：主卡固定展示接口首条（服务端 `ORDER BY execution.started_at DESC`，即最近开始服务的一单），其余收进「其他 N 单处理中」折叠列表，展开后逐单保留「继续处理任务」「详情」。
- 实现：新增 `primaryService` / `otherServices` computed 与 `othersExpanded` 状态，企业切换重置时一并收起；新增 `.current-service__others*` 样式，展开条目复用既有 `.current-service__item` 次级样式。
- 验证：eslint、stylelint、`vue-tsc --noEmit` 全部通过；`node --test scripts/pagination.test.mjs scripts/runtime.test.mjs scripts/example.test.mjs` 30/30 通过（新增 6 条断言锁定"只渲染一条 + 折叠入口保留"）。
- 文档回写：`02-技术设计/13-...md` 第 79 行改为单卡 + 折叠规则；`specs/2026-09-18-服务人员小程序完整开发计划.md` 阶段 3 实施清单新增已完成条目。
- 未提交 Git（用户本次未要求 commit）。
- 遗留差异：`04-页面原型/小程序/worker.js` 的 `servingGroup` 仍是多单平铺聚合卡，本次只改小程序实现，原型未同步。

## 服务人员小程序：逐页与原型对齐（第二轮）

- 范围：原型 `worker.js`/`worker.css` 的 w-tasks／w-detail／w-service／w-returned／w-mine 与已实现页面逐页比对，按页面域分四组并行只读子代理调查，换算 1px ≈ 2rpx。
- **用户明确要求任务首页不修改**：本轮对 `src/pages/task/index.vue` 与 `src/components/task/TaskCard.vue` 做的原型对齐改动（快捷筛选行、清除筛选、说明文案、日期格式、任务卡时间栏条件、琥珀状态、时间字号）已全部手工回退，两文件现与 HEAD 一致。
- 本轮实际修正（4 个文件）：
  - `taskFlow/detail/index.vue`：状态卡标题 38→50rpx；「拨打客户电话」圆角 999→28rpx、字色改主色（字号保留 30rpx，触控可读性优先）；时间线补左侧竖线与圆点（首条高亮）并把顺序改为「内容→时间」；编辑页只读提示改信息蓝，取消场景新增 `task-form__notice--warning` 保留警示色；产品卡第三行改显任务备注，服务方式并入 `productMeta` 避免丢信息。
  - `taskFlow/process/index.vue`：提交按钮文案改原型「提交完工资料」；证据组标题「沟通证据」→「沟通记录」。
  - `mine/index.vue`：补回原型品牌语「TOTO · 专业服务，安心相伴」；身份卡层级改为姓名在上、「服务人员 · 企业 · 服务站」在下。
  - `scripts/runtime.test.mjs`：新增 7 条断言锁定上述改动。
- 判定为有意偏离、未按原型回退：处理页头部与步骤条的浅蓝填充卡（runtime 断言要求 `--app-accent-light`）、核对提交清单多「上传材料」行、到达签到移出步骤条、补资料页固定底栏、审核中／历史任务整页进度视图、首页缺少原型 chips 与列表 heading、详情页备用电话为 1／2 两字段、异常页缺件卡片依赖未接入的配件模块。
- 验证：`node --test`（pagination／runtime／example）30 / 30 通过，eslint、stylelint、`vue-tsc --noEmit` 通过。改动未提交。
- 重要发现：期间 `git log` 出现 `ccdcb26 feat(miniapp): 收口服务人员任务页交互与共同底座`，上一轮首页改动与既有工作区改动已被并发提交收口。判断改动归属应查 `git log` + `git diff HEAD --stat`，不要假设工作区差异都未提交。

## 工作区全量提交（按修改功能分组）

- 授权范围：用户「按修改的功能，提交工作区所有修改」→ 只做 `git commit`，不含 push／发布。
- 本轮补交两组，加上此前三笔，共 5 个提交跨 3 个仓库：
  - `mobile/gaia-after-sales-uni` `5e19a45` `chore(miniapp): 同步双端共同底座（Node 26 工具链／根导航／列表无感刷新）`，19 文件（15 改 4 新）。
  - `docs/toto` `eacf096` `docs(toto): 客服工作台原型升级至 v0.17 统一连续办理`，15 文件。
  - 此前：`backend/gaia-after-sales` `bfc724d`、`mobile/gaia-customer-service-uni` `ccdcb26`、`docs/toto` `fb37bcd`。
- 提交后六个仓库（docs/toto、gaia-after-sales、gaia-saas-proj、gaia-ui、gaia-after-sales-uni、gaia-customer-service-uni）工作区均为 0 改动；docs/toto 领先 `origin/main` 两个提交，未 push。
- 为什么 `gaia-after-sales-uni` 也要提：它是与服务人员端共享底座的消费者端一半，`docs/shared-foundation.md` 记录的三项同步（Node 26 工具链、根导航滚动下沉、列表无感刷新）在两端必须等价；服务人员端那一半在 `ccdcb26` 已提交。
- 校验证据：`node --test 04-页面原型/后台系统/checks/*.test.cjs` 62/62；消费者端在 Node 26.7.0 下 `pnpm check` 全绿（类型／ESLint／Stylelint／租户／单元／路由／安全审计／派生文件）。

### 提交环境坑（复用价值高）

1. **本机 shell 没有 `pnpm`**：仓库 husky `pre-commit` 跑 `pnpm exec lint-staged` 会直接失败。改用 `PATH="/opt/homebrew/bin:$PATH"` 调用；该目录 node 是 26.7.0（正好匹配新 `.nvmrc`），pnpm 是 10.2.1。不要用 `--no-verify` 绕过。
2. **工作区里可能有并发 AI 代理**：`ChatGPT.app/…/codex sandbox` 进程对 Gaia 下多数仓库有写权限，会留下 `index.lock`，git 打印 `unable to unlink …: Operation not permitted`。先用 `pgrep -fl git` / `lsof` 确认无进程持有，确认是遗留锁再 `rm -f`，并与 `git add` + `git commit` 写在同一条命令里（否则 IDE 后台刷新会立刻重建锁）。
3. **doc-impact 检查用本地文档就能过**：`scripts/docs-impact-map.json` 里每个仓库的 `localDocumentationPatterns` 都含 `docs/**`，所以小程序仓库只要同任务改了 `docs/shared-foundation.md` / `docs/architecture.md` 等即算证据，不必额外改 docs/toto。纯文档改动（`04-页面原型/**`）不触发任何代码仓库影响。
4. 提交信息一律用 `git commit -F <文件>` 传入，避免中文和引号在 shell 里被吞。
5. 分组原则：同一「功能」的改动放一个提交；本工作区里「服务人员任务」与「客服后台原型」是两条互不相干的线，必须分开提交。

## 处理任务页证据块与确认区原型对齐（第三轮）

- 用户指令：「处理任务页，服务证据内展示的提示文字信息与原型有差异，产品与客户确认页的文字提示和样式有差异，按照原型修改」。范围仍不含任务首页。
- 关键原型事实（`worker.js` 的 `service()` / `mediaTile()` + `worker.css`）：`.w-evidence` 只有「图标+标题+状态小字+操作」**没有第二行说明**；证据提示只在卡片标题下一处；区块内嵌材料行是特例 —— `.w-confirm-section>.w-evidence{margin-top:12px;padding:12px 0 0;border-top:1px solid #edf1f5;border-bottom:0}`。
- 改动文件 4 个：`components/task/TaskEvidencePicker.vue`（删 `description` 属性与节点、按原型换算内边距/图标/字号/文字按钮）、`taskFlow/process/index.vue`（`evidenceConfig` 去 description、客户确认标题补 `section-heading`、确认卡间距与来源区底色对齐、内嵌材料行改上分隔线）、`taskFlow/supplement/index.vue`（清掉残留 `description` 传参，原型退回页材料行同样无说明）、`scripts/runtime.test.mjs`（+9 条断言）。
- 保留的端侧能力：字段行「核验产品码」原型没有，保留但降级为 `.w-text-button` 规格透明文字按钮；只读来源区按正式接口字段展示，不虚构购买渠道/日期。
- 坑：**Edit 连续两次报成功但内容未落盘**（`.confirm-section :deep(.evidence-picker)` 与 `verify-action` 字号），由并发 Codex 代理或 IDE 自动刷新导致。改完样式后必须回读目标行确认，改完立即跑测试——单测第一条失败就是靠断言发现的。
- 验证：`node --test` 30/30，eslint／stylelint／`vue-tsc --noEmit` 全通过。改动未提交。

## 任务相关页面（除首页）逐状态 1:1 原型对齐（第四轮）

- 用户指令：「任务相关页面，除了首页，各种状态都逐一比对样式和文字提示与原型有差异的地方，按照原型进行1比1的修复」。任务首页 `pages/task/index.vue` 与 `components/task/TaskCard.vue` 继续不动。
- 用的方法：先把原型状态矩阵抄全再动手 —— `seedTasks` 11 条 ＋ `reviewOverview()` 5 态 ＋ `service()`/`returned()` 守卫 ＋ `nextStep()` 的 label/hint 词表。原型样式一手值用 Python 读 `worker.css` 拆规则（文件里是超长行，shell `grep` 在这台机器上对中文和长行都不可靠，必须用 Grep 工具或 Python）。
- 改动 3 个页面 ＋ 1 个测试：`taskFlow/detail/index.vue`、`taskFlow/process/index.vue`、`taskFlow/supplement/index.vue`、`scripts/runtime.test.mjs`；另更新 `design-qa.md`（第四轮，含原型事实清单、逐项修正、有意偏离、验证）。
- 处理页最大的一处是**撤销上一轮的浅蓝填充卡结论**：头部还原 `.w-context`（无卡、只有底部细线），步骤条 ＋ 自动保存提示脱离卡片，圆点改原型 `#e4ebf3` 填充式。同理补资料页把「查看原提交与审核记录」按原型移出表单卡。
- 顺手修掉的真缺陷：
  1. `--app-page-color` / `--app-text-color` 两个令牌**在该仓库从未定义**（`build/theme.ts` 只会生成 `--app-bg-color` / `--app-text-main`），共 6 处静默失效；
  2. 审核中／历史任务在处理页会落进「到达签到」起始卡 —— 起始分支现在补了 `&& !showProgress`；
  3. 测试里 `>异常 / 改期<` 是上一轮失效断言（模板已改成 `{{ abnormalFooterLabel }}`），是本轮**先跑测试才暴露**的。
- 断言写法坑：eslint `regexp/no-super-linear-backtracking` 不接受 `[\s\S]*` 与 `[^}]*` 混用。改用「`matchAll` 取同名选择器全部声明块再 `includes`」的 helper，既过 lint 也不会因共享规则（如 `.primary-button, .secondary-button {}`）误判。
- 验证：`node --test` 30／30，eslint／stylelint／`vue-tsc --noEmit` 通过（`tsconfig.node.json` 的 5 条 `jsonc/sort-keys` 是既有欠账）。本轮未跑浏览器截图，`artifacts/design-qa/*` 截图早于本次改动，不能当本轮证据。改动未提交。
- doc-impact：任务首页不动、无功能契约变化，只按实际影响更新了 `03-开发管理/06-服务人员小程序开发进度.md`，未改 `specs/`。

## 令牌与原型换算法（本轮沉淀，复用价值高）

- **本仓库确实存在的语义令牌**（`build/theme.ts` ← `src/theme.json`）：`--app-text-main #181a1d`、`--app-text-secondary #666b73`、`--app-text-on-primary #ffffff`、`--app-primary/accent #2b74aa`、`--app-primary-active`、`--app-accent-light #eaf2f8`、`--app-bg-color #f6f7f7`、`--app-surface-color #ffffff`、`--app-border-color #dadde1`、`--app-border-light #e7e9ec`、`--app-success/-surface`、`--app-warning/-surface`、`--app-danger/-surface`、`--app-info/-surface`。**不存在** `--app-page-color`、`--app-text-color`、`--app-text-secondary-*` 一类拼法，写样式前先在这两个文件里核名。
- 原型（`docs/toto/04-页面原型/小程序/worker.css`，390px 画布）常用类的一手值：`.w-status{padding:21px 20px;radius:16px;margin-bottom:16px}`、`.w-context{padding:0 1px 16px;margin-bottom:18px;border-bottom:1px solid #e0e7ef}`、`.section-card{padding:18px;border:1px solid #e5ebf2;radius:16px;margin-bottom:14px;box-shadow:0 3px 10px #16345703}`、`.w-section-head{gap:10px;margin:6px 0 12px}`、`.field{margin:0 0 15px;gap:7px}`＋`>.span{color:#405872;font-size:12px}`＋`input/textarea{border:1px solid #dce4ee;background:#fbfcfe;radius:10px;font-size:14px;min-height:46px}`、`.w-notice{padding:12px 14px;radius:10px;line-height:1.7;font-size:12px}`、`.w-contact{gap:9px;margin:13px 0 0}`、`.phone-footer{padding:12px 18px 20px}`（secondary 1fr / primary 2fr）。
- 换算固定 1px = 2rpx；`.w-status` 与 `.section-card` 的 `margin-bottom` 用「页面 flex gap 28rpx ＋ 补差 4rpx」表达，才不会被 flex 间距吞掉。


## 第五轮：首页「非正在服务」任务卡 1:1 还原（16:54）

用户本轮指令**覆盖**了前几轮的「任务首页不动」约束，明确要求 `components/task/TaskCard.vue` 按原型 1:1。

### 原型事实（本轮新提取，前几轮没细看的）
- `canArrange = queue ∈ {pending,lost} && flag!=='审核退回'`：**预约时间栏只在“已预约且可安排”时渲染**。种子数据里 review／followup／cancelled 卡即使带日期也不显示时间栏 —— 实现端原来的「有 expectedDate 就显示」是错的。
- 评审画布是 `.worker-phone`，而 `.worker-phone .w-ticket { box-shadow: none }`：**小程序对应的是无阴影版本**，`.w-ticket` base 的 `0 4px 12px #14386003` 在手机层被关掉。以后判断“要不要阴影／描边”先看有没有 `.worker-phone` 覆盖。
- 任务屏专属覆盖：`[data-screen="w-tasks"] .w-ticket-main { padding:11px 14px 9px }`、`.w-ticket-hint { padding:7px 11px }`；`.worker-phone .w-ticket-hint { margin-bottom:13px }`。
- `.w-tag` 蓝色**生效值不是它自己写的 `#e9f1ff/#1c61b7`**：文件后面 `..., .w-tag, ... { background: var(--w-blue-soft); color: var(--w-blue) }` 同优先级但更靠后 → 实际 `#eaf3f8/#2b74aa`。查 `.w-tag` 颜色别只看第一条声明。
- 卡面一手值：`margin:0 0 13px`、`1px solid #e2e8f1`、`radius:14px`、`overflow:hidden`；`.w-time{width:65px;border-right:1px solid #e3eaf2;gap:4px}`；`.w-time strong{22px;letter-spacing:-.6px;line-height:1.3;color:#245f89;650}`；`h3{15px;margin:0 0 5px}`＋`span{10px;#7a8a9c;400;margin-left:8px}`；`p{12px;1.7;#526981}`；`.w-ticket-hint{11px;1.6;radius:6px;#f4f7fc/#657b93}`＋red `#fff2ed/#a94534`＋左 2px `#e27a5c`／amber `#fff7e7/#926321`；chevron `25px;#aab5c1`。

### 改了什么
- `TaskCard.vue`：新增 `isReturned`／`tone`／`showSchedule` 计算属性；标签与队列名按 `nextStep().label`／`queueName()` 重写（**HISTORY 以前一律「历史任务」，已关闭／已取消／待回访三态全错**）；提示文案逐状态对齐，并把「顾客」统一成原型的「客户」；样式整块按一手值重写（时间字号 36rpx → 44rpx 是最明显的一处）。
- `pages/task/index.vue`：删 `.task-results :deep(.task-card) { border: 0 }` 与 `gap: 20rpx`。**这是撤销上一轮「内部白卡移除描边、只留白色表面和轻阴影」的结论**（上一轮依据的是用户给的订单页截图）。改由卡片自带 26rpx 外边距承担列表间距，只动了这两处。
- `scripts/runtime.test.mjs`：新增 30 余条卡片断言（`styleOf` 取声明块 ＋ 字面色板 ＋ 词表 ＋ 判定顺序）；列表侧断言由「必须 border:0」改为「不得 border:0、不得 gap」。

### 顺手发现的缺陷（未改，本轮范围外）
`taskFlow/detail/index.vue` 的 `statusTone`（96 行）与 `statusTitle`（114 行）用的是比 `isReturned`（103-108 行）**更宽**的 `latestReview?.decision === 'REJECT'`。审核退回后重新提交、category 已变 `REVIEW` 的任务仍会命中 → 渲染成红色「退回待补」；而 101-102 行注释恰好写着“优先用 category 避免残留 REJECT 误判”，自相矛盾。建议把这两处收敛到 `isReturned`。

### 验证
`node --test` 30／30；eslint／stylelint／`vue-tsc --noEmit` 对改动文件通过。本轮补了可视化核对：用原型一手 CSS 与实现 rpx 换算值（1rpx = 0.52px @390 画布）并排渲染 7 种状态 → `/tmp/gaia-task-card-1to1.html`（临时文件，未入库）。改动未提交。

### 补充令牌事实
- `--app-primary-active` = `#205D8B`，来路是 `build/tenant/profiles/toto.ts` 的 `theme.primaryActive`，经 `vite.config.ts` 注入 scss 变量 → `src/static/styles/tdesign-theme.scss`。原型深蓝 `--w-blue-deep` = `#245f89`，同角色。
- `--app-radius-control: 12rpx`／`--app-radius-card: 24rpx`／`--app-radius-overlay: 32rpx` 定义在 `src/static/styles/common.scss`（**不在** `build/theme.ts`）。原型 `.w-ticket` 的 14px 圆角 = 28rpx，与 `--app-radius-card` 的 24rpx 不同，只能写字面值。

### 本轮真实发生：并行写入方改同一个文件（务必记住）

执行第五轮时，`src/pages/task/index.vue` 与 `src/components/task/TaskCard.vue` 被**另一个写入方**并发修改：

- `git status` 在开工时**没有** `src/pages/task/index.vue`；收尾时它已在变更列表里，且 diff 里多出 `serving-shortcuts`／`pending-filters`／`task-panel__caption`／`task-results__heading`／`task-results__promoted`／`visibleTasks`／`promotedCount`／`resultCount` 等本轮没写过的原型首页元素。
- `TaskCard.vue` 的 `<style>` 和 `<template>` 仍是本轮写的版本，但 `<script>` 被细化：`statusLabel` 多了 `serviceActive`→正在服务 与 `APPOINTED/RESCHEDULED/PENDING_COMPLETION` 分支，`showSchedule` 收紧成 `status==='APPOINTED' && !exceptionCode`，`tone` 判定顺序也调过。
- 直接后果：本轮一条「匹配表达式全文」的断言当场变红（29／30）。改为**按声明块切片做语义断言**（`card.slice(indexOf('const showSchedule'), indexOf('const productName'))` + `includes` 各关键条件）后恢复 30／30。
- **教训**：在这种多代理工作区，断言不要匹配整段表达式的字面写法，要匹配语义要点；发现文件被外部写过时，先 `git diff` + `mtime` 定位范围再决定改不改，不要盲目重写覆盖别人的改动。
- 另：`.git/index.lock`（14:44 的 0 字节遗留锁）一直在，本轮没提交，未处理。

## 第六轮：首页「正在服务」卡片该不该进顶部的分档分析（只读分析，未改代码）

- 用户诉求从「显示几张」推进到「该不该进顶部」：折叠解决的是数量，本质是**筛选**问题——顶部区应只保留"现在就要做"的单。
- 已查证的后端事实（`backend/gaia-after-sales`，`ServicePersonnelTaskMapper.xml` / `ServicePersonnelTaskAppService.java`）：
  - 进顶部唯一条件 = `afs_service_execution.service_active=1` 且 `categoryExpression='PENDING'`，接口 `ORDER BY execution.started_at DESC`（第 174-181 行）→ **主卡 = 最近开始服务的一单**，不是最该马上做的一单。
  - 进入动作只有两个：`startService`（现场定位签到 / 开始远程指导，第 244-280 行）；且要求状态 `PENDING_COMPLETION` + `exceptionCode` 为空 + category=PENDING（第 251-253 行）。仅打开服务页不开始。
  - 离开动作四个：提交完工（第 380 行）、改期（第 212 行）、缺件／断联／取消（第 218 行）都调用 `deactivateCurrentService`（`UPDATE afs_service_execution SET service_active=0`）。
  - **副作用**：改期不会结束服务，但会把单踢出顶部；缺件／断联后单掉回待处理列表，列表卡显示"等待配件/待再次联系"，**丢失"继续处理任务"入口**，草稿要动详情页才找得到。
  - `excludeCurrentService` 只作用于 `count`/`list`（第 151、166 行），`categoryCounts`（第 154 行）不含它 → 待处理计数包含置顶单，列表不含，计数与列表条数天然不等。
  - `TaskCard.vue` 的 `if (current.serviceActive)` 分支在列表上下文是**死代码**（列表已排除这些单），说明原设计本就打算让列表能显示"进行中"单。
- 给用户的分档结论（详见当轮回复）：① 顶部主卡＝预约今天／已过期且步骤 1-2；② 顶部一行＝步骤 3/3 待提交，不占卡位；③ 回列表标"进行中"＝未来预约、远程未到点、定位例外；④ 待拍板＝跨天未推进（需要"最后推进时间"，`afs_service_execution` 现只有 started_at/ended_at）。
- **硬约束提醒**：任何 "从顶部移走" 的方案都要先动 `excludeCurrentService`（移除或改成列表内"进行中"子筛选），否则单据会从师傅端彻底消失。建议顶部只做加速器、不做唯一通道。

## 第七轮：首页「正在服务」卡片折叠条收紧（已改代码）

- 用户诉求：把首页正在服务卡片底部「其他 N 单处理中／收起其他任务」这一条缩短高度，让比例协调。
- 事实核对：**原型没有这个折叠条**。`04-页面原型/小程序/worker.js:92-95` 只有单条 `w-serving` 和全展开的 `w-serving-group`（head 里写「N 单需要继续处理」）。折叠是实现的在端自定结构，样式无原型依据，属于实现侧自由调整。
- 改动（`mobile/gaia-customer-service-uni/src/pages/task/index.vue`，仅样式 + 一个 size 值）：
  - `.current-service__others`：`margin-top 24rpx→18rpx`、`padding-top 6rpx→0`（保留 `border-top: 2rpx solid rgb(255 255 255 / 15%)` 分隔）。
  - `.current-service__others-toggle`：`min-height 76rpx→60rpx`、`line-height 76rpx→1.4`（高度改由 min-height 撑，`align-items:center` 负责垂直居中）、`font-size 23rpx→22rpx`。
  - 模板 `<t-icon size="30rpx">→28rpx`，与 22rpx 文字配平。
  - `.current-service__item`：`padding 26rpx 0 22rpx→22rpx 0 20rpx`，让展开态首项与折叠条之间的空档不因 toggle 变矮而相对变大。
- 验证：`./node_modules/.bin/stylelint` + `eslint` 干净；`node --test scripts/pagination.test.mjs scripts/runtime.test.mjs scripts/example.test.mjs` 30/30 通过（未触碰任何被断言的选择器）。
- 注意：本轮 `git status` 里 `src/pages/task/index.vue` 本就在变更列表（含第五/六轮遗留改动与并行写入方的 `task-results` 相关改动），本轮只改上述几行，**未提交**。

## 第八轮：配件管理页面按钮圆角／文字居中／间距对齐（23:32～23:38）

- 用户诉求：配件管理（配件相关新开发功能）页面与原型差异明显，主要集中在**按钮圆角**、**按钮文字错位偏上**和**一些间距**。
- 范围（`mobile/gaia-customer-service-uni`，**只改 `<style>`**，模板与脚本未动）：`src/pages/part/index.vue`、`src/pages/partFlow/{parts.scss,detail,documents,documentDetail,requisition}`、`src/components/part/PartQuantityCard.vue`；`partFlow/return/index.vue` 只 `@use '../parts.scss'` 无需改。
- **三个根因结论（复用价值高）**：
  1. **按钮文字偏上 = 微信 `uni-button` 默认 `line-height: 2.5555`**。小 `min-height` 按钮里这会把单行文字顶到偏上。修法：全部按钮统一 `display: flex; align-items: center; justify-content: center` + **显式声明 `line-height`**（通常 1.2～1.45）。任务流页面早已这么写，配件页漏了，本轮补齐至同构。
  2. **圆角**：原型 `.primary`/`.secondary` 是 `14px` → **28rpx**，不要用令牌 `--app-radius-control: 12rpx`。搜索框内查询按钮 = `16rpx`，标签 `12rpx`，图标块 `20rpx`，各按原型各自规则。
  3. **间距**：`.app-page__content` 在本端是**带 `gap: 16rpx` 的 flex 列容器**，原型是块级流（外边距可折叠）。**照抄原型外边距会偏大**，须按差量折算：概览卡到搜索框原型 16px（13px 与 16px 折叠）＝ gap 16rpx ＋ 搜索框上边距 16rpx，故概览卡 `margin-bottom` 写 **0**（写 16rpx 会变成 24px，本轮先写错后修正）；搜索框 `margin: 16rpx 0`（下 16rpx ＋ gap = 16px 对应「搜索框→列表标题」）；标题 `margin: 0 0 8rpx`（＋gap = 12px 对应「标题→首行」）。折算过程必须写进源码注释，否则后人会当错值改回。
- **原型一手值（本轮像素实测，纠正了几处凭印象的猜值）**：搜索区查询按钮是**蓝色文字按钮、无底色**（不是实心主按钮）；`.w-search` 高 50px→100rpx、圆角 11px→22rpx，`margin:16px 0`；`.w-stock-summary`（生效值在 `.worker-phone` 覆盖块）padding 15px→30rpx、圆角 16px→32rpx、描边 `#cfdee8`、卡高 155px；`.w-basket` 底 `#dbeaf3`；步进按钮 `.w-quantity button` 40×40px→**80rpx×80rpx**、`23px`/400/`line-height:1`/圆角 8px→16rpx/底 `#f2f6fb`；记录卡 padding 17px→34rpx、圆角 13px→26rpx、描边 `#e0e8f2`；状态卡 padding `21px 20px`→`42rpx 40rpx`、圆角 16px→32rpx；详情图标 76px→152rpx（圆角 42rpx）、总数 48px→96rpx；列表行图标 38×43px→76×86rpx、库位数 26px→52rpx。
- **原型首页纵向节奏（像素实测）**：概览卡→搜索框 **16px**（13px 与 16px 折叠）、搜索框高 **50px**、搜索框→「我的库存」16px、标题→首行 12px、**列表行高 ≈86px**（含「品名／型号／参考单价」三行）、主按钮高 44px。
- **提取 CSS 的技巧**：同名选择器**全部**声明块要一起看。首次用「逗号分段后完全相等」匹配 targets，**漏掉了 `.worker-phone .primary`／`.worker-phone .w-stock-summary` 这类带前缀的覆盖规则**（`.primary` 的基础值圆角 11px 会被覆盖成 14px）；改用**子串匹配**（`any(t in sel for t in targets)`）才拿全。同理 `.w-part-row small`、`.w-stock-count b` 的字色也在文件后段被覆盖。
- 顺手清理：删除模板中已无样式定义的死类 `detail-records`、`reappoint-link`；时间线圆点补 `content: ''`（伪元素此前不渲染）。
- 验证：`node --test scripts/pagination.test.mjs scripts/runtime.test.mjs scripts/example.test.mjs` **31/31**（新增 1 个测试「配件页面沿用原型配件屏的按钮几何、文字居中与间距」，约 40 条断言，含对不存在令牌 `--app-page-color`／`--app-text-color` 的**负向断言**）；stylelint／eslint／`vue-tsc --noEmit` 0 错。
- 视觉证据：`mobile/gaia-customer-service-uni/artifacts/design-qa/parts-prototype-alignment.html`／`.png`（左栏原型一手声明 px，右栏实现 rpx×0.52，含逐项数值对照表）、`prototype-parts-{home,detail,records,record-detail,requisition,return}-390x844.png`（原型六屏）。`artifacts/` 已在 `.gitignore` 第 22 行，不会污染 git。
- 文档影响：更新 miniapp 仓 `design-qa.md`（第六轮，含原型一手值／纵向节奏表／逐项修正／有意偏离／验证，末行 `final result: passed`）；更新 `docs/toto/03-开发管理/06-服务人员小程序开发进度.md` 追加同日说明，并在 W05 行剩余工作中标注"已按原型一手值完成源码级样式对齐"。
- **未提交**（用户未授权）。收尾 `git status` + mtime 核对：本轮文件 mtime 23:32～23:34，而 `pages.json`／`router/*`／`taskFlow/*`／`TaskEvidencePicker.vue` 的 mtime 为 21:37～22:59（并行写入方，配件功能脚手架），**本轮无并行写入**。
- 环境备忘：本机 `agent-browser` 未安装；用 `~/Library/Caches/ms-playwright/chromium_headless_shell-1223/chrome-headless-shell-mac-arm64/chrome-headless-shell` + `--no-sandbox --disable-gpu-sandbox --disable-software-rasterizer --user-data-dir=/tmp/hs-prof-N` 可稳定截原型六屏（Google Chrome 无头会 SIGTERM/exit 137）；截本地 HTML 时加 `--hide-scrollbars --virtual-time-budget=6000`，否则图会来不及解码、窗口高度按内容留足；测像素用 `/Users/snow/.workbuddy/binaries/python/envs/default/bin/python`（PIL 12.3.0）。
