# 2026-09-19

## 服务人员小程序 W01／W02／W04 真实链路验证

- 目标：确认服务人员小程序已开发的任务功能能否完整跑通、每步接口是否通过、数据是否正确。
- 结论：**主链可完整跑通**。W01 只读 42/42、W02 写动作 62/62、W04 履约 104/105（唯一失败为刻意植入的缺陷探针）、端侧运行时结构校验 65/65。
- 环境：本地聚合后端 127.0.0.1:8080（`/api/api/...`）、Redis 127.0.0.1:16379、测试库 `gaia_wh_init_wzy`。无 mysql 客户端，用 `.workbuddy` venv 的 PyMySQL 直连。
- 会话：无可用账号密码，经用户授权直接向 `afs_service_personnel_session` 插入会话行（id=8，账号 2／人员 5／站点 5／企业 161，当日到期）。
- 夹具：自建 `W2TEST-20260919-*` 11 单（9509191001～9511，人员／站点 5）＋个人配件余额，含配套清理。
- 验证台已归档：`.workbuddy/verify/service-personnel-w01w02w04/`（含 README 说明运行顺序与三条纪律）。

### 踩过的坑（复用价值高）

1. **夹具必须每轮重跑**：`fixture_w2.py` 先清空 9509191xxx 区间再重建。沿用上轮数据会让草稿 revision、幂等请求号、执行事实残留，产生大批假失败。
2. **库存单据必须一起清理**：`afs_part_inventory_document` 唯一键 `uk_..._request` = `document_type` + `client_request_id`，而 `consumeParts` 的 `client_request_id` 由「工单号 + 请求号」派生。只删工单不删单据 → 下次完工提交撞唯一键 → `503 数据操作未完成`。**这不是业务缺陷**，一度被误判为事务问题。
3. **手工种数据后必须校准主键序列**：`GaiaIdGenerator` 走 Redis `ID_KE_<table>`，计数器小于 `max(id)` 时 fail-closed 抛 503「售后业务主键序列未同步」。夹具用固定高段 ID，所以必须跑 `calibrate_seq.py`。
4. 移动端路由前缀是 `/afterSales/mobile/service-personnel/tasks`，不是 `/service-personnel/tasks`；路径写错会得到 4001 未登录（误导性强）。
5. 列表响应字段是 `data.rows` 而非 `records`／`total`。
6. 单次 Bash 调用 2 分钟会超时被杀（exit 137），长验证一律用后台运行。

### 发现的业务缺陷

`DEF-20260919-01`：同工单用不同请求号重复调用签到／远程开始，服务端无「已有活动执行」判断，`ON DUPLICATE KEY UPDATE` 覆盖 `started_at`、清空 `ended_at` 并追加第二条审计。同请求号的重复调用已被幂等回放正确拦截，完工提交也有回放保护，故仅签到／远程开始受影响。

### 文档回写

- `specs/2026-09-18-服务人员小程序完整开发计划.md`：14.3 新增验证记录行；「完成内容」与「剩余事项」补充结论与缺陷。
- `03-开发管理/06-服务人员小程序开发进度.md`：更新日期、当前结论、模块概览、W01／W02／W04 行；新增 3 段叙述。
- 按项目规范**不计** W01／W02／W04 的真实实现与验收计数（研发验证不等于业务验收）；G3 不变，G4／G5 未达到。
- 遗留：`W2TEST-20260919-*` 夹具数据与会话行 id=8 保留在测试库待清理（清理命令见验证台 README）。

## 服务人员小程序：首页「正在服务」卡片多单展示优化

- 仓库 `mobile/gaia-customer-service-uni`，改动 `src/pages/task/index.vue` 与 `scripts/runtime.test.mjs`。
- 问题：`currentServices.length > 1` 时顶部卡片 `v-for` 平铺全部任务，客户名／地址／进度／两个按钮逐条重复，首屏被占满。
- 关键约束：后端 `ServicePersonnelTaskMapper.xml` 的 `excludeCurrentService` 让正在服务任务不进下方列表，顶部卡片是这些单据的唯一入口；`02-技术设计/13-服务工单状态机与单据状态说明.md` 第 79 行原规定「遇到多单，全部展示并保留各自入口」。
- 用户确认方案：主卡固定展示接口首条（服务端 `ORDER BY execution.started_at DESC`，即最近开始服务的一单），其余收进「其他 N 单处理中」折叠列表，展开后逐单保留「继续处理任务」「详情」。
- 实现：新增 `primaryService` / `otherServices` computed 与 `othersExpanded` 状态，企业切换重置时一并收起；新增 `.current-service__others*` 样式，展开条目复用既有 `.current-service__item` 次级样式。
- 验证：eslint、stylelint、`vue-tsc --noEmit` 全部通过；`node --test scripts/pagination.test.mjs scripts/runtime.test.mjs scripts/example.test.mjs` 30/30 通过（新增 6 条断言锁定"只渲染一条 + 折叠入口保留"）。
- 文档回写：`02-技术设计/13-...md` 第 79 行改为单卡 + 折叠规则；`specs/2026-09-18-服务人员小程序完整开发计划.md` 阶段 3 实施清单新增已完成条目。
- 未提交 Git（用户本次未要求 commit）。
- 遗留差异：`04-页面原型/小程序/worker.js` 的 `servingGroup` 仍是多单平铺聚合卡，本次只改小程序实现，原型未同步。

## 工作区全量提交（按修改功能分组）

- 授权范围：用户「按修改的功能，提交工作区所有修改」→ 只做 `git commit`，不含 push／发布。
- 本轮补交两组，加上此前三笔，共 5 个提交跨 3 个仓库：
  - `mobile/gaia-after-sales-uni` `5e19a45` `chore(miniapp): 同步双端共同底座（Node 26 工具链／根导航／列表无感刷新）`，19 文件（15 改 4 新）。
  - `docs/toto` `eacf096` `docs(toto): 客服工作台原型升级至 v0.17 统一连续办理`，15 文件。
  - 此前：`backend/gaia-after-sales` `bfc724d`、`mobile/gaia-customer-service-uni` `ccdcb26`、`docs/toto` `fb37bcd`。
- 提交后六个仓库（docs/toto、gaia-after-sales、gaia-saas-proj、gaia-ui、gaia-after-sales-uni、gaia-customer-service-uni）工作区均为 0 改动；docs/toto 领先 `origin/main` 两个提交，未 push。
- 为什么 `gaia-after-sales-uni` 也要提：它是与服务人员端共享底座的消费者端一半，`docs/shared-foundation.md` 记录的三项同步（Node 26 工具链、根导航滚动下沉、列表无感刷新）在两端必须等价；服务人员端那一半在 `ccdcb26` 已提交。
- 校验证据：`node --test 04-页面原型/后台系统/checks/*.test.cjs` 62/62；消费者端在 Node 26.7.0 下 `pnpm check` 全绿（类型／ESLint／Stylelint／租户／单元／路由／安全审计／派生文件）。

### 提交环境坑（复用价值高）

1. **本机 shell 没有 `pnpm`**：仓库 husky `pre-commit` 跑 `pnpm exec lint-staged` 会直接失败。改用 `PATH="/opt/homebrew/bin:$PATH"` 调用；该目录 node 是 26.7.0（正好匹配新 `.nvmrc`），pnpm 是 10.2.1。不要用 `--no-verify` 绕过。
2. **工作区里可能有并发 AI 代理**：`ChatGPT.app/…/codex sandbox` 进程对 Gaia 下多数仓库有写权限，会留下 `index.lock`，git 打印 `unable to unlink …: Operation not permitted`。先用 `pgrep -fl git` / `lsof` 确认无进程持有，确认是遗留锁再 `rm -f`，并与 `git add` + `git commit` 写在同一条命令里（否则 IDE 后台刷新会立刻重建锁）。
3. **doc-impact 检查用本地文档就能过**：`scripts/docs-impact-map.json` 里每个仓库的 `localDocumentationPatterns` 都含 `docs/**`，所以小程序仓库只要同任务改了 `docs/shared-foundation.md` / `docs/architecture.md` 等即算证据，不必额外改 docs/toto。纯文档改动（`04-页面原型/**`）不触发任何代码仓库影响。
4. 提交信息一律用 `git commit -F <文件>` 传入，避免中文和引号在 shell 里被吞。
5. 分组原则：同一「功能」的改动放一个提交；本工作区里「服务人员任务」与「客服后台原型」是两条互不相干的线，必须分开提交。
