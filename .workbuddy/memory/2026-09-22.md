# 2026-09-22 工作日志

## 服务人员小程序工作区按功能分批提交（跨仓）

- 授权：用户“提交工作区所有修改，根据修改的功能分批提交”。只提交、未 push、未发布、未改数据库。
- 改动分布：`mobile/gaia-customer-service-uni`（5 个功能）、`backend/gaia-after-sales`（1 个接口切片）、
  `docs/toto`（进度＋Spec 同步）。`gaia-ui`、`gaia-saas-proj`、`gaia-after-sales-uni` 无改动。

### 提交清单

| 仓库 | 哈希 | 提交 |
| --- | --- | --- |
| gaia-customer-service-uni | `d42b518` | feat(miniapp): 新增任务产品资料页与受控文件下载 |
| gaia-customer-service-uni | `1b5297b` | feat(miniapp): 任务详情按业务分卡并接通记录与资料入口 |
| gaia-customer-service-uni | `71a4b7d` | fix(miniapp): 收拢处理任务概览并串行化草稿自动保存 |
| gaia-customer-service-uni | `663cc94` | refactor(miniapp): 统一配件记录页签与配件单详情层级 |
| gaia-customer-service-uni | `a54a258` | docs(service-mini): 沉淀业务详情与表单卡片复用规则 |
| gaia-after-sales | `4671901` | feat(after-sales): 服务人员任务提供商品档案手册列表与受控内容 |
| docs/toto | `3538403` | docs(toto): 记录服务人员端资料与配件页面优化 |

### 验证（本次实际执行）

- 服务人员端 `pnpm check`：租户 36、单元 50、路由 5、安全 12 全通过；提交后在工作区复跑一次仍全绿。
- 后端 `mvn -pl gaia-after-sales-core,gaia-after-sales-api -am -Dtest=... test`：15/15
  （AppService 8、Mapper 6、HTTP 路由 1），surefire-report mtime 确认为本次运行。
- `check-doc-impact.mjs` 三次均通过：做法是先把 `docs/toto` 三个文档 `git add`（不提交）再提交代码仓。

### 事实修正

- `mobile/gaia-after-sales-uni` 与 `mobile/gaia-customer-service-uni` 均在
  `feature/20260907_蓝鲸售后服务_志远_喆宇` 且有 codeup `origin`，不是 main／无远端。
- 后端 Maven 是 keg-only：`/opt/homebrew/opt/maven/bin/mvn`，`mvn` 不在 PATH；JDK 21。
- 一个测试文件跨多个功能时（`scripts/runtime.test.mjs` 同时断言任务页与配件页）用 hunk 级
  `git apply --cached` 拆分；剩余 hunk 编号会在每次提交后前移，必须重新取 diff。

### 未完成 / 待办

- 未 push、未发布共享宿主；真实手册文件打开、微信开发者工具复看与真机验收未做。
- 共享库 `afs_purchase_item.product_id`／`afs_product_manual_file` 缺列时新接口仍会失败；
  配件记录页的历史缺列问题未解。
