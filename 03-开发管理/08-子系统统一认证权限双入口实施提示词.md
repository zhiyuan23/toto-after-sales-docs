# 子系统统一认证权限双入口实施提示词

- 日期：2026-09-09
- 状态：方案已确认；以下提示词用于新对话规划并实施，不代表功能已经完成。
- 权威方案：[独立前端改造方案第 5 节](../02-技术设计/07-gaia-ui售后子系统独立前端改造方案.md#5-登录权限与租户)。

复制以下内容到 TOTO 工作区的新对话：

```text
请在当前 TOTO/Gaia 工作区规划并实施“售后子系统统一认证权限双入口”。先调查源码并给出简短实施计划，然后持续完成代码和必要验证，不要只交付计划；普通适配细节按现有项目惯例自主推进。

一、已确认目标
主系统和售后子系统各自提供登录、用户、角色及授权管理入口，但完全使用蓝鲸数字 Gaia 同一套认证权限后端、同一份用户角色权限数据。任一端创建用户、角色或调整授权，另一端可以查询和管理，不建设第二套账号权限数据库或数据同步任务。
子系统可以不打开主系统页面，独立完成登录和管理。子系统权限管理入口可配置关闭，关闭时仍可登录使用业务，用户角色由主系统集中管理。配置开关不能授予权限；后台授权规则保持统一。

二、先读与核对
工作区：/Users/snow/Documents/Ehsure/Gaia/docs/toto。
先读 README.md、03-开发管理/00-开发进度与下一步.md、02-技术设计/07-gaia-ui售后子系统独立前端改造方案.md，重点第 5.5～5.9 节，以及适用 AGENTS.md。
目标：/Users/snow/Documents/Ehsure/Gaia/frontend/gaia-ui/apps/after-sales。
只读来源：/Users/snow/Documents/Gaoge/gaoge-compass/apps/admin。
核对主系统当前登录、会话、用户角色管理实现，并从源码定位对应 Gaia 管理后端；不要猜接口路径、权限编码或数据结构。操作各仓库前检查分支、远端差异和未提交改动，完整保留已有工作。

三、前端复用要求
登录与权限管理的前端形式完全沿用 gaoge Admin 底座，优先直接复用 gaoge-compass 已有页面、布局、组件、样式和交互，只在 Gaia 接口或权限语义不能兼容处微调，不重新设计 UI。
重点来源：
- src/views/login.vue
- src/views/system/user/workspace.vue 及其 model
- src/views/system/role/workspace.vue 及其 model
- src/views/system/components/ 及相关样式和必要依赖
先列出“来源页面/组件 → Gaia 接口/字段/权限 → 必要调整”的对照，再实施。
继续使用子系统现有原生组件和 hooks，不复制主系统 commonV2，不引入第二套公共组件；保留蓝鲸数字售后服务名称、Logo、head 导航及导航模式切换按钮。
不复制 Compass 后端、JWT、Resource RBAC 语义、mock、权限种子或内置权限同步逻辑。来源权限编码不能直接用于 Gaia 授权。来源有而 Gaia 无对应能力的动作，不做虚假成功或空壳按钮。

四、认证与权限要求
1. 主、子系统登录均使用 Gaia 现有认证契约。当前子系统读取同源 localStorage.token 并使用原始 Authorization 请求头，不要按旧文档误改成仅 Cookie 或 Compass Bearer JWT；实施前再次核对当前源码。
2. 完成当前用户、登录失败、退出、会话过期、安全的站内返回地址及 Gaia 已支持的验证码/多因子流程。不增加未经确认的新登录方式。
3. 同源已有登录态可进入子系统；切换账号或退出时清除旧用户菜单、权限与业务缓存。不得通过 URL、Hash 或 postMessage 传递凭据和授权身份。
4. 登录入口选择、管理入口开关与业务展示模式独立管理。独立模式支持子系统自身登录；已有嵌入模式复用主系统会话，避免 iframe 内套登录页。三模式尚缺的整体集成不自动扩大成本次全部实施范围，明确未验证项。
5. 子系统增加独立的管理菜单和路由注册，按 Gaia 真实管理授权显示；不要把全局管理权限强塞入售后业务子树或增加业务入口统计。
6. 核对并实现用户、角色、用户角色分配、角色授权的现有可用操作；账户启停、密码重置等按 Gaia 既有接口和保护规则复用。资源目录定义 CRUD 不是本次默认新增范围。
7. 管理员可维护的用户、角色、可授予权限与租户范围必须由后端限制，不能仅靠前端过滤。默认聚焦售后范围，全局管理员能力沿用 Gaia 既有授权。
8. 编辑只展示售后权限的共享角色时，必须保留未展示的其他模块权限；核对保存是否为全量替换，采用有后端范围约束的增量修改或安全合并，不覆盖其他模块授权。保护现有管理员与共享角色。
9. 验证权限变更后的后端缓存失效和会话生效时点；撤权后后端拒绝旧权限操作，前端重新加载授权，不能只靠菜单隐藏。
10. 正式接入恢复子系统真实菜单、路由和按钮授权，移除默认全菜单预览状态；管理路由和接口绝不能被开发预览开关绕过。接口失败或未知权限一律拒绝，保留清晰重试体验。

五、后端与运行边界
优先直接复用 Gaia 现有管理接口和权限逻辑；发现接口或管理范围缺口时，可以实施本任务必要的最小兼容修改，遵循对应仓库规范，不建设新的认证中心、不复制表或重做权限模型。跨仓修改记录职责、契约和验证证据。
保持主系统原页面和 API 兼容，不为此重做售后业务页面。来源 gaoge-compass 只读，受保护 src/views/common 不得修改。
保持现有本地启动、/after-sales/、/api 和统一构建方式；VPN 地址 http://10.1.1.53:8011/ 是数据库相关入口，不是前端 API 代理地址，不据此修改原本地后端启动配置。
本次不拆服务、不拆库、不部署独立域名；未来独立后端部署和跨域 SSO 只记录依赖，不提前建设。
不修改共享环境账号、正式授权或数据库结构，不部署，不 commit/push。测试写入仅使用明确隔离环境和测试账号。当前授权不包含正式环境管理操作。

六、实施与验收
按“接口和复用清单 → 登录闭环 → 用户角色授权 → 入口开关与真实权限 → 双端验证”逐步完成。重大且无法从代码推断的权限语义缺口再询问；局部环境阻塞时继续其他可完成项，不用 mock 冒充完成。
运行与改动相关的类型检查、构建和必要测试，并验证：
- 不加载主系统 HTML，子系统能登录并进入已授权业务。
- 主系统创建/授权后子系统可使用，子系统创建/授权后主系统可管理，同一记录标识，无复制数据。
- 普通用户不能访问管理页面或直调管理接口；管理员仅能操作其授权范围。
- 关闭管理入口后菜单消失、直达被拒绝，普通登录和业务不受影响。
- 任一入口撤权后后端拒绝旧权限操作，两端展示正确更新。
- 跨租户、超范围授权、共享角色隐藏权限保留、账号切换和会话失效。
- 已有商品分类、商品档案及其他当前已实现页面的必要回归；两端请求协议保持兼容。
隔离验证与真实共享环境验证分别记录，缺少账号或环境时明确未验证，不把构建通过当作权限验收。
不要停止用户仍需使用的现有主/子前端或隔离测试服务；只清理本次自行启动且明确不再需要的临时进程，交付时说明保留的服务和访问地址。

七、交付
更新原技术方案和进度入口，记录实现状态、接口对照、复用与微调内容、后端是否改动及原因、配置位置、验证证据、阻塞和独立部署尚存依赖。不新增平行进度台账，不访问或维护外部知识库。
最后简述完成内容、实际验证、访问与配置方法及未完成项。没有证据的能力不得标记完成。
```
