# 服务人员小程序开发进度

- 更新日期：2026-09-21（阶段 3 已达到 G3；任务改为小程序首页，有效会话直接恢复任务页，匿名或会话失效时由页面门禁跳转登录；任务／配件保留透明展开到上滑白底紧凑的自定义导航，紧凑标题按视口居中，该能力已同步进入双小程序共同底座；“我的”改用默认白底标题导航，内容继续按原型呈现开放式身份区和账号操作组。本地开发继续复用 8080 聚合后端，日程和地图延期；变更后微信逐页复核待执行。2026-09-19 已用专用移动会话在 `gaia_wh_init_wzy` 完成 W01／W02／W04 真实 HTTP 与数据库对账，主链可完整跑通，并发现 1 个签到缺陷 `DEF-20260919-01`；2026-09-20 新增私有证据接口切片）
- 系统定位：服务人员用于接收任务、联系顾客、上门服务、配件处理、资料查询和账号操作的小程序；原截图为“任务、配件、应用、我的”；2026-09-13 原型按用户方向调整为“任务、配件、我的”，资料查询归我的，配件记录归配件，详见[服务人员 v0.2 原型](../04-页面原型/小程序/README.md#服务人员-v02-怎么体验)。
- 当前结论：**4 个模块、16 个入口；已有真实实现 0 / 16，最终验收 0 / 16。服务人员端即“TOTO 客服助手”，阶段 0～3 已依次达到 G0～G3；阶段 4 的 W02 写动作和阶段 5 的 W01 当前服务／W04 履约闭环已形成前后端源码切片。`20260918_service_personnel_task_write.sql` 与 `20260918_service_personnel_task_execution.sql` 已于 2026-09-19 在 `gaia_wh_init_wzy` 执行，结构、权限和四项一致性检查通过。此前以专用移动会话完成 W01／W02／W04 真实 HTTP 与数据库对账：W01 42 / 42、W02 62 / 62、W04 104 / 105；失败的重复签到探针 `DEF-20260919-01` 本轮已修源码并通过定向测试，待真实宿主回归。真实上传、相机／相册／扫码、真机和完整 UI 仍未验，G4／G5 未达到，W01／W02／W04 不提前增加真实实现或最终验收计数。**

2026-09-21 登录页完成二次精简：页面收口为略高于几何中线的单一登录卡，仅保留 TOTO／客服助手品牌识别、账号与密码输入框及主按钮，移除“账号登录”、说明文案、可视字段标题、品牌副标和页脚；登录按钮使用独立操作区与密码框稳定分隔，不再依赖组件根节点外边距。表单校验和接口失败统一使用工程 `Toast`，不再以行内错误文案改变布局高度。`pnpm check` 通过 35 项租户测试、45 项单元测试、5 项路由测试和 12 项安全策略测试，`pnpm build toto`、`pnpm build:h5 toto` 均按默认 test 环境通过；H5 产物实际打开检查通过，微信开发者工具 Stable 2.02.2608060 的 iPhone 15 Pro Max 模拟器已确认上移后的布局、安全区、按钮间距和空账号 Toast。该改动仅涉及服务人员端独立登录页，不同步消费者端；真机和系统深色模式目视复核仍待执行，不改变 G3 或 W01～W16 计数。同日确认测试服务器缺少 `after-sales.mobile.single-tenant-id=1001`，这是登录返回 `403 / 缺少有效租户上下文` 的直接原因，待明确授权后修改外部配置并重启测试 Tomcat。

同日继续收敛服务人员端移动交互：任务首页普通任务卡移除描边并保留轻阴影；处理任务、任务详情、退回补资料及领退料固定底栏的安全区外附加留白由 40rpx 收紧为 16rpx。W04 图片选择回到页面时不再触发服务端草稿覆盖本地临时图，处理页和补资料页上传状态改用响应式列表替换，已选图片可立即显示并持续显示到离页。产品与客户确认区按用户确认只保留非必填安装码，移除产品码、RFID、差异说明及扫码／核验入口；服务端取消安装／维修完工的安装码必填，仅校验已填写值是否属于当前工单。客户确认情况与确认记录／例外原因在点击“核对并准备提交”时即原位校验，未通过不进入下一步。端侧 `pnpm check` 通过 35／45／5／12 项，微信／H5 development 构建通过；后端 W04 定向测试 10／10。未执行数据库、真实上传、微信开发者工具或真机验证，G3～G5 和 W01～W16 计数不变。

2026-09-20 与消费者主流程接续核对：`sunzhiyuan` 账号对应本人当前站人员 `5`，有安装与维修技能；9 月 20 日请假、9 月 21～25 日可用。现有固定消费者指导单在北京且仍待核验，不能派给该人员。服务人员 H5 `9527` 已建立孙志远本人会话，“我的”身份与本站任务真实读取通过；并发出现的另一消费者已派维修单未纳入本次固定身份联调。W04 照片已改接按本人任务和 Web 审核范围鉴权的私有上传／读取接口，同图重试与跨人员、跨租户拒绝及 HTTP 错误契约定向测试 3 / 3 通过，8080 新制品已启动；真实媒体上传、完工和审核仍待同单联调。服务人员端 `pnpm check`、微信及 H5 development 构建通过；完整入口与最终验收继续为 **0 / 16**。目标环境条件和逐单对账见[跨端联调 Spec](../specs/2026-09-20-消费者至服务人员安装维修主流程联调.md)。

同日独立合成消费者安装单 `SO-940918110038` 的购买仅做明确标注的模拟核验；受理请求虽遇 MySQL 临时失联，恢复后回读证实已进入待派单，派站 `5` 成功。当前 AI 策略自动选中人员 `2` 李二雷而非人员 `5` 孙志远，工单为 `PENDING_COMPLETION`；孙志远 H5 本人任务搜索该单返回 0 条，隔离正确。已确认服务站首期禁止改派，旧客服派人权限被主动停用，未绕过权限或改库人员列。孙志远履约、真实照片上传与完工审核仍未完成，另一消费者既有已派维修单没有被操作。W01／W04 完整入口与业务验收计数不变；后续取舍见[跨端联调 Spec](../specs/2026-09-20-消费者至服务人员安装维修主流程联调.md#2026-09-20-合成数据目标库联调)。

W04 H5 合成任务复核还发现“从相册选择”调用 H5 不支持的 `uni.chooseMedia`，页面直接报错；现将处理页和补资料页统一到平台图片选择器，H5 改用 `uni.chooseImage`，微信继续用 `uni.chooseMedia`。H5 热更新后已能打开文件选择器；浏览器扩展拒绝访问本地合成 PNG，因此实际文件上传与私有预览仍未取证。端侧检查、微信／H5 构建的最终结果见[跨端联调 Spec](../specs/2026-09-20-消费者至服务人员安装维修主流程联调.md#w04-私有服务证据接口切片)；W04 完整入口和业务验收计数不变。

2026-09-19 W05～W10 已进入源码实施：服务人员端新增真实配件 API、库存与领退料页面及记录入口；售后后端新增移动专用门面、独立库存权限、用途字段及领退料申请人来源标识，宿主 WAR／Jar 仅为移动路径做窄放行；W04 改为从本人库存选择实际耗用。现已按授权仅给开发环境当前移动账号（account_id=2）开通 `MOBILE_INVENTORY_READ`／`MOBILE_INVENTORY_WRITE`，微信开发者工具实际看到本人库存与详情、本站领料选择和详情预选退料；正式库存与工单读取请求成功。`20260919_service_personnel_parts.sql` 的结构变更**尚未执行**，记录页因缺少 `mobile_applicant_account_id` 列加载失败，领退料提交、撤销及管理员审批全链不可视为可用；双方库存对账、跨范围拒绝、同视口原型截图和真机仍待验证。因此 G6 未达到，入口计数仍为 **0 / 16**。跨站人员共用个人余额的占用改动及隔离验证见下方本轮复核，尚无真实 MySQL 并发证据。

本轮复核（2026-09-19）：W04 的 `DEF-20260919-01` 已在源码增加活动执行门禁，保持同请求幂等回放、拒绝换请求号重复开始；现场与远程定向测试均通过，真实宿主尚未重启并重跑缺陷探针。个人库存的处理中退料占用改为按人员跨站求和，移动申请、Web 退料申请／审批、W04 首次完工耗用共同在余额锁后校验；当前站单据列表仍只返回本人在当前站的记录。后端配件定向测试 17/17、W04 定向 9/9、API/core 编译通过，服务人员端 `pnpm verify` 通过（35 项租户、44 项单元、5 项路由、12 项审计策略及微信／H5 构建），原型检查 74/74。只读 JDBC 预检确认共享库 `gaia_wh_init_wzy` 仍缺 `purpose`、`mobile_applicant_account_id`，配件单据 6 行、领／退料 0 行，库存读写权限仅账号 2；后台领／退料菜单尚缺精确审批 POST 授权。迁移与专用测试数据各需单独授权，均未执行。正式原型本地地址被浏览器安全策略拒绝，逐状态同视口视觉对照待可访问的正式预览；微信开发者工具仅完成部分真实读路径。W04 证据上传当前落在 Gaia 后台认证路径，移动会话不能完成上传；涉及公开文件类型的候选方案被自动审批审查拒绝，需确定私有文件的工单归属鉴权方式。详见[本轮差异矩阵](../specs/support/2026-09-19-service-personnel-gap-matrix.md)、[原型状态验收矩阵](../specs/support/2026-09-19-服务人员小程序原型状态接口验收矩阵.md)和[迁移预检与恢复方案](../specs/support/2026-09-19-服务人员配件共享库迁移预检与恢复方案.md)。G4～G6 和 0/16 计数不变。

微信开发者工具本轮只读复核：iPhone 15 Pro Max 模拟器的现有服务人员会话看到本人库存 4 件／1 种，配件详情本人 4 件、本站 236 件、可退 4 件；从详情预选退料 1 件，搜索无结果仍保留并可移除已选项；本站领料选择两项后切常备用途，工单字段隐藏、补充原因显示。记录页及“领料”筛选重试均重新请求 `GET /parts/documents`，HTTP 为 200 但业务响应失败，页面仍为加载失败。以上仅是微信读路径、前端交互和错误重试证据，没有提交或改动库存；具体路径与观察见[原型状态验收矩阵第 4 节](../specs/support/2026-09-19-服务人员小程序原型状态接口验收矩阵.md#4-微信开发者工具本轮只读走查2026-09-19-23042308)。

2026-09-18 已完成[完整开发计划阶段 0](../specs/2026-09-18-服务人员小程序完整开发计划.md#6-阶段-0基线收口)并达到 G0：登录底座 `pnpm verify` 通过，三 Tab、处理任务三段、无二次交单和微信内置地图边界已与路由设计统一，27 个原型屏幕完成路由／API／权限／验收追踪，测试身份和最小数据需求已列明。该结果不增加 W01～W16 真实实现或业务验收计数；后端全量基线中的既有失败和真实账号／接口／真机未验证项见计划第 14.3 节。

2026-09-18 阶段 1 已完成源码、自动化和共享测试库真实验证：Web 新增服务人员表单必填小程序登录账号与初始密码，账号在同一后端事务内通过 `personnel_id` 稳定绑定；移动会话、上下文查询、服务端企业切换和退出均不复用 Gaia 登录；端侧在进入受保护页前解析身份，并具备错误态、草稿守卫、旧企业清理注册器、账号/企业缓存和迟到响应隔离。设计与验收证据见[服务人员移动身份与企业上下文](../02-技术设计/15-服务人员移动身份与企业上下文.md)。经授权执行迁移并创建专用账号后，**G1 已达到**；该阶段完成时尚未进入阶段 2，后续阶段 2 结果见下文。

同日追加本机联调：小程序 `dist/dev/mp-weixin` 指向 `http://127.0.0.1:18080`，请求命中本机阶段 1 Controller。宿主启动时发现并修复了事务服务 JDK 代理按具体类注入的问题，并为单租户本地运行增加无默认值的显式租户 ID；后端定向测试为 12 项。经授权在 `gaia_wh_init_wzy` 新增 4 张移动身份表，通过后台创建专用人员 `SP-G1-20260918-01` 及账号，真实完成登录、上下文恢复、两企业切换、过期／越权拒绝、Web API 隔离和退出失效。端侧真实联调另发现登录响应误读 `data.id` 导致账号分区不一致，已改为强制读取 `data.accountId` 并补自动化；普通编译后会话可恢复到受保护首页，控制台 0 个业务错误。

阶段 2 在不进入业务切片的前提下，将正式导航收敛为任务、配件、我的三个受保护 Tab，只增加任务详情和处理任务两个首批分包路由；任务、配件及任务分包页明确提示真实业务待后续阶段接入，不使用 Mock 或 fixture 冒充实现。“我的”展示阶段 1 真实人员／企业／服务站上下文，并原位承接已有企业切换和退出底座。任务、配件根页的自定义导航按窗口安全区和微信胶囊动态计算；加载、空态、错误、无权、离线、会话失效及未开放反馈统一复用 `AppState`。定位、相机／相册、扫码、上传、拨号和 `openLocation` 探针只进入条件编译示例分包。双小程序 `pnpm check`／`pnpm verify`、服务人员端探针示例构建及微信开发者工具普通编译均通过，**G2 已达到**。

同日完成阶段 3 前公共 UI 与样式基线收口：确认的 `#2B74AA` 深蓝、`#F6F7F7` 画布、白色表面、双模式状态色、4／8 间距、圆角、触控和焦点规则已转为可执行语义变量与规范；三个 Tab 复用页面容器，任务／配件未开放面板和“我的”操作组复用最小表面，`RootNavigation` 保持动态安全区／胶囊避让，`AppState` 七态补齐矢量图标和可访问语义。公共 UI 预览仅存在于 `FOUNDATION_EXAMPLES`，不进入正式导航和正式构建；未创建任务卡、任务筛选器、业务状态机、表单、媒体或固定底部操作栏。H5 已在四类视口、明暗模式、长文本和七类反馈态下实际走查，控制台无错误或警告；微信开发者工具普通编译确认任务页胶囊避让与状态面正常，0 错误且无业务／组件警告。本轮仍不计任何 W 入口实现。

随后进入阶段 3：后端新增 `/api/afterSales/mobile/service-personnel/tasks` 列表和详情门面及独立 `MOBILE_TASK_READ` 权限，查询强制使用会话解析的人员和当前服务站，按待处理／待补资料／审核中／历史互斥分类返回计数、分页、产品、沟通和进度；列表与直达详情使用同一范围。端侧任务 Tab 已实现搜索、今日／缺件筛选、分页、空态／错误／无权／会话失效、任务卡和拨号／复制，详情只读展示预约、顾客、服务内容、沟通和处理进度，不提前开放阶段 4 写动作。WAR／Jar 两种宿主均仅对白名单追加任务列表和详情路径，请求到达后仍由售后移动会话及任务权限校验。服务人员端 Node 24.19.0 下 `pnpm verify` 通过；移动认证、上下文、任务应用服务和真实 MyBatis Mapper 共 15 项定向测试通过；售后 API 安装及 `gaia-saas-web`、`gaia-saas-web-jar` 构建成功。

本轮经明确授权在 `gaia_wh_init_wzy` 执行 `20260918_service_personnel_task_read.sql`，影响 2 个既有移动账号；数据库复核 `accountId=2` 已持有 `MOBILE_TASK_READ`。随后先只读确认 `afs_order_communication` 不存在，并核对 `20260910_order_communication.sql` 只创建该表及其主键、唯一约束、外键和沟通时间索引；获授权后执行 DDL（业务影响行数 0），再执行 `20260918_service_personnel_phase3_g3test.sql`，精确新增顾客 1、购买登记 1、登记明细 8、商品实例 8、工单 8、工单产品 8、状态历史 8、完工提交 3、审核 2、沟通 8。清理脚本固定为 `20260918_service_personnel_phase3_g3test.cleanup.sql`，本轮未执行。

用户在微信开发者工具中自行输入密码建立 `sunzhiyuan` 会话后，真实 `context` 返回 `accountId=2`、`personnelId=5`、`serviceStationId=5` 和 `taskRead=true`。四分类为待处理 2、待补资料 1、审核中 1、历史 1；可见任务为 `G3TEST-20260918-TODAY`、`MISSING`、`SUPPLEMENT`、`REVIEW`、`HISTORY`。任务号搜索、可见虚构联系人／地址搜索、今日／缺件筛选、`pageSize=1` 的第 1／2／3 页与空结果均通过；五条详情的产品、沟通、状态历史、提交和审核进度均逐页核对。`9309181006`（同站其他人员）、`9309181007`（当前人员其他站点）和 `9309181008`（未正式分配）不在列表中，猜测详情均返回 Gaia 响应 `code=404`，微信页显示“记录不存在或不在当前数据范围内”。联调同时修正任务 API 异常处理范围，避免上述业务 404 落入全局 500。

微信开发者工具 Stable 2.02.2608060 普通编译后，列表、四分类、筛选、任务卡、详情、空态、实际超时错误态和越权无权态均完成逐屏走查；列表和详情延续 `#2B74AA`、`#F6F7F7`、白色表面、4／8 间距、语义状态色、44px 操作区和动态安全区／胶囊避让，与确认原型的信息顺序和分组一致，iPhone 15 Pro Max 模拟器未见横向溢出。数据库没有签到、远程开始或正式服务草稿，页面没有展示或伪造“当前服务”。由此 **G3 已达到**。用户在可见任务详情页主动点击拨号和复制入口均正常；关闭开发者工具拨号面板后控制台记录一条 `makePhoneCall:fail cancel`，按取消处理，未把拨号面板视为联系成功，也未新增联系结果。无真机拨号证据；微信系统深色模式仍只有语义令牌和自动化证据，未完成开发者工具系统切换实测。

同日根据用户指定的最新原型 `index.html?consumerVersion=0.13#w-tasks` 回归已开发页面：任务概览改为原型的横向摘要，搜索、分类和筛选收紧间距；任务卡隐藏原型未展示的工单号、手机号、服务方式和最近沟通，仅保留状态、服务类型／队列、预约时间、顾客、产品、地址和下一步；详情恢复“状态提示 → 任务摘要 → 服务安排 → 产品／问题 → 记录”的轻卡片层级，不再展示手机号和阶段说明，只保留原型已展示的拨号操作；“我的”恢复开放式身份区和右侧切换企业轻入口。服务端的 `copyMobile` 兼容动作未删除，只是按原型隐藏端侧入口。Node 24.19.0 / pnpm 10.34.5 下 `pnpm verify` 通过，包含 34 项租户、24 项运行时、5 项路由、12 项安全策略测试及微信／H5 development 构建；本轮不改变 G3、入口计数或业务验收状态，变更后的微信开发者工具逐页视觉复核仍待执行。

同日按最终确认进一步收口：首页概览卡新增“今日预约／等待配件”两个数量入口，后端列表响应增加独立 `secondaryCounts`，端侧不从当前分页结果估算；点击入口会切回待处理、清空搜索并应用 `TODAY` / `MISSING_PARTS`。原型目录删除服务人员旧版截图、并排预览、日程和地图实现与专项检查，所有入口文档均明确日程／地图延期。当前原型回归 73 / 73、后端任务计数定向测试 6 / 6、服务人员端 `pnpm verify` 及微信／H5 development 构建通过；变更后的微信开发者工具视觉复核仍待执行。

同日继续收口根导航和 TabBar：任务、配件、我的均新增 81×81 透明 PNG 普通／选中图标；“我的”与前两个根页统一使用自定义导航。三根页在顶部保持无白底的展开眉题／标题，内容上滑超过 24px 后切换为白色内容面和“任务／配件／我的”紧凑标题，回到顶部后恢复展开态。双小程序路由生成器同步支持可选 Tab 图标元数据，具体图标与根导航只作用于服务人员端。原型回归 74 / 74；双端 `pnpm verify` 通过，服务人员端为 34／25／5／12 项，消费者端为 34／19／4／12 项，微信／H5 development 构建均通过；微信开发者工具与真机视觉复核待执行。

2026-09-19 按最新确认更正应用启动与导航：`pages/task/index` 移到主包路由首位，已有有效会话启动后直接进入任务，仅在匿名／会话失效时携带安全回跳目标进入登录。任务／配件的自定义导航保留展开态，滚动白底紧凑态的“任务／配件”按视口中线居中，不再受右侧微信胶囊留白影响；“我的”移除自定义导航和滚动切换，恢复小程序默认白底“我的”标题栏。两端共同底座同时将日常 Node 版本更新为 26.7.0，引擎范围统一为 `>=24.19.0 <27`，`doctor` 改为读取该范围，未修改 `gaia-ui`。原型 74 / 74；Node 26.7.0 下双端 `pnpm verify` 通过，服务人员端为 35／27／5／12 项，消费者端为 35／19／4／12 项，微信／H5 development 构建及双端 `doctor` 均通过。本轮不增加业务页或改变 G3／入口计数，微信开发者工具和真机视觉复核待执行。

同日继续优化 W04 处理任务：页首工单上下文改为独立卡片，服务证据按原型收敛为图标／状态／单一添加入口的材料行，产品与客户确认卡明确拆分产品核对、客户确认两个分区并补齐字段标签；正式接口没有购买渠道／购买日期时不填充原型 Mock。定位失败根因确认包含微信位置权限声明缺失，以及端侧把 `uni.getLocation` 的对象错误吞成通用“开始服务失败”；现已补充 `scope.userLocation`、`requiredPrivateInfos.getLocation`，失败时原位展开定位例外说明并显示权限／备用操作提示。H5 已核对待签到和现场进行中证据页，空定位例外请求由后端以 422“无法定位时必须填写到达说明”拒绝且工单未变化；真实微信授权位置、真机签到、相机／相册和扫码仍待验证，不改变 G4／G5 与入口计数。

同日将根导航滚动切换正式下沉到双小程序共同底座：两端等价持有 `RootNavigation`、动态安全区／微信胶囊计算、24px 紧凑态阈值和 `useRootNavigationScroll`。首次实现将 `onPageScroll` 完全放入 composable，微信编译器因页面源码中没有直接生命周期调用而未生成 `__runtimeHooks`，导致滚动切换失效；现已改为公共 composable 返回 `handlePageScroll`，任务／配件页只保留 `onPageScroll(handlePageScroll)` 编译桥接，阈值、状态更新和样式仍不在页面重复。微信构建产物已确认两页均包含 `onPageScroll` 与 `__runtimeHooks=1`。消费者端本轮不改变业务路由，但后续启用自定义标题栏时必须复用同一组件、handler 和页面桥接方式。Node 26.7.0 下双端 `pnpm verify` 通过，服务人员端为 35／30／5／12 项，消费者端为 35／25／4／12 项，微信／H5 development 构建均成功。本项不改变 G3 或入口计数，微信开发者工具和真机视觉复核待执行。

同日将任务列表刷新收敛为保留已有数据的后台刷新：下拉、重新进入任务 Tab 和从详情返回时不再先清空列表，成功响应后一次替换；分类、今日／缺件筛选和关键词按查询键隔离缓存，未命中时才进入首次加载态。刷新失败保留旧数据并轻提示，企业切换继续清空全部查询缓存，分页加载仍使用独立尾部状态。共同 `usePagination`、自动化及开发约束已同步消费者端；双端 `pnpm check` 通过，服务人员端为 35／30／5／12 项，消费者端为 35／25／4／12 项，服务人员端微信／H5 development 构建成功。本项为 W01 交互优化，不改变 G3 或入口计数；微信开发者工具和真机交互复核待执行。

同日收敛本地启动链：服务人员端不再长期独占 18080，`pnpm dev TOTO`／`pnpm dev:h5 TOTO` 默认使用 `http://127.0.0.1:8080/api`，与 Web 和消费者端共同调用公共启动器。启动器通过 Web 匿名响应与移动会话响应判断统一宿主能力，使用启动锁避免并发重复装配，健康后端直接复用；共享后端独立于任一前端常驻。服务人员端完整 `pnpm verify` 通过（35／25／5／12 项及微信／H5 development 构建），实际 `pnpm dev TOTO` 复用现有 JVM，产物基址正确，停止监听后后端仍运行。历史 18080 联调证据保留为当时时点，不再是当前启动口径。本项不改变 G3 或 W01～W16 计数。

同日为工单状态页面视觉走查新增并执行 `UITEST-20260919-*` 固定测试数据，共 9 单，覆盖普通现场、远程、断联、现场／远程进行中、多项审核退回、待回访、已关闭和已取消；审核中继续复用既有 `G3TEST-20260918-REVIEW`。在 430×932 H5 视口逐页对照定稿原型后，任务详情移除默认铺开的九宫格操作区，改为服务信息优先、低频动作按需展开和固定底部主操作；联系／改约按原型拆分日期、开始时间和结束时间，详情内子流程及审核进度使用对应动态标题；退回详情去除重复提示与重复按钮，补充项改为中文；补充资料页补齐工单上下文、定向补充说明、原记录入口和固定提交栏；进行中任务改为中文步骤／序号、详情入口、三段编号步骤器和固定推进按钮。任务流按钮同时统一为 flex 垂直居中，主操作使用 15～16px 等效字号、较大圆角和不少于 50px 的触控高度，并扩大按钮与正文、底部安全区的间距；时间线和草稿恢复时间改为可读格式。浏览器已完成任务详情原型／实现同尺寸并排比较，以及联系、处理、补资料、审核页面交互检查，控制台无业务错误，`design-qa.md` 结论为 `passed`。服务人员端 `pnpm check`、微信／H5 development 构建全部通过；该证据属于研发环境视觉核对，不替代微信开发者工具、真机或业务验收，暂不改变 G4／G5 和入口计数。

同日继续优化任务首页和处理页的加载与层级：首页在会话及服务人员权限上下文尚未恢复时先显示中性加载态，权限判定完成后才进入任务内容或明确无权态，避免首次加载短暂误显“暂时不能查看任务”。参考用户提供的订单页截图，搜索框固定在一级任务状态上方，两者组成与顶部状态卡同宽的任务面板吸顶头部；两类外层卡相对默认内容区向两侧扩展，保留约 16rpx 页面边距，任务白卡在面板内再缩进 16rpx。一级状态名称与数量横向排列，内部任务白卡移除描边，只保留白色表面和轻阴影。移除一级状态下方的二级 Tab，以及“全部几单／其中几单置顶”统计行，面板从白色自然渐变到浅灰列表背景。有正在服务任务时不再展示今日预约和等待配件快捷卡，深蓝处理中卡片按定稿原型恢复绿色状态点、工单简码、客户与产品、地址、三段进度、白底继续按钮和文字详情按钮；多单继续使用同卡纵向分组，不使用会隐藏其他待办的轮播，头部只显示简洁单数，去除重复的“需要继续处理”文案。首页单独解除会截断页面级 sticky 的横向 overflow，任务面板头部按运行时状态栏及微信胶囊计算结果吸附在自定义导航下方。处理任务页顶部的工单状态与三步进度统一为浅蓝语义卡片，自动保存状态并入进度卡。Node 26.7.0 下 `pnpm check` 通过（35／30／5／12 项），微信／H5 development 构建成功；微信开发者工具和真机滚动视觉仍待复核，不改变 G3／G4／G5 或入口计数。

同日按用户要求对任务相关页面（除首页）逐状态对齐定稿原型，任务首页 `pages/task/index.vue` 与 `components/task/TaskCard.vue` 按用户明确约束不动。处理任务页头部由浅蓝摘要卡还原为原型 `.w-context` 无卡结构（撤销上一轮浅蓝填充卡结论），步骤条与自动保存提示脱离卡片、步骤圆点改原型填充式并补 ✓ 圆点；不可处理任务改为原型守卫（蓝色提示「当前任务请先处理预约、审核或退回要求。」＋主按钮「返回任务详情」），并修复审核中／历史任务误落「到达签到」起始卡的分支；核对提交清单移除原型不存在的「上传材料」行。任务详情状态卡改为原型四态 tone 并按 `nextStep()` 词表输出标题与提示，新增「查看完工材料与审核记录」入口；审核与办结进度视图按 `reviewOverview()` 重做五态标题、三阶段步骤条 done／active／issue 与空态。补充退回资料页退回原因卡标题提到 50rpx，采集块标题改用原型 `mediaTile` 文案（如「补充清晰的产品铭牌」），「查看原提交与审核记录」按原型移出表单卡。同时修正 `--app-page-color`、`--app-text-color` 两个在该仓库从未定义的语义令牌（共 6 处，改为 `--app-bg-color`／`--app-text-main`）。研发环境验证：`node --test` 30／30（本轮改写并新增 40 余条样式与文案契约断言），eslint、stylelint、`vue-tsc --noEmit` 通过（`tsconfig.node.json` 的 5 条 `jsonc/sort-keys` 为既有欠账，本轮未改动）；本轮视觉判定依据为原型 `worker.js`／`worker.css` 一手声明与 1px ≈ 2rpx 换算，`design-qa.md` 第四轮结论为 `passed`，但未重跑浏览器截图，微信开发者工具与真机逐状态视觉复核仍待执行。本项不改变 G3／G4／G5 或 W01～W16 计数。

同日按用户最新指令把任务首页「非正在服务」任务卡对齐定稿原型（本轮指令覆盖此前“任务首页不动”的约束）：`components/task/TaskCard.vue` 补齐原型 `tone()` 四态（审核退回 red、缺件／断联／改期 amber、办结／回访 green、其余 blue），标签与队列名改按 `nextStep().label`／`queueName()` 词表输出——此前 `HISTORY` 一律显示「历史任务」，使已关闭／已取消／待回访三态与原型不符；预约时间栏补 `canArrange()` 守卫，仅“待处理且非审核退回”才渲染；提示文案按 `nextStep().hint` 逐状态重写，并把「顾客」统一为原型的「客户」。卡面按 `worker.css` 一手值重写：`margin: 0 0 26rpx`、1px 描边、14px 圆角、取消 base 阴影、任务屏更紧的头部／正文／提示条内距、时间字号 36rpx → 44rpx、提示条 `#f4f7fc` 与 red／amber 彩色底及左侧竖线。同时撤销上一轮首页“内部任务白卡移除描边、只保留白色表面和轻阴影”的结论：删除 `pages/task/index.vue` 的 `.task-results :deep(.task-card) { border: 0 }` 与列表 20rpx gap，改由卡片自身的描边与 26rpx 外边距承担列表间距，这是本轮唯一一次触达首页。研发环境验证：`node --test` 30／30（本轮新增 30 余条卡片契约断言），eslint、stylelint、`vue-tsc --noEmit` 对改动文件通过；`design-qa.md` 第五轮结论为 `passed`，视觉判定依据为原型一手声明、1px ≈ 2rpx 换算与原型／实现同尺寸并排渲染，未重跑微信开发者工具与真机截图。核对中另发现详情页 `statusTone`（第 96 行）与 `statusTitle`（第 114 行）仍用比 `isReturned` 更宽的 REJECT 判定，审核退回后重新提交、`category` 已为 `REVIEW` 的任务会被误判为红色「退回待补」，与本轮范围无关，未修改，建议后续收敛。另需注意：本轮执行期间 `pages/task/index.vue` 与 `components/task/TaskCard.vue` 被另一写入方**并发修改**（`index.vue` 另增 `serving-shortcuts`／`pending-filters`／`task-panel__caption`／`task-results__heading`／`task-results__promoted`／`visibleTasks` 等原型首页元素，`TaskCard.vue` 的 `<script>` 被细化出 `status` 级标签与更紧的时间栏守卫），这些不属于本轮改动，本轮不予评判也不回退；本轮仅把一条「匹配表达式全文」的断言改为按声明块切片做语义断言以避免互相踩踏。建议同一分支上的原型对齐工作收敛到单一写入方。本项不改变 G3／G4／G5 或 W01～W16 计数。

同日继续核对任务详情及其处理、补资料页面；用户要求本次首页保持现状，已撤回本轮临时加入首页的筛选与快捷入口，保留其他并发写入方原有的首页／任务卡改动。详情页修正状态优先级：重新提交后的审核中不再被旧 `REJECT` 记录误显示为红色“退回待补”；已预约不再把“今日预约”筛选提示当作主状态；待回访、已关闭、已取消与缺件／断联／改期按当前状态呈现。审核中和历史任务从列表先到原型任务详情，再按服务端 `viewProgress` 进入审核进度；已取消在进度页不误显示为已办结。退回补资料的原记录入口与重提后的进度入口直接进入进度视图，补资料上下文显示“待补资料”。`pnpm verify` 通过（35／30／5／12 项检查及微信／H5 development 构建），`git diff --check` 通过；随后以 390 × 844 H5 视口和本地虚拟工单 API 对八种任务详情及三种进度视图重新截图，截图存于服务人员仓库 `artifacts/design-qa/implementation-task-detail-*-390x844.png` 与 `implementation-task-progress-*-390x844.png`（本地忽略产物），结果见同仓 `design-qa.md`；视觉复核中补齐退回进度页蓝色主按钮、取消态空文案和步骤状态，末次 `pnpm type-check` 与 H5 development 构建通过，微信开发者工具／真机逐状态视觉仍待复核，不改变 G3／G4／G5 和 W01～W16 计数。原型中的取消后恢复、配件与技术资料动作仍受未确认正式契约限制，未接入演示动作。

2026-09-19 在明确授权下补齐阶段 4／5 的真实链路验证：因没有可用的测试账号密码，改为在 `gaia_wh_init_wzy` 直接插入一条专用移动会话行取证（不写入或重置任何人员密码），再以 `W2TEST-20260919-*` 11 单定向夹具覆盖现场／远程闭环、草稿隔离、退回补充和 W02 白名单修改，并在验证前清空该区间上轮残留。结果是四项任务入口主链可完整跑通：W01 只读 42 / 42、W02 写动作 62 / 62、W04 履约 105 项中 104 项通过、端侧 `src/types/task.ts` 运行时结构校验 65 / 65。覆盖范围包括四分类与二级筛选同源计数、关键词与分页、跨人员／跨站点／未分配任务的 `404` 与失效会话 `4001`、联系记录与白名单修改的掩码／版本／幂等／审计、签到与远程开始的模式互斥和坐标边界、草稿跨单隔离与版本冲突、产品码三态、不可变提交版本与 `requiredItems` 定向补充和重复请求幂等。完工提交的库存耗用经专项探针确认与提交版本、工单状态同事务：库存不足时整体回滚、不残留耗用单据；收尾对账确认工单状态、执行事实、草稿、提交版本、库存单据与流水、动作审计、状态历史七处一致。

同一轮验证发现 1 个服务端缺陷 `DEF-20260919-01`：同工单用不同请求号重复调用现场签到／远程开始接口时，服务端缺少“已有活动执行”的前置判断，`ON DUPLICATE KEY UPDATE` 会覆盖 `started_at`、清空 `ended_at` 并追加第二条动作审计；端侧签到后已隐藏 `checkIn` 动作，服务端未同等拒绝。同一请求号的重复调用已被幂等回放正确拦截，完工提交也有请求号回放保护，因此仅签到／远程开始受影响。影响服务开始时间口径与审计条数（实测第二次签到使 `started_at` 前移、审计由 1 条变 2 条），修复前该入口不计完整闭环。

本轮不修改业务代码、迁移、权限码或库结构，也不替代微信开发者工具、真机、真实上传或业务验收；验证用会话行当日到期，`W2TEST-20260919-*` 夹具数据保留在测试库待清理。G3 不变，G4／G5 与 W01～W16 计数不变。

阶段 3 联调早期，真实账号 `sunzhiyuan` 登录返回合法身份、企业和服务站，但当时运行的旧版宿主响应尚无 `capabilities.taskRead`，端侧严格结构校验将其误判为无效上下文并跳转身份错误页。现已增加窄范围向后兼容：仅当 `taskRead` 缺失时归一化为 `false`，登录可继续进入应用；错误类型、其他能力越界或其他上下文结构异常仍拒绝。在当时宿主制品与权限迁移完成前，任务页保持无权，未因向后兼容扩大读取范围；修复后服务人员端完整 `pnpm verify` 通过，后续更新宿主与权限迁移后已完成上述 G3 真实联调。

同日两端同步接入公共 `useLocation`：沿用参考工程首次定位、拒绝后引导设置并重试的流程，按本项目只返回 GCJ-02 坐标和精度；服务人员现场签到与原生探针已改用该方法。授权取消、权限未开启、设置不可用、系统定位失败、重试失败及无效坐标均有独立处理；失败时签到页继续提供到达说明，过期页面不会提交坐标。消费者端目前只有公共能力与微信位置声明，没有新增业务定位入口。两端自动化覆盖定位分支；真实微信授权弹窗、系统定位服务和真机签到仍待验证，不改变 G3／G4／G5 与入口计数。

同日将任务操作失败反馈接入双端公共 `showOperationError`：短消息显示原生 Toast，较长的服务端说明显示单按钮弹窗，取消请求静默处理。服务人员任务详情的联系／预约／编辑／状态写入、处理页的签到／扫码／选图／上传／自动保存／完工提交，以及退回补资料页的相应操作不再把失败消息单独放成红字；自动保存和失败文件仍保留可重试状态，审核退回继续按业务状态展示。消费者端仅同步底座，当前无正式业务操作页；微信开发者工具和真机提示层级待复核，不改变 G4／G5 与入口计数。

微信开发者工具 Stable 2.02.2608060 中，在 `UITEST-20260919-ONSITE` 待签到页展开“无法定位”后保持说明为空，点击“登记说明并签到”实际显示原生 Toast“请填写无法定位时的到达说明”；网络面板未新增签到请求，工单未写入。本次只验证该短错误路径，长错误弹窗、键盘遮挡和真机效果仍待复核。

同日统一按钮加载态：任务处理、任务详情保存、退回补资料和企业切换的原生按钮在忙碌时保留主色／次色／危险色／文字色，显示转圈及状态文案，并继续禁用重复点击；登录按钮改由 TDesign `loading` 独立控制，保留品牌色。两端公共样式和示例同步；运行时测试的旧审核入口 class 和补资料按钮固定属性顺序断言同步校正。双端 `pnpm check`、微信／H5 development 构建通过，本端单元测试 44／44；真机与微信业务按钮加载瞬态待验，不改变 G3／G4／G5 或入口计数。

同日按用户指出的三处差异对齐配件管理页面的视觉：以原型 `worker.css` 一手值与 390 × 844 画布（1px = 2rpx）为准，重写配件模块的 `<style>`，模板与脚本未改。**一是按钮文字偏上**——微信 `uni-button` 默认 `line-height: 2.5555`，在小 `min-height` 按钮内会把单行文字推到偏上，现已把配件模块全部按钮（主／次操作、搜索查询、库存记录入口、数量步进、标签页）统一改为 flex 垂直居中并显式声明行高，与已对齐的任务流页面按钮同构。**二是按钮圆角**——像素实测 `.worker-phone .primary`／`.secondary` 为 14px，对应 28rpx，不再沿用设计令牌里的小控件圆角 `--app-radius-control`；搜索框内的查询按钮经测量确认是**蓝色文字按钮、无底色**，已从实心主按钮改为透明底蓝字，搜索框本体 100rpx 高、22rpx 圆角；数量步进按钮为 80rpx × 80rpx、16rpx 圆角、`line-height: 1`、底色 `#f2f6fb`。**三是间距**——`.app-page__content` 在本端是带 `gap: 16rpx` 的 flex 列容器，而原型是块级流、外边距会折叠，因此按「原型折叠后的目标间距换算成 rpx 再扣掉 gap」折算：库存概览卡到搜索框的 16px 由容器 gap 与搜索框上外边距承担（概览卡自身外边距为 0），搜索框到列表标题 16px、列表标题到首行 12px 同理，三处均在源码注释中写明折算过程。同时按原型收口卡片体系：配件分区卡 36rpx 内距／32rpx 圆角、记录卡 34rpx／26rpx 圆角／`#e0e8f2` 描边、状态卡 `42rpx 40rpx` 内距／32rpx 圆角并结合语义警告色、详情页图标 152rpx 与总数 96rpx、时间线圆点补 `content: ''` 使伪元素真正渲染，并清理了模板中已无样式定义的死类两个。研发环境验证：`node --test` **31／31 通过**（新增「配件页面沿用原型配件屏的按钮几何、文字居中与间距」约 40 条断言，含按钮 flex 居中、圆角与高度、搜索按钮透明底、步进按钮几何、首页三处间距折算值、标签选中态下划线、时间线圆点与驳回色，以及对 `--app-page-color`／`--app-text-color` 两个不存在令牌的负向断言）；stylelint、eslint、`vue-tsc --noEmit` 对改动文件 0 错误。视觉证据为同仓 `artifacts/design-qa/parts-prototype-alignment.html`／`.png`（左栏照抄原型一手声明、右栏按 1rpx = 0.52px 折算实现值并排渲染，附逐项数值对照表）与原型六屏截图（`artifacts/` 已在 `.gitignore` 中忽略）；对照中另发现并修正一处真实偏差——概览卡到搜索框原为 24px，改为 16px 后与原型一致。`design-qa.md` 第六轮结论为 `passed`。本轮只改已存在页面的样式，未新增业务规则、未改接口契约、未动脚本逻辑。本项不改变 G3／G4／G5 或 W01～W16 计数；微信开发者工具与真机视觉复核待执行。

2026-09-20 配件记录页按当前原型修正了类型页签下划线位置、状态筛选横向间隔和加载态信息层级。微信开发者工具 iPhone 15 Pro Max 实看全部／退料切换、加载和失败态；`pnpm check` 与 development 微信构建通过。当前有效会话请求 `GET /parts/documents` 仍返回业务 `503`，已核对移动 Mapper 查询依赖缺失的 `mobile_applicant_account_id`，对应增量迁移及共享库预检／恢复方案已备妥，尚未获本次共享库 DDL 授权；有数据卡片、成功态、真机和业务链验收因此仍待执行。G6 与 W05～W10 入口计数不变。

[返回项目进度总览](00-开发进度与下一步.md) · [查看逻辑目录与页面路由设计](../02-技术设计/04-小程序目录与页面路由设计.md) · [查看服务人员需求](../01-功能需求/07-服务人员小程序需求.md) · [查看菜单定义](../01-功能需求/08-页面清单.md)

应用归属已解阻，但所有 W 入口仍受真实身份、接口和对应业务规则影响；下表后续功能依赖按各入口维护。Web 已有库存或工单切片、共同底座和构建通过均不等于服务人员端业务已实现；各入口最终验收均未完成。

## 模块概览

| 模块 | 入口数 | 已有真实实现 | 最终验收完成 | 当前状态 | 主要依赖／说明 |
| --- | ---: | ---: | ---: | --- | --- |
| 任务处理 | 4 | 0 / 4 | 0 / 4 | 开发中（G3 已达到；W01／W02／W04 真实 HTTP 与数据库对账已通过，存在 1 个签到缺陷） | W01／W02 只读闭环已有真实证据；当前服务、任务写动作和 W04 履约闭环已实现源码、完成测试库迁移并通过真实会话 HTTP 对账；`DEF-20260919-01` 修复、真实上传与真机验收完成前不计完整实现 |
| 我的配件 | 3 | 0 / 3 | 0 / 3 | 开发中 | W05 本人库存与详情、W06／W07 领退料选择页已在当前账号微信模拟器中读取真实数据；提交和共享环境闭环未验 |
| 应用与资料 | 6 | 0 / 6 | 0 / 6 | 开发中／部分阻塞 | W08～W10 配件记录切片已开发，当前共享库缺列导致记录页加载失败；W11 调拨仍无当前原型入口，资料来源与权限未解 |
| 我的与账号 | 3 | 0 / 3 | 0 / 3 | 阻塞 | 登录身份、多企业切换和反馈流程 |

## 1. 任务处理（0 / 4）

| ID | 菜单／页面 | 优先级 | 完整范围 | 已有真实实现 | 功能状态 | 剩余工作／证据 |
| --- | --- | --- | --- | --- | --- | --- |
| W01 | 任务搜索、状态页签与卡片 | P0 | 按任务号、联系人或地址搜索本人/授权任务；按待处理、待补资料、审核中、历史分类，正在服务独立置顶并继续本单，断联并入待处理，未下发任务不进入普通师傅端；卡片展示任务、顾客、产品、服务项目、时间和沟通信息 | 部分：G3 只读闭环已真实验证；当前服务源码已接入并完成真实会话 HTTP 对账 | 开发中 | 后端以签到／远程开始事实返回 `currentServices`，端侧置顶并进入同一草稿；2026-09-19 已用真实会话验证 42 / 42 项（四分类、同源计数、关键词、分页、跨范围 `404`、失效会话 `4001`）；进入完整实现仍受 `DEF-20260919-01`（重复签到覆盖开始时间）和真机、微信逐页视觉约束 |
| W02 | 任务详情、联系、状态与修改 | P0 | 查看任务详情；拨号/复制号码并记录联系结果；执行已预约、取消、断联、改期、缺件等状态；只允许修改备用电话、地址补充和备注等字段 | 部分：只读真实验证；写动作源码已接入并完成真实会话 HTTP 对账 | 开发中 | 联系结果、预约／改约、缺件／断联／取消和备用电话／地址补充／备注均由服务端 `actions`、版本及幂等键复核；2026-09-19 已用真实会话验证 62 / 62 项，含掩码读写、版本冲突 `409`、枚举与状态门禁 `422`／`409`、重复请求幂等和审计写入；进入完整实现仍受真机拨号与微信逐页视觉约束 |
| W03 | 任务地图 | P1 | 已延期；当前定稿原型、路由和开发范围均不提供入口或页面 | 无 | 延期 | WAPP-004 保留追踪；历史地图原型与实现材料已清理，后续重新排期时须重新确认业务、微信能力、隐私授权和验收标准 |
| W04 | 签到、服务过程、扫码、完工与审核进度 | P0 | 基于时间/位置签到；在同一自动保存草稿中记录检测、处理、配件、证据、产品码和客户确认；按必填证据一次提交完工，审核退回只补指定项，审核通过后查看后台后续进度，无需二次交单 | 前后端源码切片完成，测试库迁移完成，真实会话 HTTP 与数据库对账通过（105 项中 104 项通过）；本轮将耗用改为本人真实库存选择并加库存写权限／当前站退料占用复核 | 开发中 | 首次完工仍同事务写耗用单／余额／流水，退回重提复用首次耗用单；本轮新增配件库存读取依赖尚未迁移和真实联调；`DEF-20260919-01`、真实上传及引用归属、相机／相册／扫码、真机和全链 UI 验收仍待完成，多站预留边界见阶段 6 Spec |

任务详情需覆盖预约地址、期望时间、联系方式、沟通记录、产品/组合品番、服务类型、附加服务和备注。完工信息还需根据确认规则覆盖序列编号、RFID/QR、购买渠道、经销店、购买日期、工厂代码、用水、服务方式、照片、配件和客户确认。

## 2. 我的配件（0 / 3；源码切片开发中）

| ID | 菜单／页面 | 优先级 | 完整范围 | 已有真实实现 | 功能状态 | 剩余工作／证据 |
| --- | --- | --- | --- | --- | --- | --- |
| W05 | 我的配件与详情 | P0 | 查看配件编号、名称、图片、单价、适用品番和可用库存，区分服务站与个人库存 | 移动会话下本人／本站库存搜索、分页、详情、加载／空／错／无权；当前账号微信模拟器已实测本人库存与详情、本站库存真实读取；正式图片和单价没有确认字段，不伪造 | 开发中 | WAPP-013；跨范围拒绝、企业切换后的缓存隔离和原型同视口逐状态视觉未验（本轮已按原型一手值完成配件页面源码级样式对齐，见下方同日说明）；图片／单价契约待确认 |
| W06 | 领件申请 | P0 | 按可见工单选择服务站配件和数量，填写领料说明后提交；生成标准 `LL` 单号，处理中可撤销 | 源码切片：多项工单领料／无工单常备补充，用途在配件之后选择；任务入口锁定工单，服务端重验数量／用途／工单与请求号；微信模拟器已实测本站库存及可见工单选择态 | 开发中 | WAPP-014；结构迁移未执行，真实提交、管理员通过后双边库存／流水、重复请求和角色拒绝待共享环境核对 |
| W07 | 退料申请 | P0 | 按可见工单选择个人配件和数量，填写退料说明后提交；生成标准 `TL` 单号，处理中可撤销 | 源码切片：本人可退库存多项选择、详情预选、可见工单、当前站处理中退料占用及服务端重验；微信模拟器已实测详情预选 1 件及可退量 | 开发中 | WAPP-015；结构迁移未执行，真实提交、共享环境审批／退料占用并发待验；多站人员共用余额与跨站预留口径仍阻塞 |

## 3. 应用与资料（0 / 6）

| ID | 菜单／页面 | 优先级 | 完整范围 | 已有真实实现 | 功能状态 | 剩余工作／证据 |
| --- | --- | --- | --- | --- | --- | --- |
| W08 | 配件记录统一入口 | P0/P1 | 当前三 Tab 原型在配件首页以“单据记录”进入领退料记录；W11 调拨与资料需求继续各自追踪 | 源码切片：配件首页统一轻入口和全部／领料／退料筛选，不增加 W11 入口；微信模拟器入口可跳转，但列表因共享库缺申请人来源列加载失败 | 开发中 | WAPP-016～WAPP-020；结构迁移及业务状态验收待做，W11／资料范围不因本轮配件切片视为完成 |
| W09 | 领料单列表与详情 | P0 | 查看处理中、已完成、已拒绝和已撤销记录；详情显示服务站／个人当前库存，处理中可撤销 | 源码切片：本人当前站列表、详情、拒因、真实历史、双方余额和二次确认撤销；当前记录请求因共享库缺列失败 | 开发中 | WAPP-016；结构迁移、真实移动会话／管理员审批、重复动作与同视口视觉核对待做 |
| W10 | 退料单列表与详情 | P0 | 查看处理中、已完成、已拒绝和已撤销记录；关联工单并显示双方当前库存，处理中可撤销 | 源码切片：统一记录筛选、关联工单、真实历史、双方当前余额和申请人撤销；当前记录请求因共享库缺列失败 | 开发中 | WAPP-017；结构迁移、共享环境拒绝／撤销不动库存和跨范围核对待做；多站预留边界未解 |
| W11 | 调拨单列表与详情 | P1 | 查看或处理授权范围内调拨单，按配置完成接收确认 | 无 | 阻塞 | WAPP-018；当前小程序原型暂隐藏，正式需求追踪保留 |
| W12 | 延保资料与详情 | P1 | 按项目、区域或品番查询延保资料，展示版本、有效期和适用范围 | 无 | 阻塞 | WAPP-019；当前管理后台、后端及真实小程序均无对应能力，小程序原型入口暂隐藏，保留直达评审页与需求追踪 |
| W13 | 技术资料与详情 | P1 | 搜索安装手册、说明书和视频，支持关键词、品番及版本筛选 | 无 | 阻塞 | WAPP-020 |

## 4. 我的与账号（0 / 3）

| ID | 菜单／页面 | 优先级 | 完整范围 | 已有真实实现 | 功能状态 | 剩余工作／证据 |
| --- | --- | --- | --- | --- | --- | --- |
| W14 | 个人中心与账号入口 | P0/P1 | 展示服务人员、所属企业和账号入口，聚合反馈、修改密码、切换企业和退出操作 | 无 | 阻塞 | WAPP-021～WAPP-024；阶段 2 的“我的”Tab 已展示真实人员／企业／服务站上下文并承接切换、退出底座，但反馈、密码等完整入口未实现，故不计 W14 |
| W15 | 问题反馈 | P1 | 提交问题、图片和联系方式，并查询处理进度 | 无 | 阻塞 | WAPP-021；反馈处理责任待确认 |
| W16 | 修改密码、切换企业与退出 | P0 | 通过旧密码或二次验证修改密码；切换企业后立即重算权限和数据范围；退出时使本地与服务端登录态失效 | 无 | 阻塞 | WAPP-022～WAPP-024；阶段 1 已完成服务端校验的企业切换与会话撤销并真实验证，阶段 2 将动作接入“我的”Tab；正式修改密码和完整 W16 验收仍待完成，不计入口实现 |

## 系统配套开发项

| 配套项 | 当前状态 | 完成条件 |
| --- | --- | --- |
| 应用、仓库与底座 | 已完成 | `gaia-customer-service-uni` 已从消费者端 `289800f` 复制共同底座，应用名为“TOTO 客服助手”；当前租户 profile 的 AppID 为 `wx2aa3c38e01dbc548`，`pnpm verify` 与微信产物 AppID 核对通过；此前使用其他 AppID 的微信模拟器检查属于旧配置证据，当前 AppID 的模拟器仍待复核；根导航滚动切换及其他共同底座机制后续继续双端同步修改 |
| 身份与多企业权限 | 阶段 1 完成／G1 已达到 | `gaia_wh_init_wzy` 已执行迁移；专用真实账号完成登录、恢复、上下文、两企业切换、退出和 Web API 隔离验证，自动化覆盖未绑定、停用、无站点、跨人员／企业／租户、无权和迟到请求；微信快捷登录不在当前契约，其他环境迁移仍需单独授权 |
| 应用壳、路由与反馈 | 阶段 2 完成／G2 已达到；阶段 3 前 UI 基线已收口 | 正式构建仅含任务／配件／我的三 Tab、登录、身份错误、404 和三个任务分包页；任务是启动首页，匿名／会话失效才跳登录；三 Tab 已配普通／选中图标，任务／配件使用透明展开／滚动白底居中紧凑双态导航，“我的”使用默认白底标题栏；动态安全区、双模式语义令牌、页面容器、最小表面、七类反馈态、正式产物无示例／Mock 及双小程序校验已有自动化和 H5 实际走查证据 |
| 工单状态机与派单 | 只读切片已实现／写动作待确认 | 本人正式派单任务按现有收敛状态提供四分类只读查询；默认二次交单已取消，发布／接单模式、阶段 4 异常动作和旧数据核对仍需确认 |
| 定位、扫码与媒体 | 技术探针已完成，业务仍待确认 | 条件编译示例已覆盖定位、相机／相册、扫码、真实上传、拨号和 `openLocation` 调用；仍需在首次业务依赖前准备已授权号码／上传路径并补 iOS、Android 真机权限和业务降级证据 |
| 配件库存一致性 | 后台切片已改造 | 后台已实现领退料通过时的双边库存事务、负库存和重复动作拦截；仍需真实小程序身份接入及共享环境端到端验收 |
| 资料与反馈后台 | 待确认 | 延保/技术资料来源、版本权限、反馈受理角色和状态流程确认 |

## 本系统完成条件

- W01～W16 的 Tab、路由、返回路径、登录拦截和权限可见性确定并验证。
- 任务从派单到联系、签到、服务过程、扫码、配件耗用、完工、审核/交单的状态和数据完整贯通。
- 本人/服务站/企业数据范围由后端强制校验，切换企业后不存在数据串用。
- 弱网、重复提交、定位拒绝、扫码失败、缺件、改期、断联、审核退回和库存冲突等关键场景通过验收。
- 对应 Spec 和项目总览已同步更新。
