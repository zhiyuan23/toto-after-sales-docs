# 后续开发与交互优化待办

- 更新日期：2026-09-12
- 适用范围：`gaia-ui/apps/after-sales`
- 定位：记录已明确、但尚未全部完成的横向能力和交互优化；不替代功能端进度、业务 Spec 或验收记录。
- 协作规则：团队成员新增、更新或关闭事项时遵循[团队文档协作与提交同步规范](10-团队文档协作与提交同步规范.md)。

## 1. 优先级与待办

| 编号 | 优先级 | 待办 | 验收要点 | 当前状态 |
| --- | --- | --- | --- | --- |
| UI-001 | P0 | 标准列表“查看详情”行操作 | 当前按用户要求统一隐藏；有编辑操作时编辑排第一；关联链接仍可直达详情 | 行入口展示暂缓（2026-09-10 用户调整）；隐藏实现与验证见 OPT-20260910-01 |
| UI-002 | P0 | 统一查看详情样式 | 统一标题摘要、状态、两列事实区、关联明细、加载／失败／关闭状态；支持暗色主题与键盘关闭 | 已新增 `EsDetailDialog` 并替换已实现的详情弹窗；完成桌面视口目视与关闭验证 |
| UI-003 | P0 | 列表关联项使用真实链接跳转 | 使用稳定字符串 ID、命名路由和目标 `read` 权限；目标页可直达恢复详情；无权限或缺 ID 时降级为文本 | 第一批已实现，字符串长 ID 和降级规则自动化通过；共享环境权限组合待回归 |
| UI-004 | P1 | 详情直达与列表现场恢复 | 刷新后可恢复目标详情；关闭清理 query；跨页返回保留源列表筛选、页码和滚动位置 | 直达与清参已形成通用方法；跨页列表现场仍待统一 |
| UI-005 | P2 | 通用表格动态表头 | 表格工具区使用“列设置”图标（优先 `lucide:columns-3-cog`）；可隐藏／显示和拖拽排序；必选列不可隐藏；可恢复默认；按用户+租户+路由隔离持久化配置；列变更后兼容旧配置 | 待开发，优先级低于详情与关联跳转 |
| UI-006 | P2 | 表格列数据排序 | 表头显示可理解的升／降序状态；分页列表默认使用服务端排序；明确排序字段白名单、默认次序和并列键；排序切换回到第 1 页且只发一次请求 | 待后端列表契约统一后开发 |
| UI-007 | P2 | 详情中的关联列表支持定位与回跳 | 详情内关联商品、服务站、服务区域、配件等使用与主列表一致的链接语义 | 第一批已应用，其他模块随真实详情接口补齐 |

### OPT-20260910-01：统一隐藏表格预览按钮，编辑优先

- 类型／优先级／状态：体验优化／P1／待验证。
- 应用／角色／模块与入口：Web 后台／跨视角／售后子系统全部使用 `EsTable` 行操作的列表及树表，沿用各模块既有入口。
- 关联需求／Spec：横向交互调整，关联 UI-001 与[通用 UI 规范](../02-技术设计/05-gaia-ui售后服务UI样式规范.md)，不新增业务 Spec。
- 来源／日期／负责人：用户本次明确要求；首次提出及最后更新 2026-09-10；负责人待分配。
- 验证标准：所有 `detail` 查看按钮不出现在直接操作或更多菜单；可见的编辑操作排第一；无编辑权限时不额外显示编辑；隐藏项不占宽度，无可见操作时隐藏整列；既有详情链接保持按记录 ID 跳转。
- 实现：在 `EsTable/action.ts` 统一处理显隐和编辑顺序，保留页面详情配置及处理函数，便于后续恢复。只读列表原本仅有“查看”时不再显示操作列。
- 研发证据：Node 22.23.1 下 102 项测试、类型检查及生产构建通过；自动化覆盖只读行、编辑权限、列宽、操作排序和关联链接。9010 浏览器商品档案和商品分类验证首位编辑、无查看按钮，商品编辑表单回填及取消、商品分类链接打开对应详情和关闭清参通过，未写业务数据；完整角色与各业务数据组合尚未逐页验收。不改变功能入口完成数。

### OPT-20260910-02：展开筛选时操作按钮位于末行右侧

- 类型／优先级／状态：体验优化／P2／待验证。
- 应用／角色／模块与入口：Web 后台／跨视角／使用 `EsSearch` 的列表，沿用各模块既有入口。
- 关联需求／Spec：通用筛选交互调整，关联[通用 UI 规范](../02-技术设计/05-gaia-ui售后服务UI样式规范.md)，不新增业务 Spec。
- 来源／日期／负责人：用户明确要求；首次提出及最后更新 2026-09-10；负责人待分配。
- 验证标准：收起时按钮在首行最右列；展开时在末行最右列，条件恰好填满整行时另起一行靠右；筛选项保持等宽，展开收起不查询，筛选和重置各查询一次。
- 实现与研发证据：`EsSearch/index.vue` 按当前显示项数与列数定位按钮行；5 项现有布局测试、类型检查及文档影响检查通过，浏览器回归页验证末行剩余空间、整行填满、收起恢复、等宽及查询次数通过。实际业务页补验受阻：7004 主入口连接被拒绝，9010 独立入口需登录；未进行本轮真实业务页回归。此记录为研发自测，业务验收待完成，不改变入口完成数。

### OPT-20260910-03：统一操作列图标与更多菜单

- 类型／优先级／状态：体验优化／P2／待验证。
- 应用／角色／模块与入口：Web 后台／跨视角／售后子系统 `EsTable` 列表及树表操作列。
- 关联需求／Spec：横向交互规范，见[通用 UI 规范第 6.1 节](../02-技术设计/05-gaia-ui售后服务UI样式规范.md#61-表格操作列规范2026-09-10-确认)，不新增业务 Spec。
- 来源／日期／负责人：用户确认单支铅笔、行内保留提示、菜单纯中文，并要求先文档后实施；首次提出及最后更新 2026-09-10；负责人待分配。
- 验证标准：编辑统一单支铅笔；含更多入口最多两个图标；编辑优先；菜单无图标和占位；行内提示、危险色、权限显隐、禁用、加载和事件隔离正常。
- 实现与研发证据：规范先行落入 UI 规范与子系统 README；中央动作映射、页面图标清理、最多两个槽位、纯中文菜单和危险色已实现；34 个标准图标本地打包。Node 22.23.1 下 107 项测试、`vue-tsc -b`、生产构建及文档影响检查通过（构建保留既有大包提示）。浏览器独立回归页验证单／双／多操作、编辑优先、中文 Tooltip、菜单零图标及零占位、红色删除、权限隐藏、加载互斥、禁用和点击不冒泡；使用真实商品主档操作配置及独立测试数据回归商品分类／商品档案的编辑与启停图形。入口：`http://127.0.0.1:9010/after-sales/tests/browser/es-table-actions.html`，不调用业务 API；本轮未进行完整业务页面与真实角色组合验收。不改变功能入口完成数。

### OPT-20260910-04：关联跳转定位展示与重复加载修复

- 类型／优先级／状态：体验优化／P1／待验证。
- 应用／角色／模块与入口：Web 后台／跨视角／商品档案 → 商品分类、已登记的关联详情路由。
- 关联需求／Spec：UI-003／UI-004、通用 UI 规范第 5.2 节，不新增业务 Spec。
- 来源／日期／负责人：用户反馈跳转两次 loading、关闭弹窗再次 loading，要求重新设计落地展示并修复；首次提出及最后更新 2026-09-10；负责人待分配。
- 验证标准：分类跳转直接定位高亮、保留树层级，无自动详情弹窗；首次仅一次列表请求、零详情请求；取消定位无请求、无页面重建；无重复分类页签；无效／不可见 ID、刷新直达及筛选单次请求可验证。
- 原因与实施：`RouterView` 使用 `route.fullPath` 作组件 key，清除 query 重建页面；分类初始化串行加载列表和单条详情，列表完成后 `treeKey` 变化还会重建表格。改为显式登记展示参数并稳定页面／页签身份，分类从列表响应直接定位；移除分类详情自动请求及表格重建 key；树表支持展开目标祖先和可视区滚动。
- 研发证据：Node 22.23.1 下 111 项测试、类型检查、生产构建通过（保留既有大包提示）。`tests/browser/category-navigation.html` 使用真实分类页面和通用组件、独立请求替身计数：首次进入权限／列表／详情／挂载为 1／1／0／1；取消定位、同页切换目标、无效和不可见 ID 均不增加请求或挂载；筛选、冲突筛选恢复、失败重试各仅增加一次列表请求；35 个根分类下的长 ID 子节点可高亮并滚动可见，取消后高亮清除。9010 真实页面观察到定位提示、无需详情弹窗和取消后分类页签不重复；真实接口请求计数、其他关联详情页面和完整角色组合未逐项验收，不将独立计数结果写成真实环境全量验收。不改变业务入口完成数。

### OPT-20260910-05：统一其余跨页关联定位

- 类型／优先级／状态：体验优化／P1／待验证。
- 应用／角色／模块与入口：Web 后台／跨视角／商品、配件、代理商、门店、服务站、服务区域、服务人员、顾客购买记录、工单九类关联目标；共享顾客／预约页面沿用各视角的实际权限及接口。
- 关联需求／Spec：UI-003／UI-004、通用 UI 规范第 5.2 节、OPT-20260910-04，不新增业务 Spec。
- 来源／日期／负责人：用户要求其他跳转按分类定位规范统一；首次提出及最后更新 2026-09-10；负责人待分配。
- 验证标准：默认定位高亮、不自动弹窗；分页目标不在当前页也可按 ID 精确读取；整个请求链连续加载；取消恢复原列表和页码、零新增请求；权限失效、旧响应、无效 ID 与错误结果不能越权或错显。
- 实现：复用 `useListPage` 扩展 `useLinkedListPage`，统一 `EsListFocus` 状态条；`EsTable.focusRowKey` 负责定位高亮。当前页命中不请求详情；未命中才精确读取目标。顾客购买记录和工单提供手动完整详情；知识正文、标签版本、投诉处理的同页详情入口保留阅读语义。无后端／数据库变更。
- 研发证据：32 项定向测试通过，包含 8 项新增状态／并发／权限回归；类型检查和生产构建通过。`tests/browser/linked-lists.html` 使用九个真实目标页面及独立请求替身，逐项验证长 ID 目标不在第一页时一次列表＋一次精确读取、高亮与默认无弹窗；商品、工单取消定位请求计数不变，恢复原分页。全量 121 项中 120 通过，`tests/app-setting.test.ts` 一项关于设置面板 `colorScheme` 绑定的断言失败，涉及本轮未修改的设置面板，单独保留，不掩盖失败。提交前按 Git 跟踪测试及本任务新增测试复验，119 项全部通过；上述设置面板测试及另一个导航设置测试均为其他任务未跟踪文件，未纳入本次提交。真实租户／完整角色及业务写操作尚未逐页验收；不改变功能入口完成数。

### OPT-20260910-06：放大品牌区并区分主副项目名称

- 类型／优先级／状态：体验优化／P2／待验证。
- 应用／角色／模块与入口：Web 后台／跨视角／售后子系统公共页头品牌区。
- 关联需求／Spec：用户确认的品牌视觉调整，不新增业务 Spec。
- 来源／日期／负责人：用户要求放大 Logo 和标题、取消粗体，以黑色“蓝鲸数字”为主项目名称；对比设计方案后确认使用细竖线分隔“售后服务”，替代最初的蓝底白字标签；首次提出及最后更新 2026-09-10；负责人待分配。
- 验证标准：桌面页头 Logo 与标题放大，主副名称层级清楚且垂直居中；标题常规字重，副名称无底色、无外框，以细竖线分隔；主副名称紧凑成组，与顶部导航留出更大的间距，副名称通过字号与颜色区别于菜单；名称完整显示，品牌区与菜单及工具栏无重叠。
- 实现与研发证据：复用现有本地 Logo；桌面 Logo 为 34px，主名称为 22px 黑色常规字重，副名称为 18px 深灰色（`#4b5563`）常规字重；分隔线为 1px × 20px 浅灰竖线，两侧各留白 14px，Logo 与主名称间距 10px。品牌区末端额外留白 10px，与容器原有 30px 间距合计形成 40px 的品牌／导航分区，不扩大导航与右侧工具栏的间距。样式集中在 `Logo/index.vue`，页头通过 CSS 变量设置桌面尺寸；保留完整链接无障碍名称，装饰分隔线不参与朗读，并适配暗色主题文字与线条颜色。前轮 Node 22.23.1 类型检查通过，并在 9010 实际首页目视验证 14px 分隔留白、40px 品牌／导航分区和灰色子标题生效。本次按用户要求将 Logo、主副标题与竖线高度各缩小 2px，其余间距、颜色和字重保持原设定；SFC／样式编译及局部 diff 检查通过。当前 9010 开发服务连接被拒绝，本次缩小后的浏览器显示尚未复验。未进行暗色和其他导航模式专项验收；未改业务功能，不改变入口完成数。

## 2. 关联跳转盘点

### 2.1 已有契约，本轮应用

| 源模块 | 关联项 | 目标 | 定位键 |
| --- | --- | --- | --- |
| 门店管理 | 所属代理商 | 代理商详情 | `dealerId` |
| 商品档案 | 商品分类 | 商品分类详情 | `categoryId` |
| 配套品管理 | 主商品、配套商品 | 商品详情 | `sourceProductId` / `targetProductId` |
| 商品系列、配件档案详情 | 关联商品／适用品番 | 商品详情 | `productId` |
| 服务站详情 | 服务区域 | 服务区域详情 | `areaId` |
| 服务区域详情 | 候选服务站 | 服务站详情 | `stationId` |
| 服务人员 | 所属服务站 | 服务站详情 | `stationId` |
| 排班管理 | 服务人员 | 服务人员详情 | `personnelId` |
| 服务质量 | 统计对象 | 人员／服务站／服务区域详情 | `scopeType + scopeId` |
| 顾客档案、门店顾客 | 代理商、门店 | 代理商／门店详情 | `dealerId` / `storeId` |
| 顾客购买详情、工单详情 | 登记商品 | 商品详情 | `productId` |
| 安装预约、统一工单 | 登记单、服务站 | 顾客购买／服务站详情 | `registrationId` / `stationId` |
| 库存余额、库存流水 | 配件、库存归属 | 配件／服务站／服务人员详情 | `partId` / `ownerType + ownerId` |
| 领退调耗与盘点 | 来源、目标、配件明细 | 服务站／服务人员／配件详情 | `sourceType + sourceId` / `targetType + targetId` / `partId` |

### 2.2 缺契约，暂不生成链接

| 源模块 | 候选关联 | 当前缺口 |
| --- | --- | --- |
| 库存余额／流水 | 仓库归属 → 仓库详情 | 库存台账为同页签，需先统一 `tab + warehouseId` 直达和关闭语义 |
| 库存流水 | 单据号 → 来源单据详情 | 列表尚未返回可路由的单据资源类型 |
| 工单耗用 | 工单号 → 工单详情 | 当前 `workOrderId` 的业务号／主键语义待工单主域确认 |
| 预约／工单 | 代理商、门店 → 主档详情 | `WorkOrder` 列表契约只返回名称，缺 `dealerId` / `storeId` |
| 商品分类 | 上级分类 → 分类详情 | 当前详情只能展示 `parentId`，缺上级分类名称；补契约后再链接化 |
| 商品档案 | 营业分类 → 分类详情 | 当前列表契约有 `businessCategoryId`，但缺展示名称 |

## 3. 实施约束

- 关联列只在目标 API 可读、目标菜单可达且 ID 合法时显示链接；否则保留原文本。
- 关联跳转按字符串传递长整型 ID，不通过名称、列表位置或显示编号反查。
- 动态表头不得隐藏多选、关键标识或操作列；不得因个人配置改变后端字段白名单、权限或数据范围。
- 分页数据不做“仅当前页”的客户端假排序。后端未支持排序契约时，表头保持无排序状态。
- 新增通用能力时回归 `EsTable`、商品分类范例及本文关联模块，分开记录隔离验证与共享环境验收。


<a id="tech-20260911-01"></a>
### TECH-20260911-01：解除售后业务数据对主系统主档的依赖

- 类型／优先级／状态：技术债／P0／待验证；数据独立原则已获用户明确确认，本次已完成代码隔离及研发验证，目标环境迁移／初始化和业务验收待完成。
- 应用／角色／模块与入口：Web 后台／跨视角／商品分类与商品、系列配套及配件、代理商门店、顾客登记预约、受理工单流转、安装码、知识、投诉、报表与沟通；具体入口见核查表。
- 关联需求／Spec：沿用相关 ADM-MD、ADM-CU、DLR、CS、PRT 和报表需求及既有切片；统一边界与逐项证据见[业务数据独立边界与核查](../specs/2026-09-11-业务数据独立边界与已开发功能核查.md)。
- 来源／日期／负责人：用户指出不应关联主系统数据；首次提出及最后更新 2026-09-11；负责人待分配。
- 实际／预期：整改前生产 SQL 直接读写主系统商品／分类，或读取渠道主档；其他用例通过关联 ID、GTIN 服务和工单 scope 间接依赖。现已完成自有表、独立校验、范围及下游引用改造。预期所有业务主档、校验与业务归属由售后自有数据支撑，平台统一认证权限仍生效。
- 验证标准：移除直接 SQL、跨 Mapper include、服务校验和关系 ID 中的主系统业务依赖；在不含主系统业务表的隔离数据源完成相关真实 SQL／写路径、事务、幂等、权限和跨租户回归；旧数据关联迁移有保留／恢复方案，不误写主系统数据。目标环境迁移与验收另行记录。
- 证据／结论：2026-09-11 完成 57 项后端测试、125 项前端测试、类型检查、前后端构建及隔离页面浏览器检查；依赖树／WAR 无 gaia-md。迁移预检／DDL 和恢复说明已准备，尚未在目标 MySQL 执行，存量业务与组织归属切换待处理；暂不关闭本项，不增加业务验收数。

2026-09-11 合并补充（TECH-20260911-01）：商品结构统一采用 V3，配置／渠道补充迁移不再重复建商品表；后端 63 项测试和 WAR 构建通过。状态继续为待验证，目标环境升级与业务验收仍未执行，见[合并回归记录](../specs/2026-09-11-业务数据独立边界与已开发功能核查.md#7-跨仓合并与最终回归2026-09-11)。

### OPT-20260911-01：五个目录菜单增加卡片视图

- 类型／优先级／状态：体验优化／P1／待验证（代码、隔离及真实登录态展示研发验证完成）。
- 应用／角色／模块与入口：Web 后台／沿用现有角色／商品分类、商品系列、商品档案、配套品档案、配件档案。
- 来源／日期／负责人：用户确认设计并要求实施、抽取公共组件与兼容深色模式；首次提出及最后更新 2026-09-11；负责人待分配。
- 关联需求／Spec：[商品目录卡片视图与深色模式](../specs/2026-09-11-商品目录卡片视图与深色模式.md)。
- 验证标准：表格保留、切换零查询、筛选分页共用、用户／租户／菜单偏好隔离；图文层次和关联入口清晰；权限降级、无图／坏图、加载／空／失败、深色与定位行为可验证。
- 实现与研发证据：共享卡片、切换器和列表插槽已应用五菜单；商品列表增加首图字段，无数据库迁移。后续以真实官网图片完成 cover 铺满、上传／编辑尺寸建议、重复型号及空字段精简。前端 154 项测试、类型检查、构建及真实登录态五菜单展示复查通过；保留原后端目录 40 项测试证据。完整业务验收待完成，不改变最终验收数。
- 后续口径：上述按菜单隔离为 2026-09-11 的实现记录；2026-09-12 用户已确认改为支持切换页面共享一份偏好，见 [OPT-20260912-01](#opt-20260912-01)。

### ENV-20260911-01：本机 Redis 重建后主键计数与测试库不一致

- 类型／优先级／状态：本机联调缺陷／P1／待处理（本次目录表已临时校准）。
- 应用／角色／模块与入口：本机 16379 Redis、8080 聚合宿主与共享测试库；目录主档新增。
- 来源／日期／负责人：2026-09-11 新增真实展示资料时实测发现；负责人待分配。
- 关联记录：[TOTO 官网商品展示数据](../specs/2026-09-11-TOTO官网商品展示数据.md)。
- 现象与原因：Redis 被重建且未持久化主键计数，`ID_KE_afs_product_category` 从 1 开始；数据库已有 ID 1，新增分类触发主键冲突并回滚。
- 本次处置：仅对本批使用的售后目录表按数据库最大 ID 与现有 Redis 值取较大值后递增；不覆盖记录、不降低计数、不修改共享 Redis 或认证配置。
- 验证标准／后续：启动流程需在受控、并发安全的前提下保证数据库与主键生成器一致；本次校准不代表该长期问题已修复，重建本机 Redis 后仍需处理。


### OPT-20260911-02：目录详情弹窗与卡片阅读入口

- 类型／优先级／状态：体验优化／P1／待验证（代码及研发验证完成）。
- 应用／角色／模块与入口：Web 后台／既有可读角色／五个目录菜单。
- 来源／日期／负责人：用户要求重新设计详情并判断图片与图标入口；2026-09-11；负责人待分配。
- 关联 Spec：[商品目录卡片视图与深色模式—详情弹窗](../specs/2026-09-11-商品目录卡片视图与深色模式.md)。
- 验证标准：图片预览与详情分工明确；缺图及只读角色也能查详情；图文摘要、分组及关联数量清楚；加载／重试／关闭不串数据，深色可读，表格原规则不变。
- 实现与研发证据：卡片 Eye 入口、详情图片与分组公共组件、关联页签已实现；158 项测试、类型检查和构建通过，真实登录态五类详情与隔离权限／失败／多图场景已复查。业务验收待完成，不改变入口计数。


### OPT-20260911-03：目录表格阅读顺序与双向关联

- 类型／优先级／状态：体验优化／P1／待验证（代码及研发验证完成）。
- 应用／角色／模块与入口：Web 后台／既有可读角色／五个目录菜单（A08、A09、A10、A12、A29）。
- 来源／日期／负责人：用户确认表格信息顺序与关联入口评审后要求执行；2026-09-11；负责人待分配。
- 关联 Spec：[目录表格阅读与双向关联](../specs/2026-09-11-商品目录卡片视图与深色模式.md#表格阅读与双向关联优化2026-09-11)。
- 验证标准：名称主信息、编码次信息，默认列突出产品识别与关系；只读数量入口及分类／系列链接明确，商品反查按精确 ID 匹配；缺权限／ID 不误跳转，同模块定位不被原弹窗遮挡；深色、图片预览、成本价可选、分页和关闭隔离有效。
- 实现与研发证据：公共资料单元格、五表列重排、双向关联及配套品定位已实现；161 项前端测试、42 项后端集成测试、类型检查和构建通过，真实登录态五表与关联链复查完成。无数据库迁移，不增加入口或业务验收数。

<a id="opt-20260912-01"></a>

### OPT-20260912-01：支持切换的列表共享表格／卡片偏好

- 类型／优先级／状态：体验优化／P2／待验证（代码、自动化及本地浏览器研发验证完成，完整业务验收待完成）。
- 应用／角色／模块与入口：Web 后台／跨视角／商品分类、商品系列、商品档案、配套品档案、配件档案（A08、A09、A10、A12、A29）；仅表格页面不受影响。
- 来源／日期／负责人：用户确认所有支持切换的位置共用显示偏好并缓存恢复；首次提出及最后更新 2026-09-12；负责人待分配。
- 关联需求／Spec：延续 OPT-20260911-01，见[商品目录卡片视图与深色模式—共享显示偏好](../specs/2026-09-11-商品目录卡片视图与深色模式.md#opt-20260912-01)及 UI 规范第 6.4 节，不新增业务需求编号。
- 验证标准：任一支持页切换后，其他支持页同步；刷新与同账号重新登录恢复；账号及可获得租户范围隔离，同源跨标签同步且无回写循环；不覆盖导航／配色，不迁移没有时间戳的旧菜单偏好；仅表格页面保持原行为，切换不增加业务请求或重置筛选分页。
- 实现与研发证据：五页复用 `useListView + Pinia`，独立共享缓存键与 `storage` 同步已实现；24 项相关测试、类型检查及生产构建通过。完整测试 170 项中 167 通过，3 项已在修复前基线复现的失败另行保留。本机 9010 真实商品系列切卡片后商品档案保持、刷新恢复，以及与隔离生产组件页双向跨标签同步通过；隔离分类／配套品／配件继承偏好，商品筛选和第 2 页保留，切换前后请求计数维持 3。退出再登录及身份隔离由自动化验证，未执行真实退出或业务写入。没有后端或数据库变更，不增加业务验收数。

<a id="bug-20260912-03"></a>

### BUG-20260912-03：取消安装工单后实例仍占用导致无法重新申请

- 类型／优先级／严重程度／状态：缺陷候选／P1／S2（新建路径受阻）／待确认（取消后策略待确认，源码现状已核查，运行复现待补）。
- 应用／角色／模块与入口：Web 后台／门店、客服／安装申请与异常处理（D07、A16、A17、A20），关联 A38 安装码状态展示。
- 来源／日期／负责人：本次安装码完整逻辑核查发现；首次提出及最后更新 2026-09-12；负责人待分配。
- 关联需求／Spec：BR-003、BR-004、BR-013；[安装码标识核查](../specs/2026-09-12-安装码管理恢复与标识关系核查.md)、[派单异常与完工审核](../specs/2026-09-10-派单异常与完工审核全栈首版.md)。
- 环境与前置：当前售后后端工作分支源码；已确认购买登记的实例创建安装工单后为 `APPOINTED`，工单尚允许取消。未在共享业务库写样例或复现。
- 复现步骤：在受控环境创建安装工单 → 通过异常取消动作取消 → 使用新请求 ID 对同一实例申请新的安装工单。
- 实际结果：取消动作只将工单改为 `CANCELLED`，实例未恢复 `AVAILABLE`；新安装申请被实例状态校验拒绝。复开原异常工单与取消后重新建单是不同路径。
- 预期与待确认边界：取消后产品应有明确可执行的后续处理；须确认采用释放后重约还是仅允许复开，并统一页面提示、实例占用和关联历史，不能依靠后台手改状态。
- 证据：[CustomerAppService.createAppointment/createServiceOrder](../../../backend/gaia-after-sales/gaia-after-sales-core/src/main/java/com/ehsure/gaia/afterSales/customer/appservice/CustomerAppService.java)、[WorkOrderWorkflowAppService.exception](../../../backend/gaia-after-sales/gaia-after-sales-core/src/main/java/com/ehsure/gaia/afterSales/workOrder/appservice/WorkOrderWorkflowAppService.java)、[异常更新 SQL](../../../backend/gaia-after-sales/gaia-after-sales-core/src/main/resources/mappers/afterSales/WorkOrderWorkflowMapper.xml)。
- 验证标准：按确认的取消后策略走通后续安装，保留原工单历史；仍存在活动工单时拒绝重复申请；并发取消／重约不产生双重占用。本次只登记问题，未修改状态机。

<a id="bug-20260912-04"></a>

### BUG-20260912-04：活动工单统计未排除 CLOSED 导致再次售后受阻

- 类型／优先级／严重程度／状态：缺陷／P1／S2（流程受阻）／已确认（源码口径冲突已核查，运行复现待补）。
- 应用／角色／模块与入口：Web 后台／客服／服务受理、服务工单（A17、A18），关联 A38 产品实例查找。
- 来源／日期／负责人：本次安装码完整逻辑核查发现；首次提出及最后更新 2026-09-12；负责人待分配。
- 关联需求／Spec：BR-003；[安装码标识核查](../specs/2026-09-12-安装码管理恢复与标识关系核查.md)、[服务受理](../specs/2026-09-10-服务受理与代客建单全栈首版.md)。
- 环境与前置：当前售后后端工作分支源码；某非退货实例仅关联状态为 `CLOSED` 的历史工单。当前完工审核只推进 `PENDING_HANDOVER`，未发现完整关闭写接口，不能把此前提写成已走通正常关闭流程。
- 复现步骤：在隔离测试夹具准备一个 `CLOSED` 历史工单及实例关系 → 以新请求 ID 对同一实例创建维修或指导工单。
- 实际结果：`activeOrderCount` 只排除 `COMPLETED / CANCELLED / REJECTED`，将 `CLOSED` 计为活动，建单返回已有进行中服务工单冲突。
- 预期：统一正式工单终态口径，已关闭历史工单不占活动名额；新服务保留原实例和完整历史，真实活动工单仍按确认规则限制。跨服务类型并发策略另按 BR-003 确认，不能顺带放开。
- 证据：[CustomerMapper.activeOrderCount](../../../backend/gaia-after-sales/gaia-after-sales-core/src/main/resources/mappers/afterSales/CustomerMapper.xml)、[CustomerAppService 状态集合与建单检查](../../../backend/gaia-after-sales/gaia-after-sales-core/src/main/java/com/ehsure/gaia/afterSales/customer/appservice/CustomerAppService.java)、[完工审核终点](../../../backend/gaia-after-sales/gaia-after-sales-core/src/main/java/com/ehsure/gaia/afterSales/workOrder/appservice/WorkOrderWorkflowAppService.java)。
- 验证标准：`CLOSED` 历史工单下维修／指导可新建，进行中工单仍拦截，取消及退货边界不回归；至少覆盖实例行锁下的重复提交。本次只记录缺陷，未变更代码或业务数据。
