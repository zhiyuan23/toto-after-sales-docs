# 小程序通用底座整治

- 编号：TECH-20260914-01
- 状态：待验收（架构实施完成，外部环境验证另列）
- 来源：用户于 2026-09-14 明确要求删除当前 Demo 样式和全部业务功能代码，并执行底座评审建议；本指令覆盖本次实施，无需再次设计确认。
- 范围：`mobile/gaia-after-sales-uni`、`mobile/gaia-customer-service-uni` 及直接受影响的 TOTO 工程/进度文档。后端、Web、原型与正式业务需求不在删除范围。
- 关联：消费者 C01–C17；工程底座不计入业务实现/最终验收数。
- 范围补充：用户明确本次主要完成架构优化，不要求功能模块系统性完整实现。独立示例仅用于说明底座用法，后续不扩展为业务功能。
- 实施：Codex；业务验收负责人待分配。

## 实施契约

1. 清空旧售后页面、业务 API、登录演示、C 编号源码映射、旧 Demo/设计系统及自定义 TabBar；保留通用技术栈、基础主题、反馈、分页和构建上传能力。
2. 正式应用只含中性首页、设置、404；原生 Tab 配置由一份路由元数据生成。示例为单独条件编译分包，正式构建与上传拒绝混入示例/Mock。
3. 租户与环境分离；`pnpm build`／`pnpm build:h5` 未指定租户时默认 `TOTO`，未指定环境时默认 `test` 并使用 profile 中已确认的当前测试地址，显式目标仍可使用 `<TENANT> <ENV>`。production 必须显式提供独立 API 地址，不回退开发／测试地址。当前无已确认生产 API。
4. 提供可注入/可取消请求内核，认证状态和安全回跳、缓存作用域及脱敏诊断。真实身份、Token/响应协议和服务端权限由业务接入。
5. 新建中性列表→详情→编辑参考流程，覆盖固定数据、分页/刷新、异常重试、提交保护和登录回跳；不代表售后业务实现。
6. 统一 check 包含全部必要规则测试；补 doctor、双端冒烟、CI 作业入口及失败日志/截图。实际 Codeup 启用、真机、微信账号配置和发布需独立证据。
7. 清理并收敛 AI 事实入口与过期引用，保留用户其他任务的文档/后端变更。默认不提交、不推送、不上传。

## 验证记录

- Node 24.19.0 / pnpm 10.34.5；主工程和独立冒烟工具冻结安装通过。删除未使用的 Pinia 自动持久化插件；DCloud/Vue/Vite 组合未升级。
- `pnpm verify` 通过类型、ESLint、Stylelint、规则测试、路由/租户派生配置检查，以及微信/H5 普通优化构建。最终规则测试共 67 项：34 租户/上传、17 运行时/示例/分页、4 路由、12 审计策略；最后新增的上传环境/示例拒绝和账号切换隔离回归通过。
- 微信/H5 独立示例优化构建通过；普通产物的示例/Mock 混入校验通过。存在原 DCloud finally 循环依赖警告。
- `SMOKE_CHROME_CHANNEL=chrome pnpm smoke:h5` 通过 8 条场景：Tab、分包、分页、登录参数回跳、编辑保存返回、网络恢复、空态、保存失败保留输入。结果及截图位于源码仓库 `artifacts/smoke/h5/`（本机生成，不提交）。另用浏览器交互复核网络恢复及表单失败保留输入。
- `pnpm run doctor TOTO development` 的版本、依赖、租户环境、端口和开发者工具检查通过。
- 微信开发者工具 2.01.2510290 / 基础库 3.17.2 可启动并切换底座 Tab；`miniprogram-automator` 的元素查询无响应，UI 冒烟在 example 步骤超过 90 秒并返回失败，记录于 `artifacts/smoke/weixin/result.json`。分包导航也观察到回调超时，不能将当前微信 UI 验证计为通过。按本次架构范围停止扩展排查，后续开发微信功能前复核开发者工具兼容性与真实页面事件。
- `pnpm ci:check security` 已真实执行三棵依赖树，退出码 1：主工程 0 critical / 5 high / 19 moderate / 8 low；上传工具 3 critical / 19 high / 21 moderate / 2 low；冒烟工具 0 critical / 0 high / 1 moderate / 0 low。主工程新增 js-yaml 兼容覆盖，冒烟工具修复 jpeg-js/minimist；所有例外基线仍为空。剩余上游风险与公告见源码仓库 `docs/quality-and-security.md`；安全门禁未通过。
- CI 仅交付作业脚本和日志产物，未配置公司 Codeup；真实上传、提审、发布、真机与业务身份均未执行。
- 2026-09-21 双端默认测试构建契约同步完成：默认测试服务器根地址更新为 `http://10.1.1.53:8011`，profile 不包含 `/api`；消费者端追加 `/api/afterSales/mobile/consumer/...`，服务人员端追加 `/api/afterSales/mobile/service-personnel/...`。从开发机直连消费者隐私政策接口得到明确业务响应，确认 `/api` 路径已命中；私有网络 HTTP 仅允许 test 构建，production 仍强制显式 HTTPS 地址。Node 26.7.0、pnpm 10.34.5 下双端构建验证见本次共享底座记录；未执行上传或发布。

## 本地提交复核

- 2026-09-14 用户授权两个小程序先使用本地仓库并提交现有改动；消费者保持 `main`，客服助手初始化 `main` 且仅添加仓库说明，二者均不配置远程、不推送。
- 提交前使用 Node 24.19.0 / pnpm 10.34.5 重新执行 `pnpm verify`，类型、lint、67 项测试、派生配置和微信/H5 构建通过；`git diff --check` 通过。既有 H5 冒烟、冻结安装及安全审计证据沿用上节记录，本次未重新执行微信 UI 自动化或真实依赖审计，相关失败状态不变。

## 验收边界

业务需求和后端自报购买接口继续保留；本次不接真实消费者身份、不写共享数据库、不部署、不上传。旧三 Tab/31 页面壳的历史验证不再代表当前源码。生产 API 与 Codeup 入口未提供，交付可执行脚本和缺配置失败行为，不宣称已上线或完成业务验收。
