# 工单沟通时间线

> 2026-09-11 数据契约更正：沟通记录依赖的工单可见性／锁定校验须改为售后自有业务范围。 现源码已完成本项独立化并通过隔离研发验证，目标环境迁移及业务验收待完成；下文实现、迁移和验证仅记录原时点事实。最新约束与源码证据见[独立边界核查](2026-09-11-业务数据独立边界与已开发功能核查.md)。

- 状态：待验收（隔离研发验证通过）
- 应用与入口：Web 管理后台，工单管理 A18，CS-006。
- 来源：本次用户要求完成非工作台剩余功能；现有需求要求电话、微信、短信、人工备注按时间留痕，只可纠错、不可删除。
- 负责人：Codex（实现），业务验收负责人待分配。

## 实施范围

在工单详情新增沟通记录，记录渠道、业务沟通内容、实际沟通时间、记录人和记录时间。纠错通过追加记录关联原条目实现，保留原文与完整纠错链，不提供 UPDATE 或 DELETE 业务接口。每个工单使用请求号幂等；纠错必须基于最新条目，禁止并发覆盖。权限和数据范围复用当前工单的租户、代理商／门店范围，读取、登记、纠错独立鉴权。

沟通正文加密存储，只有通过工单范围和正文读取权限的请求才能取得业务内容；列表查询、审计、错误日志不打印正文。此入口用于记录已发生的沟通，不主动发送短信或微信，不冒充外部渠道接入。

新建显式迁移和恢复说明；先使用隔离 H2 验证，未经针对最终脚本的授权不执行共享库迁移。前端复用详情容器、列表与弹窗公共方法。

## 验证与剩余

已验证：CustomerIntegrationTest 中的沟通用例覆盖请求重试、同请求号不同内容冲突、追加纠错、重复纠错拒绝、跨组织／租户拒绝、密文存储、HTTP 隐藏字段与时间格式；该测试集共 6 项通过。前端类型检查与构建通过。隔离 H2＋HTTP 的桌面实际操作完成登记、追加纠错和原文保留，列表显示两条记录且旧记录不可再次纠错。

交付文件：后端 `database/20260910_order_communication.sql`、对应 `_permissions.json`、`.rollback.md` 和 `docs/communication.openapi.json`。尚未完成共享环境迁移／真实权限联调／业务验收。工单状态迁移、交单、回访及外部发送仍由各独立切片负责。
