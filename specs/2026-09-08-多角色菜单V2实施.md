# 多角色菜单 V2 实施与验证

- 日期：2026-09-08
- 用户授权：按最新版菜单规划更新程序、当前项目文档和 Gaia 菜单结构；管理员可查看所有菜单。
- 状态：前端 V2 导航与配置清单已实施；正式租户分发、真实角色与业务权限联调待完成。
- 基线：[后台菜单与角色权限手册](../02-技术设计/09-后台菜单与岗位权限矩阵.md)、[技术菜单设计](../02-技术设计/03-gaia-ui目录与菜单设计.md)。

## 1. 结果与范围

在 `gaia-ui` 唯一“售后服务”顶部入口下，由“运营工作台”页面提供工作视角选择器：总部运营、客服作业、服务站作业、门店业务。左侧按当前视角组织菜单：4 视角、18 展示组、75 引用、49 独立功能；63 个 Web 需求入口保持不变。既有 49 个功能 code/path、业务代码目录和数据库菜单 ID 不因展示分组改变而重建。

| 角色模板 | 默认视角 | 功能数 |
| --- | --- | ---: |
| 系统管理员 | 全部，可切换 | 49 |
| 总部管理／运营 | 总部运营 | 34 |
| 客服主管 | 客服作业 | 17 |
| 客服专员 | 客服作业 | 15 |
| 服务站管理员 | 服务站作业 | 19 |
| 代理商管理员 | 门店业务 | 4 |
| 店员 | 门店业务 | 4 |

服务结算仅供管理员查看待启用状态，未开放办理。四类全局系统能力继续由 Gaia 原有菜单承接，未复制系统管理菜单，未虚构全局快捷链接。

## 2. 导航实现

- `workspace-manifest.mjs`：四视角分组、默认首页、七种角色模板、五个导航授权节点。
- `workspace-navigation.mjs`：对新鲜 Gaia 菜单做功能与视角交集、过滤禁用节点和空分组、投影侧栏、校验直达路径。
- `navigation.ts`：保存当次授权结果与当前视角，sessionStorage 仅保存账号视角偏好；无令牌／重新初始化时清空授权快照。
- `WorkspaceBar.vue`：仅在“运营工作台”展示工作视角选择、系统管理员角色提示和离开当前页面提示；其他售后页面不再重复显示这一行。
- Gaia 路由、顶部菜单、侧栏和页面布局：接入售后导航；旧分组及根路径重定向到有效首页，越权功能转 `/401`，菜单加载失败不循环重试。
- 售后页面切换视角或路由时重新挂载，不复用旧视角页面实例；已接入列表 hooks 的清理机制取消旧请求。非售后页面保持原有布局与缓存行为。
- 功能页的分组提示使用当前视角分组，服务结算说明与管理员可查看规则一致。

工作视角不改变后台身份、岗位动作或组织数据范围。D01 门店上下文仍待真实契约；D05/D06 仍是隐藏流程骨架，实际会话及对象校验随登记切片实施。组织切换、上下文标签、待办计数及全局快捷入口不在本次假定已完成。

## 3. Gaia 授权与菜单交付

核对到现有登录页为用户写入固定 `roles: ['admin']`，不能作为真实管理员依据。正式导航使用 `/api/sys/Module/tree?categoryCode=pc_web` 中有效的功能节点和以下隐藏导航授权项：

| code | path | 用途 |
| --- | --- | --- |
| afsNavigationAdministrator | afterSales/access/administrator | 管理员可查看全部视角；功能本身仍须在后端返回 |
| afsWorkspaceHeadquarters | afterSales/access/headquarters | 总部运营 |
| afsWorkspaceServiceDesk | afterSales/access/serviceDesk | 客服作业 |
| afsWorkspaceStation | afterSales/access/station | 服务站作业 |
| afsWorkspaceDealer | afterSales/access/dealer | 门店业务 |

这些项配置为 `type=1`、`isHide=1`，共用售后根节点，承载页只重定向到有效首页。不赋予业务 API、按钮或跨租户权限。禁用父节点会屏蔽整棵子树；code 与 path 不匹配的授权不会接受；无有效视角或功能时拒绝访问。

菜单配置清单从与程序相同的常量按需生成，包括 68 个唯一节点和角色菜单引用。文档归并后不再保存 JSON 副本，保留[生成脚本](../../../frontend/gaia-ui/scripts/after-sales-menu-package.mjs)，分发前生成并核对。生成命令（在 gaia-ui 仓库）：

```sh
node scripts/after-sales-menu-package.mjs /tmp/toto-menu-v2-review.json
```

清单是按 code 复核的配置源，不是批量 API 请求。目标环境需解析实际模块、父节点、租户和角色 ID，合并原授权并走 `gaia-tenant` 既有分发与回执链；不得覆盖无关模块或直接更新运行库 `sys_module`。已核对平台 `TenantModuleDTO`、`TenantSaasModuleSaveDTO`；租户分配接口要求实际 tenantId 和 moduleOperationIdList，不能填本清单的 code 代替。

用户已确认后端菜单功能配置完成；当前前端不再补充 V1/V2 节点。是否可见完全取决于登录账号从菜单接口获得的实际功能节点和导航授权项，仍以本次允许／拒绝账号联调结果作为验收证据。

## 4. 正式配置模式

已删除 `VITE_ENABLE_AFTER_SALES_LOCAL_MENU`、运行时菜单注入和“本地菜单预览”提示；后端也删除 development 用户白名单放行。工作视角只组织菜单展示，不生成授权：系统管理员下拉项要求 `afsNavigationAdministrator`，每个功能仍须存在于当前账号的真实菜单树；API 访问统一匹配 Gaia 模块／操作 `apiList`。

## 5. 验证与剩余工作

- 22 项 Node 测试通过：15 项导航测试及 7 项既有通用组件、请求状态和商品目录测试。覆盖配置菜单投影、无权限直达、禁用节点及父节点、伪管理员标记、缺少视角、角色合并和旧链接；并检查运行路由不再引用本地菜单注入或环境开关。
- 63 个需求入口覆盖检查通过，68 个动态落地 path 存在，配置清单 code/path 唯一，角色引用全部可解析。
- 售后新增及修改文件定向 ESLint 通过；本次 10 份文档本地链接检查通过，49 项 × 7 角色的代码配置与业务矩阵逐项一致。
- 浏览器已验证总部默认首页、门店切换提示与切换后 4 菜单、服务站直达及刷新恢复、客服作业分组、管理员服务结算待启用页（无办理操作）；桌面截图检查通过，保留实际分类页面和管理员预览标识。
- 最终 `yarn build` 通过（45.43 秒，保留仓库既有大包等警告）；`git diff --check` 通过。构建不是全仓 TypeScript 检查，仓库未配置 vue-tsc 依赖，不将构建冒充全仓类型验证。
- 正式权限恢复后重新执行：后端干净编译 14 项测试通过并完成宿主重启；前端 22 项测试、63 入口覆盖检查和生产构建通过。浏览器使用当前无售后工作视角权限的账号 `00001` 验证，顶部不显示“售后服务”，手动输入售后页面地址也不可进入。

待完成：拒绝侧菜单和页面已验证；仍需用实际允许账号验证各角色菜单、按钮/API，并继续验证拒绝侧 API 及跨组织／租户边界。业务完成数不变，不把菜单可打开计为业务完成。不提交或推送 Git；未修改受保护的 `src/views/common`。

## 6. 菜单文档归并（2026-09-08）

按用户确认，将最新完整目录、角色矩阵、岗位操作分工、多角色数据范围和验收要求合并到[后台菜单与角色权限手册](../02-技术设计/09-后台菜单与岗位权限矩阵.md)，作为唯一业务阅读入口；原独立方案和目录文档不再保留。JSON 生成产物从文档目录移除，生成脚本保留，技术菜单设计和本记录改为按需生成说明。此次只整理文档，不修改运行导航、角色授权或交付进度。

归并验证：49 项功能的需求编号及 7 类角色矩阵与代码一致，75 个视角引用匹配；生成脚本可重新生成 68 个唯一节点。8 份修改文档的 104 个本地链接及锚点检查通过，指定文件已删除且无残留引用。本次未修改程序代码，因此未重复运行构建与业务测试。

<a id="v3-delivery-tool"></a>

## 7. V3 菜单交付工具收敛（2026-09-13）

当前独立子系统已收敛为 48 个唯一功能、41 个去重侧栏功能、1 个上下文功能、6 个导航外规划功能和 5 个隐藏导航授权。此前保留的根仓生成器仍读取 legacy 清单，实测会生成 53 个功能：包含当前已退出的 `afsDealerAppointment`、`afsDecorationCompany`、`afsServiceItem`，还把不由 V3 管理的下载中心三项纳入当前包，并缺少当前 `afsDashboardCustomerService`，不能继续作为数据库或租户分发依据。

本次将 `scripts/after-sales-menu-package.mjs` 改为直接读取 `apps/after-sales/src/features/navigation/manifest.json`，生成 64 个当前授权节点：售后根 1 个、稳定存储分组 10 个、功能 48 个、隐藏视角授权 5 个。跨视角展示仍复用唯一功能 code；`afsAppointment` 和 6 个导航外功能保持隐藏，服务结算保持可注册但只由管理员另授。明确退出范围的 3 个 legacy 节点只作为人工复核的退役候选列出；下载中心和经销商隐藏流程列为兼容保留节点，本次不新增、不改名、不停用，从而避免影响根系统旧入口。

生成器可同时输出只读预检 SQL，覆盖模块 code/path、父级、启停、隐藏状态、角色绑定、按钮操作和 `api_list`。交付包明确记录 `saveTenantModuleOperation` 是 `pc_web` 整份选择保存：发布前必须先读取目标租户现有选择，按真实 ID 合并售后增量，禁止只提交售后节点导致根系统无关模块被删除。

验证结果：

- `node --test scripts/after-sales-menu-package.test.mjs`：6 / 6 通过；覆盖当前数量、legacy 退役／兼容保留边界、客服工作台缺失、跨视角单注册、隐藏节点、租户全量覆盖保护和 SQL 只读性。
- 生成 `/tmp/toto-after-sales-menu.json` 与 `/tmp/toto-after-sales-menu-preflight.sql` 成功；交付包确认 64 个节点，预检只包含 `SELECT`。
- `git diff --check` 通过。

测试环境执行结果（2026-09-13）：

- 当前宿主配置关闭多租户，测试数据库 `gaia_wh_init_wzy` 的 `sys_module` 是主系统权限配置页的实际菜单来源。本次未修改根系统源码或 `gaia-tenant` 服务。
- 写入前只读预检确认 21 个现有节点存在名称／排序／显隐差异，缺少 `afsDashboardCustomerService`；3 个退出范围节点仍有角色或按钮绑定。同步采用单事务执行，校验失败自动回滚。
- 最终事务已提交：新增“客服工作台”，修正 21 个节点；`afsDealerAppointment`、`afsDecorationCompany`、`afsServiceItem` 的角色菜单与按钮绑定解除后设为停用、隐藏。下载中心三项和经销商三个隐藏流程节点保持启用、路径、角色绑定及按钮数量不变。
- 提交后二次执行完整结构漂移预检，返回 0 条；3 个退出节点的角色菜单与按钮绑定均为 0；非售后模块仍为 846 项。
- 使用测试账号 `00001` 打开主系统“系统设置 → 权限管理 → 角色管理”，在“管理员”角色权限树展开“售后服务”后，确认可选择“运营工作台”“客服工作台”“服务工单”“安装申请查询”“购买登记”等最新节点，旧“预约管理”和“家装公司”不再出现。本次只验证权限树，没有保存或改动具体角色授权。

以上仅完成当前关闭多租户测试环境的菜单注册与权限树可选性验证。具体岗位授权仍由管理员在角色管理中选择并保存；启用多租户的目标环境仍须读取租户完整选择后合并售后增量，通过 `gaia-tenant` 分发并完成真实允许／拒绝账号验收。

### 7.1 测试环境角色保存主键冲突处理（2026-09-13）

主系统保存“管理员”角色权限时曾返回 `sys_role_operation` 主键冲突。服务日志确认批量插入使用 Redis 表级计数器 `ID_KE_sys_role_operation` 生成主键，首次生成的 `907` 已被测试库占用；并非菜单 code、新增菜单 ID 或用户勾选冲突。

已按数据库实际最大主键校准两张角色关联表的 Redis 水位：`sys_role_operation` 为 `390284`，`sys_role_module` 为 `2026090801001943`。只读复核确认下一主键 `390285`、`2026090801001944` 均未占用；保存失败后角色 1、2 的按钮令牌数与 `sys_role_operation` 关联行数分别保持 `990 / 990`、`923 / 923`，未发生部分删除或半提交。此次只修正测试后台主键生成状态，未修改角色的具体菜单选择；由权限管理员在原弹窗重试保存。
