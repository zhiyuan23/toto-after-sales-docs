# 商品配套关系独立系统全栈首版

- 日期：2026-09-09
- 范围：A12、ADM-MD-010、FS-008 的商品配套关系主数据切片
- 状态：开发中；代码与隔离环境闭环完成，共享数据库迁移、正式菜单角色和真实租户验收待完成
- 实现端：`gaia-ui/apps/after-sales`、`gaia-after-sales`；未修改主系统 legacy 页面、移动端或受保护的 `src/views/common`

## 1. 本次确认的业务口径

1. 配套关系是售后独立系统自有主数据，只复用 Gaia 的登录、菜单、接口权限、租户路由、主键、响应、事务和共享商品主档。
2. 一条记录表示“主商品 → 配套商品”的有向关系，维护配套数量、含首尾日期的生效区间、备注和启停状态；不把反向关系自动视为成立。
3. 禁止商品关联自身，启用关系图禁止形成直接或间接循环；同一有向商品对的启用生效区间禁止重叠。停用记录保留历史，重新启用时重新校验商品状态、循环和区间冲突。
4. 新增启用关系时，两端商品的售后状态和共享主档状态都必须启用。系统不修改或复制共享商品整行，不提供商品或配套关系的物理删除。
5. 本切片不包含组合 BOM、替代品优先级、价格、库存、领退料、工单耗用、批量导入或历史单据反向修改。

## 2. 数据与接口

新增售后自有表 `afs_product_bundle`，只保存两端共享商品 ID、数量、有效期、备注、状态、版本及创建/更新审计字段。迁移脚本为 `gaia-after-sales/database/20260909_product_bundle.sql`，依赖目录基础迁移；脚本未在共享数据库执行。

新增接口：

- `GET/POST /api/afterSales/catalog/bundles`
- `GET/PUT /api/afterSales/catalog/bundles/{id}`
- `PUT /api/afterSales/catalog/bundles/{id}/status`

列表支持两端商品编码、名称、机能型号的统一关键词、指定生效日期、状态和分页筛选。所有 BIGINT ID 以字符串传输；写操作使用独立接口权限、租户路由、事务、目录锁、版本令牌和审计。

## 3. 独立前端

`apps/after-sales` 的 `/catalog/bundle` 已从占位页切换为真实页面，使用子系统原生 `FaPageMain`、`EsSearch`、`EsListToolbar`、`EsTable`、Element Plus 弹窗、`useListPage` 和 `useCrudDialog`。

已实现查询、筛选、重置、分页、新增、详情回填编辑、启停、日期和数量校验、远程商品检索、失败保留输入、重复提交保护与操作后自动刷新。行操作由后端 actions 决定；商品查询权限与配套维护权限分别校验。

## 4. 验证证据

- 后端：`mvn -B -o -pl gaia-after-sales-api -am test`，core 2 项、API 集成 17 项，共 19 项通过。
- 后端覆盖：方向、自关联、启用区间重叠、间接循环、停用保留、重新启用复验、停用商品拒绝关联、事务审计、版本冲突、字符串 ID、显式权限和租户隔离；商品分类、商品档案和商品系列回归继续通过。
- HTTP：真实 Tomcat + 生产 AppService/MyBatis XML + 路由 H2 返回配套关系，日期按 `yyyy-MM-dd`、BIGINT ID 按字符串传输。
- 前端：Node 原生测试 48 项通过，`vue-tsc -b` 和 Vite production build 通过；现有 Vite native-config 与大 chunk 警告未扩大为本切片失败。
- 浏览器：`127.0.0.1:9013` 隔离入口验证桌面列表、日期展示、远程商品选择、新增、保存后自动刷新、按机能型号筛选和停用后自动刷新。

## 5. 未验收与后续

- 2026-09-09 复核[脑图导出](../00-项目资料/原始摘录/TOTO售后服务系统调研.md)发现源菜单明确为“配套品上传”。本切片的关系维护已实现，但不代表上传能力已覆盖；上传对象、格式与处理规则见 [Q-MM-01](../03-开发管理/02-风险与待确认事项.md#mindmap-questions)。现有页面和 `ProductBundleAppService` 仅实现关系查询／维护／启停，与本次确认范围一致，本次不据名称猜测新增导入 API。
- 未执行共享 MySQL 迁移，未写共享业务数据，未新增或授予正式角色权限。
- 正式 7004/9010 入口及 8080 宿主仍需数据库连通后，以获授权总部账号验证允许侧、拒绝侧、租户边界和菜单装配。
- 当前严格禁止任何启用关系形成循环，不按日期窗口放宽；若未来确认需要季节性反向配套，应先形成新的业务规则再调整。
- 历史工单只应保存业务快照，不能反向阻止配套主档停用；工单模型不在本切片内，待其 Spec 中落实。
