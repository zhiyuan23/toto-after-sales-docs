# 消费者客服渠道与本人服务记录切片

- 日期：2026-09-20
- 范围：C09 客服渠道、C10 产品相关服务记录；C07 服务申请的依赖边界
- 状态：源码切片；共享测试环境已配置客服渠道，尚未完成微信平台会话、真实账号及业务验收

## 已实现契约

`GET /api/afterSales/mobile/consumer/support-channels` 返回当前租户已生效的客服电话、服务时间和微信客服开通状态。租户数据库尚无配置时，继续使用 `after-sales.consumer.support.*` 部署参数作为首次初始化兜底；管理员首次保存后，`afs_consumer_support_settings` 的租户配置完整覆盖兜底值。电话未启用、号码缺失或格式无效时不开放拨号，微信客服开关默认关闭且只有完成微信平台客服配置后才应开启。微信小程序使用原生 `open-type="contact"`，电话使用 `uni.makePhoneCall`；H5 不显示可用的微信客服会话。选择渠道只代表发起平台能力，不代表客服已接通、已收到 AI 摘要或服务已受理。

`GET /api/afterSales/mobile/consumer/products/{instanceId}/services` 只读返回工单号、类型、模式、状态、期望日期和产品名。后端要求有效消费者会话、当前账号活跃产品关系，以及订单由该账号创建或订单联系手机号摘要与该账号已验证手机号一致；对其他账号返回空列表。响应白名单不含联系人、地址、内部派单资料或问题描述。产品详情显示服务端原始状态映射及分页，不把原型演示订单充当正式数据。跨账号 H2 隔离测试通过；目标库、真实订单和移动端实机仍待验证。

同范围的 `GET /api/afterSales/mobile/consumer/products/{instanceId}/services/{orderId}` 查询单条工单。产品详情的服务记录可进入正式进度页重新读取状态；直达 URL 同样经过有效会话、本人活跃产品关系、已验证手机号或本人创建者条件，其他账号及错配产品统一不能读取。页面只展示工单状态、产品、服务类型／方式与期望时间，不从演示时间线推断已派单、已确认或已完成。H2 覆盖本人可读、跨账号拒绝、同账号错配产品拒绝；尚无真实目标环境状态流转证据。

`GET /api/afterSales/mobile/consumer/services` 用同一服务端范围条件分页汇总当前账号所有活跃产品关系的服务项目。正式“服务”Tab 读取真实记录，展示工单状态、产品和期望日期，并进入上述详情；原型订单只在视觉评审构建中使用。同一工单包含多件产品时按产品项目分别显示和计数，避免从某一件产品推断整个工单的服务资格。列表为空表示当前条件下没有可见服务记录，不表示从未有历史工单。H2 覆盖本人有记录、其他账号为空；共享目标库和真实状态流转待验。

## 尚缺与下一步

- C09：共享测试库已执行新的租户配置表、A51 菜单与权限迁移，当前配置表无记录；新版 Web／后端尚未部署，运行环境仍使用部署参数兜底。完成发布并通过后台保存后，还需确认微信小程序后台已为当前 AppID 开通客服能力，并完成真机拨号与真实会话验证。
- C10：当前只展示与本人已验证手机号或本人创建记录相符的产品相关服务单；授权家庭成员、转赠后的历史可见性规则待确认。
- C07：本人产品故障选项只读 API、维修表单选择及正式远程指导地址采集已形成[FS-035 前置切片](2026-09-20-消费者维修故障选项与目标环境核验.md)。维修／指导正式提交、媒体压缩上传与内容安全、服务资格判断和活动工单幂等仍未接入消费者 API；故障分类的运营维护与媒体规则待确认。正式入口继续导向 C09 渠道页，避免把演示表单当成可提交申请。后台受理仅允许 `VERIFIED + ACTIVE` 记录；产品已添加不构成服务资格。消费者安装入口保持未开放。

本切片不改变消费者完整入口 **0 / 18** 或最终业务验收 **0 / 18** 的计数。

2026-09-20 本机运行补证：H5 开发代理修复后，真实宿主的 `/support-channels` 返回 `code=0`、空电话与关闭的微信客服；浏览器渠道页展示对应不可用状态，没有网络错误。无效会话访问 `/services` 与 `/records` 返回业务 `4001`。真实客服号码、微信平台会话、消费者账号及实际服务记录仍未验收。

## 2026-09-23 客服调用链收口

- C09 电话按钮改为共享的直接拨号封装，空号码不调用平台能力，拨号期间阻止重复点击，取消或失败给出明确反馈。微信小程序按钮继续使用原生 `open-type="contact"`，会话来源只包含 `assistant/consumer` 与“摘要是否复制”的布尔标记，不传消费者问题、手机号或商品信息；H5 仍明确不可发起微信客服。
- C16 在正式“我的”页恢复为条件入口：读取同一 `/support-channels`，只有返回有效热线时才显示，点击直接拨号。接口失败或号码为空时隐藏，不用历史截图号码或占位号码兜底。
- `ConsumerSupportChannels` 对非法租户 ID 配置改为关闭全部渠道，不再因 `Long.valueOf` 导致应用启动失败；宿主无密钥配置模板已列出热线、时间和微信开关，实际值继续只写部署侧外部文件。
- 消费者端类型检查与 44 项单元测试通过，其中覆盖电话号码去空白、空配置拒绝、平台失败及非敏感微信会话来源；后端客服配置测试 1 项通过。开发者工具已验证原生模拟客服会话入口；真机拨号与真实客服会话未验，因此不提升入口或最终验收计数。

## 2026-09-23 共享测试环境配置与复测

- 开发机首次请求 `http://10.1.1.53:8011/api/afterSales/mobile/consumer/support-channels` 复现 `phone=""`、`hours=""`、`wechatEnabled=false`，确认页面提示“客服渠道暂未开放”与按钮禁用的直接原因是外部运行配置缺失，不是前端点击绑定失效。
- 依项目联调环境持续授权，先备份 `/yundata/saas_test/tomcat11/conf/gaia-after-sales.properties`，再配置 TOTO 官方热线 `400-820-9787`、服务时间“周一至周日 8:00-22:00（法定节假日除外）”和 `wechat-enabled=true`。首次使用原始中文值触发 Java properties 编码乱码，已二次备份并改为 Unicode 转义。两份回退备份分别位于 `/yundata/saas_test/backups/20260923094415-consumer-support/` 和 `/yundata/saas_test/backups/20260923095211-consumer-support-hours-encoding/`。
- Tomcat 两次均使用 `SIGTERM` 正常停止后由 `startup.sh` 启动；最终开发机从共享入口请求得到 HTTP 200，返回 `phone="400-820-9787"`、正确中文 `hours` 及 `wechatEnabled=true`。消费者端同时改为每次 `onShow` 刷新渠道，旧空值下点击电话会重新拉取，微信未开启态也提供刷新和明确反馈，避免页面缓存旧配置后继续表现为“点击无反应”。
- 端侧定向 ESLint、Stylelint、23 项运行时单测、TypeScript 类型检查及 TOTO/test 微信构建通过；产物确认包含 `open-type="contact"`、`session-from`、关闭态刷新按钮，并指向 `10.1.1.53:8011`。微信平台是否已对当前 AppID 开通客服、真机拨号与真实客服会话仍需人工验收。
- 当前微信开发者工具实际使用的 development 产物指向 `127.0.0.1:8080`，不是上述共享测试宿主。已将同一组客服配置加入受管本地后端启动脚本，启动器重新安装售后模块并启动聚合宿主后，`/api/api/afterSales/mobile/consumer/support-channels` 返回 HTTP 200 及同样的热线、服务时间与开关。开发者工具 iPhone 15 Pro Max 模拟器已目视“智能助手 → 直接联系人工客服”，页面正确显示号码、时间及两个可用按钮；点击在线客服后进入微信开发者工具原生“模拟进入客服会话”页，并按平台预期提示开发者工具不支持真实会话、需真机调试。

## 2026-09-23 A51 后台租户配置源码

- Web 在总部运营视角的“运营管理”分组新增“客服渠道配置”（`afsConsumerSupportConfig`，`afterSales/customerService/supportChannel`）。总部运营视角角色默认可查看；页面可维护电话客服启停、号码、服务时间和微信在线客服开关，写入由独立 `saveConsumerSupport` 操作授权控制。
- 后台接口为 `GET/PUT /api/afterSales/business-config/consumer-support`。GET 返回配置来源、写权限、版本和最近更新信息；PUT 校验当前租户、号码格式、启用电话时的必填字段及乐观版本，不接受客户端租户 ID。
- `20260923_consumer_support_settings.sql` 新增当前配置表和不可变历史快照表；每次创建／修改均记录完整渠道快照、版本、操作人和时间。公开小程序接口的响应结构保持不变，读取数据库配置优先，部署参数只在该租户无数据库记录时兜底。
- `20260923_consumer_support_settings_permissions.sql` 在既有客户服务存储域新增 A51 菜单、读 API 和“保存客服配置”操作；拥有总部运营视角的角色默认获得菜单查看权限，保存默认只跟随已有售后管理员授权，其他运营负责人可在 Gaia 菜单与角色管理中单独授予。源码编译、后端 3 项定向测试、前端 ESLint／依赖／TypeScript／i18n、28 项导航与菜单测试及菜单交付包 6 项测试已通过。共享测试库迁移已执行，但新版 Web／后端和共享环境页面尚未发布验证，因此仍不提升入口与最终验收计数。
- 2026-09-23 迁移回读：目标库为 `gaia_wh_init_wzy`（MySQL 8.4.10-10）；`afs_consumer_support_settings` 与历史表均创建成功且当前 0 行；`afsConsumerSupportConfig` 启用并挂在 `afsCustomerService` 下，GET 与 `saveConsumerSupport` PUT 权限分别正确配置；5 个总部运营视角角色均获得查看权限，4 个售后管理员角色获得保存权限，旧 `afsSystemConfig/afsBusinessConfig` 节点为 0。

## 2026-09-23 客服配置代理装配与页面整理

客服配置应用服务抽出 `ConsumerSupportSettings` 接口，管理 API 与消费者渠道服务按接口注入，兼容宿主 JDK 动态代理；渠道契约、权限与事务规则不变。`ConsumerMobileWiringTest` 验证消费者及客服配置代理与两个 API 能正确装配，`ConsumerSupportChannelsTest` 3 项通过。客服配置页同时整理为自然高度的单卡片与分段表单，减少重复说明；真实角色保存与共享环境页面仍待验。

提交前复核：Web `check` 的 ESLint、依赖、类型与国际化检查通过；全量测试 296 项中 266 项通过、30 项失败。将当前 HEAD 导出到临时目录并复用相同依赖复测，测试总数及全部失败项与工作区一致，未发现本次新增失败；现存断言与路径别名问题仍保留，不将全量检查记为通过。
