# 服务资源独立系统首版

日期：2026-09-09

范围：A26 服务人员、A27 排班管理、A28 服务质量的可验证首版。

## 已确认实现边界

- 服务人员是售后自有主档，维护编码、姓名、手机号、所属服务站和启停；编码、手机号在租户内按大小写不敏感唯一。人员必须归属一个已启用服务站，停用人员不能新增或修改日可用性。
- 排班采用按“人员 + 日期”唯一的日可用性记录，覆盖工作日（`AVAILABLE`，容量 1～999）、休假（`LEAVE`）和临时停单（`SUSPENDED`，后二者容量固定为 0）。这让后续派单可按日期过滤人员和容量，但本切片不实现派单、工单占用或周期排班模板。
- 服务质量只读读取 `afs_service_quality_daily` 的事实汇总投影，展示服务量、及时率、一次完工率、评价均分和投诉数。该表只能由后续工单完工／评价／投诉闭环的聚合任务写入；当前系统没有这些事实来源时，接口和页面如实返回空数据，绝不生成样例统计或开放人工录入。
- 三项能力均使用 Gaia 登录、租户路由、明确 HTTP 权限、BIGINT 字符串传输、版本冲突保护和审计事务。权限模型与商品分类一致：分组统一提供 `/permissions`，叶子模块登记查询 API，写动作登记模块操作，AppService 对实际接口逐项强校验；服务质量无前端写入接口。

## 明确不在本切片

- 服务技能、服务区域、企业切换、账号绑定、跨站支援和服务站级数据范围。
- 周期性排班规则、任务占用扣减、派单过滤实际接线。
- 工单时效、一次完工、评价、投诉的事实生成、指标口径和区域维度；这些必须随工单闭环确认后实现。

## 2026-09-18 账号与一人一站备注

- 服务人员账号开户绑定（A50 前置）已单独定稿，不在本首版实现范围内；见[服务人员账号开户绑定前置切片](2026-09-18-服务人员账号开户绑定前置切片.md)。
- 业务最新规定：一个服务人员只能属于一个服务站，现已落地。人员表单直接维护单值 `stationId`，列表、详情、排班、库存、派单和移动上下文均只读取人员主表的 `station_id`。旧客户端历史 `stations` 数组仅在未提供 `stationId` 时兼容读取第一项，保存后收敛为唯一主站。`20260918_personnel_one_station.sql` 已清理历史多站关系；`20260921_personnel_station_one_to_many.sql` 进一步把旧关系表限制为每人一条、`ordinal=1` 的兼容镜像，并已在当前测试库执行。

## 验收重点

- 人员的服务站、唯一手机号、启停、权限、租户隔离、版本冲突和审计失败回滚。
- 日可用性的唯一性、容量和状态约束、人员／服务站停用保护、权限、租户隔离与审计。
- 质量页仅能查询已有汇总事实，指标分母为零时显示 0，不将无数据误显示为质量达标。

## 实现与验证

- 后端新增服务资源领域 AppService、Mapper、API 与迁移 `20260909_service_resource.sql`；异常处理已纳入售后 API 范围，契约升级为 `catalog.openapi.json` 1.7.1。服务资源统一权限接口为 `GET /api/afterSales/service-resource/permissions`，按 `personnel`、`availability`、`quality` 返回能力集合。
- 独立前端新增 A26、A27、A28 的真实路由、请求适配与页面。三页以商品分类为标准功能范例，统一复用 `FaPageMain`、`EsSearch`、`EsListToolbar`、`EsTable`、`useListPage` 和 `useCrudDialog`，并按 `index.vue + components + model + schemas` 划分职责。三页共用按 Token 缓存的服务资源权限结果；`read=false` 时不请求业务列表并显示无权状态。人员和日可用性提供查询、分页、新增、编辑、冲突/失败保留及操作后刷新；质量页没有任何写操作。
- `mvn -B -o -pl gaia-after-sales-api -am test` 通过：core 3 项、API 29 项，共 32 项。覆盖人员和日可用性生命周期、唯一约束、容量/状态规则、停用保护、权限、租户隔离、BIGINT 字符串、审计事务回滚，以及质量事实投影读取和指标计算。
- 2026-09-10 按独立子系统前端规范完成三页结构整理，并新增人员、日可用性和质量查询／提交 mapper 测试。使用锁定工具链 Node 22.23.1 + pnpm 10.8.1 执行前端测试与 production build：71 项测试全部通过，构建成功；另有定向 `vue-tsc -b` 类型检查通过。OpenAPI JSON 结构校验沿用本切片原验证结果。
- 2026-09-10 已将迁移 `20260909_service_resource.sql` 实际执行到当前开发联调租户库 `gaia_wh_init_wzy`，创建并核验 `afs_service_personnel`、`afs_service_personnel_audit`、`afs_personnel_availability`、`afs_personnel_availability_audit`、`afs_service_quality_daily` 共 5 张表；迁移为纯新增表，回退时须在确认无业务数据或完成备份后按外键依赖逆序删除。
- 已在主系统配置服务资源分组和三个叶子模块的查询 API，并补齐人员／排班 5 项操作的 `action`、启用状态、顺序和写接口映射；角色菜单和操作授权仍分别由 `sys_role_module`、`sys_role_operation` 管理。
- 最新售后模块已重新装配进 Gaia 聚合宿主，`yarn dev:full` 当前在 8080、7004、9010 提供本地联调服务。按用户要求未进行浏览器验收；真实角色允许／拒绝场景由用户验收。

## 2026-09-22 总部服务质量综合分析增量

- 保留原 `GET /api/afterSales/service-resource/quality` 日事实列表，新增只读 `GET /api/afterSales/service-resource/quality/analytics`。接口按 `AREA`／`STATION`／`PERSONNEL`、关键词和最多 93 天日期范围一次返回周期汇总、按日趋势、对象排名、重点关注和分页对象明细。
- 服务量、及时量、一次解决量、评价量、评分总和、投诉量和事实数直接汇总；及时率、一次完工率、评价覆盖率、投诉率与评价均分均从汇总后的分子分母计算，不对对象百分比求平均。排名指标只接受 `TIMELINESS`、`FIRST_TIME`、`COMPLAINT`、`SERVICE` 白名单。
- Web 页已采用综合分析布局：五项指标卡、服务量柱形＋及时率／一次完工率折线、对象横向排名、重点关注和分页明细；无权限时不发统计请求，空数据和失败独立表达，导出与关联工单下钻在接口未具备前不显示。
- 后端新增聚焦单元测试和真实 MyBatis/H2 聚合测试，覆盖日期／指标白名单、分页、趋势、加权比例、排名和 HTTP 响应；前端目标 mapper 测试、ESLint、类型检查、国际化、依赖检查、production build、bundle-size 和 1488×1057／1280×720 隔离视觉验收通过。全量后端套件仍受工作区其他在途表结构／夹具变更影响，不将其记为本增量失败。
- 2026-09-22 完整应用壳联调发现页面百分比高度与外层自适应布局形成重排反馈，滚动时页面和 `EsTable` 空间持续增长；已改为与现有列表页一致的视口高度计算，并将明细表格面板固定为 390px。真实数据连续滚动后正文高度保持 717px、业务页 580px、内容滚动高度 1101px，控制台无错误。当前日事实为 `STATION` 粒度，页面默认统计对象同步改为服务站；本地真实登录态读取 56 条日事实、服务量 1248 单，无错误提示。
- 2026-09-22 视觉复核将页面标题、综合分析标识和说明收为同一行，页面卡片恢复白色背景并统一标题与指标数字字号；修正评价覆盖率／评价均分卡片辅助文字选择器命名错误，真实页面辅助文字恢复为 12px 次要色，未改变接口与统计口径。
- 新接口权限登记脚本为 `database/20260922_headquarters_analytics_permissions.sql`，回退脚本只移除新增只读接口；尚未执行到共享或测试环境，真实角色与数据范围仍待部署验收。

## 主系统权限配置清单

以下配置与商品分类采用同一结构，主系统保存后才形成完整运行时授权：

| 模块 | `sys_module.api_list`（只读） | `sys_module_operation`（写操作） |
| --- | --- | --- |
| 服务资源分组 `afsServiceResource` | `GET$/api/afterSales/service-resource/permissions` | 无 |
| 服务人员 `afsServicePersonnel` | `GET$/api/afterSales/service-resource/permissions,GET$/api/afterSales/service-resource/personnel,GET$/api/afterSales/service-resource/personnel/*` | `add`: `POST$/api/afterSales/service-resource/personnel`；`edit`: `PUT$/api/afterSales/service-resource/personnel/*`；`status`: `PUT$/api/afterSales/service-resource/personnel/*/status`；`delete`: `DELETE$/api/afterSales/service-resource/personnel/*` |
| 排班管理 `afsSchedule` | `GET$/api/afterSales/service-resource/availability,GET$/api/afterSales/service-resource/availability/*` | `add`: `POST$/api/afterSales/service-resource/availability`；`edit`: `PUT$/api/afterSales/service-resource/availability/*` |
| 服务质量 `afsServiceQuality` | `GET$/api/afterSales/service-resource/quality,GET$/api/afterSales/service-resource/quality/analytics` | 无 |

- 每项操作的 `code` 与 `action` 保持一致，`enable=1`，并设置稳定顺序。
- 角色同时配置 `sys_role_module` 和所需的 `sys_role_operation`；仅授予菜单不会自动获得新增、编辑或启停接口权限。
- 验收至少覆盖允许角色的菜单／按钮／成功请求，以及拒绝角色直接调用列表或写接口时返回 403。

## 2026-09-10 派单切片接入

- 服务人员主档已增加安装、维修、指导三类技能关系，新增／编辑使用字段白名单增量保存；启用人员至少配置一类技能。
- A19 派单已实际读取人员所属站点、启用状态、对应工单类型技能、期望日期可用状态和当日容量，并统计活动工单占用；冲突候选可见原因但服务端拒绝提交。
- 本节是后续切片对首版边界的增量更新；人员区域、账号、企业、跨站支援和周期排班仍未实现。完整证据见[派单、异常与完工审核首版](2026-09-10-派单异常与完工审核全栈首版.md)。

## 2026-09-12 服务人员二版补充（历史设计，主站关系已于 2026-09-21 收敛）

- 服务人员新增／编辑弹窗扩展为“个人信息”和“所属服务站”两个页签。个人信息补充身份证号、服务项目、合同编号、在职状态、入职日期、工号、银行卡号、居住地址、身份证正反面照片、员工卡照片、技术认证照片和其它附件；服务项目新增“清洗”，兼容历史“指导”数据。
- 当时实现曾支持多站点关联；该设计已被“一人一主站”规则取代。当前弹窗为单个服务站下拉，后端和数据库均以 `station_id` 为唯一业务依据，旧关系表只保留单行兼容镜像。
- 2026-09-15 曾使用通用实体选择器和轻量编辑列表维护多站关系；该交互已于 2026-09-21 被个人信息表单中的单服务站下拉替代。历史验证仅作演进留痕，不再作为当前验收标准。
- 2026-09-16 服务人员详情曾以多站只读列表展示；2026-09-21 已改为只展示主表单个服务站并按该站定位详情。接口仍可返回单项 `stations` 兼容投影，但前端不再以该数组决定归属。
- 2026-09-16 服务人员新增／编辑中的“其他附件”移除原始表格，改用通用文件列表；上传、预览、删除、格式与 20MB 限制及提交结构不变。证件照、身份证、员工卡和技术认证照片继续使用图片卡片，不与普通文件附件混用。
- 后端新增 `afs_service_personnel_station` 和 `afs_service_personnel_attachment`，并在 `afs_service_personnel` 上补充个人资料字段。增量脚本为 `20260912_service_personnel_v2.sql`，回退脚本为 `20260912_service_personnel_v2.rollback.sql`；按钮/API 权限脚本为 `20260912_service_personnel_permissions.sql`。
- 本地验证记录：`pnpm run typecheck` 通过；`mvn -B -pl gaia-after-sales-api -am test` 通过；`mvn -B -pl gaia-after-sales-api -am clean install -DskipTests` 通过；`mvn -B -pl gaia-saas-web -am package -DskipTests` 通过。
- 本地库已执行二版表结构和服务人员按钮权限迁移，权限接口返回 `personnel.read/create/edit/status=true`。Tomcat 已重启到 PID 7844，8080、7004、9010 均在监听；匿名接口探测返回未登录业务响应，说明新后端已接管 `/api`。
- 浏览器留痕：使用 `admin / 123456` 登录 `http://localhost:7004/#/login`，进入 `http://localhost:7004/after-sales/#/serviceResource/personnel`，已验证“新增服务人员”按钮可见；弹窗展示个人信息字段、图片/附件上传入口；所属服务站页签可打开“选择服务站”，勾选服务站 `111 / 111` 后回填到待保存列表并显示删除按钮。截图已在当前 Codex 任务中留痕。
