# 四视角首页工作台

- 编号／类型：FS-045，首页聚合与交互改造；复用 FS-019 服务站作业及 FS-006 门店信息相关能力。
- 应用／视角：唯一 Web 管理后台；总部运营、客服作业、服务站作业、门店业务。
- 模块／入口：A01 总部运营工作台与客服工作台、A02 服务站工作台、D02 门店工作台。四个首页共对应三个既有需求入口，不新增应用或历史需求编号。
- 关联需求：STA-001、CS-004、DLR-002、DLR-015；首页指标沿 FS-045 共享基础承载，门店代客报修复用 FS-018，不虚构新入口。
- 来源／日期：2026-09-12 用户确认“改造现有三个首页，新增客服工作台，各视角纵览并同页办理主要工作”。
- 优先级／状态：P0／开发中；首页改造与本地研发验证完成，正式业务验收待完成。
- 负责人：研发实施由本次任务执行；业务验收负责人待分配。

## 1. 首页与导航契约

| 视角 ID | 首页 | 固定 code | 子系统路由 | 入口 |
| --- | --- | --- | --- | --- |
| `headquarters` | 运营工作台 | `afsDashboardOverview` | `/dashboard/overview` | A01 |
| `serviceDesk` | 客服工作台 | `afsDashboardCustomerService` | `/dashboard/customerService` | A01 |
| `station` | 服务站工作台 | `afsDashboardStation` | `/dashboard/serviceStation` | A02 |
| `dealer` | 门店工作台 | `afsDashboardDealer` | `/dashboard/dealer` | D02 |

四个固定路由与总首页 `/` 共用 `apps/after-sales/src/views/dashboard/index.vue`；总首页直接展示当前授权视角工作台，原分组链接总览不再作为中转页。客服工作台放在“工单处理”首位并作为默认首页；客服“服务分析”保留服务质量与运营报表，移除总部运营工作台引用。系统管理员是权限身份，不新增第五个工作视角。

2026-09-13 名称收敛：总部视角 A01 的菜单和页面显示名称由“运营概览”调整为“运营工作台”，以强调待办、监控、下钻和专业分析入口的工作属性；固定 code `afsDashboardOverview`、子系统路由 `/dashboard/overview`、入口 A01、权限及统计口径均不变。

2026-09-12 首页菜单选中修正：左上角标题及面包屑返回 `/` 时，侧栏按当前授权视角的工作台路径选中菜单；保留其他页面的 `meta.activeMenu` 优先规则。原因是原逻辑仅定位了主菜单组，叶子菜单仍使用 `/` 与具体工作台路径比较。修复位于 `SubSidebar/index.vue`，覆盖侧栏 `single` 和顶部 `head` 模式；四视角标题返回后的真实 DOM 选中状态、普通业务菜单切换及类型检查通过，不改变路由、权限和入口计数。

首页是已授权视角的页面外壳。普通账号取得有效 `afsWorkspace*` 导航授权后，即使新客服菜单尚未分发，也能进入 `/` 及本视角固定首页；未获任何业务权限时显示空工作台。管理员仍按实际已授功能确定可用视角，管理员标记本身不生成四套业务授权。

`resolveAccess().features` 只保留真实 `/api/sys/Module/tree` 授权；首页菜单投影不向其中补造模块。每个数据队列、快捷入口和操作继续核对原功能 code、原资源 read／动作权限、对象范围及后端返回的 `actions`。其他视角首页及业务直链不能借首页入口越权访问。

页头切换通过目标路由执行，表单确认通过后才应用新视角。浏览器合法业务深链保持登录回跳；共享功能尽量保留当前授权视角。首页、页头和已有专业页面沿用同一认证及租户体系，不因切换视角更改后端身份或组织。

## 2. 首版信息与统计口径

首页不新增独立业务状态机或另一套聚合权限。客服、服务站和门店视角继续调用已有顾客／工单、派单异常审核、投诉、运营报表和服务资源 API；总部地图工作台使用同一 `api/dashboard` 前端入口，并通过只读摘要接口聚合首页事实，仍逐项复用原资源权限和数据范围。通用适配器为 `views/dashboard/model/access.ts`，队列定义为 `model/queues.ts`。

| 视角 | 首屏主要数量／队列 | 严格口径 |
| --- | --- | --- |
| 总部 | 所选周期工单量及上周期变化、当前在途、SLA 预警／超时、一次完工率／及时率、异常关注；近 30 天工单趋势 | 周期工单按创建日期与省份汇总，和紧邻的同长度上周期比较；在途只含当前待分站、待派工、待完工、待审核；SLA 按当前状态映射已配置策略并基于状态变更时间判断，未配置策略单列且不混入风险；质量指标按所选周期服务站日事实的分子／分母加权计算 |
| 客服 | 待受理、待派单、待完工审核、待定责投诉；补充待分配服务站、顾客识别与受理、全部工单 | 分别为 `PENDING_ACCEPTANCE`、`PENDING_DISPATCH`、`PENDING_REVIEW`、投诉 `OPEN`；待定责数量不代表全部未结投诉。各岗位只出现其获授权队列 |
| 服务站 | 待派单、今日期望服务、待完工、缺件工单；全部工单 | 今日期望服务按工单期望服务日期为当天，包含各状态，不等于今日新增或今日实际排班；缺件只指 `MISSING_PARTS`，不把其他异常混入 |
| 门店 | 未申请安装、部分申请安装、全部购买记录 | 按购买记录的 `NOT_APPOINTED`／`PARTIALLY_APPOINTED` 统计，不是商品件数或可申请件数；执行申请时再检查商品实例状态 |

数量取对应后端分页响应的 `total`，不得以当前列表行数充当总量。统计按固定队列条件读取，不随临时关键词或手机号筛选改变口径；无筛选的当前列表总数可复用，减少重复请求。列表及数量分别显示加载、失败与重试，失败不伪装为 0。趋势校验日期范围、完整分组和合法数量，未完整返回时显示错误，不将截断结果求和展示。

总部侧栏补充近 30 天服务质量日汇总，按对象与日期展示前 3 条及总条数；逐条及时率和服务量沿用真实汇总，不对对象均值再次求平均。服务站侧栏展示今日已配置排班前 5 条及总条数，包括可用状态与已配置日容量，不将容量推断成剩余工位或人员负荷。两个侧栏分别要求 `afsServiceQuality`／`afsSchedule` 菜单和相应 read 权限，失败可独立重试。

当前数据范围是已有后端实施的**授权组织范围**。账号到服务站的绑定、严格“本站”数据隔离及门店上下文仍须相关切片和正式账号验收；不得因为页面命名为“服务站工作台”就宣称已实现或通过本站隔离验收。总部 SLA 风险只使用已配置的 `afs_sla_policy` 目标时长与预警提前量，不推导人员负荷或另造待办状态；未匹配到策略的在途工单单独统计为“未配置 SLA”。

### 2.1 总部全国地图工作台实现（2026-09-18）

总部 `/dashboard/overview` 已由原列表优先首页调整为全国地图工作台，其他三个视角继续使用原工作台组件。实现遵循已确认地图原型的信息顺序，并保留原首页的真实队列和工单详情能力。2026-09-18 样式复核后取消顶部全部工单、待分配服务站、待完工和缺件工单四张指标卡，页面收束为紧凑标题栏、主地图和一体化运营侧栏；全部工单下钻整合到所选区域工单总量，待办与异常队列继续通过流转节点和异常关注进入同页队列抽屉，可查询、分页和打开工单详情。

2026-09-18 总部决策层补全：在不恢复原四张队列卡的前提下，首屏改为横向“经营主指标 + 风险信号”带，突出所选全国／省份的周期工单量、紧邻同长度上周期变化、全国当前在途、SLA 风险、一次完工率／及时率和异常关注总量。主体保持“地图 + 决策侧栏”，侧栏新增区域异常榜和完整质量摘要；风险色只用于超时、投诉、取消等需关注事实。1280px 桌面宽度保持一行主指标，地图与侧栏各自承担空间分布和处置判断，不再以同权卡片平均铺陈。

2026-09-18 大屏高度优化：保持内容区随工作区宽度伸展，不增加固定宽度或最大宽度限制；主区域由固定 660px 高度改为以 660px 为最低基线并占满剩余可用高度。常规桌面不足时继续由页面滚动，纵向大屏则由地图与侧栏共同撑满，不在底部保留无意义空白。该调整不改变顶部指标卡、地图／侧栏横向比例、数据口径或交互。

2026-09-18 可读性优化继续收敛顶部重复信息：页头只保留工作台名称与日期，授权范围及操作说明不再重复常驻；浅色地图使用独立画布底色、可辨识的无数据省份填充和更清楚的省界。图层、周期与全国范围控件提高字号和点击高度，右侧运营侧栏扩大宽度、字号与分区留白，四个可办理流转节点调整为单行，质量指标调整为 2 × 2 网格。区域概况进一步压缩为工单数、取消数和涉诉工单数单行展示，“当前已关闭”不再常驻占位但仍保留在地图悬浮摘要，报表口径说明收进可聚焦的信息提示；区域异常排名保持在工单趋势之前，默认显示 Top 3，并可展开到 Top 5，使趋势位置前移。该调整不改变接口、权限、统计口径、队列下钻或入口状态。

2026-09-18 地图可视化升级为 ECharts 6.1，并继续使用仓内省级 GeoJSON，不引入运行时外部地图服务。全国视图按所选周期工单量显示分级着色与运营热度脉冲点；单击省份或使用无障碍省份入口只选中该省并联动区域指标与侧栏，继续保留全国对比上下文，只有点击侧栏“进入省级地图”后才切换为该省聚焦地图，并通过“全国 > 省份”面包屑返回。省级视图沿用同一省份汇总、站点、质量及覆盖事实，不伪造市级边界或区县数据。省份悬浮提示按当前图层分别展示工单、服务站、质量或覆盖关键指标；服务站图层显示真实坐标及配置日上限，质量图层按及时率区分点位，覆盖图层按已配置服务区域数量展示；右侧近 30 天趋势由静态柱形改为带渐变面积的平滑折线。浅色、暗色及系统“减少动态效果”偏好均有独立呈现，减少动态效果时关闭脉冲和过渡动画。此次升级只改变可视化与地图下钻，不改变原接口、权限、统计口径和明细入口。

地图数据全部来自现有真实接口及当前账号授权范围，不使用原型演示数字：

- 工单态势由总部摘要接口返回 `PROVINCE` 分组，支持包含首尾日的 7／30／93 天周期；全国与省份卡只汇总同次返回的省份分组事实。近 30 天趋势和各队列总数由同一摘要请求返回，不再为首页数量调用分页列表及其明细装饰查询。
- 服务站网络摘要按当前数据范围返回全国及省份的档案总数、已配置坐标数和启用数；完整档案不进入首页响应。地图点位首次切换到站点、质量或覆盖图层时调用轻量点位接口，只返回名称、坐标、启停、配置日上限及聚合数量。配置日上限只作为档案字段展示，不解释为实时产能。
- 服务覆盖按当前可见的已配置服务区域统计。未配置区域保持无数据状态，不命名为服务盲区。
- 服务质量增加所选周期全国／省份加权汇总，只纳入当前数据范围内 `STATION` 类型日事实，分别汇总服务量、及时量、一次解决量、评价量、评分总和、投诉量和事实数；及时率、一次完工率、评价率及平均评分均由汇总后的分子／分母计算，不对对象百分比再次平均，也不混入区域或人员粒度的重复事实。原近 30 天质量事实仍供地图点位分层使用。
- 区域异常榜按所选周期省份工单事实中的“取消工单数 + 涉诉工单数”排序，二者只是关注信号，可点击联动地图、区域指标与质量摘要，不解释为综合绩效评分。
- 当前在途按 `PENDING_DISPATCH`、`PENDING_ASSIGNMENT`、`PENDING_COMPLETION`、`PENDING_REVIEW` 汇总。SLA 预警／超时仅统计能按当前阶段匹配有效策略的在途工单，并以 `status_changed_at`、目标分钟数及预警提前量判断；审核退回统计所选周期发生过 `REJECT` 决策的去重工单数。

2026-09-18 加载策略优化：总部首屏调用 `GET /api/afterSales/dashboard/headquarters/summary?from=...&to=...`，服务端一次取得当前登录人的 Gaia 授权集合并逐项判断工单、异常、审核、报表、服务站、服务区域和质量原资源 read 权限；响应只返回已授权分区，前端据此建立队列与图层能力，不再额外调用各领域 `permissions` 接口。该接口不新增工作台专属业务授权，也不依赖菜单可见性替代资源授权。轻量服务站点位使用 `GET /api/afterSales/dashboard/headquarters/station-markers`，再次按原服务站读取权限和组织数据范围校验。

业务数据采用保留旧值的后台更新：每 60 秒自动刷新一次摘要，手动刷新同样保留地图、趋势、区域汇总和侧栏最近一次成功结果，完成后原位替换；局部失败保留旧数据并显示错误，已有地图不出现加载遮罩。服务站点位和服务区域首屏均不请求，首次进入相关图层后按需加载并在当前组件内缓存 5 分钟；摘要刷新不重新拉取未过期的低频主档。总部摘要只返回队列数量、近 30 天趋势、所选周期省份汇总、服务网络摘要、质量事实和生成时间，不返回工单列表或完整主档；队列明细仍在用户下钻时调用原领域接口，服务区域明细继续分层按需读取。

2026-09-18 刷新空白修复：前端兼容滚动发布期间的旧版总部摘要响应；当 `previousProvinces`、`qualitySummary` 或 `qualitySummaries` 尚未由后端返回时，分别按空列表／无事实处理并展示 `--` 或质量空态，不再把 `undefined` 写入列表状态导致渲染异常。隔离夹具新增旧响应模式，完整刷新、7 天周期切换和手动刷新后页面均保持可用；待后端升级后自动展示新增指标，无需再次切换前端版本。

地图内置省级边界数据，不依赖运行时外部地图服务；省份、周期和图层切换均在当前页完成。服务站点位点击仅在同时具备 `afsServiceStation` 功能授权和服务站读取权限时进入既有服务站列表定位。缺件、待审核等队列同样继续要求对应原功能 code、领域权限和后端数据权限；无授权时显示不可用，不因首页展示而补造权限。

当前后端工单状态仍使用既有 `PENDING_DISPATCH` 表达“尚未分配服务站”的阶段，首页显示名称按需求保持“待分配服务站”；未新增状态机或数据库写入。当前已补齐审核退回工单数、配置内 SLA 预警／超时工单数和全国／省份质量加权汇总，但不把退回工单数解释为退回率。实时位置、到客距离和区域利用率仍无可靠接口／口径，不在页面推断展示。

## 3. 同页办理与复用边界

- 总部从关注队列查看工单详情；运营报表和质量等专业功能保留获授权入口。
- 客服从队列打开详情、记录沟通，按既有权限与行 `actions` 办理分配服务站、派单／改派、异常、完工审核及投诉阶段处理；顾客识别与受理复用已有购买记录和代客建单表单。
- 服务站复用派单、异常及完工提交动作，保留人员、排班和配件快捷入口；完工审核仅属于客服视角，不向服务站复制。
- 门店在同一首页查看购买记录、打开详情和购买登记；服务动作统一为购买记录内“发起服务”，进入后选择安装或维修。安装调用已有安装申请 API，维修调用独立报修 API 并由后端固定维修／门店来源；二者都进入客服待受理，门店不分站或派人。
- 详情、沟通、门店发起服务、客服代客建单、流转及投诉表单复用已有业务组件；打开办理前重读详情与动作，处理成功后更新当前队列和数量，继续处理下一项。

复杂流程仍允许进入专业页面。返回首页时在当前会话内恢复队列、已应用查询和分页；顾客查询值不写 URL 或持久存储，账号变化清空暂存位置。快速切换、组件卸载及权限失效时取消或隔离过期响应；统计重试不覆盖更晚结果。

## 4. 表单与离页保护

工作台通过既有表单的提交／未保存状态统一判断 `canLeave()`。提交过程中禁止离开；有未保存内容时显示确认，取消则保留当前视角与表单。`onBeforeRouteLeave`、`onBeforeRouteUpdate` 覆盖首页切换、专业页面跳转和同路由更新。取消导航时不更新目标页标题、清理缓存或滚动复位。

普通关闭、提交失败、权限拒绝和网络重试保持原业务表单行为，不因首页复用而略过错误展示、重复提交保护、敏感信息保护或后端校验。

2026-09-12 用户追加确认：页头切换工作视角后，只保留目标视角的工作台标签。执行顺序为检查未保存内容及提交状态、完成目标导航、再清理旧标签与旧页面缓存，并重置标签插入位置。导航取消、异常或未到达目标首页时不清理标签；后置导航失败且仍停留原页面时恢复原视角，允许再次尝试。同视角重复选择、普通菜单跳转和点击左上角标题返回首页均不触发标签清理。

专业页面通过公共表单及少量独立表单接入视角切换前检查，复用原有未保存确认和提交状态；确认本身不关闭弹窗或修改表单，便于导航失败后继续编辑。工作台继续使用已有整页离开保护，内部弹窗不重复确认。此机制只负责视角切换，不新增跨会话草稿保存能力。

实现覆盖 `EsFormDialog`、独立购买登记、SLA 配置、商品系列／配件关联窗及用户／角色行操作。用户／角色原确认回调改为可等待的异步函数；SLA 按行记录已保存基线，保存单行不清除其他行的未保存状态。其他页面原有普通路由跳转规则保持。本地开发热更新新增状态存储方法后需完整刷新页面再验证。

## 5. 验证与交付边界

| 检查 | 当前证据／待补内容 |
| --- | --- |
| 导航权限与取消保护 | 27 项 `navigation*.test.ts` 已通过；包含真实 Vue 内存路由挂载下的取消／接受切换、提交阻止离页与同路由更新保护 |
| 类型检查 | 最终 `npm run typecheck`（`vue-tsc -b`）通过 |
| 四视角队列／统计 | 定向 Node 测试 45 / 45；真实挂载工作台状态与侧栏回归 10 / 10，控制台警告／错误为 0。包含筛选／重置／分页单次查询、卡片不随搜索缩小、返回恢复、快速切换和卸载晚响应隔离、权限失败、侧栏读取门控、保存后立刻离页的统计取消，以及数据刷新复用已解析权限且不重新初始化工作台 |
| 同页办理与桌面显示 | 隔离浏览器页面使用真实 Workbench 与业务表单、内存 API 替身，覆盖客服建单／分配服务站／完工审核、服务站派单、门店购买登记，以及购买记录内统一发起安装／维修；两类门店服务分别使用已有 API 替身。每次模拟写入一次并触发当前列表和数量刷新；真实角色与业务数据仍待验收 |
| 构建与文档一致性 | `npm run build` 通过；317 处本地文档链接、47 项菜单映射及 `git diff --check` 通过 |
| 总部地图工作台 | 省份报表、地图投影／名称归一化、四个总部主队列、导航离页及既有工单契约定向测试 60 / 60；新增文件 ESLint、国际化键与差异检查通过；Vite production build 及 166 个 JS／CSS 资源大小门禁通过。去除顶部四张指标卡并优化布局后，相关定向测试 27 / 27 通过；1280 × 720 隔离浏览器实测无整页溢出，地图列约 831px、右栏 360px 且独立滚动，浅色和暗色主题均完成视觉复核；省份联动及整合后的全部工单下钻通过。2026-09-18 请求优化后，总部首屏和常规摘要刷新收敛为 1 次业务请求、0 次独立权限请求；队列明细只在打开抽屉时读取，站点点位及覆盖区域按图层懒加载。决策层补全及大屏高度自适应后，后端摘要服务 4 项定向测试通过，前端地图／数据／导航定向测试 28 / 28、ESLint、国际化 18 个模块 2483 个键和 Vite production build 通过；1440 × 1000、1280 × 900、2560 × 1440 的浅色／暗色隔离页面完成视觉复核，1440px 页面无横向溢出；内容宽度继续随工作区伸展，主区域在 1440 × 1000 下由 660px 扩展到约 713px，在 2560 × 1440 下扩展到约 1153px。区域异常榜点击后能联动广东省地图、周期对比与质量指标，控制台无页面错误。省份选中与地图下钻拆分后，隔离浏览器验证单击省份保持全国地图、右侧显式进入省级地图、站点图层关键指标及四项流转单行选中态，控制台错误和警告均为 0；地图模型定向测试 4 / 4、ESLint、国际化 18 个模块 2484 个键、Vite production build 和 166 个 JS／CSS 资源大小门禁通过。区域概况与异常排名压缩后，隔离浏览器验证工单／取消／涉诉单行概况、默认 Top 3 及展开 Top 5 正常；ESLint、国际化 18 个模块 2486 个键、地图模型定向测试 4 / 4、Vite production build 和 166 个资源体积门禁通过。全量前端测试 283 项中 271 项通过，12 项为目录、旧表单、图标、网络及标签等既有失败；全量类型检查也被顾客表单、派单弹窗、普通工作台等非本次文件的既有错误阻断，统一 `check` 另被既有未使用依赖 `vxe-pc-ui` 阻断。真实环境耗时、SLA SQL 执行计划与实际质量事实仍待部署后采样 |
| 全量前端测试 | 标签隔离追加后的 `npm test`：205 项中 202 通过、3 项已有失败；分别为部分旧表单缺少 placeholder、`network-master.test.ts` 的 Node ESM 扩展名解析、`service-resource.test.ts` 的旧 fixture 缺字段。这三项与改造前已记录失败一致，未计为本次验证通过 |
| 视角切换标签隔离 | 相关 42 项通过：12 项导航／失败恢复、2 项真实标签和缓存存储、7 项真实 Vue 注册及 `EsFormDialog` 联测、21 项原访问和会话测试。实际 Chrome 完整刷新后，四视角切换均只留下选中的目标工作台；同视角打开业务页及 Logo 返回仍保留业务标签。异步取消／批准不会关闭或修改表单，提交优先阻止，后置导航失败恢复原视角后可重试；类型检查、构建与差异检查通过 |
| 后端契约 | 既有 `ReportIntegrationTest`、`ComplaintIntegrationTest` 共 4 项通过；总部只读摘要及轻量站点接口复用原资源权限和组织数据范围，不修改状态机或业务表。新增同周期省份汇总、SLA 风险、审核退回和质量加权汇总后，`HeadquartersDashboardAppServiceTest` 4 项通过并包含 Mapper XML 注册校验；共享 MySQL 真实数据性能与执行计划验收待完成 |
| 原生刷新／关闭页签 | 本次路由检查不等同于原生浏览器卸载保护，尚未取得该场景验证证据 |
| 开发库配置 | 经用户明确批准，`gaia_wh_init_wzy` 已创建 `afs_complaint`／`afs_complaint_event` 两张空表，并追加 6 项只读接口配置；既有角色和写权限不变，备份与回退记录齐全。当前登录操作员四首页读取正常，总部趋势、客服投诉列表与服务站排班显示真实空数据，无读取错误；未写入真实业务记录，详见[执行证据](support/2026-09-12-dashboard-env/README.md) |
| 共享环境与业务验收 | 正式角色、真实业务写入与本站隔离验收待完成；开发库配置成功也不等同于完整业务验收 |

完成条件：四首页按授权进入且数据口径准确；列表筛选／重置／分页单次查询；权限不足与加载失败可辨认；允许动作同页完成并刷新；取消离页不丢表单；原登记与客服专业页回归；桌面显示通过。本次不增加应用分母或历史需求入口：A01、A02、D02 三项由占位进入真实实现，已有真实实现统计由 34 / 59 调整为 37 / 59，最终验收仍为 0 / 59。

定向命令在 `gaia-ui/apps/after-sales` 执行：

```sh
node --experimental-strip-types --test tests/dashboard-data.test.ts tests/navigation-access.test.ts tests/navigation-home-leave.test.ts tests/navigation-icons.test.ts tests/navigation-session.test.ts tests/customer.test.ts tests/work-order-workflow.test.ts
```

浏览器状态回归入口为 `/after-sales/tests/browser/dashboard.html`；同页办理隔离入口为 `/after-sales/tests/browser/dashboard-ui.html`。测试数据只存在于测试页面内存，不进入业务库或生产路由。专业购买登记与客服页面继续使用同一业务表单，其 payload／状态字典回归已通过；正式角色在专业页中的真实写入闭环仍归业务验收。

### 2026-09-19 客服工作台 v03 原型落地

- 页面：客服视角采用 v03 的顶部聚焦与指标带、岗位流转条、左侧统一工作篮／队列、右侧当前任务和服务上下文布局。队列、搜索、分页、下一项、任务选择及上下文联动已接入；实际写入继续复用已有业务表单，并在打开动作前重读详情、权限和后端 `actions`。右侧先展示真实任务摘要，复杂写入在原业务弹窗中完成，避免复制分站、审核和投诉表单规则。
- 数据：新增 `GET /api/afterSales/dashboard/service-desk/summary`，一次返回当前授权队列数量、流转条的今日转入数量和各队列最多 8 条预览。服务端调用既有受理、派单、审核和投诉服务，沿用原资源权限与组织数据范围；未授权资源不返回。今日转入按服务端当天统计：新建工单进入待分站、状态历史中进入站内待派人或待审核的去重工单数，查询沿用工单组织数据范围。`workbasket` 为待分站、站内待派人、待审核、待定责投诉四队列数量之和，页面再按当前菜单可达队列过滤预览和显示总数。购买记录现有接口提供的是“可受理记录”，并非待受理工单，因此单独展示且不计入工作篮。完整工单由独立“服务工单”入口查询，汇总接口不再为该入口额外计数。完整搜索／分页仍调用各业务列表接口。接口契约见[后端 OpenAPI](../../../backend/gaia-after-sales/docs/service-desk-dashboard.openapi.json)。
- 验证：后端服务层 3 项定向测试通过（全授权汇总、投诉单项授权、无授权拒绝）；前端相关 25 项测试通过，其中新增模拟工单验证超时／预警优先级及无授权队列过滤。ESLint、国际化检查和 Vite production build 通过；1440 × 900 隔离浏览器用内存模拟数据复核了布局、队列切换、审核表单、上下文抽屉、摘要失败及恢复。模拟数据没有写入业务库。全量 `check` 仍被仓库既有未使用依赖 `vxe-pc-ui` 阻断；独立类型检查只报顾客表单、派单弹窗、库存和人员排班等本次未改文件的既有错误，本次修改文件无新增类型错误。
- 本地运行态复核：首次请求 9010 摘要接口得到通用 `code: 500`，宿主日志对应 `No static resource api/afterSales/dashboard/service-desk/summary`，原因是运行中的共享后端仍为新接口创建前的旧 JAR。通过 `scripts/local-backend.mjs` 装配并重启后，使用本地开发登录态请求原 9010 地址得到 HTTP 200、`code: 0`，返回七类数量键与五类预览键。另将新 Controller 纳入售后异常处理器，新增 API 测试验证无资源授权会返回业务 `code: 403`；后端服务和 API 共 4 项定向测试通过。第二次装配重启后再次请求原地址仍为 HTTP 200、`code: 0`。
- 页面复核：流转条恢复 v03 的统一办理标记、阶段责任方、分阶段选中色和“今日转入”数量；顶部五张队列卡片在选中、悬停和键盘聚焦时保留顶部蓝条。工作篮仅汇聚当前待办，完整历史工单仍由独立“服务工单”入口查询，因此移除篮内重复“全部工单”按钮。任务区以阶段提示和字段网格展示状态、日期、服务站及中文 SLA；办理辅助突出责任方、下一步和下一项，办理按钮固定在任务区底栏。内存模拟工单覆盖未分站、预警、超时、队列切换和动作可见性；ESLint、国际化检查、相关测试与生产构建通过。今日转入新增的服务端定向测试通过；本地共享后端重新装配后，真实登录态工作台成功显示三段当日数量 21／5／3，未再返回 500。该运行态检查属于本地研发验证，不替代目标环境的正式业务验收。
- 待联调：目标环境需在客服工作台授权节点增加 `GET$/api/afterSales/dashboard/service-desk/summary`，安装更新后的后端并以真实登录态核对允许／拒绝、脱敏、队列数量和实际处理时延。当前未改共享菜单配置、未部署，也未把模拟数据写入共享库；正式业务验收仍为未完成。

### 2026-09-19 客服工作台详情与办理弹窗优化

- 客服工作台的分配服务站弹窗改用当前工单候选站卡片，显示站点名称、编号与当日派单／容量；自动推荐标示首个可用站，点击卡片直接切为人工选择。候选项仅取原工单 `options` 接口返回的服务站，已饱和站点显示但不可选；不再要求客服输入服务站 ID，偏离首选站时要求填写覆盖原因。其他业务入口原有表单保持现状，提交仍走既有分站接口和服务端权限、版本及匹配校验。
- 完工审核改为按服务结果、产品码／签名、配件说明和逐项照片引用阅读，顶部显示工单、联系人及服务站；退回时在弹窗内要求填写审核意见。客服工作台的工单详情增加完工材料 Tab，购买记录与工单详情沿用既有统一详情壳；投诉详情也使用同一壳层，服务上下文由传统描述表格改为摘要与分组字段。以上仅改变客服工作台的展示与本地提交约束，不改变审核状态流转或材料版本。
- 研发验证：ESLint、国际化键检查及 19 项关联测试通过；隔离浏览器已核对候选站选择、完工审核和待审核工单的完工材料详情。全量 `check` 被仓库现有未使用依赖 `vxe-pc-ui` 阻断，全量测试存在其他模块旧断言及模块解析失败，类型检查仍有其他页面的既有错误；目标环境真实角色、数据与提交闭环仍待业务验收。

### 2026-09-19 客服工作台队列入口精简

- 移除顶部指标下方重复的状态切换卡片，顶部队列卡片继续作为唯一的状态切换入口；统一工作篮仍由待办列表标题区进入。原流转条中的真实“今日转入”数量放入对应顶部卡片的辅助位置，仅在接口返回该指标时展示，保留返回零与未返回的区别。阶段责任方继续在当前任务的办理辅助区显示；队列数据、权限、状态和办理动作均未改变。
- 研发验证：国际化键检查与 18 项关联测试通过；隔离浏览器确认第二行已移除，顶部卡片显示“今日转入”并能切换队列与任务。全量 `check` 仍被仓库现有未使用依赖 `vxe-pc-ui` 阻断；类型检查仅报顾客表单、派单弹窗、库存和人员排班等本次未改文件的既有错误。

### 2026-09-20 客服工作台任务操作按钮优化

- 当前任务底栏将已授权办理动作统一显示为主题色实心按钮，创建工单与分配服务站采用相同视觉层级；“查看详情”在左、办理动作在右。有办理动作时，查看使用中性色描边及浅灰悬停，办理动作保持主色并在悬停时加深；仅有查看权限时，它作为唯一主按钮。按钮可用性仍取现有权限和后端 `actions`，打开动作前的详情重读与服务端校验不变。
- 任务区无数据时，图标与提示在可用高度内居中。左上“当前聚焦”改为始终蓝底的信息卡，补充已有的脱敏手机号与可用商品名，并恢复原型中的半透明圆弧；切换状态队列不改变卡片底色。待办列表标题区另设带待办数的“统一工作篮／返回统一工作篮”按钮；待办数仍按已授权的工作篮队列计算，不冒充全部工单数量。
- 研发验证：全量 ESLint、国际化键检查和工作台数据测试 12 项通过；隔离浏览器核对办理／查看主次顺序与悬停颜色变量、非工作篮队列下的蓝色聚焦卡及返回入口、零条投诉队列的居中空状态和返回工作篮。全量 `check` 仍停在既有未使用依赖 `vxe-pc-ui`；类型检查仅报本次未改的顾客表单、派单弹窗、库存和人员排班页面错误，本次修改文件无新增类型错误。真实角色与业务数据验收仍未完成。
- 后续微调：“查看详情”保留文字按钮并去掉图标。右上“新建服务”直接打开既有两步建单弹窗，从服务类型及顾客手机号查询开始，不再只切换到受理记录队列；入口继续按服务受理创建权限显示，提交后刷新工作台摘要与当前队列，离开页面时沿用未保存内容和提交中的拦截。
- 验证：更新文件 ESLint、国际化键检查及 22 项相关测试通过；隔离浏览器确认“查看详情”没有 SVG 图标、“新建服务”直接打开两步建单弹窗。独立类型检查仍只报其他未改文件的既有错误；本次未在真实业务数据上提交工单。
- 客服工作台左上标题改为“客服待办与服务处理”，日期置于标题右侧并使用与总部工作台一致的标题字号、字重、基线和间距；中英文文案同步。隔离浏览器已确认标题与日期同行、右侧操作按钮正常显示；ESLint、国际化键检查和工作台数据测试 12 项通过。全量 `check` 仍停在既有未使用依赖 `vxe-pc-ui`。
- 标题区上边距与总部工作台同为 18px，标题区至下方指标卡片留白调整为 16px；客服任务区其余紧凑布局间距保持原值。隔离浏览器实测标题高 32px、上边距 18px、标题至卡片 16px，任务区仍完整显示。

## 6. 关联文档

- [页面清单](../01-功能需求/08-页面清单.md)、[需求索引](../01-功能需求/09-AI开发需求索引.md)。
- [菜单设计](../02-技术设计/03-gaia-ui目录与菜单设计.md)、[角色权限手册](../02-技术设计/09-后台菜单与岗位权限矩阵.md)。
- [Web 进度](../03-开发管理/04-Web管理后台开发进度.md)、[项目总览](../03-开发管理/00-开发进度与下一步.md)。
- [顾客预约与工单基础](2026-09-10-顾客预约与工单查询全栈首版.md)、[服务受理](2026-09-10-服务受理与代客建单全栈首版.md)、[门店代客报修](2026-09-13-门店代客报修全栈首版.md)、[派单异常与完工审核](2026-09-10-派单异常与完工审核全栈首版.md)。
