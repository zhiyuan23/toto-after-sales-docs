# 菜单稳定后六项前端目录整理

日期：2026-09-25。用户已授权执行六项调整，共涉及七个功能目录。范围为 `gaia-ui/apps/after-sales` 的源码位置与直接引用，不修改功能规则、公开路由、后端接口或授权。本次未提交 Git、未部署共享应用。

## 当前目录

以下路径从 `apps/after-sales/src/views/` 起算。

| 调整项 | 原目录 | 新目录 |
| --- | --- | --- |
| 1 评价标签配置 | `memberOperation/serviceSurvey` | `businessConfig/evaluationTags` |
| 2 标签管理 | `memberOperation/tag` | `businessConfig/tag` |
| 3 字典管理 | `dataCenter/dictionary` | `businessConfig/dictionary` |
| 4 客服渠道配置 | `customerService/supportChannel` | `businessConfig/supportChannel` |
| 5 安装码查询 | `antiDiversion/installationCode` | `customer/installationCode` |
| 6 运营分析 | `dataCenter/report`、`serviceResource/quality` | `analytics/report`、`analytics/quality` |

迁移 29 个文件，更新 22 个既有引用文件。功能内 components、model、schemas 一起迁移；29 个迁移文件中，只有服务质量页面的共享 model 相对 import 需要变更，其余文件内容与迁移前一致。工作台图表／模型、API 类型引用、路由组件映射和测试／浏览器夹具同步调整，不复制第二份页面。

`src/features/navigation/manifest.json` 与本次迁移前逐字节一致；`src/router/routes.ts` 只改变七个组件的 import 路径。页面 URL 继续沿用 `/memberOperation/serviceSurvey`、`/memberOperation/tag`、`/dataCenter/dictionary`、`/customerService/supportChannel`、`/antiDiversion/installationCode`、`/dataCenter/report`、`/serviceResource/quality`。这些路径是稳定访问标识，不再用它们直接推导源码目录。

本次没有新增或退役菜单节点，不修改 Gaia 权限树、数据库、角色授权、i18n 键或业务 API；无需为源码目录搬迁写库。原有未提交菜单、配置和抽屉改动全部保留。共享 API 类型进一步归位和其他历史实现清理不属于本次六项目录迁移，未额外执行。

## 验证

- 扫描 `src`、`tests`、`scripts`，无七个旧 `views/...` 源码路径引用残留；旧的公开访问路径仍按契约保留。
- ESLint、Knip、应用及测试 TypeScript、国际化检查通过。
- 完整 `check` 共 323 项，293 通过、30 失败；失败名称与迁移前基线一致，没有新增或消失的失败。完整门禁仍非零退出，不能称为全量通过。
- 迁移涉及的评价标签展示、标签保存载荷、报表查询、质量查询、分析页面布局、安装码查询映射及导航权限相关现有测试通过；安装码页面生命周期等既有失败保留在上述基线中。
- Vite production 构建单独运行通过，215 个 JS／CSS 产物通过现有体积检查。完整 `build` 前置的全量测试仍受既有失败影响。
- [验证摘要](evidence/2026-09-25-view-directory-reorganization/verification.txt)。本机原始日志：`/tmp/toto-directory-check.log`、`/tmp/toto-directory-build.log`。

页面核对通过原有 URL 和真实本机账号进行，只读查看，不提交业务表单：评价标签卡片、字典及字典项、客服渠道配置、安装码查询列表、运营报表汇总与图表均加载完成；服务质量展示正常空数据状态。标签管理页面组件加载完成，但当前账号的接口被后端提示“没有此操作的权限”，未扩大授权，也未把页面可加载当作数据读取验收。迁移前后的该页面与接口实现逻辑没有变化。

七个原访问地址均已核对，新目录不新增对应 URL。临时验证页已关闭。业务入口与验收计数不因目录整理增加。

当前目录与稳定 URL 的对应关系已更新到[目录与菜单设计](../02-技术设计/03-gaia-ui目录与菜单设计.md#当前源码目录与稳定访问路径2026-09-25)。
