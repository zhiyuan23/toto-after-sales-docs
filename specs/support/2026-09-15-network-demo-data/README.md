# 渠道与网点模拟正式数据

- 目标环境：本机 8080 聚合宿主当前连接的 `gaia_wh_init_wzy` 开发联调库。
- 数据标识：新增记录统一使用 `DEMO-` 编码及“（演示）”名称／地址，避免与真实业务数据混淆。
- 组织范围：代理商、门店和服务站统一归属当前开发账号可见的组织 `161 / 00038 / 天长`；这是数据权限归属，不代表地理区域。
- 关系场景：1 个华东一级代理商，下挂上海、苏州 2 个二级代理商；2 个二级代理商下挂 3 家门店；上海 2 个服务站对浦东、闵行形成主备优先级，苏州 1 个服务站负责吴中区。
- 数据安全：负责人姓名、手机号、热线、公司信息和地址均为模拟值，不对应真实个人或企业；未上传虚构证照附件。
- 主键隔离：代理商使用 10～12，避开审计表中 2～4 的已删除历史记录；其他主档也已核对当前实体、审计和业务引用后分配。
- 执行文件：[seed.sql](seed.sql)；需要回退时先确认没有被购买登记、工单、人员或其他业务引用，再使用 [rollback.sql](rollback.sql)。

执行后还需把本机 Redis 的对应 `ID_KE_*` 序列提高到数据库最大主键，避免后续页面新增产生主键碰撞。回退数据时不降低序列。

## 执行与复核

2026-09-15 在 `gaia_wh_init_wzy` 执行：写入事务成功提交，复核为代理商 3、门店 3、服务区域 3、服务站 3、区域关系 5；每条主档均有本次 `CREATE` 审计。代理商层级、门店所属代理商和上海区域主备优先级均按上面的关系场景返回。

列表排序代码装配后，本地聚合宿主已切换到新运行包；8080 监听正常，匿名权限接口返回预期 `4001`，说明接口链路可用。由于没有在自动化检查中使用业务账号，未通过 HTTP 重复写入或修改本批数据。

首次写入后复核发现 `afs_dealer_audit` 仍保留已删除 2 号代理商的 4 条旧历史。为避免演示代理商串入旧审计，在受控事务内将本次代理商改用 10～12，并同步 3 家门店关联和本次新增审计；失败尝试均已自动回滚，最终临时编码为 0，三条演示代理商各只有一条本次创建审计，旧 2 号审计保持原样。Redis 序列使用只升不降方式同步到：代理商 12／审计 8，门店 6／审计 7，门店类型 2，服务站 8／审计 9，服务区域 6／审计 8。
