# 服务人员小程序：当前原型状态、接口与验收矩阵

> 核对时点：2026-09-19；只读依据为当前 `04-页面原型/小程序/worker.js`、`worker.css`，服务人员端 `src/router/definition.ts`、`src/api/modules/{task,part,servicePersonnel}.ts`、现有页面源码，以及[逐入口进度](../../03-开发管理/06-服务人员小程序开发进度.md)和[完整开发计划](../2026-09-18-服务人员小程序完整开发计划.md)。以下记录“源码中有何状态与请求”，不代表视觉或业务验收通过。原型页面入口固定为 `index.html?consumerVersion=0.13#w-tasks`。
>
> **视觉门禁**：当前 `file://` 原型被浏览器安全策略拒绝，未能实际加载页面；未尝试 `localhost`、本地服务或替代浏览器绕过。此表只能由当前加载目标的源码建立检查清单，**没有逐页截图或一比一对照证据**。须取得可访问的正式预览 URL，再在同一微信视口逐页进入原型及正式页面，记录截图、交互与返回路径。H5 截图和构建不能替代微信或真机证据。

## 1. 当前范围及证据口径

- `worker.js` 当前直接定义 **25 个** `screen('w-…')`；日程和任务地图已经从当前原型移除。开发计划与原型 README 的当前计数已校正为 25；历史 27 屏记录不作为本轮基线。
- 正式路由：任务、配件、我的三 Tab；任务详情／处理／补资料三页；配件详情／领料／退料／记录／单据详情五页；登录和身份错误页。无日程、地图、调拨、资料、反馈、密码页面。`我的`已有真实身份、切企业、退出；原型中技术资料、反馈、改密为评审态，正式入口当前未显示。
- W01／W02／W04 曾有专用移动会话 HTTP 与数据库对账，分别为 42/42、62/62、104/105；W04 余下的重复签到缺陷编号 `DEF-20260919-01`。真实上传、微信逐状态视觉、真机仍缺。W05～W07 本人库存／本站选件／详情预选有微信模拟器读路径证据；W08～W10 单据请求因共享库缺 `mobile_applicant_account_id` 失败，迁移 `20260919_service_personnel_parts.sql` 未执行，不能作为可用或已验收。
- W03 日程／地图和 W11 调拨均**不在当前 25 屏中**，正式也无路由和入口。W03 已延期；W11 范围、权限及接收确认待确认。不得用历史原型或 W08 的“配件记录”入口恢复它们。
- 每页至少检查首次加载、有权数据、无权、空态、业务错误及重试、会话失效；列表另查筛选切换、分页和尾部加载；写入另查校验、重复点击、失败保留输入、成功后的真实状态和返回。服务端权限与数据范围由真实接口核对，不能以原型的本地模拟动作代替。

## 2. 原型 25 屏 × 状态 × 正式接口 × 验收场景

接口均位于 `/api/afterSales/mobile/service-personnel` 根路径下；`—` 表示当前无正式业务接口或页面。表中“待截图”均指同视口原型与微信页面的内容区、固定底栏及返回路径证据。

| 原型屏幕／入口 | 必查原型状态与操作 | 正式路由及接口 | 必查验收场景与当前证据 |
| --- | --- | --- | --- |
| `w-tasks` W01 | 概览；四分类；待处理全部／今日预约／等配件；历史筛选；搜索、排序、正在服务单／多单、空态；卡片进入详情 | `/pages/task/index`；`GET /tasks` | 同一可见范围的计数、关键词、分页和当前服务；跨人员／站／租户任务不可见；筛选切换不闪旧数据；切企业清缓存。42/42 真实 HTTP 已记录；微信新版滚动、吸顶、返回保留条件待截图。 |
| `w-detail` W02 | 待处理、服务中、缺件、审核中、退回、通过、关闭、取消的状态提示；联系／编辑／异常／处理／进度按钮随状态变化 | `/pages/taskFlow/detail/index`；`GET /tasks/{id}` | 服务端 `actions` 决定按钮；猜 ID 404；关联配件记录及详情返回路径；无权、错误、重试、长时间线。已有真实接口对账，逐状态微信待截图。 |
| `w-appointment` W02 | 联系结果三态、日期／起止时间、联系备注；确认与暂缓或断联后返回详情 | 任务详情内联表单；`POST /tasks/{id}/communications`，必要时 `POST /tasks/{id}/actions` | 时间合法、状态／版本／幂等、审计；失败保留表单；保存后详情与列表同步。62/62 写动作总账已有，逐状态微信待截图。 |
| `w-edit` W02 | 备用电话、地址补充、备注；主电话等只读提醒；提交返回详情 | 任务详情内联表单；`PATCH /tasks/{id}` | 仅白名单字段可写；版本冲突与掩码、失败保留输入；成功后刷新显示。微信表单展开和返回待截图。 |
| `w-exception` W02 | 改期、断联、缺件、取消；取消后的恢复申请；缺件转本单领料／记录 | 任务详情内联表单；`POST /tasks/{id}/actions`；配件子页按工单参数打开 | 只执行服务端允许动作；原因必填、重复请求及取消恢复不擅自改变目标状态；缺件往返保留工单。微信逐状态待截图。 |
| `w-service` W04 | 现场待签到／已签到、定位失败说明、远程开始；三段同一草稿；耗用、上传失败重试、产品码、客户确认、核对后提交；上下步及固定底栏 | `/pages/taskFlow/process/index`；`POST /tasks/{id}/check-ins` 或 `/remote-starts`，`GET/PUT /draft`，`POST /product-code-verifications`、`/completion-submissions`，配件 `GET /parts/stock` | 首次提交事务、本人可用库存及处理中退料占用、重复签到拒绝、失败上传保留草稿、真实位置／相机／扫码、退回后不重复扣减。104/105 真实对账；`DEF-20260919-01` 和真实媒体／微信／真机仍阻断。 |
| `w-review` W04 | 审核中、退回、通过待回访、关闭；提交版本展开、时间线；无二次交单 | 任务详情进度视图；`GET /tasks/{id}/progress` | 版本不可变、进度事实与审核结果一致；服务人员不能审核；长内容与固定返回按钮待截图。 |
| `w-returned` W04 | 仅指定退回项、补充材料、失败重试、查看原提交、重新提交 | `/pages/taskFlow/supplement/index`；`GET/POST /tasks/{id}/supplements`，媒体上传／码校验 | 只补 `requiredItems`、旧版不改、重提新版本、首次耗用不重复扣；跨单与越权拒绝。真实 HTTP 已有，真实上传与微信待验。 |
| `w-resubmitted` W04 | 重提后的审核进度和返回任务列表 | 补资料成功后返回详情进度视图；`GET /tasks/{id}/progress` | 重复请求回同一结果；刷新仍审核中；返回不重新提交。微信跳转和返回待截图。 |
| `w-parts` W05／W08 | 个人库存概览、领配件／退料、搜索、列表、单据记录；切企业空数据 | `/pages/part/index`；`GET /parts/stock?scope=PERSONNEL`，记录入口 | 本人／本站范围、无权／加载／空／错／重试／分页，切企业清缓存。本人库存已有微信模拟器读证；概览、固定 Tab、滚动导航待同视口截图。 |
| `w-part-detail` W05 | 库存归属、可用量、适用信息；可退／不可退两态，领退记录 | `/pages/partFlow/detail/index`；`GET /parts/stock/{partId}` | 猜 ID 不越权；预选退料携配件 ID 且返回保持列表；价格／图片／颜色缺正式字段时不编造。详情预选已有微信读证，逐状态截图待做。 |
| `w-requisition` W06 | 多项选件；工单领料／常备补充两用途；工单锁定、数量、说明、提交后单据详情 | `/pages/partFlow/requisition/index`；`GET /parts/stock?scope=STATION`、`GET /parts/work-orders`、`POST /parts/requisitions` | 空明细、非正整数、超本站库存、跨工单／站／租户、重复提交与并发；申请不直接变库存，后台批准后双方余额／流水一致。本站选件微信读证；迁移前提交和审批无真实验收。 |
| `w-return` W07 | 个人可退库存多项、工单、退料说明、详情预选、处理中占用、提交后详情 | `/pages/partFlow/return/index`；`GET /parts/stock?scope=PERSONNEL&returnableOnly=true`、`GET /parts/work-orders`、`POST /parts/returns` | 空明细、非正整数、超可退量、跨人员／站／租户、重复提交与并发；拒绝／撤销不动库存，批准反向转移且双边流水一致。详情预选微信读证；跨站占用已有隔离测试，迁移及真实 MySQL 并发未验。 |
| `w-records` W08～W10 | 全部／领料／退料及处理中／已完成／已拒绝／已撤销，空态、分页、进入详情 | `/pages/partFlow/documents/index`；`GET /parts/documents` | 本人当前站单据、类型和状态筛选、分页／重试／切企业隔离；不显示 W11。当前共享库缺申请人列，请求失败，未获可用证据。 |
| `w-record-detail` W09／W10 | 处理中、完成、拒绝、撤销；拒因、工单、明细、双方余额、历史；处理中撤销、拒后重申及返回工单 | `/pages/partFlow/documentDetail/index`；`GET /parts/documents/{id}`、`POST /parts/documents/{id}/actions` (`CANCEL`) | 猜 ID 404；仅本人处理中可撤；重复撤销、批准后撤销拒绝；审批只允许后台；拒绝／撤销库存和流水零变化，完成后对账。迁移前无真实记录页验收。 |
| `w-mine` W14 | 人员／企业身份；技术资料、反馈、改密、切企业、退出 | `/pages/mine/index`；`GET /context`，切换／退出接口 | 当前正式页仅身份、切企业、退出；技术资料／反馈／改密无正式入口。页面为默认白底标题导航；身份加载／错误／会话失效和操作反馈待微信截图。 |
| `w-technical` W13 | 关键词、品番、版本、类型、空结果；进入详情 | — | 真实资料源、发布权限和文件访问未确认；当前正式无路由／接口，不能用原型虚构数据宣称完成。 |
| `w-document` W13 | 资料元信息、版本、适用品番、预览／打开、返回结果 | — | 需资料访问再次鉴权和真实文件；当前未开发，正式预览只能核对原型，不做业务验收。 |
| `w-warranty` W12 | 延保搜索／列表；仅直达评审页 | — | 当前导航隐藏，无实体／接口／正式路由；数据源与适用范围待确认。 |
| `w-warranty-detail` W12 | 项目详情、有效期和适用品番；仅直达评审页 | — | 不把示例日期作为保修判定；隐藏且不可从生产路由直达。 |
| `w-feedback` W15 | 问题、联系方式、示例图片、本人反馈记录、提交反馈 | — | 受理责任、状态流转、媒体归属与本人范围待确认；正式无路由／接口。 |
| `w-password` W16 | 旧密码／新密码／确认、验证错误、修改结果 | — | 需确定旧密码或二次验证、密码策略及改密后会话处理；正式无页面／接口；不得输入真实密码到原型。 |
| `w-company` W16 | 候选企业、未提交资料确认、切换后新企业空数据 | “我的”操作；`GET /context`、`POST /context/switch` | 候选必须来自服务端；切换清旧企业列表、草稿、缓存和迟到响应；切换失败保持原上下文。已有真实身份联调，正式确认交互与微信待截图。 |
| `w-logout` W16 | 取消／确认退出，返回登录 | “我的”确认弹窗；`POST /auth/logout` | 当前会话服务端撤销、本地先失效、保护页无法返回旧内容；微信对话框和返回待截图。 |
| `w-login` 基础能力 | 登录演示及重新进入 | `/pages/login/index`；`POST /auth/login`、`GET /context` | 真实账号登录、错误凭据、重复点击、会话恢复／失效与回跳；原型示例身份不作真实登录证据。 |

## 3. 需要补采的同视口证据

1. 正式预览 URL 可访问后，记录 URL、实际加载的 `worker.js`／`worker.css` 标识、浏览器视口、微信模拟器型号与内容区尺寸。逐屏按上表同一数据状态截图；特别覆盖任务四分类和当前服务、详情各动作、W04 三段与定位／媒体失败、配件无权／有权／空／错／记录四状态、领退料表单展开与详情撤销。
2. 对每个可点动作记录「起点 → 操作 → 目标 → 返回后原筛选、工单和滚动位置」，覆盖任务↔本单领料／记录、配件详情→预选退料、申请成功→单据详情、详情→取消与回单据列表、我的→切企业／退出。检查长内容不遮挡固定底栏、系统状态栏／胶囊／安全区由微信运行时处理。
   领退料另以两项配件跨分页／搜索选取，确认待提交集合可见、可调整且最终提交项一致；申请成功后从单据详情返回表单，确认不会因旧选择再次误提交。
3. 真实接口及数据库证据按专用测试账号和数据 ID 留存请求／响应、权限拒绝与余额／流水前后快照。共享库迁移和测试数据写入须本次单独授权；未授权前仅做结构预检和只读检查。每个入口只在代码、结构、权限、交互、微信画面和必要数据库对账共同满足后标记完成。

## 4. 微信开发者工具本轮只读走查（2026-09-19 23:04～23:08）

环境：微信开发者工具 Stable 2.02.2608060，项目为 `gaia-customer-service-uni/dist/dev/mp-weixin`，iPhone 15 Pro Max 模拟器，现有专用服务人员会话。以下是当次辅助功能树、模拟器画面和 Network 面板即时观察；没有保存成独立截图文件，不能当作原型同视口对照。当前 `dist` 可能早于领料跨企业旧工单清理的小修。

| 路径／动作 | 观察结果 | 结论边界 |
| --- | --- | --- |
| 任务 Tab → 配件 Tab | `GET /context`、`GET /tasks`、`GET /parts/stock?scope=PERSONNEL&page=1&pageSize=15` 的 Network 状态均为 200；配件首页显示“个人可用库存 4 件、1 种”，列出一件有余额配件及零余额配件，单据记录／领配件／申请退料可见。 | 仅证实当前账号和当前站的读路径；未测其他身份、企业或站点。 |
| 配件首页 → `305mm 排水连接件 HP101T3` 详情 | 详情显示本人可用 4、FC 高歌服务站库存 236、处理中退料占用 0、当前可退 4、适用产品，并有“申请退料”“查看领退记录”；返回后仍可回配件首页。 | 正式图片、价格、颜色无字段时未伪造；没有原型画面比对。 |
| 详情 → 申请退料 | 退料页预选该配件 1 件；汇总区显示 `1 种 / 1 件`、配件名、加减和移除。以 `NO-SUCH-PART-VERIFY` 查询，先出现加载态，随后显示“暂无可退配件／请调整查询关键词”，汇总区仍保留 1 件；点移除后变为 `0 种 / 0 件`。返回键回到详情。 | 仅本地选择和只读查询；未选择工单、未提交、未改变库存。跨页选择未测，因为当前页面只有 9 个库存记录，少于 15 条一页。 |
| 详情 → 查看领退记录 | 正式记录路由打开；全部／领料／退料和四状态筛选显示，列表区域为“加载失败／服务暂时不可用，请稍后重试”及重试按钮。点“领料”和“重试”分别重新发起 `GET /parts/documents?type=REQUISITION&status=&page=1&pageSize=15`，界面回到相同错误态；返回键回到配件详情。 | 当前共享库缺申请人列，故 W08～W10 列表、详情、撤销及分页无法验证。Network 面板显示 HTTP 200，但业务响应失败；不能把 HTTP 200 写作记录可用。 |
| 配件首页 → 领配件 | 本站选料页显示 FC 高歌服务站，`HP101T3` 可领 236、`HP101T4` 可领 14；库存 0 的配件加号禁用。选择上述两项后汇总区为 `待提交 2 件`，两项名称、数量、加减、移除均显示。用途选择器切“常备库存补充”后，工单字段隐藏，改显“补充原因”和“提交补充申请”；返回键回配件首页。 | 未触发提交或审批；跨页选择、真实单号、余额流水、失败重试和返回已提交单据仍待迁移及授权测试数据。 |

源码只读复核：各正式配件页在读取前调用 `ensurePageAccess`；列表使用 `usePagination` 的查询键隔离、尾部加载和错误反馈；`scopeKey` 包含账号、租户、企业、站点并注册切企业重置。领退料新增可见的已选清单、移除与数量调整；成功分支设 `submitted` 并 `redirectTo` 单据详情，避免返回旧表单再提交。**这些是源码条件，不等于其运行时及真实接口验收。** 曾发现领料工单锁定页切企业时 `lockedTask` 未清造成旧工单短暂显示，端侧已在源码中清空并改为“正在核验关联工单”；这项小修尚待最终微信构建后复核。
