# 四视角首页开发环境补齐记录

- 日期：2026-09-12。
- 状态：用户明确授权“执行开发库补齐并验证”后，已于 2026-09-12 13:18 执行两表迁移和 6 项读取权限追加，结构与配置核验通过；主任务已在当前真实登录会话下完成总部趋势和客服投诉空列表的页面读取验证。未新增角色／操作授权、未写入业务记录、未重启宿主。
- 关联任务：四视角首页工作台改造；由功能 Spec 链接本目录作为环境阻塞和待执行依据。
- 目标：当前本机聚合宿主使用的开发库 `gaia_wh_init_wzy`，MySQL `8.4.10-10`。凭据仍仅由既有 `.local/application.properties` 提供，本目录不保存凭据、连接串或会话信息。

## 执行前的只读结论

运行中的聚合 JAR 已包含售后 `ReportApi`，不需要为本问题重新装配后端代码。执行前确认的缺口有两类（现已按下述范围补齐）：

| 对象 | 实际状态 | 影响 |
| --- | --- | --- |
| `sys_module.code=afsReport` | 模块启用、显示，`api_list=NULL`；已有 `admin`（ID 1）及“管理员”（ID 2）角色菜单关系 | `GET /reports/permissions` 被售后应用服务拒绝，首页显示部分权限未读取 |
| `sys_module.code=afsComplaint` | 模块启用、显示，`api_list=NULL`；同上两个既有角色 | 客服投诉队列的权限接口同样不可用 |
| `afs_complaint` | 不存在 | 报表 SQL 统计有投诉工单数时引用该表；只补权限仍会查询失败 |
| `afs_complaint_event` | 不存在 | 投诉历史、阶段处理及请求幂等事实缺失，投诉切片不能完整装配 |
| 报表／投诉模块的操作配置 | 两个模块均无 `sys_module_operation` 记录 | 报表本来只读；投诉的建立、定责、处理、结案按钮仍应不授予、不显示 |

工单、购买登记、产品实例、工单商品、服务站和自有代理商组织映射已存在。`afs_service_order.id` 是有符号 `BIGINT NOT NULL`、InnoDB，兼容投诉迁移外键；服务器默认存储引擎为 InnoDB。

完整缺表范围为 **2 张表**，没有额外的幂等主表：投诉主表通过 `(order_id, request_id)` 唯一约束保留建档幂等，事件表通过 `(complaint_id, request_id)` 唯一约束保留阶段动作幂等，并分别存储请求摘要。原迁移还包含 2 个外键、投诉状态 CHECK 约束、2 个业务唯一约束和 3 个查询索引（不计主键／外键隐式索引）。

结构化证据见 [readonly-evidence.json](readonly-evidence.json)。权限来源分别为后端 `database/20260910_report_permissions.json` 和 `database/20260910_complaint_permissions.json`，不新增首页专用接口，也不授予通配写权限。

## 已授权并执行的范围

1. 执行前已按 [01-preflight.sql](01-preflight.sql) 的条件重新读取数据库与配置，确认目标正确、2 张新表均不存在、2 个模块各恰好一行且 ID／code／path／enable 一致；保存完整模块配置、4 条角色菜单关系和空操作清单。该预检条件现已不再满足，**不得重复执行建表脚本**。目标或结构变化时必须停止并重新评估。
2. 执行 [02-create-complaint-tables.sql](02-create-complaint-tables.sql)：这是后端既有 `20260910_complaint.sql` 的逐字副本，仅新增上述 2 张空表，不更新工单、顾客或其他业务记录。SHA-256：`d6afc08e2765e38efb836075bd1a756b8ac30853676506e3c13ff7fd34db71d4`。
3. 执行 [03-append-read-permissions.sql](03-append-read-permissions.sql)：在事务中锁定 2 个既有模块，仅追加缺失的 6 项读取权限，保留所有既有 `api_list` 项。报表追加 `/reports/permissions`、`/reports/work-orders` 两项 GET；投诉追加根路径、`/permissions`、`/*`、`/*/history` 四项 GET。只按当前核验的模块 ID／code／path 更新，不新增或改动任何角色、用户、人员、组织归属或操作权限。
4. 运行 [05-verify.sql](05-verify.sql)，确认表、外键、唯一约束、CHECK、索引和权限追加成功；新表应保持 0 行，既有角色数量各仍为 2。再由当前真实登录页面完成总部趋势和客服投诉队列的读取验收，避免插入业务样例。
5. 售后权限快照缓存最长 30 秒。等待缓存自然失效后重新加载页面即可；本问题本身不要求重启宿主或 Redis。若仍失败，根据实际错误继续定位。

执行工具必须遇到 SQL 错误立即停止，不使用忽略错误／强制继续选项。MySQL DDL 不可事务回滚；两张表创建若部分失败，先核对现场，不重复执行整个脚本。

## 回退与未开放能力

- 权限回退使用 [04-revert-added-permissions.sql](04-revert-added-permissions.sql)，只移除本次预检确认原先不存在且实际追加成功的 6 项 API，保留后来追加的其他权限。若执行前证据发生变化，要据实际差异调整回退，不能删除原先已存在的配置。
- 应用回退保留投诉表及不可变历史，不提供自动 DROP。仅在表确实为空、完成备份且另获删除授权时，才单独评审撤表；已有业务数据不得删除。沿用后端既有 `database/20260910_complaint.rollback.md`。
- 本方案补齐已授权模块的只读数据展示。投诉建立、责任认定、处理和结案的按钮／API 仍无操作授权，继续拒绝，不因为工作台需要展示就扩大账号业务权限。
- 本方案不建立账号与门店／服务站的绑定规则；首页继续按后端已有授权组织范围展示。

## 已完成验证

`mvn -B -o -pl gaia-after-sales-api -am -Dtest=ReportIntegrationTest,ComplaintIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过，**4 项测试、0 失败、0 错误**。使用既有 H2、真实 MyBatis／HTTP／事务测试，覆盖投诉阶段、幂等、组织／租户权限、报表去重与日期边界。

该测试证明既有迁移和业务实现的隔离研发结果。开发库执行与结构验证现已完成；真实登录态查询结果另列下方，不把结构成功计作完整业务验收。


## 2026-09-12 实际执行与核验

- 用户授权：本次会话明确批准“执行开发库补齐并验证”。
- 时间／目标：2026-09-12 13:18（Asia/Shanghai），`gaia_wh_init_wzy`。
- 备份：宿主 `.local/backups/dashboard-env-20260912-131849/`，目录权限 `700`，文件权限 `600`；包含 `before.json`、`after-before-commit.json` 和 `restore-module-config.sql`。内容仅为本次模块／角色／操作配置及其恢复 SQL，不含连接凭据或登录会话。
- 创建：`afs_complaint`、`afs_complaint_event` 两表，均为 InnoDB、0 行；2 个外键、2 个请求唯一约束、1 个投诉状态 CHECK 和预定索引均存在。
- 权限：报表追加 2 项 GET、投诉追加 4 项 GET；在事务内按实际模块加锁、精确核对 ID／code／path 后追加。4 条既有角色菜单关系逐字段保持，操作配置仍为 0，没有新增写入权限。
- 影响：没有写入工单、顾客、投诉或其他业务记录；没有宿主或 Redis 重启。
- 可还原性：本机备份中的恢复 SQL 仅在 `api_list` 仍与本次执行后值完全一致时恢复执行前配置；如之后存在追加配置，使用本目录差异撤销脚本并重新核对，不覆盖团队后续变更。投诉表保留，禁止自动 DROP。
- 证据：[execution-evidence.json](execution-evidence.json) 保存执行时间、目标、表结构、约束、索引、追加后的 API、备份位置和未变更边界。
- 真实登录态读取：主任务使用 Chrome 既有“操作员01”会话核验。总部点击“重新加载”后，权限警告消失，出现近 30 天建单区域，当前授权范围合计显示 0 单，30 个日期位置正常展示；客服视角出现“待定责投诉”卡片（0），进入同页投诉队列后加载结束，显示空态和共 0 条，无错误提示。
- 浏览器证据边界：上述结果来自真实页面 DOM 与交互；未取得 HTTP 网络响应码，不能把页面结果写成特定 HTTP 状态码。30 个日期位置由前端补齐展示，不表示后端返回了 30 条非空统计分组。未读取、复制或导出浏览器 Token／Cookie，未写入业务记录。
- 验收边界：本次完成环境缺口补齐和当前账号的真实读取冒烟；空列表不能证明投诉写入、阶段流转、拒绝角色、完整跨组织／跨租户业务验收已经完成。
