# 配件库存独立系统全栈首版

日期：2026-09-10

范围：A30～A35 / PRT-002～PRT-007 的 Web 管理端首版；A29 继续沿用既有配件基础主档。2026-09-15 的配件管理详细需求是 A30～A32 的最新业务基线，与首版冲突处以该详细需求为准。

## 业务边界与数据模型

- 库存按“配件 + 归属类型 + 归属 ID”唯一。归属类型为仓库、服务站、服务人员；仓库是售后自有主档，服务站和服务人员引用既有售后主档。
- 数量按整数件管理。每次余额变更在同一事务中锁定余额行，扣减后不得小于零，并生成不可编辑、不可删除的库存流水；流水保存单据、明细、归属、变动量和变动后余额。
- 服务站入库单保存为待提交，提交后变为已完成并在同一事务增加服务站库存；待提交可编辑或物理删除，单号和服务站不可编辑，已完成只读。
- 领料由服务站师傅在小程序申请，流向为服务站 → 本站师傅；后台管理员通过后直接完成，并在同一事务扣减服务站库存、增加师傅库存。拒绝必须填写原因且不改库存；处理中可由小程序撤销。
- 退料由服务站师傅在小程序申请，流向为师傅 → 所属服务站；后台管理员通过后直接完成，并在同一事务扣减师傅库存、增加服务站库存。拒绝必须填写原因且不改库存；处理中可由小程序撤销。
- 调拨允许仓库、服务站和人员之间流转，来源和目标必须不同；先出库确认并扣减来源，再入库确认并增加目标，途中状态明确为已出库确认。
- 工单耗用只允许从服务站或人员库存扣减，创建即完成，以“单据类型 + clientRequestId”保证重试幂等，并保存请求内容指纹；同请求号、不同内容会被拒绝。当前工单主域尚未实现，因此本切片不声称已校验工单状态、执行人归属或与完工原子提交。
- 盘点创建时保存实盘数，授权确认时在事务内读取当前账面数、计算差异并形成调整流水；可在确认前撤销。
- 所有单据保存状态历史和乐观版本。单据创建最多 100 行，配件不可重复且必须已启用；仓库存在正库存时不能停用。

## 实现

- 后端新增 `inventory` 领域的 API / AppService / mapper / model，统一根路径为 `/api/afterSales/inventory`；2026-09-15 详细需求改造后的 OpenAPI 版本为 1.9.0。
- 新增迁移 `database/20260910_part_inventory.sql`，包含仓库、仓库审计、余额、单据、明细、流水和状态历史 7 张表。脚本依赖配件、服务站和服务人员迁移；2026-09-10 经用户明确授权执行到共享测试库 `gaia_wh_init_wzy`。
- 前端 `apps/after-sales` 已提供真实的配件入库、库存、领料审核、退料审核、调拨、工单耗用和盘点页面。入库页实现权限过滤的服务站选择、实时单号、动态明细合计、配件档案选择、待提交保存／编辑／提交／物理删除；库存和领退料查询、字段、详情与处理按钮按 2026-09-15 需求校正。页面继续复用 `FaPageMain`、`EsSearch`、`EsListToolbar`、`EsTable`、`EsFormDialog`、`EsDetailDialog`、`useListPage` 和 `useCrudDialog`。
- 后端库存服务已增加 `INBOUND` 类型、`RK/LL/TL + 服务站编号 + YYYYMMDD + 三位流水号`、入库编辑／提交／物理删除、领退料管理员通过／拒绝、服务站与师傅双边库存更新、服务站数据范围、字段查询和详情当前库存；单号和工单号继续由数据库唯一约束／应用校验保护，本期按需求不处理并发取号。
- 服务人员小程序本轮只修改可交互原型：新增领料／退料申请、说明和明细，处理中允许撤销；未修改真实小程序业务代码，也不把原型视为接口联调完成。
- 配件档案 A29 的编号、名称、工厂、启停和适用品番继续使用既有实现。图片依赖对象存储和安全扫描契约，价格依赖币种、精度、生效历史及维护责任，这两项没有足够业务依据，本次未猜字段或伪造上传/价格功能。

## 状态机

| 单据 | 初始状态 | 可执行动作 | 结果 |
| --- | --- | --- | --- |
| 配件入库 | `PENDING` | `SUBMIT` | `COMPLETED` |
| 领料／退料 | `PROCESSING` | `APPROVE` / `REJECT` / `CANCEL` | `COMPLETED` / `REJECTED` / `CANCELLED` |
| 调拨 | `PROCESSING` | `CONFIRM_OUT` / `REJECT` / `CANCEL` | `OUTBOUND_CONFIRMED` / `REJECTED` / `CANCELLED` |
| 调拨 | `OUTBOUND_CONFIRMED` | `CONFIRM_IN` | `COMPLETED` |
| 工单耗用 | 创建即处理 | 无人工状态动作 | `COMPLETED` |
| 盘点 | `PENDING` | `CONFIRM` / `CANCEL` | `COMPLETED` / `CANCELLED` |

非法动作、旧版本重复动作、负库存、停用或不存在的归属和配件均由后端拒绝；前端只展示后端返回的行级 `actions`，不复制状态机。

## 主系统权限配置清单

主系统保存以下映射后才形成完整运行时授权；`code` 与 `action` 保持同义且 `enable=1`：

| 模块 | `sys_module.api_list`（查询） | `sys_module_operation`（写操作） |
| --- | --- | --- |
| 配件库存分组 `afsInventory` | `GET$/api/afterSales/inventory/permissions,GET$/api/afterSales/inventory/options` | 无 |
| 配件入库 `afsPartInbound` | `GET$/api/afterSales/inventory/inbounds,GET$/api/afterSales/inventory/inbounds/*,GET$/api/afterSales/inventory/inbounds/next-number` | `add`: `POST$/api/afterSales/inventory/inbounds`；`edit`: `PUT$/api/afterSales/inventory/inbounds/*`；`submit`: `POST$/api/afterSales/inventory/inbounds/*/actions`；`delete`: `DELETE$/api/afterSales/inventory/inbounds/*` |
| 库存台账 `afsPartStock` | `GET$/api/afterSales/inventory/stock,GET$/api/afterSales/inventory/ledger,GET$/api/afterSales/inventory/warehouses,GET$/api/afterSales/inventory/warehouses/*` | `addWarehouse`: `POST$/api/afterSales/inventory/warehouses`；`editWarehouse`: `PUT$/api/afterSales/inventory/warehouses/*`；`warehouseStatus`: `PUT$/api/afterSales/inventory/warehouses/*/status` |
| 领料管理 `afsPartRequisition` | `GET$/api/afterSales/inventory/requisitions,GET$/api/afterSales/inventory/requisitions/*` | `action`: `POST$/api/afterSales/inventory/requisitions/*/actions`；申请创建接口由服务人员小程序权限单独授权，不作为后台新增按钮 |
| 退料管理 `afsPartReturn` | `GET$/api/afterSales/inventory/returns,GET$/api/afterSales/inventory/returns/*` | `action`: `POST$/api/afterSales/inventory/returns/*/actions`；申请创建接口由服务人员小程序权限单独授权，不作为后台新增按钮 |
| 调拨管理 `afsPartTransfer` | `GET$/api/afterSales/inventory/transfers,GET$/api/afterSales/inventory/transfers/*` | `add`: `POST$/api/afterSales/inventory/transfers`；`action`: `POST$/api/afterSales/inventory/transfers/*/actions` |
| 工单耗用 `afsPartConsumption` | `GET$/api/afterSales/inventory/consumptions,GET$/api/afterSales/inventory/consumptions/*` | `add`: `POST$/api/afterSales/inventory/consumptions` |
| 盘点管理 `afsStocktake` | `GET$/api/afterSales/inventory/stocktakes,GET$/api/afterSales/inventory/stocktakes/*` | `add`: `POST$/api/afterSales/inventory/stocktakes`；`action`: `POST$/api/afterSales/inventory/stocktakes/*/actions` |

角色菜单和操作仍须分别授权；共享环境验收至少覆盖总部／服务站允许角色与无库存权限拒绝角色的菜单、按钮和直接 API 请求。

## 验证

### 2026-09-15 详细需求改造

- 后端 `mvn -pl gaia-after-sales-api -am -DskipTests compile` 通过；`InventoryAppServiceTest` 2 项通过，覆盖入库提交、领退料通过／拒绝／撤销的新状态迁移，以及旧 `RECEIVE` 和重复库存动作拒绝。Inventory MyBatis XML、OpenAPI JSON 和差异空白检查通过。
- 后端库存集成用例已改为服务站入库、本站师傅领退料和 `APPROVE → COMPLETED`，但当前隔离测试在执行库存用例前被既有 `database/20260912_network_org_scope.sql` 的 H2 不兼容 `UPDATE ... LEFT JOIN` 阻断；该失败不是本次库存代码产生，不能据此声称数据库关键写路径已通过。
- 前端 Node 24.21.0 下 lint、类型检查、i18n 检查、库存／导航 21 项测试、Vite production build 和 bundle-size 检查通过。全项目依赖检查仍被既有未使用依赖 `vxe-pc-ui` 阻断，本次未越界删除或改造该依赖。
- 2026-09-15 弹窗样式复核后，调拨、工单耗用和盘点的动态配件明细统一改为 `large` 弹窗及 `EsDialogTablePanel + el-table`，双列间距调整为 24px，明细表统一 360px 最大高度并补齐逐行配件、数量和删除控件的可访问名称；入库新增／编辑明细同步采用同一表格容器。定向 4 项测试、i18n 2220 键参数检查、类型检查、相关文件 ESLint、production build 和 bundle-size 检查通过；真实浏览器确认三类共享单据弹窗宽度 1200px、无横向溢出，配件入库仍因当前账号缺少菜单授权未完成页面级复验。
- 后台配件专业原型 5 项、服务人员小程序回归 13 项，共 18 项通过；覆盖单号、筛选、动态合计、入库状态、领退料通过／拒绝／撤销和双方库存变化。
- 菜单 manifest 已注册 `afsPartInbound` 且导航回归通过；只读菜单交付包生成器当前被既有下载中心路径缺少存储分组的问题阻断，因此本次未生成或应用共享菜单包。

- 目标库核对：`afs_part`、`afs_service_station`、`afs_service_personnel` 主键及引用列均为 `BIGINT`；共享库存迁移已按授权执行并完成结构复核。
- 后端：`mvn -B -o -pl gaia-after-sales-api -am test` 通过，core 3 项、API 32 项，共 35 项。库存新增测试覆盖盘点建账、领料、退料、调拨双确认、工单耗用幂等、负库存、并发扣减、非法路径、过期版本、仓库审计、仓库停用保护、权限拒绝和字符串 ID；最新版模块已安装到本地 Maven 仓库，`gaia-saas-web-jar` 离线生产打包通过。
- 前端：锁定 Node 22.23.1 / pnpm 10.8.1，73 项测试、`vue-tsc -b` 和 production build 通过；构建仅保留既有 Vite native config 兼容提示和大 chunk 提示。
- OpenAPI JSON、Inventory MyBatis XML 和 `git diff --check` 通过结构检查。
- 共享测试库：迁移后核对 7 表、69 列、22 组索引和 29 项约束，初始业务数据为 0；7 个库存模块均已配置查询 API，新增并启用 12 个写操作，账号 `00001` 的超级管理员角色获得 12 项操作授权。
- 真实浏览器：库存台账、领料、退料、调拨、工单耗用、盘点 6 个菜单均不再出现无权提示，空列表请求成功；5 个单据创建入口及仓库新增入口按授权显示。权限实时读取数据库，本次不依赖重启缓存生效。

## 尚未完成与迁移门禁

- 既有租户库必须在明确授权后执行增量迁移 `database/20260915_part_management_detail.sql`，放开 `INBOUND` 单据类型；本次没有执行数据库变更。应用升级前还需盘点历史 `RECEIVED` 领退料单及对应流水，保留历史状态和审计，不批量重命名或删除。
- 新菜单 `afsPartInbound` 及入库查询／新增／编辑／提交／删除操作必须在主系统保存并按总部、服务站角色分别授权；领退料后台角色只保留查询与审核动作，既有后台 `add` 授权须移除，申请创建接口改由服务人员小程序身份单独授权。当前不写共享权限数据；共享环境还需用真实角色验证菜单、按钮、直接 API、站级数据范围、负库存和重复请求。
- 服务人员小程序交付物仍是原型；真实账号、工单、配件主档、库存和领退料 API 尚未接入。后台实现完成不代表移动端联调或业务验收完成。
- A34 尚待 FS-028 工单与完工主域提供权威工单状态、执行人和事务边界后完成整链联调。
- A29 图片、价格仍待对象存储、安全和计价契约确认；不影响库存按配件 ID 记账，但影响 A29 完整验收。
- 下一步须先应用增量迁移和菜单／操作权限，再用脱敏业务数据完成入库、领料、退料写入关键路径及拒绝角色、跨租户和站级数据范围验收。

## 回退与恢复

应用回退时先关闭库存菜单和写接口。迁移未产生业务数据时，可在明确授权后按 `afs_part_inventory_history` → `afs_part_stock_ledger` → `afs_part_inventory_document_line` → `afs_part_inventory_document` → `afs_part_stock_balance` → `afs_part_warehouse_audit` → `afs_part_warehouse` 的顺序删除；一旦产生业务数据，不得直接删表，必须同一时间点备份和恢复单据、明细、余额、流水及历史，并与配件、服务站和人员主档引用一起对账。
