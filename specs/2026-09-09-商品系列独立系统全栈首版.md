# 商品系列独立系统全栈首版

> 历史说明：本文首版的共享商品引用已由 2026-09-10 V3 独立商品模型废止。当前系列只关联售后 `afs_product`，不读取、写入或复用 `md_material` 主键；以 [商品与服务独立商品模型纠偏 V3](2026-09-10-商品与服务独立商品模型纠偏.md) 为准。

- 日期：2026-09-09
- 范围：A10、ADM-MD-008、FS-008 的商品系列基础主档切片
- 状态：开发中；代码、隔离环境与共享测试库建表完成，正式菜单角色、写入和真实租户验收待完成
- 实现端：`gaia-ui/apps/after-sales`、`gaia-after-sales`；未修改主系统 legacy 页面、移动端或受保护的 `src/views/common`

## 1. 本次确认的业务口径

1. 商品系列是售后自有主数据，不复用或依赖蓝鲸数字主系统中的系列业务实现；平台只提供登录、菜单、接口权限、租户路由、主键、响应和事务等通用能力。
2. 系列基础字段为编码、名称、排序、备注和状态；编码在同一租户内大小写不敏感唯一。系列不是商品分类枚举，名称和值不写死在代码中。
3. 系列与共享商品主档为多对多关系。关联维护采用一次提交新增集和移除集的差异方式，单次最多变更 100 项；重复新增或移除不存在的关联按幂等差异处理。
4. 停用系列保留历史商品关联并允许移除，不允许新增关联。停用商品或共享主档已停用的商品不能新增关联；历史关联不被自动清理。
5. 不提供商品或系列物理删除，不修改 `md_material` 结构，不复制共享商品整行，不自动灌入市场系列数据。

公开市场口径只用于确认系列维度和录入示例。TOTO 中国官网当前产品体系包含 NEOREST、WASHLET 等品牌/系列，且龙头存在 G 系列；这些名称不构成系统枚举，也不替代 TOTO 内部最终主档。参考：[TOTO 中国产品分类](https://www.toto.com.cn/cn/products/categories.html)、[TOTO 中国产品目录](https://www.toto.com.cn/cn/service_catalogue.html)、[G 系列](https://www.toto.com.cn/cn/products/categories/faucet/commonseries/G/index.html)。

## 2. 数据与接口

新增售后自有表：

- `afs_product_series`：系列基础字段、启停、版本和创建/更新审计字段。
- `afs_product_series_product`：系列—共享商品多对多关系，只保存 ID、创建人和创建时间。

迁移脚本为 `gaia-after-sales/database/20260909_product_series.sql`，依赖此前目录基础迁移；2026-09-09 经明确授权已执行到测试库 `gaia_wh_init_wzy`，两张目标表创建成功且初始均为 0 行。

新增接口：

- `GET/POST /api/afterSales/catalog/series`
- `GET/PUT /api/afterSales/catalog/series/{id}`
- `PUT /api/afterSales/catalog/series/{id}/status`
- `PUT /api/afterSales/catalog/series/{id}/products`

列表支持系列编码/名称、关联商品编码/名称/机能型号、状态和分页筛选。详情返回当前关联商品摘要。所有 BIGINT ID 以字符串传输；新增、编辑、启停和关联变更均校验真实接口权限，写操作串行锁定目录、使用事务、版本令牌和目录审计。

## 3. 独立前端

`apps/after-sales` 的 `/catalog/series` 已从通用占位页切换为真实页面，使用子系统原生 `FaPageMain`、`EsSearch`、`EsListToolbar`、`EsTable`、Element Plus 弹窗、`useListPage` 和 `useCrudDialog`。

已实现：

- 系列查询、筛选、重置、分页、列表和状态展示；
- 新增、详情回填编辑、字段校验、失败保留输入和重复提交保护；
- 启停确认、操作后自动刷新；
- 关联商品远程检索，编码/名称与机能型号并行查询、结果去重；
- 批量差异关联、停用系列提示、商品查询权限缺失提示；
- 行操作由后端 actions 决定，不从菜单开放推断业务接口授权。

## 4. 验证证据

- 后端：`mvn -B -o -pl gaia-after-sales-api -am test`，核心 2 项、API 集成 15 项，共 17 项通过。
- 后端覆盖：系列编码唯一、创建/编辑/版本冲突、多对多关联、按商品筛选、停用保留关系、停用后拒绝新增、停用商品拒绝关联、关联移除、事务审计、字符串 ID、显式关联权限和租户隔离；原商品/分类回归继续通过。
- 隔离 HTTP：真实 Tomcat + 生产 AppService/MyBatis XML + 路由 H2，10 个商品系列请求检查通过；不是 Mock 或只检查 HTTP 200。
- 前端：Node 原生测试 47 项通过；`vue-tsc -b` 通过；Vite production build 通过。现有 Vite native-config 和大 chunk 警告未扩大为本切片失败。
- 浏览器：`127.0.0.1:9013` 隔离入口验证桌面白卡列表、新增弹窗、关联弹窗、`MODEL-4` 远程检索、保存后关联数自动刷新、关联商品筛选与重置；导航图标和原有导航模式控件保留。

## 5. 未验收与后续

- 共享测试库迁移已完成，未写共享业务数据，未新增或授予正式角色权限。
- 7004/9010 入口及 8080 真实宿主已用当前登录账号验证列表返回空结果；新增按钮因当前账号无业务写权限保持禁用。允许侧写入、拒绝侧、租户边界和正式菜单角色仍待验收。
- 市场系列初始数据、编码治理和 TOTO 内部维护责任仍需业务提供正式主档；当前功能允许按确认后的数据录入，不自动生成。
- 商品图片、配套品、服务项目和配件档案不在本切片内，按已确认的主数据优先顺序继续开发。

## 6. 系列归属模型 V2 修订（2026-09-10）

本节保留为 V2 历史实现证据。2026-09-10 已由 [商品与服务独立商品模型纠偏 V3](2026-09-10-商品与服务独立商品模型纠偏.md) 替代商品引用层：商品系列仍是售后自有主数据，关系模型为“一系列可包含多个商品、一个商品只能归属一个系列”，但商品不再是共享物料扩展。

- `afs_product.series_id` 是商品唯一系列归属；商品档案创建或编辑时只能选择一个已启用系列。调整系列时会转移该商品归属，解除关联会清空 `series_id`，不保留并行关联记录。
- 系列的关联商品维护入口保留为兼容操作，但提交语义为批量调整商品归属，不再生成多对多关系；系列列表商品数按当前 `series_id` 统计。
- 系列补充图片和创建/更新审计展示，系列编码仍在本模块唯一。商品图片、故障和说明书归属商品档案，不归属系列。
- `E:\workspace\gaia-after-sales\database\20260910_catalog_v3_independent_product.sql` 为新售后商品建立 `afs_product.series_id`；不复制、不转换也不删除 V2 旧数据，历史记录仅保留留痕。
- V3 后端目录集成测试 34 项和售后子系统类型检查通过。正式多租户、允许/拒绝角色及历史数据处置仍需单独验收。
