# 顾客预约与工单查询全栈首版

> 2026-09-11 数据契约更正：门店／商品选项、写入校验、登记关联和查询数据范围须使用售后自有主档与归属。 现源码已完成本项独立化并通过隔离研发验证，目标环境迁移及业务验收待完成；下文实现、迁移和验证仅记录原时点事实。最新约束与源码证据见[独立边界核查](2026-09-11-业务数据独立边界与已开发功能核查.md)。

- 日期：2026-09-10
- 对应入口：A15 顾客档案、A16 预约管理、A18 工单管理、D03／D04 顾客登记、D07 门店顾客、D08 门店预约，以及总部／客服共用的安装码查询
- 需求依据：ADM-CU-001～003、DLR-003～005、DLR-009～011、BR-001～006、BR-013、FS-010～017
- 状态：共享迁移与管理员允许侧空数据验收完成，业务写路径、拒绝角色与完整业务规则待验收

## 1. 首版范围

1. 建立售后自有的顾客、购买登记、购买明细、产品实例、隐私同意、统一工单、工单产品及状态历史数据基础，不复用代理商主档 `md_customer` 存消费者。
2. 顾客登记由已授权门店发起，保存购买日期、来源类型、购买人手机号、使用者姓名和地址、VIP、备注及商品数量；门店和商品均从现有权威主档读取并在服务端复核可见性与启用状态。
3. 每个购买商品数量展开为可追踪产品实例，由服务端生成登记号、实例号和安装码；同一安装码唯一。
4. 安装预约从同一购买登记的可预约实例中选择部分商品，保存联系人、结构化地址、期望日期和时间说明，并原子创建 `INSTALLATION / PENDING_ACCEPTANCE` 统一工单及首条状态历史。
5. 顾客档案／门店顾客提供权限范围内的查询、分页和详情；预约管理／门店预约查询同一预约工单；工单管理读取同一统一工单，不复制客服工单表；安装码查询读取同一产品实例。
6. 列表均复用 `FaPageMain → EsSearch → EsTable` 和 `useListPage`；有页面级动作时再使用 `EsListToolbar`，不渲染空工具栏。表格撑满剩余空间，空数据与加载状态分页位置稳定。

## 2. 主动不实现

- 顾客扫码填写、短信验证码、签名图片、隐私撤回与注销仍依赖消费者身份、短信和对象存储，本轮只接受已授权店员确认当前政策版本，不伪造验证码或签名。
- “稍后确认”、确认后再次编辑商品、整单／部分退货、VIP 自动判定、导入导出规则尚未确认，不开放对应写操作。
- 预约只创建待受理工单；服务站自动匹配、人工覆盖、派师傅、改派、缺件、完工、审核、回访、投诉和结算由后续工单切片实现。
- 不把目标状态草案直接做成任意状态编辑；所有后续迁移继续使用专用动作接口并写不可变历史。
- 不向共享测试库写业务样例；本轮仅执行已授权的结构迁移、权限 DML 和本地运行配置。

## 3. 隐私与数据安全

- 手机号、姓名和详细地址不以明文写库。应用使用至少 32 字节的 `gaia.after-sales.pii-key` 进行 AES-GCM 加密，并以派生 HMAC-SHA256 摘要支持手机号／姓名精确检索；数据库另存脱敏展示值。
- 列表和当前首版详情仅返回脱敏值；页面、日志、异常、审计和版本 token 不包含明文或密文。
- 未配置密钥时读空列表仍可用，但登记和预约写入必须明确失败，不回退到明文；已配置但长度不足时宿主拒绝启动。
- 省／市／区县／街道作为路由和统计维度明文保存；详细地址单独加密。

## 4. 数据和并发

- 新表：`afs_customer`、`afs_purchase_registration`、`afs_purchase_item`、`afs_product_instance`、`afs_consent_record`、`afs_service_order`、`afs_service_order_item`、`afs_order_status_history`。
- 所有表使用不可变主键、租户路由数据源、创建／更新时间和创建／更新人；主聚合使用 revision 与版本 token。
- `client_request_id` 在购买登记和预约工单内唯一，用于避免前端重试重复创建；同一产品实例存在活动工单时拒绝再次预约。
- 购买和预约在单事务内保存头、明细、实例／工单关系和历史；任一步失败全部回滚。

## 5. API 与权限

- `GET /api/afterSales/customer/permissions`：按 `customerArchive`、`appointment`、`dealerRegistration`、`dealerCustomer`、`dealerAppointment`、`workOrders`、`installationCodes` 返回读／创建能力。
- `GET /api/afterSales/customer/options`：返回当前用户可登记门店与可选商品，接口自身要求登记权限，不依赖主档管理菜单。
- `POST /api/afterSales/customer/registrations`：创建购买登记。
- `GET /api/afterSales/customer/records`、`GET /records/{id}`：顾客／购买档案分页与详情。
- `POST /api/afterSales/customer/appointments`：对部分实例创建安装预约工单。
- `GET /api/afterSales/customer/appointments`、`GET /appointments/{id}`：预约分页与详情。
- `GET /api/afterSales/customer/work-orders`、`GET /work-orders/{id}`：统一工单分页与详情。
- `GET /api/afterSales/customer/installation-codes`：安装码分页。
- 门店和代理商范围通过 `sys_user_data_permission → sys_organization_relation → md_customer → md_store` 强制过滤；前端选择的门店 ID 不作为授权依据。

## 6. 首版验收

1. 有权限用户可在授权门店登记多个商品，生成一个购买登记和对应数量的实例／安装码；重复请求 ID 不产生第二单。
2. 可只选择部分实例预约；已存在活动工单的实例再次预约返回冲突，未预约实例保持可预约。
3. 顾客、预约、工单和安装码查询只返回当前授权门店范围；跨租户和未授权门店返回空或 404。
4. 明文手机号、姓名和详细地址不出现在业务表、审计、版本 token、API 列表或异常信息中；精确手机号检索可命中脱敏记录。
5. 前端完成筛选／重置／分页／空状态／失败重试／重复提交保护；顾客详情能查看商品实例和预约状态。
6. 后端集成测试、前端测试、类型检查、生产构建及管理员允许侧桌面浏览器关键页面通过；拒绝角色和业务数据验收单独记录。

## 7. 研发验证记录

- 后端 `mvn -pl gaia-after-sales-api -am test` 通过，共 37 项；本切片 3 项真实 HTTP／事务／MyBatis／H2 隔离集成测试覆盖 PII 加密和脱敏、精确检索、登记与预约幂等、活动工单重复拦截、状态历史、安装码购买投影、权限、组织数据范围和租户隔离。
- 前端 `vue-tsc -b` 通过，Node 测试 82 项全部通过；新增测试覆盖查询参数、提交白名单和 7 个真实路由状态。
- Vite production build 通过，顾客档案、预约管理、顾客登记、门店顾客、门店预约、工单管理和安装码管理均生成独立懒加载产物。
- 2026-09-10 已在共享测试库 `gaia_wh_init_wzy` 执行 `20260910_customer_appointment.sql` 和 `20260910_customer_permissions.sql`：8 / 8 张业务表、7 / 7 个菜单 API 清单、2 / 2 项写操作和管理员角色 2 / 2 项操作授权核对通过；对应回退脚本保留在仓库。
- 本地宿主已配置不入库的 `gaia.after-sales.pii-key` 并重新装配启动，后端 `8080`、主系统 `7004`、售后子系统 `9010` 均就绪；未在文档、日志或提交中记录密钥值。
- 真实管理员登录态下，顾客档案、预约管理、门店顾客、门店预约、工单管理和安装码管理均从“暂无访问权限”恢复为真实空列表；顾客登记字段与“确认登记”按钮可用。未写入共享业务样例，拒绝角色、跨租户和真实登记／预约写路径仍待验收，因此不标记最终完成。

## 8. 2026-09-16 业务查询闭环补充

本轮在不扩张未确认业务规则的前提下，补齐总部“业务查询”三个入口之间的内部数据闭环：

1. 顾客购买详情返回当前权限范围内、与该购买登记关联的服务工单摘要；有工单菜单和读取权限时可定位到工单查询入口，无权时只展示业务事实。
2. 服务工单详情返回 `afs_order_status_history` 的不可变状态轨迹，并继续展示关联产品实例、配件和沟通记录；没有增加任意状态编辑能力。
3. 安装码新增 `GET /api/afterSales/customer/installation-codes/{id}`。详情返回脱敏顾客／地址、购买与核验状态、商品实例状态及关联工单；接口复用安装码独立权限，并在 SQL 层强制租户和门店／代理商数据范围。
4. 新增 `20260916_business_query_detail_permissions.sql` 及回退脚本，将安装码详情路径加入 `afsInstallationCode` API 清单。迁移未在共享环境执行，部署前仍须按租户执行并核对正式角色。
5. 同时修正预约建单对 V2 工单字段的完整赋值：安装预约写入同日起止日期和空故障字段，避免目标结构的非空约束失败；查询中将数值故障 ID 显式转换为字符串，保持跨数据库行为一致。
6. 顾客购买记录、服务工单、安装码管理三个列表恢复明确的“查看详情”按钮。通用表格仍默认隐藏详情动作，只有业务显式声明 `showInList` 才展示，因此不改变其他模块既有列表行为。
7. 三类详情统一采用当前商品档案的 1040px“身份摘要 + 分组概览 + 关联页签”结构：购买详情将登记商品与关联工单分栏，工单详情将商品、配件、状态轨迹与沟通记录分栏，安装码详情将关联工单独立成页签。页签内部不再使用主列表 `EsTable`，商品、工单、配件、轨迹和沟通记录全部改用商品档案同款 `EsEditableListPanel` 只读轻量列表；沟通记录通过通用组件新增的可选分页能力保留原分页和纠错操作。轻量列表空态采用次级文字色，安装码等普通值使用常规文字色与正常字重，商品名／工单号等身份信息继续保持强调。仅调整信息层级和展示样式，不改变接口、脱敏、权限与状态规则。

研发验证：前端相关 Node 测试 44／44、类型检查、i18n 检查和定向 ESLint 通过；详情样式统一及内部轻量列表替换后再次执行详情结构、安装码与表格动作定向测试 24／24、类型检查和相关文件 ESLint，结果均通过。补充目录详情测试时 5 项中 4 项通过，既有“分类父级名称”断言仍失败，与本次业务查询文件无关。全量 Node 测试 258／266 通过，剩余 8 项为商品目录旧断言、已删除配套品表单、既有对话框规范及测试环境 `@/utils` 别名解析，不涉及本次业务查询文件。后端新增闭环场景的 HTTP／事务／MyBatis 集成测试 1／1 通过，覆盖购买记录关联工单、工单状态轨迹、安装码详情、跨租户 404。整类 `CustomerIntegrationTest` 在当前本机连续重建多个 Spring／H2 上下文后仍出现 JVM `Abort trap: 6`，没有形成完整整类通过证据，因此不替代共享环境及正式角色验收。

## 9. 2026-09-16 购买登记查询与新增故障修复

1. 购买登记页只展示姓名、手机号和购买日期三个筛选项，`EsSearch` 因此只回传这三个字段；原查询构造器仍直接对未展示的 `keyword` 调用 `trim()`，点击筛选后发生前端运行时异常并清空列表。查询构造器现接受部分筛选模型，对缺失文本和状态统一补空值；真实页面验证首次加载 2 条、点击筛选后仍为 2 条且无错误提示。
2. 本机 Redis 重建后购买登记五个 `ID_KE_afs_*` 键为空或落后，而共享测试库五张表最大 ID 均为 2，新增先后撞到 `afs_customer.PRIMARY` 的 1、2。售后公共 `GaiaIdGenerator` 现通过独立 Mapper 读取安全 `afs_*` 表的数据库最大 ID，仅返回严格更大的 Gaia Redis ID；有限次数内无法追平时返回明确 503，不再把原始主键冲突暴露给页面。
3. 门店真实登录态使用专用合成数据成功创建 `REG-3`，顾客、登记、明细、产品实例和隐私授权五张表全部落库，五个 Redis 序列均推进至 3。验证后已按精确登记号和安装码清理该回归记录及关联数据，业务列表恢复原 2 条，序列保留安全高水位。
4. 定向验证通过：前端 customer 测试 5／5、相关 ESLint、应用与测试类型检查；后端 `GaiaIdGeneratorTest` 3／3，core 模块 16／16，宿主重新打包并启动成功。API 全量测试仍受当前并行开发分支中既有目录、服务站和工单断言影响，未作为本次通过依据。

## 10. 2026-09-20 已确认展示与登记调整

购买登记的隐私同意采集改为不在门店登记中展示、校验或写授权记录；其余必填规则保留。售后子系统内所有角色的列表、详情和现有导出按产品确认改为明文展示，PII 仍以加密列保存。全国街道数据、购买登记查看动作及两类工单列表列顺序同步调整。异常工单 `SO-940918110036` 的状态字典外值已核实并按确认精确删除。实现范围、验证和待验收边界见[2026-09-20 购买登记与工单展示调整](./2026-09-20-购买登记与工单展示调整.md)。
