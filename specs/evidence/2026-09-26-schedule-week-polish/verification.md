# 排班周视图：表头、人员列、配色与周导航验证

日期：2026-09-26。关联 A27／STA-003／OPT-20260926-02。当前实现承接上一版颜色卡片；截图来自真实页面组件的隔离夹具，人员和排班为合成数据，不代表业务验收。未提交、未部署。

## 实现

- 表头采用日期大字与星期／月份并列，今天使用实心日期块；人员列 196px，头像 40px，姓名／工号和服务站分层，角标为本页人数。
- 工作青绿、休息灰蓝、休假琥珀、停单浅玫瑰，保留文字、图标、发布状态及草稿虚线。浅色对比度最低 5.66:1、深色最低 7.57:1，见 `contrast.txt`。
- 上一周／本周／下一周通过现有 dateFrom/dateTo 查询完整自然周；保留已生效筛选，同步草稿日期，页码回到 1。自定义日期范围以开始日期所在周为基准；本周以客户端当地日期为准。权限、初始化、查询和写入忙碌时禁用导航，查询失败不阻止切周重试。

## 浏览器证据

入口：`http://127.0.0.1:9017/after-sales/tests/browser/schedule.html`，桌面 1280×720；对应截图 `light.png`、`dark.png`。

1. 本周查询 `2026-09-21…2026-09-27`，请求计数 1；下一周 `2026-09-28…2026-10-04`，计数 2，空周仍保留导航；本周恢复原范围与 41 条记录，计数 3。
2. 应用 WORK 筛选后计数 4；切下一周计数 5，实际查询保留 stationId=1、dayType=WORK、pageSize=100，page=1。
3. `scenario=paginated` 查询第二页时计数 2、page=2；切下一周计数 3、page=1，未重复请求。
4. 上一周实际范围 `2026-09-14…2026-09-20`，单次查询后可返回本周。
5. 默认七列下内部可视宽度／内容宽度均 1113px，无额外横向溢出；深浅主题卡片、日期与人员信息可读。沿用上一轮整卡详情、独立编辑、键盘焦点和权限验证，未更改写接口。

## 自动检查

- `yarn after-sales:pnpm check`：ESLint、Knip、Vue/测试 TypeScript、翻译检查通过（19 对模块、3063 个键）；全量测试 331 项，301 通过、30 失败。
- 将失败名称与首轮 `/tmp/toto-schedule-check.log` 比较：相同 30 项既有失败，无新增失败。涉及目录、安装码、透视导航、旧页面断言等；完整输出见 `check.log`。之前并行完工材料组件的类型错误与两项测试失败在本次检查已不再出现。
- 排班专项 `tests/service-resource.test.ts`：6/6 通过，含周日、跨月／年、闰日与当前周回退，以及不误判筛选／分页缺失为空排班。见 `targeted.log`。
- `git diff --check` 通过；文档影响检查已执行。Spec 第 4 节、A27 与已有 OPT 更新，总览计数及正式开放状态不变。

限制：隔离夹具不写业务数据；真实登录账号与共享环境仍需发布后验收。本轮未改动其他并行任务文件。
