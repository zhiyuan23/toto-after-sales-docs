# 快捷排班验证记录

日期：2026-09-26。范围：A27／STA-003。全部写入操作在隔离 H2 或浏览器内存夹具执行，未写真实业务排班。

## 自动化

- 后端：`ScheduleWeekIntegrationTest` 7 项通过，覆盖人员分页含未排班者／读取字段边界、发布失败回滚班次审计批次、版本冲突整批回滚、已发布状态保持、原因必填、工单冲突、锁定／重复／空范围／技能限制、跨站／停用／拒绝权限、最后一位在岗保护。
- 后端回归：`ServiceResourceRealtimeWorkOrderMapperTest` 2 项及 `CustomerIntegrationTest#personnelSmartAiAndManualCapacityOverrideLeaveTraces` 1 项通过，共 10 项通过。修正 H2 对无长度 `CAST AS CHAR` 截断 ID／日期的问题，采用明确宽度并验证长 ID。
- 前端新 `schedule-editor.test.ts` 6 项通过：复制跳过原因／历史正常班次可复制、不得覆盖、字段与长 ID、在岗计算、工单影响、跨月跨年日期。
- 完整 `pnpm --ignore-workspace check`：lint、依赖、业务／测试类型检查、国际化检查通过；340 项测试中 310 通过、30 失败。失败集合与已有基线一致（将临时工作区绝对路径归一为 `tests/...` 后比较）；未增加失败。既有失败涉及通用表单／导航／库存等检查及测试环境导入问题，未在本任务扩展修复。
- 生产 Vite 构建通过（最终 6.69 秒）；bundle-size 219 项通过，最大 JS 1310.7 KiB（末次纯样式修订另执行 ESLint、构建与体积检查）。

## 浏览器操作

隔离入口使用 `tests/browser/schedule.html`；真实 API 写请求由隔离服务器阻断，新增周表保存只更新夹具内存。

- 18 人夹具按人员分页为 15＋3，两页每人均保留完整七天；加载失败后重试恢复列表。
- 首次仅一次周表查询，显示 6 位人员，包含完整未排班人员。
- 单格新建工作班次 → 预览 → 保存草稿成功。
- 下一周仅新增一次查询；沿用上周生成 25 格，17 格显示例外／缺失等跳过说明。
- 默认周规则为 6 人补齐 42 格；撤销一次恢复 0 处待保存。
- 勾选一位人员选中七天，统一设置工作，发布预览精确显示 7 格，确定后为已发布。
- 原工作班次回显工作及三个原服务项目；更改休息并填写原因后，预览显示冲突 `WO-81`，确定禁用。
- 保存失败保留预览与 1 处待保存，再次确定保存成功，显示草稿。
- 有修改时切换下一周出现未保存提示，“继续编辑”保留原周及修改。
- 只读场景无编辑、复制、规则与发布按钮；有编辑但无发布权限时只出现保存草稿，无发布入口。

新增操作英文切换已核对，测试结束恢复中文；既有页面筛选字段等存量中文尚未在本次全量重构。

该周表编辑方案后来撤回，过程截图已清理；验证结果保留为历史文字记录。

## 本机运行与限制

- `node scripts/local-backend.mjs` 已重新装配本机宿主，旧工作区进程退出后，新实例 `http://127.0.0.1:8080/api` 健康就绪；主站 7004／售后子站 9010 保持运行。
- 本机真实登录页面已读取完整 6 人／7 天班表，包含未排班人员，已发布班次、服务项目和全站在岗人数正常显示；没有进行真实保存。修正头像换行、在岗汇总被人员列遮挡及桌面周表末列横向裁切。
- 没有迁移、菜单配置、真实排班写入、Git 提交或共享环境发布。
- 正式普通岗位允许／拒绝验收、MySQL 并发、跨页全站业务流程与完整端到端验收仍待完成。上述结果属于研发验证。

## 同日：保存请求 502 运行态核对

用户报告 9010 代理的 `availability/batch` 返回 502，范围为服务站 5／人员 1／2026-09-21 至 09-27，发布七天工作班次。核对时前端 9010 正常监听、后端 8080 暂无监听；日志显示上一实例 14:38:06 开始优雅退出，14:38:08 退出完成，新实例 14:41:24 启动完成。此处为后端重启窗口导致代理不可达，未发现该请求的业务异常证据。

新实例恢复后，8080 正常监听，经 9010 读取周表返回 HTTP 200／业务 4001（未登录请求），确认代理链路恢复。另通过只读 JDBC 核对：人员 1 属于服务站 5 且启用，指定七天排班记录数为 0，没有已保存班次。本次没有重放发布请求、修改业务数据或再次重启后端；用户可在原预览界面重试，成功发布仍由业务校验决定。
