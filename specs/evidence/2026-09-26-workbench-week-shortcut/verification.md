# 工作台周排班快捷入口验证

> 提交证据整理：研发结论保留，原始日志和中间截图不入 Git；统一检查结果见[提交验证摘要](../2026-09-26-schedule-save-publish/test-summary.md)。历史过程记录中的“未提交”描述对应当时状态，本次已随代码收口提交；应用仍未部署。

> 后续规则：用户已确认取消日常草稿操作，当前快捷入口改为保存并发布，且同时校验生成与发布权限。以下保存草稿为本轮早期历史行为；现状与验证见[保存并发布](../2026-09-26-schedule-save-publish/verification.md)。

- 日期：2026-09-26
- 编号：OPT-WEB-WEEK-SHORTCUT-20260926
- 来源：用户要求在工作台服务人员区域增加一个常用排班入口。
- 决策：采用“安排周排班”，用于多人、多天的常规安排；这是业务操作特征上的选择，未声称有使用频次统计。
- 状态：源码与隔离研发验证完成，未提交、未发布；真实岗位业务验收待完成。

## 实现

服务人员标题旁增加带日历图标的轻量主色按钮，人数并入标题，不新增独立工具栏。直接复用现有周排班弹窗，默认本周，沿用本周／下周等日期快捷选项、人员选择、三项服务默认全选及仅补空缺策略。保存仅生成草稿，正式发布沿用排班管理入口。

入口要求排班菜单授权、availability.read 和 generate 权限（generate 未返回时兼容 create，显式 false 不回退）。未选择具体授权站点、站点加载中或站点读取失败时不可打开。弹窗按站点隔离，保存后刷新人员和排班信息；保留未保存离开确认与提交期间保护。未改变菜单注册、角色授权、接口或数据库。

## 验证

本机 9017 浏览器夹具加载真实工作台及周排班组件，业务读取与写入均由测试替身隔离。

| 场景 | 结果 |
| --- | --- |
| 正常入口与桌面布局 | 服务人员区域标题旁可见，不增加独立行；截图（过程材料已本地归档） |
| 打开周排班 | 原地打开，带当前站名，默认本周、人员未选、工作日三服务全选；截图（过程材料已本地归档） |
| 选择张师傅并安排下周 | 请求 stationId=12、personnelIds=[31]、2026-09-28 至 2026-10-04、SKIP_EXISTING，生成 1 次；读取计数从 1 到 2，成功消息可见并关闭弹窗 |
| 全部服务站 | 按钮禁用，不能生成跨站排班 |
| 切到备用服务站 | 弹窗显示备用服务站，提交 stationId=13；提交载荷（过程材料已本地归档） |
| 只读权限 | generate=false、create=true 时入口仍隐藏 |
| 缺少排班菜单 | 即使接口权限允许，入口仍隐藏 |
| 首次保存失败 | 显示错误并保留已选李师傅；再次提交成功，写入次数 2、排班读取次数 2 |

`yarn after-sales:pnpm check` 静态部分（lint、依赖、应用及测试类型、i18n）通过；344 项测试中 315 通过、29 失败。失败名称集合与前一轮三类服务调整检查完全相同，无新增失败，见 check.log（过程材料已本地归档）。源码／文档差异检查及文档影响检查通过。上述为研发隔离验证，不替代真实岗位授权和业务验收。


## 后续调整：标题同行的浅色“排班”按钮

按用户要求移除服务人员标题下方的站名，将快捷按钮缩为“排班”（英文 Schedule），去掉日历图标与主色描边，改用浅灰背景和柔和文字。按钮与服务人员标题／人数同一行对齐在右上角，悬停提示完整“安排周排班”。当前服务站仍由工作台选择继承，弹窗标题与站点摘要保留。

桌面截图确认无副标题、按钮位置和浅色样式，点击仍打开本站周排班弹窗，见 紧凑入口（过程材料已本地归档）。本项仅展示调整，不改权限、生成及刷新行为；未提交、未发布。

本轮完整检查静态部分通过，315 项测试通过／29 项既有失败，失败集合与前轮一致；检查输出（过程材料已本地归档）。源码及文档差异检查通过。


### 后续确认：恢复蓝色与日历图标

按用户最新要求，按钮恢复 Element Plus primary plain 蓝色描边与 CalendarDays 日历图标，继续使用“排班”短文案、标题同行及右上角位置，不恢复标题下方站名。此项取代上一轮浅灰按钮样式；桌面截图已核对：[蓝色紧凑入口](blue-compact-entry.png)。行为、权限和提交范围不变，未提交、未发布。

完整检查静态部分通过，全量 315 通过／29 项既有失败，失败集合不变，见检查输出（过程材料已本地归档）。文档影响及源码／文档差异检查通过。
