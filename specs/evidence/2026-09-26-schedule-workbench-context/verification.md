# 排班复用工作台服务站验证

> 提交证据整理：研发结论保留，原始日志和中间截图不入 Git；统一检查结果见[提交验证摘要](../2026-09-26-schedule-save-publish/test-summary.md)。历史过程记录中的“未提交”描述对应当时状态，本次已随代码收口提交；应用仍未部署。

- 日期：2026-09-26；A27／STA-003；按用户要求移除排班页顶部额外的“当前服务站”切换栏。
- 范围：售后 Web 工作台、排班页及共享选择状态；未修改后端、菜单权限或数据库。

## 当前行为

- 工作台与排班页复用 `station-context` 状态。工作台切站后，排班查询、日／周／批量操作及发布均继承同一具体服务站。浏览器当前标签页刷新可恢复；按账号、租户及登录会话隔离。
- 尚无选择时按授权选项初始化默认站点；显式选择“全部授权服务站”或当前站点不在排班可用站点中时，排班不查询、不允许写入，提示回工作台选择具体站点，不自动扩大范围或替换为另一站。
- 重置保留站点；切站清空人员条件、旧列表与操作弹窗，重查权限与人员，查询回第一页。初始化过期响应不回写，列表继续复用 `useListPage` 的请求隔离；异步新增预填／发布确认失效后不继续旧上下文操作。

## 验证

- 完整 `yarn after-sales:pnpm check` 已执行：lint、依赖、应用／测试类型、国际化检查通过；341 项测试中 312 通过、29 项失败。失败名称与前一轮人员密度验证完全一致，无新增失败，完整 check 仍返回非零。日志（过程材料已本地归档）
- 新增共享状态行为测试通过：跨页面共享、刷新恢复、“全部”不被默认站覆盖、无效站点不被静默替换、账号／租户／登录会话隔离、存储写入失败仍可内存切换。
- 浏览器隔离夹具 `schedule.html?scenario=multi-station&station=2`：首次仅一次列表请求，参数 stationId=2；页面无额外当前服务站切换栏。重置仍为 stationId=2 且只增加一次请求；日排班和周排班摘要为徐汇站，候选人员仅为该站人员。
- 模拟工作台从站点 1 切至 2：原人员筛选 personnelId=1 被清除，日期保留、page=1，列表请求只增加一次；新站空数据正常展示。选择全部或无效站点不增加业务列表请求，旧数据清除、四个写操作禁用并显示指引。
- 桌面有数据周历与空态已目视核对，沿用原彩色边条和分页布局。夹具的站点下拉仅供模拟工作台事件，不属于业务页面。
- `git diff --check` 通过。未重新构建；本次未变更依赖、资源、构建配置或应用入口。

隔离夹具没有写真实业务数据；正式账号跨工作台导航及共享环境验收未执行。本轮未提交、未推送、未部署，入口和业务验收计数不变。
