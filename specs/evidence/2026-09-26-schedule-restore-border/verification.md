# 排班功能恢复彩色边条版本

- 日期：2026-09-26；A27／STA-003。
- 用户指定：[原截图](../2026-09-26-schedule-border-style/dark.png)，并明确“排班功能整体都恢复……包括布局样式和操作”。
- 已恢复：截图版本的周表、列表布局和交互。后续快捷周表编辑、集中排班方案均已撤下；相关历史设计及证据保留并标记撤回。

## 恢复范围

- 顶部恢复新增日排班、新增周排班、批量调整、独立发布及日历／列表切换。
- 周表恢复人员头像／编码／服务站、七日日期和今天标识、周导航、类型图例、彩色左边条、草稿虚线、详情和铅笔编辑、底部按排班记录分页。
- 列表恢复日期优先列序、人员信息组合展示和原行高样式。
- 恢复原日排班表单、周规则生成草稿、范围批量调整、独立发布确认；使用原 availability／generate／batch／publish API。
- 删除本任务后续新增的周表编辑器、集中表单、专用模型、前端 API 调用和对应 29 项测试；旧功能测试继续保留。撤回源码另有本机临时备份。
- 未回退其他并发任务的服务项目调整，因此当前合成卡片显示“安装／维修／指导”；原参考截图中的“清洗”不是本次布局恢复的业务基线。
- 本轮未修改后端或数据；后端既有兼容接口扩展不被恢复页面调用，未为恢复 UI 反向改动派单等共享服务。

## 验证

- `pnpm --ignore-workspace check`：lint、依赖、应用／测试 TypeScript、文案检查通过；334 项测试，304 通过，30 项既有失败。与恢复前逐名称比较，无新增失败。数量减少对应退役的 29 项新编辑器／集中表单模型测试。
- production build 通过；217 个 JS/CSS 资源体积检查通过，最大约 1310.4 KiB。
- `git diff --check` 通过。
- 浏览器隔离入口确认四个主按钮、日排班弹窗、周规则弹窗、批量调整弹窗、发布确认及取消、卡片铅笔编辑与列表切换均恢复。
- 上一周／本周各一次列表查询；日历和列表切换复用当前结果，不额外重复查询。
- 保存和发布只验证到确认入口，未提交真实排班。隔离服务器继续阻断实际业务写请求。
- [当前深色截图](dark-current.png)：当前预览窗口约 744px 宽，七日网格按原最小宽度横向滚动；与原 1280px 截图的可见列数不同，未改写响应式规则。

已按用户授权提交；未推送、未部署，本次提交未重启服务。正式业务账号及共享环境验收仍待执行；本轮不将隔离入口验证写为真实环境验收。

## 本次提交范围核对（2026-09-26）

- 按用户明确授权，仅提交排班相关前端、后端兼容扩展及文档；完工审核、小程序和服务项目调整继续留在工作区。
- 混合文件逐项暂存，保留原 `CLEANING` 业务基线；隔离夹具提交版本使用安装／维修／清洗，本机其他任务的指导调整不纳入本次提交。
- 从 HEAD 和暂存内容导出独立快照验证：后端 7 项排班集成、2 项实时工单 Mapper 及 1 项人员分配回归全部通过。Mockito 附加受沙箱限制时，在沙箱外完成同一隔离 H2 测试。
- 历史日志仅去除行尾空白，不改动结果；撤回的方案与截图作为历史证据保留。

- 前端提交快照完整检查：lint、依赖、应用与测试类型、国际化检查通过；328 项测试中 298 通过、30 项既有失败，与恢复验证失败名称集合一致，无新增失败。比工作区少 6 项来自未纳入本次提交的完工材料等测试。
- 提交快照生产构建通过，217 个 JS/CSS 资源体积检查通过，最大 1310.3 KiB。
- 代码提交：前端 `f46ddddc`；后端 `389106d`。文档影响检查、暂存格式和本地文档引用核对通过。
