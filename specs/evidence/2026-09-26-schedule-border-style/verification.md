# 排班页彩色边条风格验证

日期：2026-09-26。关联 A27／STA-003／OPT-20260926-02。用户确认参照工作台服务人员卡片，统一当前排班页及详情的视觉风格。未提交、未部署。

## 变更

- 工作／休息／休假／停单复用 Element Plus success／info／warning／danger。左侧 3px 实线彩色边，其余三边 1px 浅色边；背景为 4% 类型色混合页面底色。
- 类型名称／图标使用较深类型色；服务项目、发布状态及编辑入口使用中性文字。草稿保留虚线三边、实线左边和草稿文字，过期／作废为中性灰，空格含义未变。
- 图例同步为微型边条卡片，表头以中性底色为主，今天改为蓝色边框与左强调线；人员头像底色减轻。
- 新版详情身份摘要复用相同颜色和边条，固定头尾及分组阅读方式不变。无共享组件、接口、权限或业务流程变更。

## 浏览器验证

隔离入口 `http://127.0.0.1:9017/after-sales/tests/browser/schedule.html`，合成人员和排班，1280×720。

- 保留作为后续恢复基线的[深色截图](dark.png)：四种状态、草稿和未排班均能区分；DOM 计算确认左边 3px，其他边 1px，草稿左实线／顶部虚线。
- 七日网格可视宽与内容宽均 1113px，没有新增水平溢出。未改变人员／七日结构、周导航、按钮与分页布局。
- 班次详情打开成功，身份摘要左侧 3px 边条，底色与班次卡片一致；1040px 弹窗内容可视／滚动高度均 480px，常规字段无需滚动。
- 按浏览器实际颜色计算，标题／服务项目／发布状态最低对比度浅色 5.34:1、深色 9.70:1，详见 `contrast.json`。

## 自动检查

`yarn after-sales:pnpm check` 的 ESLint、Knip、应用及测试 TypeScript、翻译检查通过；334 项测试中 304 通过、30 项既有失败。失败名称与前一轮详情检查一致，无新增失败，完整日志 `check.log`。样式变更未新增复述实现的测试。

`git diff --check` 与文档影响检查通过。Spec 第 4 节、A27 与已有 OPT 更新，正式验收及总览计数不变。当前证据为隔离研发验证，未代表共享环境或业务账号验收。
