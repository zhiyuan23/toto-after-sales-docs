# 三类服务顺序与默认勾选验证

> 提交证据整理：研发结论保留，原始日志和中间截图不入 Git；统一检查结果见[提交验证摘要](../2026-09-26-schedule-save-publish/test-summary.md)。历史过程记录中的“未提交”描述对应当时状态，本次已随代码收口提交；应用仍未部署。

- 日期：2026-09-26
- 关联：REQ-SERVICE-TYPES-20260926；Web 工作台、服务人员、服务站、排班、派单候选、评价标签。
- 用户确认：三类平级，顺序为安装、维修、指导；仅新建默认三项全选，编辑保留旧记录实际选择。
- 范围：前端源码与隔离研发验证，未提交、未发布；未修改后端、数据库或历史记录，业务验收未完成。

## 行为

- 新建服务人员、服务站、日排班、周排班工作日、批量调整和评价标签默认三项全选。周末休息规则仍为空；单张工单保持单选。
- 工作台人员技能、人员列表／详情、服务站卡片／详情、排班服务文本、派单候选和评价标签子集按安装、维修、指导排序；不补齐缺少的技能，不修改接口原数组，未知历史值保留在已知类型之后。
- 已有记录编辑与保存继续使用原始服务范围，用户主动取消的选项不会自动补回。

## 自动检查

执行 `yarn after-sales:pnpm check`：ESLint、依赖检查、应用及测试类型检查、i18n 检查通过；344 项测试，315 通过、29 失败。新增 3 项行为测试均通过，覆盖排序不改变集合／源数据、各新建默认值与独立数组、旧记录编辑和取消勾选的提交值。

失败名称集合与前一轮 `/tmp/toto-date-shortcuts-solid-check.log` 完全一致，无新增失败；保留既有失败不作本次无关修复。完整输出见 check.log（过程材料已本地归档）。源码及文档 `git diff --check` 通过。

## 桌面浏览器隔离验证

使用本机 Vite 的真实组件夹具，无业务 API 写入：

1. 新建日排班显示安装、维修、指导三项均选中；选中测试人员并取消指导，保存请求仅为 `INSTALLATION,REPAIR`。
2. 周排班周一至周五三项全选，周六／日休息且不安排服务。
3. 批量调整三项全选、相同层级和样式，见 批量默认值（过程材料已本地归档）。
4. 工作台夹具输入乱序 `GUIDANCE,REPAIR,INSTALLATION`，张师傅实际显示安装、维修、指导；李师傅仍只显示安装，王师傅仍只显示指导。可访问名称同步顺序，见 工作台顺序（过程材料已本地归档）。

服务人员、服务站旧记录回填与提交以纯函数行为测试覆盖；评价标签默认值由源码及类型检查验证，未执行真实账号端到端保存。本轮不新增菜单或验收计数。
