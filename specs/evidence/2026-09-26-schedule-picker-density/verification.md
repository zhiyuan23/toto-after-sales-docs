# 排班人员密度与统一选择验证

> 提交证据整理：研发结论保留，原始日志和中间截图不入 Git；统一检查结果见[提交验证摘要](../2026-09-26-schedule-save-publish/test-summary.md)。历史过程记录中的“未提交”描述对应当时状态，本次已随代码收口提交；应用仍未部署。

- 日期：2026-09-26；A27／STA-003。
- 承接[站点外置与头像选择](../2026-09-26-schedule-scope-selection/verification.md)，本轮仅调整操作弹窗，不修改列表／日历样式、权限、API 或后台规则。

## 实现

- 日期快捷按钮从文案自适应宽度改为五列等宽，统一 32px 高度；中英文沿用同样布局。
- 人员卡片由固定三列改为按宽度排列；960px 桌面弹窗为五列，区域最高 260px，可完整展示四行，多于四行内部滚动。卡片保留头像、姓名、工号、勾选状态，长字段省略并可悬停查看完整姓名／工号。
- `SchedulePersonnelPicker` 为排班功能内共用组件。周排班／批量调整继续多选；日排班新增及表格／日历编辑改为同样卡片的单选模式，选中其他人替换原选择，不提供全选。已选名单、搜索和逐人移除一致。
- 日排班清空人员后禁用保存，提交仍是单个 personnelId；编辑保留 version。沿用原选项加载、失败重试、过期响应保护及保存失败保留机制。

## 验证

- 完整 check：lint、依赖、应用／测试类型及中英文文案检查通过；340 项测试中 311 通过、29 项既有失败。与上一轮失败名称集合对比无新增，check 仍返回非零。日志（过程材料已本地归档）
- 排班操作、服务资源与页面提示定向测试 15／15 通过。日志（过程材料已本地归档）
- 单独生产编译及体积检查通过：217 个 JS/CSS 产物，最大 1310.4 KiB；不等同于完整 build 门禁通过。日志（过程材料已本地归档）
- 浏览器隔离夹具，桌面 1440 × 1000：
  - 周排班 18 人场景为五列四行，网格内容高度 250px，无内部溢出；五个快捷按钮外框约 109.4 × 32px，差异仅子像素舍入。英文按钮无文本溢出，深浅主题均检查。
  - 多选人员 1、16 后搜索 FW-018 并选中结果，已选人数为 3；清除搜索后恢复 18 人候选，原选择保留。
  - 日排班依次选择人员 1、2，仅人员 2 被选中；保存的内存载荷为 personnelId=2，首次写入后列表请求从 1 增至 2。
  - 列表首行编辑回填人员 1；清空后保存禁用。搜索 FW-004，键盘空格选择人员 4，保存载荷含 personnelId=4、version=1；第二次写入后列表请求从 2 增至 3。
  - 批量调整选择人员 1、2，提交明确 personnelIds=["1","2"]，范围、服务项目及原因正确保留。
  - 无人员场景显示空态且保存禁用；默认隔离夹具阻断保存后，错误提示可见，人员 6 和备注仍保留。
- `git diff --check` 通过。

## 截图

- 周排班五列人员与等宽快捷按钮（过程材料已本地归档）
- 新增日排班单选人员（过程材料已本地归档）
- 表格编辑回填人员（过程材料已本地归档）
- 英文深色周排班（过程材料已本地归档）

人员头像为空时使用姓名首字，截图未使用伪造照片。隔离保存只记录内存载荷，不写真实业务数据，不代表真实后端和业务岗位验收完成。本轮未提交、未推送、未部署；入口与业务验收计数不变。


## 后续调整：移除选择后新增的已选行

用户明确仅去掉选中人员后才出现的“已选 X 人 + 姓名／头像 + 删除叉”整行。共用人员选择组件已移除该区域及其专用样式、图标、文案，日排班／编辑、周排班与批量调整同步生效。保留卡片选中高亮与勾选、搜索、全选及清空选择；已有弹窗顶部范围摘要保留，不新增“仅看已选”等入口。

浏览器隔离核对：日排班选中后 aria-pressed=true，已选名单区域不存在；周排班选择两人后卡片 aria-checked=true，再点其中一人变为 false，已选名单区域始终不存在。共用人员网格选择前后高度均为 123px。未执行真实业务写入。

完整检查静态部分通过，341 项测试中 312 通过、29 项既有失败，失败名称与工作台上下文调整后完全一致；日志（过程材料已本地归档）。未提交、未部署，正式业务验收和入口计数不变。


## 后续调整：工具栏紧凑已选摘要

用户确认在既有搜索工具栏显示简要选择状态：未选择时为中性“未选择”，选中后为主题色“已选 X 人”；与全选、清空同一行，不恢复此前移除的名单行。悬停或键盘聚焦时使用 Element Plus 提示浮层列出姓名；名单从完整人员集合计算，搜索不会隐藏已选摘要。浮层限制宽高，长名单可滚动，保留单／多选和提交逻辑。

隔离浏览器验证：未选择、选择两人、搜索 FW-006 和清空四种状态下工具栏高度均为 34px；选中两人后搜索仍显示“已选 2 人”，键盘聚焦提示完整“陈文斌、王建华”，清空恢复“未选择”且清空按钮禁用。已目视核对桌面布局。静态检查通过，341 项测试中 312 通过／29 项既有失败，失败集合不变；日志（过程材料已本地归档）。未执行真实写入，未提交、未部署。


## 后续调整：日期快捷范围分段样式与匹配反馈

用户确认按界面评估建议实施。五个日期快捷项收拢在浅中性底色容器内，去掉独立描边，间距 3px；按钮仍为五等分、32px 高度。当前完整匹配日期的选项使用浅蓝底、主题色文字和加粗，保留独立键盘焦点；以 `aria-pressed` 表达选中状态。“下一周”统一简化为“下周”，同一文案键的周历导航也同步简化。周排班和批量调整共用该组件，不改变日期计算规则、接口及提交范围。

选中态由当前起止日期计算，不保存最后点击项；初始回填本周即高亮本周，点击下月后移开焦点仍高亮下月。浏览器将结束日期从 2026-10-31 手动改为 2026-10-30 后，五项 pressed 均为 false；再手动改回 31 日，自动恢复下月高亮。键盘空格激活“下周”正确生效。

桌面浅色中文周排班与深色英文批量调整已核对，五项宽约 113.2px、高 32px（外层含上下 2px 内边距共 36px），英文无文本溢出；未来两周仅该项 pressed=true。截图：浅色中文（过程材料已本地归档）、深色英文（过程材料已本地归档）。

完整检查静态部分通过，341 项测试中 312 通过／29 项既有失败，与前一轮失败集合一致，无新增失败；日志（过程材料已本地归档）。`git diff --check` 通过。未执行真实业务写入，未提交、未推送、未部署，业务验收与入口计数不变。


### 最终选中态：实心蓝底、白字与勾选

用户认为浅蓝匹配态不够明显，确认改为实心蓝色选中块、白色加粗文字及 Lucide 勾选图标。背景由现有主题蓝适度加深，以保证白字可读；只有选中项显示勾选，键盘聚焦选中项使用白色内轮廓。其余分段组、五等宽和 32px 按钮高度保持。

浏览器验证：选择“下月”并移开焦点后，仅下月 pressed=true 且显示勾选；手动将结束日期改为 2026-10-30 后，全部 pressed=false、图标数为零。深色英文批量调整使用键盘空格选择 Next 14 days，文字加图标无溢出。截图：[中文实心选中态](date-shortcuts-solid.png)、[深色英文](date-shortcuts-solid-dark-en.png)。

完整检查静态部分通过，312 项测试通过／29 项既有失败，失败集合未变；日志（过程材料已本地归档）。未提交、未推送、未部署，真实业务验收状态不变。
