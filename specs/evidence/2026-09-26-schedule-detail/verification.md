# 新版排班详情验证

日期：2026-09-26；关联 A27／STA-003／OPT-20260926-02。基于真实排班组件的隔离夹具，不访问业务写接口。未提交、未部署。

## 变更

- 功能目录新增 `ScheduleDetailDialog.vue`，组合现有 `EsDetailDialog + EsDetailSection`；页面只调用详情 `open(id)`。
- 1040px 桌面弹窗、最大 88vh、固定标题／关闭区、正文内部滚动；人员身份摘要、状态标记、排班信息、变更与备注分组。保留原姓名、编码、服务站、日期、类型、服务项目、发布状态、原因、备注。
- 继续复用 `useDetailDialog` 请求、错误和重试；增加关闭／路由离开／卸载时旧请求失效，与新版目录详情一致。无通用组件、接口、权限或写操作变更。

## 浏览器检查

使用 `test:i18n:browser` 的 `schedule.html`，桌面 1280×720。

1. 工作班次打开后姓名、日期、类型、服务项目和已发布状态正确；旧 `.es-detail-dialog__facts` 网格数量为 0。
2. 默认详情外框宽 1040px，内容区可视高度与滚动高度均 442px，短内容无需滚动。
3. 关闭后列表请求计数仍为 1，筛选／周次保留；打开另一条休息班次后日期为 2026-09-24、类型为休息、服务项目为空占位，没有沿用上一条工作项目。
4. `scenario=detail-error` 首次加载显示“排班详情加载失败”和重试；点击重试恢复对应详情，未通过列表行伪装读取成功。
5. `scenario=detail-long&theme=dark` 原因和多行备注通栏；正文滚动高度 1009px，可视高度 494px，overflow-y 为 auto；弹窗仍限于 88vh，关闭区位于可视窗口内。
6. `scenario=detail-slow` 模拟 1.5 秒加载，可在加载期间关闭；迟到响应后弹窗数量仍为 0；重新打开另一条记录正确展示新日期与休息状态。

## 检查结果

- 最终 `yarn after-sales:pnpm check`：ESLint、Knip、应用及测试 TypeScript、i18n 通过（19 对模块／3076 键）；全量 334 项，304 通过、30 失败。失败名称与上一轮周视图检查相同，无新增失败。见 `check.log`。
- 排班专项 6/6 通过，见 `targeted.log`。未为纯布局编写复制实现的测试。
- 首轮完整检查碰到并行新增的完工材料历史组件翻译键错误；最终检查中该问题已消失，本任务未改动该组件。
- `git diff --check` 与文档影响检查通过；Spec、A27 和已有 OPT 同步，总览及正式验收计数不变。

限制：当前证据为隔离研发验证，正式业务账号与部署环境未验收。
