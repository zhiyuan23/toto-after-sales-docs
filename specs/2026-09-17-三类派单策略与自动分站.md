# 三类派单策略与自动分站首版

- 编号：FS-20260917-01
- 应用载体：Web 管理后台
- 角色视角：客服作业（策略配置为具备配置权限的管理角色）
- 模块与入口：A17 服务受理、A18 服务工单、A19 派单调度
- 状态：开发中
- 最后更新：2026-09-17

## 1. 已确认范围

首期在全局范围维护一份“派单策略”，创建服务工单时按当前策略处理。自动派单仅把工单关联到**服务站**，不自动指派服务人员；原人工派单与改派能力继续保留。

工单新增及编辑补充地图选点产生的经纬度，沿用现有高德地图选点坐标体系。服务站无有效坐标时不进入自动派单候选；工单无有效坐标时自动派单失败并转人工处理。

满意度仅统计同时满足以下条件的工单：已完成、已有回访结果、评价值为“满意”或“不满意”。

## 2. 策略与处理规则

| 策略 | 创建工单后的处理 | 后续动作 |
| --- | --- | --- |
| 手动派单 | 保持待派单，记录 `NOT_APPLICABLE` | 客服从现有候选服务站中人工选择并关联 |
| 智能派单 | 创建事务内写入待处理任务；后台调度器异步计算并分配服务站 | 成功进入待服务站派单；失败保持待派单并允许人工派单 |
| AI 派单 | 创建事务内写入待处理任务；后台调度器将策略文本和脱敏候选指标交给 DeepSeek | 成功进入待服务站派单；失败保持待派单并允许人工派单 |

自动处理只在“创建工单”后触发；编辑已有工单、策略变更和人工改派均不会重新触发。

### 2.1 智能派单候选与评分

候选服务站必须同时满足：启用、服务类型匹配、服务区域匹配、维护有效坐标、期望日期未满载，且距工单坐标不超过配置的最大距离。首期默认最大距离为 50 公里，可在 1 至 500 公里之间修改。

饱和度沿用派单候选口径：

```text
饱和度 = 当日期望日期、状态为待服务站派单或待完工的本站工单数 / 服务站日容量
```

服务站满意度为该站符合本 Spec 开头筛选条件的满意工单数除以有效回访工单总数。服务站没有有效回访数据时，以全体服务站的有效回访满意度均值补齐，避免新服务站被不合理地固定排末；全局也没有有效回访数据时，回退为 100%。

默认权重为距离 40%、饱和度 35%、满意度 25%，管理员可调整但三项合计必须为 100%。评分公式：

```text
距离分 = max(0, 100 × (最大距离 - 实际距离) / 最大距离)
饱和度分 = max(0, 100 - 饱和度百分比)
满意度分 = 满意度 × 100
综合分 = 距离分 × 距离权重 + 饱和度分 × 饱和度权重 + 满意度分 × 满意度权重
```

综合分相同时，依次选择距离更近、服务站编码更小的候选，确保结果可复现。

### 2.2 AI 派单边界

管理员可配置优先级和策略说明。服务端仅发送候选服务站编码、名称、距离、饱和度、满意度和智能评分；不发送顾客姓名、手机号、详细地址、工单描述或 API Key。提示要求模型严格以 JSON 返回 `selectedStationCode` 和不超过 200 字的理由。

服务端再次校验返回编码必须位于当次候选集内，才执行分站；模型超时、非 2xx 响应、非 JSON、空编码或候选集外编码均视为失败。DeepSeek 参数优先读取宿主 `application.properties` 的 `gaia.after-sales.dispatch.ai.*`，再兼容 JVM 属性和环境变量；Key 不保存到数据库、前端、日志、迁移或 Git。宿主本地配置中的 Key 必须保持为未提交的私有环境改动。

## 3. 状态、任务与留痕

```text
创建工单（智能 / AI）
  └─ 待派单 + 自动任务待处理
       ├─ 成功：关联服务站 → 待服务站派单
       └─ 失败：保持待派单 → 客服人工派单
```

后台每 5 秒获取待处理任务，每张任务通过原子更新获得一次处理权。成功时写入原有工单状态历史与服务站分配历史，并将自动派单状态标为成功。失败时不改变业务状态，记录自动派单方式、失败状态、失败原因、尝试次数和独立任务／决策留痕；工单列表展示结果，客服可据此人工处理。

任务在首期以当前应用数据源中的全局单策略执行。启用多租户的部署需在上线前补齐租户任务枚举或按租户投递，不能假定默认数据源会覆盖全部租户。

## 4. 数据、接口与权限

迁移位于 `gaia-after-sales/database/`：

- `20260917_dispatch_policy_auto_dispatch.sql`：工单坐标与自动派单字段、全局策略、自动任务、决策留痕；配套 rollback 脚本。
- `20260917_dispatch_policy_permissions.sql`：客服服务下“派单策略”菜单、查询与保存操作权限；配套 rollback 脚本。

新增接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/afterSales/dispatch-policy` | 获取全局策略 |
| PUT | `/api/afterSales/dispatch-policy` | 保存策略和权重 |

前端新增“派单策略”页，手动模式不显示额外参数；智能模式显示三个权重和最大距离；AI 模式增加策略文本。服务工单创建弹窗要求完成地图选点，列表增加自动派单方式、结果与失败原因展示。

## 5. 实施与验证记录

2026-09-17 已完成后端策略、队列、定时调度、智能评分、AI 适配器、留痕模型与集成测试覆盖；完成 Web 策略页、工单地图坐标和列表结果展示；本地测试库已执行两份新增迁移并核验策略默认值、六个工单字段和菜单／保存权限存在。

已使用 Java 21 通过后端定向集成测试 `CustomerIntegrationTest#smartAndAiDispatchLeaveSuccessAndFailureTraces`：智能分站成功、AI 分站成功、AI 调用失败仍保持待派单并留下原因均已断言；前端导航访问测试 20/20、国际化键检查和 TypeScript 类型检查均已通过。完整 `CustomerIntegrationTest` 仍有维修故障必填、旧接口方法、下载模板文件名、权限预期和历史外部同步事务等 6 项既有失败，与本 Spec 的定向用例无关，须在对应切片单独处理。

2026-09-17 已完成 `gaia-after-sales-api` 本地 Maven 安装和宿主 `gaia-saas-web` WAR 打包；新售后 JAR 已进入 IntelliJ Tomcat 所指的 exploded WAR。部署时发现宿主 AOP 生成 JDK 代理导致定时方法未在代理接口上暴露，进而阻断整套售后应用启动；已将定时入口抽取为接口并重新通过定向测试、安装和打包。Tomcat ROOT 于 09:15 成功部署，8080、派单策略接口、主前端 7004 和售后子系统 9010 均已监听。

AI Key 已配置到当前宿主本地 `application.properties`。此前两次最小合约请求均在 TLS 阶段被关闭；网络恢复后，不带 Key 的 `curl` 请求已取得 DeepSeek 401，说明 DNS、TCP、TLS 与 HTTPS 网关路径正常；携带本地私有配置 Key 的最小候选请求随后成功返回候选内服务站编码，已验证模型调用与 JSON 候选校验。真实登录 API 目前要求图形验证码，自动化浏览器通道不可用且不得绕过验证码，所以尚未创建真实智能／AI 工单；待在已登录浏览器会话中完成建单后，应复核自动分站、状态流转及留痕。上述研发验证不构成业务验收或目标环境验收。

## 6. 验收要点与遗留项

- 手动、智能、AI 三种策略保存后，对新建工单按本 Spec 的触发规则生效。
- 智能派单可通过可控距离、容量和回访数据验证排序、同分规则与 50 公里阈值。
- AI 返回候选内服务站时完成分站；故意配置不可用 Key、超时或候选外编码时保留待派单并有可见失败原因。
- 使用真实登录态验证策略页菜单、查询／保存接口允许和拒绝情况，以及人工兜底派单。
- 部署时由运维以安全的环境变量或密钥管理方式注入 Key，并完成真实 DeepSeek 联调；不得把测试密钥作为任何文件内容或命令记录的一部分。
