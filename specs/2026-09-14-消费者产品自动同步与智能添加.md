# 消费者产品自动同步与智能添加

- 状态：核心流程已确认；独立消费者身份、临时手工手机号绑定／换号、可信购买手机号自动同步、无码申报、结果详情、本人记录、本人已有安装码找回及已核验正式购买安装码绑定已有源码切片；测试库已完成 `MANUAL_TEMP` 约束迁移，同号旧测试来源兼容修复待发布和目标环境复测；其他类型扫码认领未实施
- 更新日期：2026-09-21
- 范围：消费者 C03～C06、C10、C11；关联账号、购买、产品实例和服务资格
- 关联方案：[消费者产品自动关联与智能认领方案](../02-技术设计/14-消费者产品自动关联与智能认领方案.md)

## 1. 目标行为

1. 消费者完成微信身份和手机号验证后，系统自动匹配已接入渠道的可信购买明细，并将对应产品展示在“我的产品”；不要求短信链接或逐笔确认。
2. 未自动匹配的产品使用统一“扫一扫添加”。前端不要求用户选择码类型，服务端识别产品码、溯源码、SN、安装码或购买凭证码。
3. 没有码、无法识别或普通商品码只能确认型号时，才允许手动选择产品并填写购买日期，保存为“用户申报、实物待核验”。
4. 三种入口共用一套产品实例和账号产品关系；产品已出现不代表免费安装、保修或延保资格已经通过。

## 2. 用户流程

```text
首次进入 → 授权手机号 → 自动同步 → 我的产品
                                      ↓ 未找到其他产品
                         扫一扫 → 识别并查重 → 添加／提示已有
                                      ↓ 不能定位具体实物
                         手选产品 + 购买日期 → 待核验产品
```

“我的产品”和“购买记录”必须显示来源：手机号同步、扫码添加或用户申报；用户申报同时显示待核验。

## 3. 服务端契约边界

接口名称可按后端规范调整，但职责必须分开：

| 能力 | 输入 | 输出／约束 |
| --- | --- | --- |
| 手机号授权 | 微信一次性授权凭证或验证码结果 | 服务端验证并绑定稳定 `account_id`；客户端明文手机号不能作为可信证明 |
| 购买同步 | 当前登录账号 | 同步结果、增加／已有／冲突数量；只匹配有效、未退货、来源可信的购买明细 |
| 编码解析 | 原始编码、发行方上下文、幂等键 | 码类型、可信级别、候选产品、允许动作；未知／失效／冲突不得返回绑定成功 |
| 确认扫码添加 | 解析结果令牌、幂等键 | 复用或创建的 `product_instance_id`、关系状态、资料来源 |
| 无码添加 | 商品 ID、购买日期、必要使用信息、幂等键 | 待核验产品实例及关系；不得产生已核验权益 |
| 我的产品 | 当前登录账号 | 统一产品关系列表、来源、实物识别和核验状态；服务权益单独返回 |

解析结果应使用短时、一次性服务端令牌承接，不能由客户端回传“这是 SN／已通过”并被直接信任。

## 4. 数据一致性

- 账号产品关系唯一：`account_id + product_instance_id`。
- 可信产品标识唯一：`issuer + identifier_type + identifier_value`。
- 外部购买明细唯一：`source + external_order_no + line_no/unit_no`。
- 所有写操作在事务内执行“查购买—查标识—查关系—能复用则补充—否则创建”，并保存幂等键。
- 普通商品码、型号和购买日期不是实物唯一键；用户可声明另一件同型号产品，但新实例必须保持待核验并关联疑似重复项。
- 合并重复实例时保留原 ID、关系、凭证、工单和审计，不物理删除历史。

## 5. 异常与权限

- 家庭共用手机号、购买人与使用者不同、虚拟号、换号、退货、他人已关联或数据冲突：不得静默改绑，可返回脱敏候选或人工核验入口。
- 手机号换绑不重写历史产品关系；需记录新旧验证信息、时间和操作来源。
- 消费者只能查询当前账号有权查看的产品和购买记录；脱敏在服务端完成。
- 订单同步失败不阻止扫码或无码添加；扫码失败不得自动降级为绑定成功。
- 短信、微信消息只用于提醒或找回，不属于绑定成功条件。

## 6. 验收场景

- 新账号授权手机号后自动出现两件已同步产品；重复同步数量不增加。
- 已同步产品再扫同一可信标识，只补充原实例并提示已存在。
- 扫可信 SN／逐件溯源码可定位具体实例；扫购买凭证码可关联对应购买；扫普通商品码只识别型号。
- 未知、失效、撤销或冲突码不建立关系，并提供手选或人工协助。
- 手选同型号可新增另一件，但显示“待核验”，且不自动获得免费权益。
- 申请服务时独立核验本次资格；待核验产品不可因已加入列表而绕过。
- 并发重试、页面返回和弱网重放不会重复创建关系或产品实例。
- 本人数据隔离、换号、归属冲突、退货撤销和实例合并均有审计记录。

## 7. 本次交付与未完成项

- 已更新文档原型：手机号授权自动同步、扫码优先、无码手选、码类型分流及可信标识防重。
- 已实现首个可运行切片：消费者手选一件商品并提交购买信息后，直接写入统一购买登记、产品实例和消费者关系；来源固定为 `USER_REPORTED`，核验为 `PENDING`，生命周期为 `ACTIVE`，资格为 `NOT_ASSESSED`，且不生成安装码。
- 后台 A15 在同一购买记录列表展示来源与核验状态。有独立“核验购买记录”权限的人员可人工凭证核验、标记冲突、驳回，或从同手机号、同商品、当前数据范围内的正式记录候选中完成关联。关联成功移动消费者关系，原申报登记和实例标为 `MERGED`，不复制正式实例。
- 服务受理和预约只接受 `VERIFIED + ACTIVE` 记录；用户申报进入列表不等于正式购买或服务资格通过。核验轨迹追加写入，不覆盖历史。
- 数据库升级和权限脚本、消费者侧后端 API、Web 核验界面及小程序无码申报／本人记录页面已有源码；隔离后端集成测试及 Web 前端检查通过。迁移未应用到共享数据库，真实微信身份/API 权限尚未验收。
- 手机号可信购买自动关系和临时免校验换号已形成源码切片，但增量迁移、目标环境与真实购买数据尚未验证；完整扫码解析、可信标识、正式订单接入及权益规则仍未实现。C03～C06、C10、C11 均不计完整入口实现或最终验收；源码和构建不能代替业务联调。

### 2026-09-20 C03 独立身份源码切片

- 新增独立消费者微信登录、手机号凭证核验、会话恢复／退出和本人上下文 API。消费者账号与会话不使用 Gaia 主系统登录；宿主仅按精确路径放行，由业务层校验会话、停用状态和租户上下文。OpenID 与会话令牌只保存摘要；手机号需要 PII 密钥后加密保存，唯一约束与服务校验共同拒绝重复归属。已有手机号的账号不能通过本接口直接换号，待二次验证契约确认。
- 消费者小程序的微信手机号授权按钮接入上述 API，恢复会话时向服务端核对账号，失效时清除本地登录态和产品数据。首页的手动添加入口先进入手机号授权；H5 无法调用微信手机号授权，明确提示在微信小程序完成；开发视觉样例仍与正式数据隔离。
- 源码验证：后端身份单元测试 4 项通过；`gaia-after-sales-api`、宿主可执行 Jar 编译通过；消费者端 `pnpm check`、微信与 H5 development 构建通过。尚未在微信开发者工具或真机以真实授权、真实迁移库和真实密钥联调，不计 C03 完成，也不计业务验收。
- 运行阻塞：身份表迁移未获目标库授权；微信服务端 AppID／密钥及 PII 密钥未配置。自动同步还需首期可信订单来源、归属冲突与退货口径；现有 `/customer/consumer/*` 使用后台 Gaia 身份，不能充当消费者本人 API。C05／C10／C11 需先分离消费者账号与历史后台用户的关系命名空间，避免数值 ID 碰撞。
- 同日共享宿主启动修复：`yarn dev:afs` 的 240 秒超时由 Spring 上下文中 `consumerAuthenticationService` 的 JDK 代理无法注入具体类引起。认证与购买服务现声明业务接口，Controller 和服务间按接口注入；代理装配回归测试 1 项通过。重新执行完整启动后，8080 统一后端与 9010 售后前端均就绪；消费者 `/context` 无效会话返回预期 `4001`。此证据只证明宿主装配和接口映射，真实微信授权与目标库迁移仍未验收。
- 随后收紧消费者租户边界：登录及每次会话读取都要求 `after-sales.mobile.single-tenant-id` 已配置，且请求租户上下文若存在必须与其一致；跨租户上下文在查询会话前拒绝。当前消费者账号表未持有租户列，因此按已确认的 TOTO 单租户运行口径封闭；若未来需同库多租户消费者账号，须另行设计账号、手机号与会话的租户键及迁移，不可仅开放其他租户 Header。单元测试覆盖跨租户登录与会话拒绝，目标运行配置仍待核验。

### 2026-09-20 C03 本地 H5 固定测试身份

- 微信获取手机号尚未具备真实联调条件时，`pnpm dev:consumer:h5 TOTO` 提供固定测试别名。页面依次调用正常的消费者登录和手机号绑定 API，服务端以固定 OpenID 和本机忽略配置中的固定测试手机号换取正常账号、会话及本人上下文；后续产品、购买和服务记录继续使用真实 API。客户端不能提交任意账号 ID、租户 ID、权限或明文手机号。
- 后端适配器默认关闭，只在 `develop` profile、显式开关和本机请求同时满足时处理固定别名；普通微信凭证仍交原微信网关。端侧测试入口只由专用 H5 开发命令启用，独立保存该模式的会话；普通 H5、微信及生产构建不可使用此入口。固定身份仅供研发自测，不代表真实微信登录、购买核验或服务资格。
- 隔离验证：固定别名、本机限制、缺失手机号拒绝及正常会话／绑定组合测试通过；原消费者身份、本人记录及宿主装配受影响测试通过；消费者端检查与微信、普通 H5、专用 H5 development 编译通过。`pnpm dev:consumer:h5 toto` 已复用本机后端并在浏览器显示固定身份按钮，无 console error。目标库迁移、租户 ID `1001` 与目标库匹配情况、固定测试手机号配置、真实账号和浏览器完整登录链尚未验证，C03 完整入口及最终业务验收状态不变。

### 2026-09-20 C03 微信开发构建临时手机号关联

- 本次仅调整开发构建：消费者每次点击微信登录后重新调用 `uni.login`，服务端换取真实 OpenID、创建或恢复独立消费者账号和会话；会话切换／失效立即清空旧账号页面缓存，旧请求不得回填。正式构建继续使用微信手机号授权。`uni.login` 不证明手机号，页面和服务端上下文明确区分“测试关联”与“微信已验证”，不能把临时号码用于购买核验或服务资格判定。
- 本机忽略配置保存用户提供的测试号码，默认关闭。仅 `develop` profile、显式开关和精确消费者账号 ID 白名单同时满足时，登录事务将该号码加密关联到该账号；客户端不传手机号或账号 ID。同号重复登录不重复写入，跨账号占用拒绝；正式微信验号同号可升级为已验证。此前 H5 固定身份的手机号也标记为 `LOCAL_TEST`。开发测试使用该关联号码访问本人登记 API，服务端仍按消费者账号限域。
- 启用顺序：先完成目标库身份及业务迁移并核对单租户 `1001` 的实际归属；用微信开发者工具首次 `uni.login` 后读取登录响应的 `accountId`，将其写入本机 `after-sales.consumer.dev-wechat-phone.account-id`，核对后启用 `after-sales.consumer.dev-wechat-phone.enabled` 并重启本机后端，再次登录核对 `/context` 中 `phoneBound=true`、`phoneVerified=false` 和脱敏号码。未拿到真实账号 ID 前不得启用开关或将号码绑定到猜测的 ID。
- 源码切片验证与业务验收分开：后端指定账号、非 `develop`、重复登录、占用冲突、H5 测试来源、真实微信验号升级及 H2 身份表／MyBatis 映射等受影响测试共 15 项通过；消费者端 `pnpm verify` 含检查、微信／H5 development 构建通过，三仓 `git diff --check` 通过。本机 `pnpm dev TOTO` 在当前执行沙箱以 `spawn EPERM` 中止；提权请求被自动审批系统以凭证刷新失效拒绝，因此未复核开发工具实时运行。共享测试库仍缺身份表、业务归属列及 `1001` 映射，且未获得结构迁移授权；尚无真实微信会话、本人数据或手机号落库证据，C03 及 18 个完整入口、最终业务验收计数均不变。

#### 2026-09-20 C03 运行缺表与直登修复

- 微信开发工具实际点击原按钮后返回业务 `503`。本机后端日志定位在 `ConsumerAuthenticationService.login → accountByOpenidDigest`，MySQL 报 `gaia_wh_init_wzy.afs_consumer_account` 不存在；通用异常处理将其转换为“数据操作未完成”。这不是 `uni.login` 授权失败。目标库仍须按 `database/20260920_consumer_mobile_identity.sql` 建身份表，并按依赖执行消费者业务增量迁移；执行前核对租户归属、备份及授权。
- 开发环境的欢迎页和产品空态按钮已改为一次点击直接发起 `uni.login`；正式微信构建由按钮直接触发微信原生 `getPhoneNumber`，授权码仍交服务端核验。微信端不再显示自定义确认弹层；其他入口需要授权时进入欢迎页直达按钮。重复点击期间禁用入口并显示“登录中”，会话切换时重新取得微信身份。开发登录的 `503` 提示检查身份表及密钥。未执行共享库 DDL 或数据写入，因此此改动只完成交互与故障定位，不能宣称真实登录成功。

#### 2026-09-20 C03 目标库迁移与首次登录复测

- 用户已明确授权修改数据库。只读预检确认目标为 MySQL 8.4 测试库 `gaia_wh_init_wzy`：身份两表和消费者归属两列缺失，`20260914_consumer_purchase_verification.sql` 所需的核验结构已存在，购买登记 42 行、产品关系 0 行。执行前将受影响两表的原结构与 42 行数据导出，随后移至宿主本机 `.local/backups/toto-consumer-migration-backup-20260920.sql`（目录权限 `0700`、文件 `0600`，SHA-256 `a7a6ebb55768114cd2503629895ee39c4afd417a852fe6f9cab1ac90e5cf8bbc`）；未写入仓库。两份消费者迁移各 2 条 DDL 均执行成功，后验两张身份表、两项增量列和命名空间唯一索引完整，原业务行数未变。
- 迁移后微信开发工具重试，原缺表 `503` 消失，但首次创建账号返回 `403 账号已停用`：Mapper 写入 `enabled=1`，Service 沿用未回填默认值的内存对象。Service 改为插入后读取已持久化账号，测试 Mapper 独立保存对象以覆盖首次登录路径；受影响 8 项测试通过。重新装配后的微信开发构建实际点击登录，服务端返回 `code=0`、账号 `2`，随后发起上下文请求，页面未出现自定义确认弹层。数据库确认该账号启用、首个会话已保存，手机号关联前 `phoneBound=false`。
- 本机忽略配置将用户提供的测试号码精确限定到账号 `2`，仅在 `develop` profile 启用。后端重启后第二次微信登录直接进入本人产品页，`/context` 返回 `code=0`、`phoneBound=true`、`phoneVerified=false` 与脱敏号码；数据库确认号码只存密文，来源 `LOCAL_TEST`，账号启用且无微信验号时间。再次登录仍为单一账号，3 个会话对应 3 次成功登录；商品选项、本人购买记录和服务记录接口均返回 HTTP 200，本人记录、产品关系和服务工单为空。个人中心只显示脱敏号码。微信开发工具可见 C05 的真实商品选择、购买日期和个人信息第三步，页面显示“测试关联手机号”；隐私政策版本与正文未配置，最终同意及提交按钮禁用，因此 C06 无本人结果详情可做正向验收。可信购买来源仍未接入，测试号码不构成购买核验或服务资格。
- 当前库 `tenant_info` 与 `tenant_data_source` 均无 `1001`；消费 API 依据本机单租户配置运行，平台租户归属仍需配置责任人确认，未自行创建平台租户或映射。此轮是源码／测试库联调证据，完整 C03 与业务验收尚不能据此计数；正式微信手机号授权、真机、可信购买同步与正向本人业务数据仍待验。
- 未登录开发首页明确显示“微信登录”并禁用重复点击；已登录但暂无产品时直接提供“查看我的产品／手动添加产品”，不再无谓重复建会话。购买记录空态说明当前只是测试号码、可信购买来源尚未接入，正式空态只承诺查看已关联记录，不将空列表写成购买已核验。登录、测试号码、本人购买与网关的受影响后端测试共 13 项通过；消费者端最终 `pnpm verify` 覆盖类型、Lint、规则测试及微信／H5 development 构建。

<a id="2026-09-21-c03-首次授权返回首页"></a>

#### 2026-09-21 C03 首次授权返回首页

- 登录完成后的导航按发起页面收口：首页直接发起时不再导航，独立欢迎页完成授权后切回原生首页；若“我的产品”页自身发起授权，则继续留在当前列表页。手动添加目的仍进入登记流程，不改变其原有落点。
- 本地 H5 固定测试身份、微信开发登录和正式微信手机号授权共用上述收口逻辑；账号、手机号绑定、本人商品加载、错误处理和重复提交保护均未改变。
- 消费者端 `pnpm check`、微信与 H5 development 构建及 `git diff --check` 通过。未在本轮重新执行微信开发者工具授权或真机验证，因此不增加 C03 实现或验收计数。

<a id="2026-09-21-c03-临时手工手机号与静默登录同步"></a>

#### 2026-09-21 C03 临时手工手机号与静默登录同步

- 按用户当前决定，暂时取消微信 `getPhoneNumber` 和短信验证码：首次绑定及个人信息页换号统一使用一个手机号输入弹窗，不要求验证码或微信手机号凭证。服务端只接受 11 位大陆手机号格式，并把来源保存为 `MANUAL_TEMP`、`phoneVerified=false`；该号码是当前临时登录与匹配依据，不得在页面、接口或验收记录中表述为微信已验证手机号。
- 微信小程序每次冷启动先调用 `wx.login`（UniApp `uni.login`）换取 OpenID 对应的独立消费者会话；静默登录临时失败时才回退到本机尚未过期的旧会话。手工手机号通过受保护的 `/mobile/consumer/phone` 绑定到该微信账号。未绑定时首页显示“绑定手机号”；已绑定时不再要求点击登录或绑定，直接加载本人目录和产品记录。
- 手工绑定、换号及后续微信静默登录均在服务端同步可信购买实例：手机号摘要必须完全匹配，购买登记须为 `ACTIVE + VERIFIED` 且来源为 `OFFLINE`／`VIRTUAL`／`EXTERNAL`，商品须启用，实例不得退回／合并且不能已有其他消费者活动关系；成功建立 `PURCHASE_MATCH`，重复登录保持幂等。待核验记录、用户自报、归属冲突和其他账号已认领实例不会自动绑定。换号不删除或改写原有历史产品关系，只对新号码执行同一保守同步。
- 临时手机号输入／换号不再使用自绘遮罩层，统一改为 TDesign `Dialog + Input`；同批盘点出的网点拨号／导航确认也改为 TDesign `Dialog`，联系客服底部抽屉改为 TDesign `Popup`。TDesign 继续统一继承租户主题变量，消费者端 TOTO 租户主色及其激活／禁用／浅色状态已与程序默认交互主题 `#2B74AA` 对齐。系统定位权限等平台原生提示继续由微信／UniApp 原生能力承载，不与业务对话框混用。
- 既有身份表需先执行 `database/20260921_consumer_manual_phone.sql` 扩展手机号来源约束。本轮没有执行共享数据库迁移、部署或真实数据写入；正式微信手机号恢复方案、临时号码升级／撤回规则、目标环境与真机验收仍待完成，因此 C03／C13 和最终业务验收计数不变。
- 源码验证：后端手工绑定、换号、重复静默登录与可信购买筛选定向测试 13 项通过，`gaia-after-sales-api` 受影响 Reactor 编译通过；消费者端 `pnpm verify` 通过，包含完整检查及微信／H5 development 构建。微信构建产物已确认包含冷启动 `uni.login`、手工手机号请求和统一输入弹窗，且不再包含 `getPhoneNumber`。本轮浏览器自动化能力启动失败，未完成 H5 目视复核；共享数据库迁移、宿主运行态、微信开发者工具和真机仍未验证。

#### 2026-09-22 共享测试服务器消费者登录配置修复

- 消费者测试构建请求 `POST http://10.1.1.53:8011/api/afterSales/mobile/consumer/auth/login` 返回 `503 / 消费者身份保护配置不可用`。只读核对确认请求已进入当前消费者登录实现，实际运行的外部配置 `/yundata/saas_test/tomcat11/conf/gaia-after-sales.properties` 仅有 AI 派单密钥和移动单租户 ID，缺少消费者 PII 密钥及微信 AppID／AppSecret；本地正常是因为本机忽略配置已包含这些项，不是接口制品未更新。
- 经用户明确授权，将本机联调使用的同一套消费者 PII 密钥和微信应用配置安全写入测试服务器外部配置，文件保持 `0600 root:root`；原文件备份到 `/yundata/saas_test/backups/20260922-consumer-auth-config-bmcAEh/gaia-after-sales.properties`。密钥值未写入仓库、日志或验证记录。
- 重启时旧 Tomcat Web 应用已停止但 JVM 被遗留后台线程拖住；核对目标进程的 `catalina.base` 后仅终止 TOTO 实例，没有影响服务器上的其他 Tomcat。新进程 `1518911` 启动并恢复监听 `8011`，根入口 HTTP 200。用无效诊断 code 调用登录后，响应已从 PII 配置错误推进到 `微信授权暂不可用，请稍后重试`，证明 PII 配置已加载并进入微信网关；无效 code 不创建消费者账号。真实 `wx.login` code、微信开发者工具和真机仍需单独复测，本记录不增加 C03 实现或业务验收计数。

#### BUG-20260922-02：旧测试手机号同号绑定后仍提示绑定

- 应用与入口：消费者小程序，C03／C13；关联本 Spec 的“临时手工手机号与静默登录同步”。来源为 2026-09-22 微信开发者工具联调反馈，优先级 P0，严重程度高，状态为**待验证**，负责人待分配。
- 环境与复现：共享测试 API `10.1.1.53:8011`、测试库 `gaia_wh_init_wzy`。账号 `2` 冷启动登录成功后输入原测试关联手机号，`POST /api/afterSales/mobile/consumer/phone` 返回 `code=0`，但 `phoneBound=false`；再次冷启动仍生成新会话并提示绑定手机号。
- 根因：账号 `2` 已有密文手机号，来源仍为 `LOCAL_TEST`，当前共享环境未启用旧开发账号白名单，因此该来源不能作为可用手机号。手工绑定相同号码时服务端命中“号码未变化”捷径，只同步购买关系并返回旧账号对象，没有把来源转换为 `MANUAL_TEMP`。同时测试库旧检查约束仅允许 `WECHAT／LOCAL_TEST`，尚不允许新来源。
- 修复与数据：经用户授权，执行 `database/20260921_consumer_manual_phone.sql` 前保存 `afs_consumer_account` 表结构，随后在目标库完成约束迁移；复核约束已同时允许 `WECHAT／MANUAL_TEMP／LOCAL_TEST`。未读取、输出或直接改写账号手机号明文，账号 `2` 仍保持原 `LOCAL_TEST` 数据，等待正常接口转换。服务端同号捷径现只对已验证号码或已是 `MANUAL_TEMP` 的号码生效；旧 `LOCAL_TEST` 同号手工绑定会正常执行更新并返回 `phoneBound=true、phoneVerified=false`。
- 研发验证：`ConsumerAuthenticationServiceTest` 新增“开发白名单关闭后同号手工绑定转换来源”回归场景；核心身份 10 项和本地微信网关 2 项测试全部通过。后端修复尚未发布，共享接口仍会返回旧行为。
- 验证标准：发布后由账号 `2` 在页面输入原号码，绑定响应为 `phoneBound=true、phoneVerified=false`；数据库来源变为 `MANUAL_TEMP` 且不产生明文；冷启动重新登录后的 `/context` 仍为 `phoneBound=true`，首页不再提示重复绑定。通过前不关闭本缺陷，也不增加 C03／C13 入口或业务验收计数。

#### 2026-09-20 C05 开发态无政策联调切片

- 用户明确要求先跳过隐私政策，让本地主流程可提交。后端增加默认关闭的 `after-sales.consumer.dev-policy-bypass.enabled`，并以 `after-sales.consumer.dev-policy-bypass.account-id` 限定单一测试账号：仅 `develop` profile、政策版本与正文同时为空时返回 `developmentBypass=true`；非开发环境、配置缺半、开关关闭或账号不符时继续拒绝。若存在完整政策，始终要求匹配版本和确认。消费者端仅在开发构建识别此响应，显示“开发联调、未发布政策”，确认项只确认测试申报信息，不展示虚构条款；提交空 `policyVersion`。服务端此路径不写 `afs_consent_record`。
- 登记仍要求服务端会话手机号、启用商品、有效日期、地区和地址及幂等请求号，初态仍为 `USER_REPORTED / PENDING / ACTIVE / NOT_ASSESSED`；不自动生成安装码、核验购买或授予服务资格。接口细节见[源码契约](../../../backend/gaia-after-sales/docs/consumer-mobile-registration.md)。本机忽略配置已启用开发开关，未写入可提交配置。后端单元测试覆盖开发与非开发、半配置、无同意记录、重复提交和跨账号拒绝；H2 隔离测试覆盖无政策登记、重复请求、跨账号／跨租户隔离，均通过。消费者端 `pnpm check` 与微信／H5 development 构建通过。目标 MySQL 和微信页面正向联调结果另记；完整入口和业务验收暂不计完成。
- 目标测试库联调：共享 8080 的隐私政策接口返回 `code=0`、空版本／正文及 `developmentBypass=true`。微信开发工具中账号 `2` 选择真实目录商品、填写当日购买日期、测试姓名、地区及地址并确认测试申报；`POST /registrations` 成功，C06 本人结果详情返回“产品已添加／购买待核验／服务资格尚未评估”，不展示安装码。C10 本人产品资料显示“已添加 · 待核验”，维修按钮拒绝直接建单；C11 购买记录显示 1 条用户申报、1 件产品。只读 MySQL 对账确认唯一测试登记 `940918010035` 为 `USER_REPORTED / SELF / PENDING / NOT_ASSESSED`、1 件实例、0 条隐私政策同意。此笔仅为开发测试数据，不是可信购买或业务验收。联调发现结果详情的使用方式漏传，已补消费者响应白名单与前端映射；后端重启后微信产品资料重新读取本人 API，展开信息显示“使用方式：自己使用”。

### 2026-09-20 C05／C10／C11 独立归属与本人记录源码切片

- 增量迁移 `20260920_consumer_mobile_business.sql` 为统一购买登记增加消费者账号归属，为账号产品关系增加 `CONSUMER` 与历史后台用户两个命名空间。新增移动端商品选项、无码申报、本人记录列表／详情 API；手机号从已验证消费者会话取得，客户端不能传入手机号冒用。用户申报仍写统一购买登记和产品实例，初始为 `USER_REPORTED / PENDING / ACTIVE / NOT_ASSESSED`，不生成安装码。
- 移动端页面接入真实商品选项、地区选择、申报提交、来源／待核验结果、我的产品和购买记录分页、空态及加载失败重试。移动 API 使用响应字段白名单，只返回本账号关联的商品实例和安装码，不输出后台登记的内部订单／审计字段；关联到他人的正式购买登记时不返回购买人的脱敏姓名、手机号及地址。隐私政策版本与正文通过服务端配置发布；缺任一配置时服务端拒绝申报，客户端禁用提交，不用原型文案冒充已生效政策。扫码编码解析、可信订单自动同步和权益判断仍未接入。
- 验证：核心单元测试 6 项；H2 隔离数据库对独立消费者申报、重复请求、同租户跨账号拒绝、关联购买时只返回本人商品及 Web 核验后状态读取的集成测试 1 项通过；旧后台申报和关联核验回归 2 项通过；消费者端 `pnpm check`、微信及 H5 development 构建通过。H5 本地构建在浏览器确认首页不宣称自动同步，点击“授权后手动添加”先出现授权弹层并提示须在微信小程序授权。完整 `CustomerIntegrationTest` 连续执行在本机 JVM 中止，逐项关键场景通过。迁移、真实微信账号、隐私正文、微信开发者工具／真机以及共享环境均未验证，不计 C05、C10、C11 完成或业务验收。
- 待确认／外部条件：目标库迁移与密钥配置、实际隐私政策正文和版本、首期可信购买来源及归属规则。既有 `/customer/consumer/*` 仍是后台入口，只供旧业务与回归使用；消费者端固定调用 `/mobile/consumer/*`。服务记录和授权家庭成员查询尚未实现。

### 2026-09-20 C06 登记结果详情源码切片

- 无码申报成功后，结果页携登记 ID 调用 `GET /api/afterSales/mobile/consumer/records/{id}` 重新读取本人详情；页面刷新仍可恢复本次结果，接口失败显示错误并可重试。既有服务端本人范围限制决定能否读取，端侧不使用客户端手机号查询。直接从“我的”进入时读取本人记录并展示当前产品。
- 结果页按服务端字段分别展示购买核验状态（待核验／已核验／冲突／驳回）、产品关系及服务资格。`NOT_ASSESSED` 明示尚未评估，具体服务申请仍须单独核验；没有真实安装码时不显示编码。服务端没有返回使用方式时显示“未提供”，不推断为“自己使用”。登录会话失效会清除页面详情。
- 验证：消费者端 `pnpm check`、微信和 H5 development 构建通过，`git diff --check` 通过。未执行目标库迁移、真实微信账号／接口、微信开发者工具或真机验证；扫码添加及真实关联码尚未接入，C06 不计完整入口实现与业务验收。

### 2026-09-20 C04 保守扫码识别源码切片

- 正式微信端接入 `uni.scanCode`，也可手动输入编码；客户端只提交原值，不凭前缀判断码类型。`POST /api/afterSales/mobile/consumer/codes/lookup` 使用独立消费者会话，仅精确查询当前账号活跃关系中的既有安装码，命中时返回本人登记与实例 ID，转本人结果详情；他人码、未知码、未接入发行方均统一返回 `UNKNOWN`，不暴露他人是否存在，也不建立关系或产生权益。
- 本切片只能找回本人已有产品，不能证明安装码是真实购买凭证或可信逐件标识。TOTO 安装码、SN、溯源码和外部购买码的发行方、粒度、验证接口与生命周期仍需确认；在此之前不提供跨账号认领、按型号自动建实例或宣称可信标识去重。H5 显示手动输入提示，不假称可用微信扫码。
- 正式购买列表使用后端 `sourceType` 区分用户申报、线下门店、虚拟门店和外部购买登记；已有购买关系不再称为“手机号同步”，以免将尚未实现的自动同步说成已发生。
- 后端单元测试覆盖已有、他人和未知码；H2 隔离测试覆盖本人／跨账号查询。目标库迁移、真实扫码及微信开发者工具／真机仍待验证，不计 C04 完整入口实现或业务验收。

## 8. 已实现接口与状态契约

2026-09-20 扫码可信绑定增量：在既有本人安装码找回之外，`POST /api/afterSales/mobile/consumer/codes/lookup` 可对精确命中的已核验正式购买商品返回 `MATCHED`；消费者核对商品后调用 `POST /codes/claim`，仅在有效实例、无其他活动账号关系时建立 `PURCHASE_MATCH` 关系。该关系与正式购买的 `ACTIVE / VERIFIED` 状态共同用于服务申请直接进入工单的判定；型号条码、自报记录、未知码均不能以扫码本身取得可信资格。手机号自动同步及其真实授权仍未实现；本增量的运行环境与业务验收状态见[服务申请分流 Spec](2026-09-20-消费者维修故障选项与目标环境核验.md#2026-09-20-可信商品直接进入服务工单)。

| 接口 | 用途 | 关键约束 |
| --- | --- | --- |
| `GET /api/afterSales/customer/consumer/options` | 加载可选售后商品 | 只返回启用商品 |
| `POST /api/afterSales/customer/consumer/registrations` | 创建用户自报购买 | 当前账号归属；首版每次仅一件；隐私同意和幂等键必填 |
| `GET /api/afterSales/customer/consumer/records[/{id}]` | 查询本人记录 | 只按服务端账号 ID 查询，不接收手机号越权条件 |
| `GET /api/afterSales/customer/records/verification-options` | 加载核验可选门店 | 按核验权限和当前岗位数据范围返回，不依赖购买登记权限 |
| `GET /api/afterSales/customer/records/{id}/verification-candidates` | 生成正式记录候选 | 只返回同手机号、同商品、已核验有效且当前岗位可见的记录 |
| `PUT /api/afterSales/customer/records/{id}/verification` | 后台核验 | 独立按钮权限、乐观版本、动作白名单和不可变历史 |

来源、核验、生命周期和服务资格是四个独立维度。人工核验只改变购买核验状态；`entitlement_status` 仍由后续具体服务资格规则判断，不能因 `VERIFIED` 自动改为免费服务。
