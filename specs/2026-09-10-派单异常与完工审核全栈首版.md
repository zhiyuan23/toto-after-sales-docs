# 派单、异常与完工审核全栈首版

> 2026-09-11 数据契约更正：工单 scope 和读取／锁定校验须解除对主系统门店、代理商业务记录的依赖。 现源码已完成本项独立化并通过隔离研发验证，目标环境迁移及业务验收待完成；下文实现、迁移和验证仅记录原时点事实。最新约束与源码证据见[独立边界核查](2026-09-11-业务数据独立边界与已开发功能核查.md)。

- 日期：2026-09-10
- 范围：统一管理后台 A19 派单调度、A20 异常工单、A21 完工审核
- 关联需求：CS-004、CS-005、CS-007～CS-009、CS-011、STA-004、STA-005
- 当前状态：开发中（共享联调中）

## 1. 本轮目标

在已有统一工单、服务区域、服务站、服务人员和按日可用性基础上，形成不依赖模拟数据的首个后台工单流转闭环：

1. 按行政服务区域推荐服务站，支持人工分配、转站和原因留痕。
2. 按服务类型技能、日期可用性和当日容量筛选服务人员，支持派单、改派和冲突拦截。
3. 处理取消、拒单、断联、改期、缺件、投诉升级和复开，并保留不可变异常事件。
4. 配置受理、派单、预约、完工和回访阶段 SLA，在列表中给出正常、预警或超时状态。
5. 创建不可变完工提交版本，审核通过或退回；退回后重新提交形成新版本，不覆盖原始材料。

## 2. 前端实现

独立售后子系统新增三个真实页面：

| 菜单码 | 页面 | 当前能力 |
| --- | --- | --- |
| `afsDispatch` | 派单调度 | 工单筛选、详情、服务站推荐与分配、人员候选与冲突说明、派单／改派、完工材料提交 |
| `afsWorkOrderException` | 异常工单 | 异常筛选与详情、七类异常动作、升级标记、复开、五阶段 SLA 配置 |
| `afsCompletionReview` | 完工审核 | 待审工单、最新提交版本与材料核对、通过、退回及原因记录 |

页面继续复用 `FaPageMain`、`EsSearch`、`EsListToolbar`、`EsTable` 和列表／弹窗通用方法。三个列表均采用纵向弹性布局，表格容器占满筛选和工具栏之外的页面剩余高度。

## 3. 后端契约

接口统一位于 `/api/afterSales/work-order`：

- `GET /permissions`：返回三个模块的查询和操作授权。
- `GET /dispatch`、`GET /dispatch/{id}`、`GET /options/{id}`：派单列表、详情和候选项。
- `PUT /dispatch/{id}/station`：自动推荐或人工分配／转站。
- `PUT /dispatch/{id}/personnel`：派单或改派服务人员。
- `POST /dispatch/{id}/completion-submissions`：新增完工材料版本。
- `GET /exceptions`、`GET /exceptions/{id}`、`PUT /exceptions/{id}`：异常查询和动作。
- `GET /sla-policies`、`PUT /sla-policies/{stage}`：SLA 查询和配置。
- `GET /completion-reviews`、`GET /completion-reviews/{id}`、`PUT /completion-reviews/{id}`：完工审核。

写操作使用精确操作权限；列表、详情、状态流转、历史和版本写入遵循 Gaia 租户、数据范围、事务及乐观并发机制。

## 4. 数据与不可变记录

迁移 `20260910_work_order_workflow.sql` 在统一工单增加服务站分配、人员派单、状态变更时间和当前异常摘要字段，并新建：

- `afs_service_personnel_skill`：人员支持的安装、维修或指导技能。
- `afs_order_assignment_history`：服务站和人员每次分配／改派历史。
- `afs_order_exception_event`：每次异常动作及状态迁移历史。
- `afs_completion_submission`：按工单递增的不可变完工提交版本。
- `afs_completion_review`：审核决定及其关联的提交版本。
- `afs_sla_policy`：五阶段 SLA 目标与预警提前量。

迁移与权限脚本均提供回退文件。本轮已按授权应用到共享测试库：新增 12 个工单字段、6 张业务表、3 个模块查询 API、6 项操作权限和管理员授权；未插入模拟业务数据。

## 5. 关键业务约束

- 自动分站使用当前地址匹配到的已启用行政区域候选站；人工偏离推荐或转站必须填写原因。
- 人员必须启用、属于当前服务站、具备对应工单类型技能，并在期望日期处于可用状态且未超过日容量；不满足时服务端拒绝派单。
- 异常页面只提供已定义动作，不提供任意状态编辑器；拒单时解除当前人员，复开恢复异常前状态。
- 完工提交要求服务结果、与产品实例一致的产品／安装码、1～20 个照片引用和客户签名引用。
- 审核只新增审核记录并迁移工单状态；退回后原提交不可修改，新材料生成下一版本。
- SLA 没有持久化配置时不伪造已生效默认值；保存后才参与真实时限判断。

## 6. 验证证据

- 后端工单流集成测试覆盖：自动分站、无冲突人员候选、派单、异常与复开、完工提交、退回、第二版重提、审核通过、SLA 保存及不可变历史数量。
- 前端测试覆盖三个菜单路由、权限动作、技能字段和工单状态映射；类型检查和生产构建通过。
- 共享测试库结构与权限数量核对通过。
- 本地真实管理员会话下，A19、A20、A21 均进入真实接口空列表，无“暂无访问权限”；A20 的五阶段 SLA 配置弹窗可打开；桌面列表表格填满页面剩余空间。

## 7. 暂缓边界

以下能力没有已确认规则或外部基础，本轮不虚构实现：

- 地图围栏、地址地理编码、地图任务看板和批量派单。
- 师傅抢单、混合派单模式及派单通知。
- 周期排班模板、跨站共享人员和更复杂的技能等级／服务区域规则。
- SLA 后台定时扫描、自动升级及通知任务。
- 师傅端现场履约、对象存储真实上传、配件耗用与完工提交的联合事务。
- 完工审核责任角色、配件核销标准、回访评价、关闭规则及拒绝角色／跨租户完整验收。

因此 A19～A21 计入“已有真实实现”，但仍保持“开发中（共享联调中）”，不计入最终业务验收完成数。


## 8. 2026-09-13 取消默认二次交单

### 已确认范围

用户确认：完工资料一次提交，审核通过后不再要求师傅交单；同步原型、状态说明与涉及的真实程序。A21 的审核通过从 `PENDING_REVIEW → PENDING_HANDOVER` 改为 `PENDING_REVIEW → PENDING_FOLLOW_UP`，师傅本次处理结束。退回仍为 `PENDING_COMPLETION`，补充新版本再审；不将审核通过直接当作工单关闭。

状态字典、正常／异常流转图、角色和配件单区别统一见[服务工单状态机与单据状态说明](../02-技术设计/13-服务工单状态机与单据状态说明.md)。本段是现有首版 Spec 的已确认变更，不改变其他未确认业务。

### 程序改动

- 后端：审核通过直接待回访，保留权限、版本、锁、事务、审核版本及不可变状态历史；待回访、旧待交单和已关闭阶段不开放旧异常复开动作，也拒绝直接调用绕过。
- Web 独立子系统：待回访／已关闭使用完成服务后的视觉色，旧待交单显示“待交单（历史状态）”；英语同步。审核浏览器夹具改为新目标。未修改 legacy 或受保护 common 子仓。
- 服务人员原型：删除交单页与可提交表单，只保留待处理／审核中／历史；审核通过后查看后续回访进度；旧链接转进度、旧提交拒绝。状态变化不重复扣配件库存，不自动关闭。
- 未发现移动端源码仓库内已有交单业务实现，本轮不为页面壳新建业务模块；后台 HTML 原型未命中该交单状态，不改其并发编辑内容。

### 数据兼容与后续边界

- `PENDING_HANDOVER` 保留为历史查询值，不再由新审核产生；本轮不批量改写数据库或审计，不执行迁移。
- 旧状态治理须核对具体旧单及材料后单独处理；不能用 UI 改名把旧单伪装成已回访／已关闭。
- 回访、评价、结算、关闭的正式接口和条件仍属后续切片。新审核进入已有待回访状态，不表示 A22 或服务人员应用已完成。
- 未安装后端产物、重启聚合宿主或发布 Web；当前修改经本地隔离验证，运行中共享环境需按后续部署验证，不改变原验收统计。

### 验证记录

- Java 21，离线 `mvn -B -o -pl gaia-after-sales-api -am test`：core **4** 项、API **63** 项，共 **67 项通过**。H2 隔离库、测试权限上下文，不连接共享业务库。工单用例新增通过的实际 API 响应、无权／旧版本／重复审核拒绝、材料不重复创建、审计目标一致，以及旧交单数据查询和异常快照复开拦截。
- Node 22.23.1，`corepack pnpm --ignore-workspace check`：lint、依赖、类型、国际化和 **232 项测试通过**。`corepack pnpm --ignore-workspace build` 通过，**129 个 JS/CSS 产物**满足体积限制。
- 原型 **65 项回归**与 **10,260 组模板组合**通过；服务人员 28 屏、消费者 29 屏，全部页面路由及素材检查通过。
- 浏览器：新版首页只有待处理／审核中／历史任务；黄先生从审核中通过后直接待回访，审核计数 1 → 0、历史计数 2 → 3，后续跟进展示两单；保留第 1 次提交，手机内无交单按钮、无自动关闭。
- `node scripts/check-doc-impact.mjs --working-tree --all` 与三仓 `git diff --check` 通过。
- 本轮研发验证不能替代正式账号／多租户部署／微信真机及业务验收。改动保留工作区，不自动提交或推送。


## 2026-09-26 补充材料与私有照片审阅修复

关联 `BUG-20260926-02`，问题定义、前置条件与完整验收标准统一见[服务人员 Spec 15.13](2026-09-18-服务人员小程序完整开发计划.md#1513-2026-09-26-退回补充草稿与后台材料审阅修复)。

- 根因：移动重提已保存补充说明及替换后的分类证据，后台 `latestSubmission` 仍只查询旧摘要字段；工作台直接用私有接口作为图片 `src`，无法附带 Gaia 登录凭证；独立审核弹窗只有照片计数。
- 后端：最新不可变版本查询补齐处理记录、RFID、客户确认／说明、四类证据、父提交 ID、补充项和补充说明，版本相同时以 ID 稳定排序。沿用已存在的数据列及模型，不调整提交、扣库、审核状态和权限。
- Web：工作台与审核弹窗统一展示补充说明、补充项、确认记录、分类照片与遗留照片；通过既有 Gaia 请求组件和受控证据地址获取图片 Blob，校验内容类型，提供点击放大及失败重试；切换材料取消旧请求并释放对象 URL，业务错误不当作图片显示。不修改 legacy 或受保护 common 子仓。
- 后端隔离测试 24 项通过：MyBatis 读取第 2 版补充字段和原版不变；移动执行、审核状态与私有媒体 API 原有边界。Java 21 离线执行，Mockito 使用显式 Java agent 解决本机测试进程自附加限制。
- Web lint／依赖／应用及测试类型／i18n 检查通过；新增 3 项测试覆盖证据分类无遗漏、仅受控地址使用鉴权加载器、HTTP 200 业务失败与非图片内容拒绝。全量结果 300 通过／30 失败；独立临时目录导出 HEAD 基线为 296 通过／同 30 失败，工作区另含并发排班新增测试，不混算为本任务用例。生产 Vite 构建通过，216 个 JS／CSS 产物体积检查通过；最后补充 RFID 展示后已重新通过类型检查及构建。标准 `build` 中的全量测试门禁仍受上述既有失败阻断，未把直接构建记为全量检查通过。
- 验证边界：浏览器连接失败，未实测图片放大和真实第 2 版；未发布后端／宿主／Web，运行环境仍需加载新产物后完成允许／拒绝账号及跨端验收。源码与隔离研发验证不等于业务验收，不改变 A21 入口或最终验收计数。


### 同日本机运行态跟进

用户复测仍看不到补充说明／图片，并确认使用本机开发页面。核对 8080 受管运行包 `gaia-web-20260926122724-48314.jar` 内实际 Mapper，仍为旧摘要查询，缺少 `supplement_note` 与四类分类证据；前一轮只修源码、未更新宿主是本次未生效的直接原因。

本轮使用统一启动器安全更新本机后端，Gaia 系统依赖、售后模块与聚合宿主装配均成功；新运行包 `gaia-web-20260926131041-72116.jar` 已确认包含补充说明及四类证据映射，Web／移动健康探针就绪。9010 实际提供的材料组件也包含补充说明与鉴权预览代码，未修改共享测试服务器。

对反馈工单 `960921110027` 做只读核对：第 1、2 版均保留；第 2 版父提交指向第 1 版，补充项为 `SITE_EVIDENCE`，补充说明非空，现场与铭牌图片引用均存在。资料已经保存，无须重新填写或上传。新增本地测试账号接口登录遇验证码，未绕过验证码、未完成该会话的鉴权媒体读取；浏览器连接仍失败，实际审核页显示由用户刷新后复核。不把启动探针或数据库回读记为真实角色页面验收。实施时未提交、审核或改写该工单。


### 同日提交批次展示与图片内容核对

用户截图确认补充说明和私有图片已能显示，但混排的最新快照无法区分首次／补充提交，图片整行占用空间过大；继续按 `BUG-20260926-02`／`OPT-20260926-03` 跟进。

1. 审核详情在原有接口权限与工单可见范围校验后返回 `completionSubmissions`，按提交版本倒序，每版保留独立字段和图片引用；列表仍仅带最新摘要，不在每行展开历史查询。无新 API、数据迁移或审核状态变化。
2. 工作台、审核弹窗和完工材料详情统一按“第 N 次提交”分组，显示首次／补充／重新提交、提交时间及本次照片数；默认展开当前提交，历史可单独展开。补充版本只展示指定修改项、补充说明及该批证据，沿用字段不重复占版面；首次和旧式完整重提保留完整快照，不伪装成补充差量。
3. 同次提交各类别照片在同一行流式排列，标注类别，空间不足换行，仍支持点击放大、鉴权加载及失败重试；历史折叠时不加载图片，展开后加载，收起释放对象 URL。空产品码／签名不再占独立空行。
4. 图片“仍为第一次”的实际核对：工单 `960921110027` 的第 2 版现场引用与第 1 版不同，现场图片确实替换；但服务器对应的图片文件内容完全一致。两版现场／铭牌均为 67,766 字节，SHA-256 相同（`b2b62c940de43202…`），打开第 2 版文件就是用户截图中的企鹅。因此本单是不同上传记录存了同一图片内容，不能据外观认定第二次没保存，也不能声称另一张未收到的图片已恢复。未改写图片或业务提交。

验证：后端 Mapper／服务层 17 项及完整派单—提交—退回—重提—审核 API 流程 1 项通过，新增历史顺序、两版图片独立、拒绝权限／不可见工单不查历史的断言。旧集成夹具缺少已有移动完工字段，本轮补齐夹具；首次全类运行遇本机 JVM abort，改用受限堆及单层编译运行相关流程通过。前端证据／批次测试 6 项通过，含连续三次版本、跨工单排除、最新快照优先、补充只展示本批与旧完整重提兼容；静态检查通过，全量 304 通过／30 失败，与 HEAD 基线同 30 项失败（工作区另有并发排班测试）。生产构建与体积检查通过。浏览器工具仍因请求头策略连接失败，未取得新版页面目视证据。

本轮已更新本机受管宿主并通过 Web／移动健康探针，运行 PID 82205 的实际包已回读确认 `submissionHistory` 查询；9010 实际提供的新版本组件亦已回读。恢复开发服务时发现 9010 已由并发进程启动，复用该子站，仅补启缺失的 7004 主站。共享服务器未发布，未操作实际审核。用户页面确认与真机全流程仍不计已验收。

本次代码已分别提交：后端 `231c9d5`，Web `32f59590`；仅本地 Git 提交，未推送或发布。提交前后定向测试通过；后端整类测试另有 3 项与本次材料查询无关的失败，Web 全量仍为同 30 项既有失败。正式业务验收状态不变。


## 2026-09-26 三类服务与指导方式

当前服务类型与完工约束以[安装维修指导三类服务流程](2026-09-26-安装维修指导三类服务流程.md)为准：不再新建清洗；指导区分现场／远程，远程免现场证据和配件，正常闭环保留。远程转现场经用户确认暂不实现。
