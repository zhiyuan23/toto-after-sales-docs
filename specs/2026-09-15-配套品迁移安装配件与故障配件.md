# 配套品迁移为安装配件与故障配件

- 状态：已开发并完成本地真实浏览器留痕验收
- 日期：2026-09-15
- 应用：售后服务 / 商品资料 / 商品档案
- 关联记录：[商品与服务档案删除与配套品配件改造](2026-09-11-商品与服务档案删除与配套品配件改造.md)

## 1. 已确认的业务规则

- 退役“配套品档案”及其菜单、接口、数据表和商品详情入口。
- 既有配套品若不与配件档案重复，则迁移为配件档案，并转为商品的安装配件关系。
- 配件编号相同、非空型号相同，或品牌和名称同时相同的既有配套品视为重复；直接删除，不迁移其旧关系。
- 商品必须维护至少一条安装配件；每条关系都必须填写数量，默认值为 `1`，可编辑为 `1` 至 `9999`。
- 每条故障及维修记录可选维护所需配件；该项非必填，但已选择的配件同样维护数量。
- 历史已完成服务工单不在本次保留范围，旧配套品衍生的工单物料行随迁移清理。

## 2. 实施内容

### 2.1 后端与数据

- 新增商品安装配件关系与故障配件关系的数据读写，并在商品创建、更新时校验安装配件和数量。
- 故障、详情和工单创建的返回模型均带回配件编号、名称、品牌、型号和数量。
- 生成安装物料时统一使用 `PART` / `INSTALLATION_PART`；故障物料使用 `FAULT_PART`。
- 移除了配套品应用服务、目录接口和对应鉴权策略。
- 新增一次性迁移脚本：`gaia-after-sales/database/20260915_replace_product_bundle_with_installation_part.sql`。
- 本地迁移实际结果：原 3 条配套品中 1 条唯一数据迁移为配件；2 条与现有配件型号重复的数据及其旧关系已按确认规则删除。迁移后的商品安装关系保留在配件关系表中。

### 2.2 前端

- 移除配套品页面、路由、菜单清单、图标、详情入口和中英文文案。
- 商品新增/编辑在“基本信息”后新增必填的“安装配件”页签；弹窗可按配件档案选择，列表展示配件图片、编号、名称、品牌、型号，支持添加、删除与数量编辑。
- “故障及维修”中每条故障均可选维护所需配件，支持多选、删除与数量编辑。
- 商品详情新增“安装配件”“故障与维修”“安装手册”页签；安装配件和故障配件均展示数量。
- 工单创建中原配套品来源改为商品安装配件来源。

## 3. 本地浏览器留痕验收

环境仅为本地联调环境；未对 `10.1.1.53` 测试环境执行迁移、写入或部署。

| 步骤 | 浏览器操作 | 留痕数据 | 结果 |
| --- | --- | --- | --- |
| 1 | 配件档案新增并保存 | `TRACE-PART-20260915-01` / 留痕安装配件 / `TOTO` / `TRACE-PART-01` | 保存后在配件档案列表可见。 |
| 2 | 商品档案新增，选择安装配件并将数量改为 2 | `TRACE-PRODUCT-20260915-01` / 留痕安装商品 | 保存后商品列表可见。 |
| 3 | 在同一商品的故障及维修页新增故障，选择所需配件并将数量改为 3 | `TRACE-FAULT-01` / 留痕测试故障 | 保存后故障行显示“留痕安装配件 x3”。 |
| 4 | 商品详情页打开“安装配件” | `TRACE-PART-20260915-01` | 详情表格展示编号、名称、品牌、型号，数量为 `2`。 |
| 5 | 商品详情页打开“故障与维修（1）” | `TRACE-FAULT-01` | 所需配件表格展示同一配件，数量为 `3`。 |

上述记录为用户要求保留的本地真实界面测试数据，未通过直接接口写入。

## 4. 自动化与构建校验

| 范围 | 命令或方式 | 结果 |
| --- | --- | --- |
| 后端 | `mvn -o -pl gaia-after-sales-api -Dtest=CatalogIntegrationTest#productCreatePersistsFaultsManualsAndAccountNameInOneTransaction test`，JDK 21 | 1 项通过，0 失败、0 错误。 |
| 后端制品 | `mvn -o -pl gaia-after-sales-api -am clean install -DskipTests` | 成功安装售后 `3.0-SNAPSHOT` 制品。 |
| 宿主装配 | `mvn -o -pl gaia-saas-web -am package -DskipTests` | 成功，已装配本地 exploded WAR。 |
| 前端 | Node `24.21.0`、pnpm `10.8.1` 下执行 `pnpm run typecheck` 和 `pnpm run check:i18n` | 类型检查通过；18 个双语模块、2161 个键校验通过。 |
| 菜单与导航 | `node --test scripts/after-sales-navigation.test.mjs scripts/after-sales-menu-package.test.mjs` | 22 项通过。 |
| 真实页面 | 本地登录后以浏览器 UI 新增、保存、详情回读 | 安装配件必填、安装数量与故障配件数量均已留痕验证。 |

## 5. 交付状态

- 本地功能改动已提交到各开发分支：`gaia-after-sales`、`gaia-ui`；宿主仅补充本地协作说明，不包含本功能业务代码。
- 未推送到远端，也未对共享／线上环境执行迁移、部署或菜单同步；线上现网行为不受影响。
- 本地后端、主前端和售后子系统可用于继续验收。
- 迁移脚本为一次性脚本，目标环境执行前需备份数据并停止售后写入；执行后不可重复运行。
