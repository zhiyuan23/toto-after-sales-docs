# 投诉管理独立系统全栈

> 2026-09-11 数据契约更正：投诉复用的工单 scope 须改为售后自有业务范围。 现源码已完成本项独立化并通过隔离研发验证，目标环境迁移及业务验收待完成；下文实现、迁移和验证仅记录原时点事实。最新约束与源码证据见[独立边界核查](2026-09-11-业务数据独立边界与已开发功能核查.md)。

- 状态：待验收（隔离全栈验证通过），业务验收待安排。
- 应用与入口：Web 管理后台 A24 投诉管理，CS-013。
- 来源：用户要求完成非工作台剩余功能；统一管理后台需求要求建立投诉、责任认定、处理和回访子流程，关联原工单并独立计时。
- 负责人：Codex（实现），业务验收负责人待分配。

## 实施范围和阶段

投诉关联有权访问的真实工单，投诉编号独立生成。列表按主题／工单号和阶段查询。建档记录主题、受理时间和投诉正文；主题供列表检索，不填写电话、地址等敏感信息。正文和各阶段记录加密保存，只在具有对应详情／历史权限且工单范围可见时解密，列表不返回正文。

依据需求中的人工子流程，实施阶段为待认定 → 处理中 → 待回访 → 已结案。责任认定、处理完成、回访结案分别提交阶段结论，操作人和时间由服务端记录；系统不自动判责、不预设责任单位或赔偿规则。此阶段拆分是本次实现方案，尚未经业务验收。

每次动作追加不可变历史。按投诉行锁、版本令牌和请求号防止跳阶段、并发覆盖与重复提交；同一请求号不同内容拒绝。受理时间、创建时间、各阶段时间、结案时间独立保存，前端显示从受理至当前／结案的经过时间。投诉流程不改变原工单状态，不触发通知或费用。

## 权限和数据范围

列表、详情、历史、工单选项、建档与三个阶段操作分别校验 API 权限。数据范围复用工单查询的代理商／门店授权以及租户路由，不复制组织主档、不扩大范围。工单关系在建档后不可更改。没有删除或覆写历史接口。

## 验证

已完成：ComplaintIntegrationTest 两项集成用例通过，覆盖实际迁移、阶段完整流程、幂等冲突、过期版本、跳阶段拒绝、历史插入失败事务回滚、加密、跨范围／租户拒绝、HTTP 字段白名单和历史分页。前端两项测试覆盖提交白名单／请求号及独立计时，类型检查与构建通过。

桌面隔离 H2＋HTTP 实操完成建档 → 责任认定 → 处理完成 → 回访结案，详情保留四条原始阶段记录，列表已结案且隐藏后续状态动作；独立计时停止于结案时间。详情滚动区和固定关闭按钮显示正常。

交付文件：后端 `database/20260910_complaint.sql`、对应 `_permissions.json`、`.rollback.md` 与 `docs/complaint.openapi.json`。

## 交付边界

迁移、权限配置和恢复说明随代码交付。共享库未执行迁移；按最终脚本另行确认执行权限。无业务验收前保持 development 状态，不标记已开放。其他自动升级、通知、赔偿、投诉重开规则尚未形成确认契约，不自动启用。

## 2026-09-25 消费者投诉扩展（C19）

### 入口与投诉对象

消费者投诉的对象是“本人某次服务体验”，不要求消费者选择客服、服务站或服务人员作为责任对象。主入口位于服务记录详情“投诉本次服务”；低分或选择“部分解决／未解决”的评价显示正式投诉提示；“服务 → 投诉记录”和客服渠道页提供补充入口。无关联服务的一般意见仍走人工客服，首版不创建无工单投诉。

提交字段为问题类型、5～2000 字情况说明和电话／微信／均可联系偏好。服务端使用当前消费者会话校验 `account + instance + order` 归属，不能信任前端工单号；同一工单同时只允许一条未结案的消费者投诉。请求号保证重试幂等，相同请求号改内容返回冲突。

### 流程与对客边界

消费者提交后进入既有 A24 `OPEN → PROCESSING → FOLLOW_UP → CLOSED` 流程，对客文案为“待核实 → 处理中 → 待回访 → 已办结”。投诉与原工单、消费者评价相互独立，不自动改变工单状态，不自动判责、赔偿或承诺处理时限。A24 列表和详情区分消费者／客服登记来源，展示对客编号、问题类型和联系偏好；内部责任认定、处理结论和操作人不通过消费者接口返回。

首版不支持投诉附件。既有服务申请媒体上传有服务阶段和用途约束，不复用为投诉附件；后续需单独确认对象归属、内容安全、保留期和访问审计后再增加。

### 实现与验收状态

后端新增消费者本人发起、列表和详情接口，正文列表不解密、详情仅向本人解密；数据库迁移为既有投诉表增加来源与消费者归属字段，不新增后台菜单或岗位权限。消费者端新增“我的投诉／投诉本次服务／投诉进度”三页及四处入口，Web A24 增加来源字段展示。WAR／可执行 JAR 两种聚合宿主均只精确放行投诉及评价路径进入售后域消费者会话校验，不使用宽泛的消费者通配匿名规则。隔离投诉专项测试覆盖本人读写、他人拒绝、幂等、重复未结案拦截、密文和内部历史不外泄。

2026-09-25 共享测试库执行证据：只读预检确认目标为 MySQL 8.4.10-10 `gaia_wh_init_wzy`，既有 `afs_complaint`、`afs_service_order`、`afs_consumer_account` 依赖完整，6 个目标字段和 3 个目标索引均不存在；迁移前将投诉表结构和全部密文行备份到工作区忽略目录 `.local/backups/20260925-consumer-complaint/`，目录权限 `0700`、文件权限 `0600`。执行后 6 个字段、3 个索引均存在，投诉表行数保持 `0 → 0`。应用尚未发布；真实微信账号／工单和客服岗位正反向联调仍待完成，因此不计完整入口或业务验收。
