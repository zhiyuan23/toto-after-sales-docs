# 菜单联动同步与测试环境全量发布

日期：2026-09-26。用户授权提交当前菜单相关全部改动，并将完整 Web 前后端更新至共享测试环境。当前状态：代码与文档已提交，完整 Web 前后端已发布共享测试环境，菜单联动及登录后只读回归完成。

## 范围与联动

本次提交包括四视角菜单重排、导入导出配置归入菜单与权限、工作台任务抽屉、六项前端目录整理，以及手动折叠菜单后切换回弹修复；保留各功能原有契约与证据。发布范围为 `gaia-after-sales` 售后依赖、`gaia-saas-proj` 实际运行宿主 WAR、`gaia-ui` 主站与售后子站组合产物，不包含小程序发布或 Git push。

按菜单联动规范核对侧栏投影、导航中英文／图标、菜单管理、主系统角色勾选树的数据来源和目标库配置。四视角展示分组与稳定权限分组职责不同，未将展示分组复制成权限节点。

测试机确认使用 `10.1.1.106:3306/gaia_wh_init_wzy`，关闭多租户。菜单交付包预检发现 16 处旧差异：4 个工作台名称、11 处稳定权限树排序和 `afsDispatch` 显隐；备份后按 code／原 ID 原地更新。回读漂移为 0；模块 ID、code、路径、注册父级和全部角色菜单／按钮关系及操作定义摘要完全一致。此前的重命名、普通业务角色场景配置权限收敛及隐藏任务入口也已包含在本次回读清单中。同步不扩大任何账号权限。

仍使用旧“下载中心-模板管理”路径的两处后端错误提示、模板页面标题与任务／模板无权提示已统一到当前名称。

## 验证

- 前端静态检查通过；全量 326 项测试，296 通过、30 失败，失败集合与此前菜单切换修复基线一致，无新增失败；完整门禁仍非零退出。
- 侧栏定向 18 项、菜单包 7 项通过。
- 本轮后端管理员菜单与导入导出定向 6 项通过；售后制品安装与宿主 WAR 构建完成，宿主自身测试未在本轮执行。
- 测试库精确菜单同步与授权摘要验证完成；应用发布、线上产物及登录后菜单核验结果见下节。

证据：[菜单交付包](evidence/2026-09-26-menu-release/menu-package.json)、[只读预检](evidence/2026-09-26-menu-release/preflight.sql)、[同步 SQL](evidence/2026-09-26-menu-release/apply.sql)、[精确回滚](evidence/2026-09-26-menu-release/rollback.sql)、[授权摘要](evidence/2026-09-26-menu-release/verification.txt)、[零漂移回读](evidence/2026-09-26-menu-release/menu-drift.txt)、[检查摘要](evidence/2026-09-26-menu-release/checks.txt)。完整变更前记录保留在忽略目录 `.local/backups/20260926-menu-release/`。

## 共享测试环境发布结果

- 提交快照：前端 `f4125d53`、售后后端 `eb9ad4a`、宿主既有 `a170d11`、发布前文档 `8bdece6`。本次未 push，未拉取或合并远端，不包含小程序发布。
- 完整构建成功：售后 API／Core 安装、宿主 WAR、主站 `productionSAAS` 和售后子站 `production`；组合前端入口与 19 项资源引用、215 项子站 JS／CSS 包体检查通过。
- 发布目标：[共享测试环境](http://10.1.1.53:8011/after-sales/)。包目录 `/yundata/saas_test/packages/20260926002000-menu`，可回退备份 `/yundata/saas_test/backups/20260926002000-menu`。平滑停止旧 Tomcat 后启动新进程，部署回执 `DEPLOY_OK`，主站／子站／API HTTP 均为 200。
- 保留 5 份原内嵌运行配置；外部 `conf/gaia-after-sales.properties` 摘要未变。线上 WAR、售后 API／Core JAR 和主／子站入口 SHA-256 与本次构建逐项一致，19 项售后入口静态资源均可访问。
- WAR SHA-256：`160e1f30df0d0c2b1a288d7ed6a60fcfcfff4418ca2b3f9f27410b0a8013f780`；组合前端包：`5f9c558cb47a4e165a68d3865970758fa8fdfb0ee085094fbd1724b5ab7fc378`。

登录后核对四视角侧栏及工作台：系统管理均为正常末组；总部商品资料位于配件库存之前；服务站人员排班位于配件库存之前，知识库位于作业支持。任务抽屉读取 13 条真实既有任务，通用表格／筛选／分页显示完整，关闭回原工作台。从菜单与权限手动折叠系统管理后切换工作台，分组保持折叠。

管理员菜单管理树与主系统真实角色勾选树均显示新名称；后者只展开检查后取消退出，未改动或保存授权。权限树沿用稳定注册分组，与按工作视角展示的侧栏分组不同属于设计约束，不代表菜单同步遗漏。管理员详情可见导入导出配置页签；本轮未修改配置或创建新任务。

验证边界：本轮线上以管理员切换四视角，尚不能代替普通业务账号逐一允许／拒绝验收；匿名场景接口返回 Gaia 无权限业务响应（HTTP 200、业务码 500），未返回配置数据。公开隐私政策接口 HTTP 200 但业务码 503，提示尚未配置，这仅证明服务可达，不计业务成功。既有 30 项前端测试失败、宿主自身测试未重跑及完整业务链未验仍保留，入口与业务验收计数不变。

发布证据：[部署回执](evidence/2026-09-26-menu-release/deployment-receipt.txt)、[线上制品摘要](evidence/2026-09-26-menu-release/live-hashes.txt)、[HTTP 探测](evidence/2026-09-26-menu-release/http-check.json)、[浏览器核验记录](evidence/2026-09-26-menu-release/browser-verification.txt)、[总部菜单](evidence/2026-09-26-menu-release/headquarters.png)、[任务抽屉](evidence/2026-09-26-menu-release/task-drawer.png)、[菜单管理与配置](evidence/2026-09-26-menu-release/menu-config.png)。
