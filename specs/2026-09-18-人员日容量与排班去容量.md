# 服务人员日容量与排班去容量、分配饱和度

- 状态：研发实现完成；测试库迁移与共享测试环境已发布（待登录态业务验收）
- 日期：2026-09-18
- 关联入口：服务资源 / 服务人员、排班管理、工单分配、人员分配策略
- 关联前置：[服务人员三类自动分配策略](./2026-09-18-服务人员三类自动分配策略.md)

## 1. 已确认业务契约

| 项 | 结论 |
| --- | --- |
| 容量归属 | 方案 B：日容量只存在 `afs_service_personnel.daily_capacity`；排班表删除全部容量列 |
| 排班 UI | 新增/编辑日排班、周期生成、批量调整均去掉日/上/下/晚容量；日历与详情只展示排班类型与服务项目 |
| 人员日容量 | 新增/编辑必填，整数 1–999；存量迁移默认 5 |
| 改人员容量 | 不回写历史排班行（排班已无容量列）；分配立即按人员主档新值计算 |
| 当日工单数 | `personnel_id` + `expected_date`（预约日）+ `status IN ('PENDING_COMPLETION','PENDING_REVIEW','COMPLETED')` |
| 饱和度展示 | `floor(assignedCount * 100 / dailyCapacity)%`，超容显示实际值（可 >100%） |
| 智能打分 | 仍用剩余空间 `(1 - assigned/capacity) × 饱和度权重 + 满意度 × 满意度权重`；分子分母与手动一致 |
| AI | 入参饱和度与手动展示同一占用率口径 |
| 客服派站 | 不改；站级饱和度仍用 `afs_service_station.daily_max_orders` |

## 2. 数据与接口

迁移：

- `gaia-after-sales/database/20260918_personnel_daily_capacity.sql`
- 回退：`20260918_personnel_daily_capacity.rollback.sql`
- 基线 `20260909_service_resource.sql` 已同步为人员带 `daily_capacity`、排班无容量列

关键代码：

- 后端：`ServiceResourceAppService`、`PersonnelAssignmentAppService`、`ServiceResourceMapper.xml`、`PersonnelAssignmentMapper.xml`、`WorkOrderWorkflowMapper.xml`（旧派人员旁路同步读人员日容量与口径 C）
- 前端：`apps/after-sales` 人员表单/列表、排班三弹窗与日历、工单分配 `PersonnelCandidateCard` 饱和度

## 3. 验证记录

| 场景 | 结果 | 证据 |
| --- | --- | --- |
| 后端编译 | 通过 | `mvn -pl gaia-after-sales-api -am -DskipTests compile` BUILD SUCCESS |
| 前端 service-resource 定向测试 | 通过 | `tests/service-resource.test.ts` 4 项 |
| Customer 人员分配集成测试 | 通过 | `CustomerIntegrationTest#personnelSmartAiAndManualCapacityOverrideLeaveTraces` |
| 测试库迁移 | 已执行 | `gaia_wh_init_wzy`：人员 `daily_capacity` 已存在且 5 行均为 5；排班表 capacity 列已删除 |
| 共享测试环境发布 | 已执行 | `10.1.1.53` 替换 after-sales core/api JAR + `/after-sales/`；`2026-09-18 16:33:48` Started；`8011` API/售后页 HTTP 200 |

## 4. 2026-09-26 排班周视图重设计

关联 `OPT-20260926-02`，A27／STA-003。用户否定首轮弱状态样式，明确要求保留每位人员按周的布局，并让是否排班更容易识别。最终页面以本节末“整体恢复指定截图版本”为准，各轮截图仅保留过程证据。

- 保留人员 × 日期网格、日历／列表切换、现有筛选、权限与写入契约；使用原 Element Plus、Lucide 和系统字体，不引入 UI 依赖。
- 班次为完整可点击卡片，当前视觉以本节末“彩色边条风格统一”为准：工作绿色、休息中性灰、休假琥珀、停单柔红，同时使用不同图标与 15px 状态文字，颜色不是唯一判断依据。草稿上下右侧为虚线、左侧为实线色条并明确显示草稿；已发布、锁定、过期和作废继续显示实际状态。
- 点击班次块查看详情；有后端 `edit` action 时显示独立铅笔入口。两个入口为原生按钮、具有含人员与日期的可访问名称，保留忙碌／失败禁用和键盘焦点。
- 空格只在完整且未按排班类型／发布状态筛选的结果中显示“未排班”。筛选后显示“无匹配记录”；日记录分页未全部加载时显示“本页未显示”。不据当前页缺失推断人员实际无排班，不扩大为所有未排班人员的完整名册。
- 人员列 196px、圆角头像 40px；姓名和工号为主层级，服务站单独一行；角标明确人数仅为“本页”。表头采用 24px 日期数字搭配星期／月份，今天以实心日期块强调；表头、人员列与分页固定。
- 日期范围旁支持“上一周／本周／下一周”：上一周和下一周以已生效开始日期所在周为基准，统一查询周一至周日；本周回到客户端当地日期所在周。保留已生效的服务站、人员、类型与发布状态，同步日期筛选并重置第一页，每次切换只发起一次查询；自定义日期范围切换后回归完整周。无记录时保留周次导航，加载及无权限时禁用，不增加后端接口。
- 班次颜色使用页面级语义变量配对深浅主题；无自动动画。桌面优先，保留横向溢出时内部滚动，不改变项目移动端验收边界。

隔离夹具：`apps/after-sales/tests/browser/schedule.html`，复用 `test:i18n:browser` 启动入口。默认 6 名合成人员／41 条日排班；`scenario=paginated` 为 18 人／125 条；另支持 `readonly|denied|empty|error` 和 `theme=dark`。读接口在夹具内替换，写请求由已有隔离服务器阻断。

首轮颜色卡片见[重设计验证记录](evidence/2026-09-26-schedule-redesign/verification.md)；当前表头、人员列、配色与周切换见[周视图完善验证](evidence/2026-09-26-schedule-week-polish/verification.md)。排班相关 6 项测试通过；当前完整检查中 lint、依赖、类型及翻译检查通过，331 项测试中 301 通过、30 项既有失败，无新增失败。未提交、未部署；正式账号业务验收、入口及总览计数不变。

### 同日：排班详情改用新版分组展示

按用户要求，将排班页原 720px 的带边框键值详情替换为新版商品／网点详情的信息层级：复用 `EsDetailDialog + EsDetailSection`，在功能目录新增 `ScheduleDetailDialog.vue`；1040px 标准桌面宽度、固定头尾、正文内部滚动，标题统一为“排班详情”。顶部呈现人员头像／姓名／编码／服务站及发布状态，正文分为“排班信息”和“变更与备注”，保留原字段与空值占位。日期和排班类型成对，服务项目通栏；原因／备注均为空时成对，有正文时均通栏，不新增无实际内容的页签。

继续由 `useDetailDialog` 按 ID 读取详情，支持骨架加载、失败重试、加载中关闭；关闭、离开路由及卸载时失效旧请求。保留日历与列表原有详情入口，写入契约与权限不变，未改动共享详情组件。

验证见[新版排班详情验证](evidence/2026-09-26-schedule-detail/verification.md)：桌面深浅主题、短字段密度、长正文溢出、失败重试及关闭重开已检查；最终完整检查中 lint／依赖／类型／翻译通过，334 项测试中 304 通过、30 项既有失败，无新增失败。研发验证不等于正式账号验收；未提交、未部署，总览计数不变。

### 同日：彩色边条风格统一

用户确认参照工作台服务人员卡片，排班页统一采用左侧 3px 彩色实线、其余三边 1px 浅色边框、极浅底色。工作／休息／休假／停单复用 Element Plus success／info／warning／danger 主题色；底色仅混入 4% 类型色，标题和图标使用对比度足够的类型文字色，服务项目与发布状态恢复中性文字。已过期／作废维持中性处理，未排班／分页未显示／无匹配记录的判断和文字保持。

图例同步为小型左色条卡片，星期表头恢复中性背景，今天使用蓝色细边及左侧强调线；人员头像使用更浅的中性底色。新版详情的身份摘要使用相同语义色、左侧强调线和浅底边框；其余分组结构保留。范围为当前排班页及其详情，不改写整个系统或工作台的公共样式。

[验证记录与截图](evidence/2026-09-26-schedule-border-style/verification.md)：深浅主题、网格宽度、卡片／图例左边框、详情密度已核对；实际文字对比度浅色最低 5.34:1，深色最低 9.70:1。完整检查 304 项通过、30 项既有失败，无新增失败；未提交、未部署，正式业务验收及入口计数不变。

### 同日：列表视图恢复通用表格密度（历史，已撤回）

按用户反馈优化列表视图。该页原已使用 `EsTable`，但局部 `.schedule-page .el-table .el-table__cell { height: auto; }` 覆盖通用 40px 高度，实测表头 24px；人员姓名插槽重复堆叠头像、姓名、编码、服务站，实测数据行约 72px。当前删除该高度覆盖，增加标准 `min-h-0 flex-1` 包装层承接 `EsTable` 高度／分页契约；无公共表格组件修改。

列顺序调整为人员姓名、人员编码、日期、排班类型、发布状态、服务项目、服务站、备注、操作。固定左侧人员列仅展示 28px 小头像与单行姓名，原有人员关联跳转权限保留；编码独立成列，不重复展示；服务站后移到服务项目之后，保留跨视角识别能力。备注继续使用通用溢出提示，完整正文在详情阅读。日期使用有完整可访问名称的详情按钮，沿用原按 ID 读取详情与忙碌禁用逻辑。

[列表验证记录](evidence/2026-09-26-schedule-list/verification.md)：两条记录／41 条记录／空列表验证，表头及数据行恢复 40px、表格撑满剩余区域、分页留在底部；日期详情、长备注、只读权限及日历切换已核对。完整检查 304 通过、30 项既有失败，无新增失败；未提交、未部署，入口和正式验收计数不变。


### 同日：周表快捷操作改造（历史，前端已撤回）

用户确认新的人员排班操作模式后，默认周表改为按启用人员分页、每人完整七天并纳入未排班人员，支持单格／多选、沿用上周、周规则补空格、撤销及统一保存。当前周表不再依赖排班记录页推算人员与空格；上文关于记录分页未显示占位的说明仅保留为历史实现。新接口、操作范围、事务与研发验证统一见[快捷排班 Spec](2026-09-26-排班周表快捷编辑与统一保存.md)。列表／详情和日容量边界保留。

### 同日：整体恢复指定截图版本（当前基线）

按用户要求恢复 `schedule-border-style/dark.png` 的布局、样式和操作：新增日排班、新增周排班、范围批量调整、独立发布，卡片详情与铅笔编辑；恢复原列表列序、组合人员信息和记录分页。上面后续列表密度及周表快捷编辑不再代表当前前端。后端精确格子保存、并发锁和校验作为兼容扩展保留；不被当前页面调用。最终验证与提交记录见[恢复验证](evidence/2026-09-26-schedule-restore-border/verification.md)。
