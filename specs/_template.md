# <功能名称> Spec

> 复制为 `YYYY-MM-DD-功能名.md` 后填写。按实际风险删减不适用项并注明原因，不为填模板虚构角色、接口或数据。本文是模板，不是业务授权。

- 状态：草稿 / 待确认 / 已确认 / 开发中 / 待验收 / 已验收 / 暂缓（选择一项）
- 日期、版本：待填写
- 应用载体：Web 管理后台 / 消费者小程序 / 服务人员小程序
- 角色视角：总部运营 / 客服作业 / 服务站作业 / 门店业务 / 跨视角 / 不适用
- 业务模块：待填写
- 入口与需求：Axx / Dxx / Cxx / Wxx；ADM-* / DLR-* / CAPP-* / WAPP-* 等
- 需求来源：项目内需求文档、原始资料索引或确认记录
- 用户确认记录：待确认；记录确认日期、版本和实际确认范围，不由 Agent 自行认定
- 关联材料：原型、既有契约、依赖功能（按需）
- 研发任务：MNU-xx（入口骨架）或 FS-xxx（业务切片）

`Axx` 与 `Dxx` 均属于同一个 Web 管理后台，只代表历史入口编号；门店业务通过角色视角区分，不单独写成应用端。本 Spec 保留实现与验收明细，完成后回写对应应用入口明细和 `03-开发管理/00-开发进度与下一步.md`，不另建菜单或切片进度表。

## 1. 目标与范围

用一个具体场景说明当前问题和完成后的行为。列出本期包含、不包含及成功标准。

## 2. 角色、流程与页面

说明谁在什么前置条件下触发操作、成功后的状态和可见结果，以及异常、撤销或重试行为。涉及状态流转时列明允许的迁移、操作人和不可跳过的条件。

| 应用、角色视角与页面 | 使用者与操作 | 加载、空态、失败处理 | 对应验收项 |
| --- | --- | --- | --- |

## 3. 接口、数据与权限契约

| 方法与路径 | 调用端 | 请求与响应、类型及必填约束 | 错误语义 | 身份、租户与资源权限 |
| --- | --- | --- | --- | --- |

接口注明网关/Servlet 上下文和 Controller 路径如何组成最终 URL。按需要补充脱敏示例、分页、时间与金额单位、枚举、空值、幂等、并发及重复提交处理；已有契约引用其唯一来源。

数据说明权威来源、读取或写入责任、引用或快照选择；新表和字段、索引、事务边界及迁移/恢复仅在确有需要时设计。权限同时覆盖菜单、按钮、API 和资源，不用前端隐藏代替后端授权。

## 4. 实施范围与顺序

仅列真正涉及的仓库；引用参考仓库不等于授权修改它。

| 仓库或配置系统 | 文件/模块及具体动作 | 前置依赖 | 必要验证 |
| --- | --- | --- | --- |

说明是否需要 Maven 本地安装、宿主装配、菜单配置或测试数据准备，以及执行环境和影响。涉及数据变更时给出恢复方式。提交、推送、上传或发布须另有明确授权记录。

## 5. 验收场景

| 编号 | 前置状态与身份 | 操作 | 可观察的预期结果 | 验证层级 |
| --- | --- | --- | --- | --- |

覆盖主路径及与本功能有关的越权、跨租户、非法状态、重复操作、依赖失败场景。标明哪些可自动化、哪些需要真实环境或真机；不把编译通过等同于业务验收。

## 6. 待确认项与风险

| 问题 | 影响或阻塞哪一步 | 所需事实/决策来源 | 结论及确认记录 |
| --- | --- | --- | --- |

阻塞项未解决时继续不依赖它的工作，不把占位值或暂定方案当成已确认事实。

## 7. 实施与验收记录

实现开始后维护本节，不另建重复任务台账。

| 日期 | 仓库、分支与版本基线 | 命令/场景及环境 | 实际结果与证据 | 未验证项 |
| --- | --- | --- | --- | --- |

- 完成内容：待填写。
- 与确认版本的偏差及补充确认：无 / 待填写。
- 跨仓交付：逐仓记录提交标识；尚未提交则写“未提交”及变更路径，不自动提交。
- 剩余事项、阻塞及下一步：待填写。
- 知识维护：无需更新 / 候选及目标 / 已获授权并完成（以实际情况填写）。
