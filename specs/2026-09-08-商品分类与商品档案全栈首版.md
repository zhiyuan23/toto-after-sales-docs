# 商品分类与商品档案全栈首版

- 状态：开发中；方案随实现验证修订，未声明业务验收。
- 日期：2026-09-08。
- 用户授权：实现商品分类和商品档案，先设计后端规范，完成验证后写入 AI 开发文档；用户另已确认“复用现有主档，售后特有字段单独扩展”。
- 需求：ADM-MD-006、ADM-MD-007，A08、A09，FS-008 的基础主档切片。图片、系列和配套品另行交付。

## 1. 架构与数据归属

沿用 Gaia 单体模块和 MyBatis，不增加微服务、通用 CRUD 父类或第二套响应/身份框架。接口层处理 HTTP 和响应；应用服务负责权限、事务及用例；领域规则负责分类关系和商品校验；Mapper 明确列出 SQL。跨域主档操作集中在适配层，禁止前端传实体或任意查询 Map。

- 基础商品：`md_material`，只处理成品 `material_type=3`。编码、名称、GTIN、规格、商品分类继续是共享字段。
- 基础分类：`md_material_type_category`，复用 ID、父子关系、名称、排序、备注和路径。已有未扩展分类按商品分类读取。
- 售后扩展：以主档 ID 为主键建立分类、商品扩展。分类保存维度和售后启停；商品保存机能型号、本体品番、水箱品番、颜色、营业分类和售后启停。共享物料 `enable` 与售后启用共同决定能否用于新售后业务。
- 编辑只更新授权白名单字段，不覆盖共享主档的图片、单位、工厂、品牌、自定义字段等其他能力。禁止物理删除共享主档；停用保留 ID 和历史。
- 商品分类与营业分类作为不同维度，不允许跨维度父子关系；商品引用两种分类时分别校验。商品分类界面展示大/中/小层级。

## 2. 公共契约

- URL：Controller `/api/afterSales/catalog`，宿主 context `/api`；前端继续用 Gaia request 和现有代理。
- 响应复用 `APIResponse<T>`：`code=0` 成功、`errMsg` 为可展示错误、`data` 为 DTO；分页复用 Gaia `PageResponse` 的 `rows/records`，前端适配为 `list/total`。
- 查询统一 `page`（从 1 开始）、`pageSize`（15/30/50/100，服务端上限 100）；搜索有界、参数化，排序固定稳定次序。
- ID 作为字符串传输，数据库继续 BIGINT；版本 token 随详情/列表返回，编辑/状态变更必须带原版本，过期提示刷新。
- 登录和租户使用 Gaia Shiro 与数据源上下文；多租户模式缺上下文时拒绝。不接受客户端 tenantId/userId。
- 权限读取 Gaia 用户菜单/API 和操作授权，售后接口额外采用未授权即拒绝，不依赖宿主“未配置路径放行”。页面通过权限结果和行可执行动作展示按钮。
- 创建、编辑、启停与审计在同一事务。分类结构变更和商品引用校验使用数据库锁；并发失败不部分保存。

## 3. 实施与验收

1. 实现后端请求/响应、权限适配、主档与扩展访问、分类和商品用例。
2. 提供显式迁移、恢复说明与接口文档；不让启动自动改表。
3. 分类补树表能力；商品替换开发态内存入口，沿用 commonV2 与公共 hooks。
4. 验证真实 SQL 映射、事务回滚、树循环/引用/启停、重复及过期更新、分页、权限和租户隔离；再执行前端组件测试、类型/lint/构建和页面检查。
5. 验证后的规范和证据回写 AI 文档及统一进度。

`local-development.md` 明确禁止直接对当前共享测试库运行迁移/初始化。因此先在隔离数据库验证并交付可审阅迁移；共享测试库部署与真实账号验收须在准备完成后处理，不以模拟或构建代替。

## 4. 实施记录

- 三个源码仓库均在 `feature/20260907_蓝鲸售后服务_志远_喆宇`，与已知 upstream 差异 0/0；保留 gaia-ui 既有组件/范例改动、宿主 .gitignore/POM 改动。
- 不修改 `src/views/common`、gaia-md 源码和全局样式；不自动 commit/push。
- 已实现分类树增改查、排序和启停，商品筛选分页、增改查、售后启停、版本冲突、引用保护与审计。正式页面调用真实 API；旧内存示例移至 examples，仅供公共 hooks 回归。
- 新增 4 张售后表的显式迁移、OpenAPI 契约和权限配置说明。未执行共享测试库迁移，未配置正式菜单或授予真实账号权限，未重启现有 8080 服务。
- 对共享测试库仅查询表结构元数据：现有商品分类外键为 INT，接口增加容量校验；共享主档 ID 生成继续使用 Gaia 表名序列，部署前必须核对 Redis 高水位，避免隔离 Redis 从低值生成冲突 ID。

## 5. 研发验证记录（2026-09-08）

| 验证 | 结果与边界 |
| --- | --- |
| 后端 `mvn -B -o -pl gaia-after-sales-api -am clean install` | 13 项通过：core 2 项、API 集成 11 项；实际重新编译执行，无跳过测试 |
| SQL、事务和租户 | 使用真实 MyBatis 映射、Spring 事务及 Gaia DynamicRoutingDataSource，两个隔离 H2 MySQL 模式库；验证树移动/循环、引用启停、分页/转义、并发重复、过期版本、扩展失败回滚、主档字段保留、旧主档直接读取及租户切换 |
| 权限 | 真实 Shiro 会话及租户上下文测试，权限来源使用测试替身；验证未登录、无租户、无授权拒绝及读权限不能写，不代表真实账号授权链验收 |
| 前端自动化 | commonV2、公共 hooks、catalog 共 19 项通过；范围包含响应错误、白名单提交、树处理、查询分页和弹窗状态 |
| 类型、lint、页面覆盖 | 售后范围类型检查及 ESLint 通过；63 个售后页面覆盖检查通过 |
| 构建与宿主装配 | `yarn build` 通过；宿主 `mvn -B -o -pl gaia-saas-web-jar -am package -DskipTests` 通过（仅装配验证，不计宿主测试） |
| 桌面浏览器真实 HTTP 联调 | 独立 Vite 7016 → 测试类路径 Tomcat 18081 → H2；生产页面、API、应用服务和 SQL。验证分类新增/编辑/停用，商品必填、重复编码失败保留表单、创建/编辑/停用、查询、翻页、草稿不影响已应用查询，售后停用与主档启用独立显示 |

按用户本轮要求，本项目暂不做移动端/窄屏样式专项检查。测试身份与测试数据库仅用于隔离验收，不进入生产包和正式入口。H2 结果不能替代 MySQL 的 DDL、锁、排序规则和实际部署验证。

## 6. 后续复用与剩余验收

- 后续 Web 功能参考商品档案目录，复用 `commonV2` 与公共 hooks；API 使用类型化请求、响应适配和提交白名单；业务权限、状态和事务留在功能切片。规范已写入 [AI 开发执行规范](../03-开发管理/03-AI开发执行规范.md)和[后端开发规范](../02-技术设计/06-售后后端开发规范.md)。
- 共享测试环境仍待：确认允许执行已准备的 4 表迁移，核对主档序列高水位，指定测试账号/角色并按权限清单注册和授权两个菜单，启动新版宿主后完成真实 MySQL、会话和权限联调。尚未完成前不开放生产菜单、不计业务完成。
- 机能型号唯一开关复用 Gaia 参数服务，GTIN 唯一性复用原校验器；真实参数配置和历史数据冲突需在共享环境核验。当前目录锁保障本切片并发写入，其他历史主档入口仍需另行协调，不能宣称已解决全部入口的并发唯一性。
- 图片、商品系列、配套品、DWH 同步及历史批量清洗不在本次首版范围；无物理删除共享主档能力。


## 7. 本机权限接口 500 修复（2026-09-08）

- 用户在 7004 页面触发权限接口得到 `code=500`。旧后端日志为 `NoResourceFoundException: No static resource api/afterSales/catalog/permissions`：8080 仍是首版接口之前启动的 JVM，重新打包没有更新运行中的接口。
- 加载新包时进一步发现真实宿主的事务/Shiro 双重代理造成具体类注入失败。分类、商品应用服务新增 `CategoryCatalogService` / `ProductCatalogService` 业务接口，Controller、测试及隔离启动器改按接口注入；未修改宿主全局代理或其他领域实现。
- 集成测试加入与宿主一致的事务 XML 及额外代理创建器，复现旧注入方式失败后完成修复。`mvn -B -o -pl gaia-after-sales-api -am clean install`：core 2 项、API 12 项，共 14 项通过；其中新增代理链和权限接口回归。宿主重新打包成功，`git diff --check` 通过。
- 修复后的宿主于 13:40:28 启动成功，8080 保持运行。通过 7004 当前登录页面重新请求权限，成功进入返回权限结果的分支，分类和商品档案页面分别提示当前账号无对应访问权限，已不再进入 500/通用请求失败分支。匿名代理请求仍正确返回 4001。
- 本次只完成运行修复与真实会话的权限读取验证，未迁移 4 张售后表、未注册菜单/授予角色、未执行业务写入；完整业务验收仍待前述部署与授权准备。后端开发规范和本机联调说明已补充宿主代理兼容与重启后已登录请求验证要求。
