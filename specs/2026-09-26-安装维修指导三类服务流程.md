# 安装、维修、指导三类服务流程

- 编号：REQ-SERVICE-TYPES-20260926
- 状态：用户确认范围；源码与研发验证完成情况见下文，业务验收未完成。
- 来源：2026-09-26 用户要求移除清洗，恢复安装／维修／指导；随后明确“本次只完成三类正常流程，转现场暂不实现”。
- 关联：Web 建单、派单、服务资源／排班、完工审核、统计与评价配置；消费者服务申请；服务人员任务执行。

## 业务契约

服务类型为 INSTALLATION、REPAIR、GUIDANCE。GUIDANCE 分 ONSITE 现场指导和 REMOTE 远程指导；安装和维修不允许 REMOTE。新建入口、筛选、技能、排班、评价配置不再提供 CLEANING。历史数据不强制改写，旧类型的历史展示与数据库约束保留兼容。

| 环节 | 安装 | 维修 | 现场指导 | 远程指导 |
| --- | --- | --- | --- | --- |
| 服务申请 | 注册产品与联系人、时间、使用地址 | 另含故障和问题 | 指导问题、联系人、时间、地址 | 指导问题、联系人、联系时间、产品使用区域／地址，无需定位 |
| 分配 | 地区服务站与人员技能／排班容量 | 同左 | GUIDANCE 技能，沿用站点与人员分配 | GUIDANCE 技能，沿用站点与人员分配；智能／AI 候选不计距离 |
| 开始服务 | 现场开始／到达记录 | 现场开始／到达记录 | 现场开始／到达记录 | 开始远程指导，无需到场 |
| 完工记录 | 过程、结果、客户确认及说明 | 同左 | 同左 | 沟通过程、结果、客户确认及说明 |
| 证据 | 现场和铭牌照片必填 | 现场和铭牌照片必填 | 现场／铭牌照片可选，未新增强制标准 | 沟通附件可选，不要求现场／铭牌照片 |
| 配件 | 按既有规则 | 按既有规则 | 按既有规则 | 不显示领料和耗用入口，服务端拒绝耗用及工单关联领料 |
| 审核及关闭 | 不可变完工版本 → 待审核 → 通过关闭，退回补充／继续服务 | 同左 | 同左 | 同左；退回不得要求现场／铭牌证据 |

产品码、签名均保留为可选信息。消费者申请的服务资格审核、重复提交、组织／租户与岗位权限、人员容量冲突和不可变历史沿用既有机制。评价与回访沿用已有能力，本次不新增业务验收结论。

远程转现场不在本次范围；不提供转换按钮或接口，不重新分配原单，不清空远程历史。消费者自主预约安装仍遵循原需求范围，本次未擅自开放新入口。

## 实现范围

- 后端：建单类型及模式校验；远程申请免坐标；资源配置、实时工单、报表与评价类型；自动派单远程不计距离；后台完工提交与人员端证据规则对齐；远程配件和审核补充项约束。
- Web：建单指导方式选择、远程联系时间与定位展示；服务站／人员／排班与筛选选项；完工过程、客户确认和分类证据字段；移除当前业务清洗文案与配置。
- 消费者端：现场指导名称统一，远程申请跳过定位校验与选点，保留使用地址用于现有区域路由。
- 服务人员端：远程沟通作业保留，隐藏配件相关操作、改约使用联系时间文案；服务端校验防止绕过界面。
- 公共底座和公共页面子仓库未修改。各仓库保留原有并发改动，本次提交仅包含三类服务流程及必要支撑。

## 测试库配置迁移

已核对目标为 `gaia_wh_init_wzy`，迁移前完整备份位于本地忽略目录 `.local/backups/20260926-guidance/afs-data.sql.gz`（105 张售后表／1132 行）。执行后端 `database/20260926_service_types_guidance.sql`，四张带日期后缀备份表保留原值，回退说明随脚本交付。

服务站 16 行、排班 45 行、评价标签 18 行完成归一化；8 条人员清洗技能替换为指导（已有指导时不重复添加）。回读旧清洗配置合计 0。现有 4 张安装／维修工单未改写，未清理或转换任何历史材料。共享应用未发布，部署新版前须保持配置与新版代码配套。

## 研发验证

- 后端定向 22 项通过：四种服务组合建单→分站→派人→提交→审核关闭；消费者远程免坐标；非指导拒绝远程；拒绝清洗；远程智能／AI 分配免距离；远程与现场执行规则；远程配件不可申请但历史可读；审核与实时工单查询。
- 后端 CustomerIntegrationTest 全类执行仍有 3 项既有失败：Excel 文件名断言、未核验申报的 actions 顺序、外部同步 H2 测试缺少商品系列表。新增用例已单独全部通过。
- Web 静态检查和生产构建通过；全量 334 项中 304 通过，仍有 30 项既有失败（分类、主档状态、权限、视角／菜单等），未作为全量通过宣称。
- 两个小程序完整 `check` 与 TOTO development 微信构建通过。微信真机、正式岗位跨端验收尚未执行。
- 本机已通过统一启动器重新装配售后模块及聚合宿主，8080 双探针就绪，管理员登录后运营接口返回现有 4 张工单。浏览器验证建单类型只有安装／维修／指导，指导显示现场／远程单选，切换维修后隐藏指导方式。未提交真实业务工单，完整四组合闭环以隔离集成测试为证据。
- 未部署共享服务器应用，入口与业务验收计数不变。

## 本次提交范围核验

按本次修改前备份拆分混合文件，完工材料历史展示、人员端草稿／补充界面等其他任务改动保留未暂存。仅为本次新增完工字段纳入 H2 测试列与必要翻译。

仅含待提交片段的独立快照验证：后端 21 项定向测试通过（不含另一任务的历史权限测试），服务人员端类型检查与 56 项单元测试通过；Web 类型与国际化检查通过，定向 16 项中 15 项通过，剩余 1 项为已有客服菜单分组断言失败，与上一轮全量失败一致。此次未推送、未发布应用。

代码提交：后端 `bce1fe2`、Web `e29f0f6e`、消费者端 `70dafe5`、服务人员端 `9a2adc9`。四个代码提交的文档关联检查及移动端提交钩子均通过。
